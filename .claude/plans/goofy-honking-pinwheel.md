# Plan: Commit & Push

## Context
사용자가 현재 브랜치(feature/nbl-ui-completion)의 변경사항을 커밋하고 원격 저장소에 푸시 요청.

## Task
1. `git status` / `git diff` / `git log`로 변경사항 확인
2. 변경 파일 스테이징
3. 컨벤셔널 커밋 메시지로 커밋
4. `git push origin feature/nbl-ui-completion`

## Verification
- `git status`로 클린 상태 확인
- 원격 브랜치 업데이트 확인

# Recent Completions

> CLAUDE.md에서 분리된 최근 완료 작업 이력. 시간순(최신→과거).
> 안정적인 규칙·아키텍처는 `CLAUDE.md` / 메모리(MEMORY.md) 참조.

## 2026-05-19

- **주식 양도세 PR-4A 해외주식 + PR-4B 국외전출세 + FS-09 분할 수령 완료**. 커밋 `b56a031`·`a32fa5a`·`d06b6e9`·`d2edffb`·`5e67a78`·`175d02c`·`bc3d610`. 디자인 로드맵 "후속: §97의2·국외전출세·해외주식" 중 해외주식+국외전출세 본체. **계획·디자인 R1·R2·R3 정정** KoreanLaw MCP 위임 체인 끝까지 검증 — §55 8구간 6~45% (검토 라운드 환각 "16~55%" 정정, `BASIC_PROGRESSIVE_BRACKETS` 재사용 확인) / §104①11 가목2) 20%·25% 2구간 (`STOCK_MAJOR_PROGRESSIVE_BRACKETS` 재사용). **PR-4A 해외주식** (§94①3다목·§118의2~§118의8·§178의5): `foreign-stock.ts` 신규(289→368줄) + `types/foreign-stock.types.ts`(123→206줄). §178의5 취득일/양도일 별도 기준환율 + §118의5 외국납부세액공제 한도식(credit/expense 분기) + §118의7 기본공제 250만(독립 그룹). `ForeignStockBlock` 6섹션 + `ForeignStockResultCard`(원 생략 한국어 산식) + `validate-foreign.ts`(243→307줄). anchor 48+16건. **PR-4B 국외전출세** (§118의9~§118의16·시행령 §178의2~§178의10): `exit-tax.ts` 신규(477줄) + `types/exit-tax.types.ts`(204줄). 거주자 5년 요건 + 대주주 임계 + 간주양도가 `departureDayValuationMode` 2분기(상장 §99 1개월평균 / 비상장 §165④ 보충적평가) + §118의12 조정공제(실양도가<간주양도가) + §118의13 외국납부세액공제(1·2호 배제 enum 3값) + §118의16 납부유예(이자상당액 v1 미계산 + 안내) + §118의15 보유현황 미신고 가산세 2% + §118의10④ 기본공제 250만(독립). `ExitTaxBlock` 7섹션 + `ExitTaxHoldingsMatrix` 종목 배열(`holdings[]`) + `ExitTaxResultCard` + `validate-exit.ts`(254줄). anchor 42+33건. **FS-09 §178의5② 장기할부 시점별 분할 환율 정식 구현**: `transferReceiptMode: "single"|"installments"` 분기 + `transferInstallmentReceipts: Array<{receiptDate, amountForeign, exchangeRate}>` + 시점별 floor 합산 + 합계 일치 superRefine + `InstallmentReceiptsMatrix.tsx`(187줄) 행 추가/삭제. anchor 30건(FS-09a~e, "v1 총액 근사" 4건 폐기). **14지점 동기화 전수**: ①폼·②initial·③normalize·④API 변환·⑤UI 위젯·⑥사이드바·⑦결과 카드·⑧validation 분리(`validate-foreign`/`validate-exit`/`validate-step2`)·⑨ Zod enum·⑩refine·⑪acquisitionDate fallback (holdings 배열 내 개별 toDate())·⑫Zod 입력 객체·⑬callStockTransferAPI body spread·⑭Route handler 매핑(`handleForeignStock`/`handleExitTax`). 800줄 정책: `stock-transfer-tax-schema.ts` 753줄 → `stock-transfer-exit-tax-schema.ts` 분리(145줄). **Low 정합 정리**: `StockTransferResult.taxCategory` enum에 `foreign_stock`·`exit_tax` 추가(+라벨) / `stock-classification.ts` `isForeignTaxCategory()` 헬퍼 추가·분기 적용(단일 source of truth) / `exit-tax.ts` §118의13②1·2호 인라인 문자열 → `STOCK_EXIT_TAX.EXCLUSION_REASON_1_MESSAGE/_2_MESSAGE` 상수 교체. **UI 정리**: `MarketTypeBlock` 2열 그리드(`sm:grid-cols-2`) + FieldCard 제거(120px 라벨 컬럼 잔존 버그로 카드 폭 ~30px 붕괴 → 직접 카드 컨테이너 교체). **문서**: `docs/00-pm/stock-transfer-exit-tax-foreign-stock.plan.md`(635줄) + `docs/02-design/features/stock-transfer-exit-tax.engine.design.md`(783줄, FS-09 정식 구현 갱신) + `docs/02-design/features/stock-transfer-exit-tax.ui.design.md`(785줄). 검토 이력 R1·R2·R3 표 포함. **검증**: typecheck 0 / 전체 3,812 PASS / 0 FAIL (회귀 0건) / matchRate 100% (FS-02 내국법인 해외 DR §157의3②2호는 스코프 외 확정·v1 후속). KoreanLaw §178의5② 본문 직접 인용(시행령 MST 285631, 2026.4.23.). 브라우저 수동 확인 미수행(사용자 측 직접). **v1 미지원 명시**: FS-02 DR / 납부유예 이자상당액(시행령 §178의11). 신규 정책 후보: `feedback_korean_law_82_vs_81_2_drift` 강화(§55 환각 사례 추가).

## 2026-05-18

- **주식 양도세 §165⑤ 취득 후 상장 PDF 사례 완전 재현 완료**. 양도코리아 PDF "주식-취득후 상장.pdf" 3개 다이얼로그(상장시 주당 평가액 21 거래일 종가표 + 순손익 24행 × 2열 + 순자산 20행 × 2열) 완전 재현. **Phase A KoreanLaw MCP 검증으로 핵심 정정 2건**: (1) §165⑤ 시기 = "**상장일 이후 1개월**" 확정 (v3 "직전 1개월" 모호 표현 정정 — 본문 "코스닥/코넥스 상장일 현재의 제4항 평가액" 분모 + 상장일 이전 비상장 기간 종가 미존재 + PDF 일자 2009-08-21~2009-09-21 일치). (2) **환원율 10% 위임 체인 §82 오기 → §81② → 상증령 §17 정정** — 소법 시행규칙 §82는 "신축주택·미분양주택 요건"(전혀 다른 조문). 정확 체인: 소령 §165④1 가목 → 소법 §81② → 상증법 시행규칙 §17 "연간 100분의 10". **80 신규 폼 필드**(모드 2 + 종가 4 + 순손익 36 + 순자산 38) + `unlistedDetailMode` 3분기(simple/listing_only/full) RadioCardGroup. **신규 어댑터** `post-listing-flat-adapter.ts`(446줄) — `adaptFlatToApiBody` 3 분기 + 환원율 %→decimal + `buildPostListingFromDetail`. **엔진 헬퍼 H-01~H-04 export** (`calcMonthlyClosingAverage`·`calcNetIncomePerShare`·`calcNetAssetPerShare`·`calcUnlistedPerShareWeighted` + isHeavyRE 인자) — UI 미리보기·anchor 직접 import 단일 진실. **타입 확장**: `PostListingDetailInput`·`NIYear`·`NAYear` + `StockTransferResult.acquiredBeforeListing` echo + `postListingDetail.detail` 모드별 활성 + `floor80NotApplied: true` echo. **엔진 본체** `synthesizePostListingInput` 자동 합성. **UI 4 컴포넌트**: `PostListingValuationCard`(256줄) + `PostListingClosingPriceTable`(124줄, listingDate 자동 채움 + 2-col grid) + `PostListingNetIncomeStatement`(172줄) + `PostListingNetAssetStatement`(187줄, 부채 10행 정정). 모두 800줄 미만. **결과 카드** PostListingDetailCard — 6 중간값 + 80% 하한 미적용 안내 + 4 법조문 배지. **anchor 36건** 4 파일: PL-CLOSE 5 + PL-NI 5 + PL-WEIGHT 4 + PL-NA 8 + PL-CONV 1 + PL-FULL 3 + PL-RE 2(부동산과다) + PL-FLOOR 2(80% 하한 양방향) + PL-LEGACY 1 + PL-BUILD 4 + 헬퍼. **Validate 모드별 매트릭스** (simple 4 / listing_only 22 / full 80) + Round 4 H-03 tradingHalt 조합 차단. 14지점 ①②③④⑤⑥⑦⑧⑨⑫⑬⑭ 동기화 (⑩⑪ N/A). **전체 회귀 3,452/3,452 PASS / 0 FAIL** + typecheck 0건. Phase J 브라우저 수동 미수행(명시). **계획서 Round 1~4 (총 68건) + 디자인 문서 Round 3·4 (총 36건) 검토 반영** — plan v4 + engine.design v2 + ui.design v3. **PDF 5,352 vs 엔진 5,351 (1원 차이)**: H-03 floor — 1주당 환산에서 흡수되어 총 29,120,000 정합. 후속 PR 6건: Case 6 거래정지 §165③ / §99①3 양도일 종가 화면 / §81④ 자동 판정 / 환원율 시간별 / stock-transfer-tax.ts 800줄 분할(796줄 한도 근접) / Playwright 자동. 신규 정책 후보 4건 — feedback_pdf_table_row_one_to_one_mapping / feedback_flat_vs_nested_form_field_decision / feedback_multi_year_statement_input_ux / feedback_korean_law_82_vs_81_2_drift.

## 2026-05-17

- **주식 양도세 F-9 — 시장 변경 시 자동 동기화 완료**. F-8 후속 — MarketTypeBlock(부모 컴포넌트 Step1.tsx)의 onChange도 wrapper 패턴 적용. **신규 모듈** `components/calc/stock-transfer/major-sync.ts`(80줄) — `computeAutoIsMajor()` + `withAutoSyncMajor()` 유틸 export. F-8에서 MajorShareholderBlock 내부에 중복 정의되어 있던 `computeAutoIsMajor` 헬퍼를 본 유틸로 이동(DRY). Step1.tsx의 `syncedChange = withAutoSyncMajor(form, onChange)` wrapper로 MarketTypeBlock onChange 감싸 시장 변경 시 `isMajorShareholder` 자동 동기화. 사용자가 코스피→비상장 변경 시 임계값 변화에 따른 ToggleCard 상태 즉시 반영. typecheck 0건 / stock-transfer 179/0. 거동 변경 0(엔진은 자동 우선) — UI 즉시성 향상만. 주식 양도세 도메인 PDCA 사이클 최종 클로즈.

- **주식 양도세 F-8 — 토글 자동 동기화 완료**. F-5 옵션 A(엔진 자동 산출 우선) 정책을 UX에서도 강제 — 사용자가 폼 토글을 클릭하여 자동 산출과 다른 값을 입력하던 경로 차단. **핵심 설계**: 입력 onChange 시점에 wrapper(`handleAutoSyncChange`)가 자동 산출 결과를 함께 갱신(`useEffect → store` 미러링 금지 정책 준수). ToggleCard `onCheckedChange={() => {}}` no-op으로 사용자 클릭 무효화 (상장 3시장 + 비상장). 기타자산(`other_asset`)만 사용자 직접 입력 유지. `computeAutoIsMajor()` 순수 함수 모듈 레벨 배치 — 가상 form 시뮬레이션으로 patch 적용 후 임계 비교. warning 메시지 "⚠ 자동 판정과 폼 토글이 다릅니다" 완전 제거 — 불일치 자체가 발생 불가. MajorShareholderBlock.tsx 226→305줄(800줄 정책 준수). 거동 변경 0 (엔진은 어차피 자동 우선) — UI 일관성 + 사용자 혼란 차단만. typecheck 0건 / stock-transfer 179/0 회귀 0. 후속 PR 잔존: 시장 선택 라디오(부모 컴포넌트) onChange도 wrapper 패턴 적용(별도 후속 PR 검토 가능). 주식 양도세 도메인 PDCA 클로즈 최종.

- **주식 양도세 F-6 + F-4 정정·시각화 완료**. **F-6**: 비상장 매트릭스 부정합 2건 정정 — 2013-01-01 행 지분율 `0.02→0.04` (코스닥 2% 패턴 잘못 차용), 1900-01-01 fallback `0.02→0.05` (§94①3 나목 일반 표준). KoreanLaw MCP `efYd=20260423` 본문 + WebSearch 6건 cross-check. AT-14 검증값 갱신 + AT-16/17 신규 회귀 차단 anchor (2014년·2010년 비상장 3~4% → 비대주주 정정). 영향 사용자: 2020년 이전 양도일 + 비상장 2~4% 지분율 사용자. **F-4**: historical timeline 시각화 신규 `MajorThresholdTimeline.tsx`(147줄) — 매트릭스 4종(KOSPI 7행·KOSDAQ 8행·KONEX 3행·UNLISTED 5행) 엔진 export 단일 진실 import + 시장별 시기 표 + 현행 행 violet highlight + 비상장 전용 벤처 40억 안내 + 법제처 연혁 미확인 경고. `MajorShareholderBlock` `<details>` 펼침으로 통합(+15줄). 기타자산·미지원 시장은 자동 미렌더. anchor 179/0 / typecheck 0건. F-5 후속 PR 잔존 0건 (F-4·F-6 모두 완료) — 주식 양도세 도메인 PDCA 클로즈.

- **주식 양도세 F-5 — 비상장 자동 판정 통합 완료**. 직전 PR(상장 3시장 echo) 후속. `UNLISTED_MAJOR_THRESHOLDS` 매트릭스 신설 (KoreanLaw MCP `§167의8①2호` 검증 — 비상장 시총 임계는 **10억(벤처 40억)** 으로 상장 §157의 50억과 다름). `judgeIsMajorShareholder` 비상장 패스스루 제거 → 자동 산출(옵션 A). 폼 토글-자동산출 불일치 시 `result.warnings` 메시지 + UI 노란 경고 표시. **거동 변경**: `input.isMajorShareholder` 무시, 자동 산출 우선. `getMajorShareholderThreshold`·`resolveThresholdFromDate` 시그니처에 `"unlisted"` 추가. `buildAppliedThreshold` 가드 축소(`other_asset`만 undefined). `StockTransferResult.appliedThreshold.marketType` 에 `"unlisted"` 추가. UI: `MajorShareholderBlock` 비상장 미리보기 활성화 + `§157④` vs `§167의8①2호` 라벨 분기 + 벤처 40억 안내 + ToggleCard description "자동 산출 우선 적용" 명시 + 자동판정 미적용 카드는 `other_asset`만. `MARKET_LABEL` 에 `unlisted: "비상장"` 추가. **% 입력 정상화 동반 정정**: `selfShareRatio`·`combinedShareRatio` 폼 단위를 decimal(0.03)→% (`3`) 로 변경 — API `* 0.01` 정규화, UI 비교 동일 정규화, FieldCard `unit="%"` 표시. 비상장 false positive(`shareRatioThreshold ?? 0` fallback으로 3 ≥ 0 true) 차단 — threshold null 가드 추가. anchor 11→**15건**(AT-12 비상장 비대주주 경계·AT-13 byCap·AT-14 fallback·AT-15 warning). stock-transfer 177/0 / 양도세 1395/0 / typecheck 0건. 후속 F-4(historical timeline)만 잔존. KoreanLaw 비상장 부칙 5개 시기 매트릭스는 미확인 표기 — 법제처 연혁 API 조회 불가 상태.

- **주식 양도세 대주주 임계 echo + UI 단일 진실 완료**. `judgeIsMajorShareholder()`가 시기별·시장별 §157④ 임계를 정확히 산출하지만 `StockTransferResult`에 누락되고, `MajorShareholderBlock.tsx`가 로컬 함수로 임계를 재계산하면서 **코스닥·코넥스 동일 2% 매핑** 등 엔진 진실과 불일치 → 코넥스 3% 비대주주 사용자가 UI 미리보기에서 "대주주 해당" 오표시 회귀 차단. **엔진**: `StockTransferResult.appliedThreshold?` optional 5필드 추가 (shareRatio·marketCap·marketType·priorYearEndDate·fromDate, marketType은 kospi/kosdaq/konex 한정). `buildAppliedThreshold` → `stock-transfer-helpers.ts` + `resolveThresholdFromDate` → `stock-rate-tables.ts` export. 정상 + exempt(`buildExemptResult`) 양 경로 propagation. **UI**: `market-label.ts` 신규 단일 모듈(3종 매핑 입력·결과 공유) + `MajorShareholderBlock` 자체함수 2개(`getMarketCapThreshold`·`getShareRatioThreshold`) + historical 4구간 정적 안내 카드 삭제 → 엔진 함수 import + 동적 박스(현재 적용 임계 §157④ 표시) + `StockTransferTaxResultView` `MajorShareholderResultCard` 서브 + `isMajorTaxCategory` exact 비교(`c === "listed_major" || c === "unlisted_major"`) + `result.isExempt` 시 "→ 비과세 (§94①3 가목 단서)" 보조 라벨. 비상장·기타자산은 `appliedThreshold===undefined` 가드로 카드 미렌더. **anchor 11건**(AT-1~AT-11): AT-3(코넥스 3%·30억 비대주주 UI 버그 차단 핵심) / AT-7(exempt 경로 propagation) / AT-11(코넥스 2013-06-30 fallback `sorted.length-1` 가드) 등. Pre-Do AT-3·AT-7 우선 실패 메시지 확보 후 구현. stock-transfer 173 passed / 부동산 양도세 1395 passed / typecheck 0건 / build 성공(번들 증가 5KB 이하). matchRate 100%. **PDCA 3-round 정정**: Plan v1→v2→v3(범위 축소 — 비상장 자동 판정 통합을 F-5로 분리) / Design v1→v2(AT-2/7 isKOTCTrading 명시·AT-3 isExempt 검증·UI 800줄 산수 정정·MARKET_LABEL 단일 모듈화). **신규 정책 2건**: [[feedback-ui-engine-dual-truth-avoidance]] ★★★ (UI 자체 계산 ↔ 엔진 이력 테이블 단일 진실 강제) + [[feedback-enum-substring-match-forbidden]] ★★★ (enum 분기 substring 매칭 금지). 후속 PR: **F-4** historical timeline 시각화 / **F-5** 비상장 자동 판정 통합 + 폼 토글 `isMajorShareholder` 의미 재정의 + `UNLISTED_MAJOR_THRESHOLDS` 매트릭스 + `legal-codes` §157④ 1호 가/나/다목 라벨 상수. 14지점: ⑤(입력 위젯) + ⑦(결과 카드) + 결과 type echo + 신규 모듈 1개. 미수행 — 브라우저 수동 4종 시나리오 S-1~S-4(사용자 측 `npm run dev` 후 직접 검증 필요).

## 2026-05-15

- **사례 40·41 — 재개발/재건축 APT 양도 + 청산금 불입 (§166②1호) 완료**. 양도코리아 PDF 사례 40·41 「재개발 아파트(청산금 불입): 토지출자 실가 / 재건축 아파트(청산금 불입): 공동주택 출자 환산」. **★ 엔진 변경 0 — Pre-Do 검증으로 현행 인프라가 정확 처리 확인**. 사례 44 환산 인프라(`acquisitionHousingPrice`/`managementDisposalHousingPrice` 단일 D 패턴)가 PDF 2-point(분자=취득당시 공동주택공시가격 120M, 분모=인가일 직전 150M)와 산식 동일(`floor(권리가 × 분자 / 분모)`). LTHD 묶음 해제도 기존 작동(preApproval/postApprovalExistingHouse 묶음 30%·20% vs settlement 별도 18%·12%). floor 잔액 흡수 패턴 이미 적용(settlement.gain = 48,974,094, PDF 48,974,093 +1원). 본 PR 범위: ①anchor 작성 ②UI 라벨 정정 ③validate 차단 가드 해제. **사례 40 anchor 31건**: preApproval 본래 차익 150M / postApprovalExistingHouse 안분 종전 90,025,906 / settlement 안분 청산금 48,974,094 / total.gain 289M / lthd 80,823,107 / taxableIncome 208,176,893 (PDF 208,176,892 +1원) / **산출세액 58,217,219** (양도일 2023 §55 1.5억~3억 38% 누진공제 19,940,000) / 지방세 5,821,721 / **합계 64,038,940**. **사례 41 anchor 30건**: 환산취득가 200M / 개산공제 자동 3.6M / preApproval 46.4M / postApprovalExistingHouse 90,025,906 / settlement 48,974,094 / total.gain 185,400,000 / lthd 33,162,072 / taxableIncome 152,237,928 / **산출세액 36,968,274** (88M~1.5억 35% **누진공제 15,440,000** — 계획서 v1 오기 정정: 15,360,000 → 15,440,000) / 지방세 3,696,827 / **합계 40,665,101**. UI 정정: RedevelopmentBlock 토글 라벨 3건 (출자 자산 "사례 40~43 후속 PR" → "사례 37·40 본 PR" / 양도 대상 / 청산금 방향). validate 정정: `transfer-tax-validate-redev.ts:59` 「토지 출자 + 완공 APT 양도 조합은 후속 PR (사례 40~43)」차단 가드 해제 — `land + apt + pay` 통과 / `land + apt + receive` (사례 42·43) 차단 유지 / `land + right + 실가` 차단 유지 / 환산 모드일 때만 §166③ 분자·분모 입력 검증. 전체 2,915 PASS / 0 FAIL(사례 36·38·39·44~47 회귀 0). 14지점 동기화 0건(엔진 필드 신규 없음 — 사례 44 인터페이스 재사용). 브라우저 수동 검증 완료. **신규 정책 후보**: floor 잔액 가산 패턴(settlement +1원 trade-off — 자기일관성 우선) + 누진공제 정확값 출처 정책(소득세법 §55 88M~1.5억 = 15,440,000 — 외부 PDF/엑셀 추종 금지, memory `feedback_transfer_year_tax_rate` 강화).

- **사례 37 — 조합원입주권 + 토지 출자 + 환산취득가 (§166③) 완료**. 커밋 `f1b74ff`. 양도코리아 PDF 사례 37 「조합원입주권(청산금 불입): 취득실거래가 확인되지 않는 경우」. `originalAssetType="land"` + `subject="right"` 분기 신규. 신규 모듈 `redevelopment-land-contribution.ts`(180줄) — `calcRedevLandContribEstimated()` 순수 함수. 라우터 `runLandContribEstimated()` + `RedevelopmentResult.landContribDetail` echo(8필드). `computeRightLthd` export — 사례 36 mirror 재사용 (별도 LTHD 모듈 불필요). `REDEVELOPMENT.LTHD_RIGHT_TABLE1_ANNOTATION` 상수 추가(§95② 별표2 [비고] 1호). UI: RedevelopmentBlock land 토글 활성화 + LandContribValuationContent 신규(LandPriceLookupField × 2 — Vworld 자동 조회 + 단가×면적) + violet §99①1호 시점 경고 + redev-right-land-pay ColumnMode 3열 분기(합계/인가전/인가후). 800줄 정책 위반 해소: `transfer-tax-api-redev.ts` 분리(helpers 809→674) + `FilingFormTableRowDefs.ts` 분리. **신규 핵심 정책 2건 도출**: (1) `feedback_redev_filing_form_acquisition_inverse` ★★★ — 재개발 합계 취득가액 = 양도가 − 필요경비 − 양도차익 역산 (전 분기 공통, 자기일관성 보장). (2) `feedback_redev_filing_form_holding_period` ★★★ — 각 분기 보유기간 = 표시 일자 차이(LTHD 절사 holdingMonths 아님). subject="right" 인가후 분 = "-". anchor 10건(L37-1~10): 환산취득가 200M / 인가전 차익 97M / 인가후 차익 120M / 합계 217M / 인가전 LTHD 13,580,000 / 인가후 LTHD 0(별표2 [비고] 1호) / 과세표준 200,920,000 / **산출세액 56,409,600** (소법 §55 2023, 1.5억~3억 38% 누진공제 19,940,000) / 지방세 5,640,960 / **총 62,050,560**. 14지점 ①②③④⑤⑦⑧⑫ 완료(⑬⑭ spread 자동). 정정 5건: 누진공제 오기(53,409,600 → 56,409,600)·`localTax→localIncomeTax`·인가후 분 데이터 위치(`r.settlement`)·인가후 분 표시값(양도가 520M/취득가 300M/필요경비 100M)·보유기간 표시(LTHD 84개월 → 정확 7년 6월). 전체 3,057 PASS, 회귀 0건. 14지점 sync-checker matchRate 100%. 후속 PR: L-PAY-ACT(토지+실가)/L-RCV-EST·ACT(토지+청산금 수령)/L-APT-EST(완공APT+토지, 사례 40~43)/redevPreApprovalExpenses 입력 노출/토지기준시가 총액 직접 입력(totalOverride prop 패턴 재시도).

## 2026-05-14

- **사례 36 — 조합원입주권 양도 4분기 통합 PR 완료**. subject="right" 입주권 자체 양도 분기 UI 게이트 해제 + 환산모드 + 청산금 수령 + §89①4호 가목 1세대1입주권 비과세 + 12억 초과 안분 통합 (사례 44~48 후속). 신규 헬퍼 `applyOneRightExemption()` — subject="right" 가드로 사례 47 `applySettlementExemption`과 격리. 트리거 4조건 AND(subject=right + exemptionEligibleAtApproval + housing=0 + right=1). ≤12억 전액 마스킹 / >12억 right 전용 ratio 분기(`oneRightHighValueApplied` 플래그 — 12억 안분 게이트 `isOneHouseSingle`(housing=1) 가정 환류). 신규 1필드만(`priorHouseHoldingMonths`) + 기존 재사용 2필드(`exemptionEligibleAtApproval` 사례 47 / `priorHouseResidenceMonths` 사례 45). `redevelopment-lthd.ts:208` zeroBranch 주석 정정 — §94①2호 + §166①1호·2호 가목 구조 근거. UI: `RedevelopmentRightExemptionSection.tsx` 신규(199줄) — §0-A 안내(sky) + §⑥ 비과세(violet) + **C-1 안전장치 3종**((a) 자동 검증 useMemo / (b) rose 경고 카드 §115 + 국기법 §47의2~5 가산세 리스크 / (c) 토글 ON 시 면책 문구). 800줄 정책: RedevelopmentBlock 755 / RightExemption 199. anchor 53건 100% 통과(CORE-36 11 + A1 3 + A2 9 + A4 5 + A5 17 + 회귀가드 2 + 보강 6). 외부 검토 Pre-Do B-1 국세청 해석례 3건 메타데이터 첨부([64158]·[299260]·[152604]) — 12억 안분 분모 transferPrice 단일 해석 A 일관 지지. PDCA 4차 누적 정정 19건 (1차 엔진 코드 검증 7 / 2차 외부 검토 B·C 3 / 3차 자가재검토 8 / 4차 저심도 보완 4) + 디자인 환류 1건. gap-detector matchRate 100%. 전체 3,001 passed / 177 files / 0 failed. 후속 PR: 36-A3 승계조합원+입주권(§95④ ↔ 사례 48 준공일 충돌) / 36-B1 빈집소규모정비법 §29 / 36-B2 일시적 1입주권+1주택(§89①4호 나목) / 36-B3 5년 이내 단기양도 세율. 커밋 `d4d35b5` + `ad64892`.

## 2026-05-13

- **사례 35 후속-1 — §99-164-10 환산주택가격 완료**. 양도소득세 집행기준 99-164-10 적용 — 주택으로 최초공시 후 상가로 용도변경한 경우 취득가액 불명 시 환산주택가격 = 최초공시주택가격 × (취득당시 합계 ÷ 최초공시 당시 합계) 산식으로 자산별 환산기준시가 override. `general-building-converted-housing.ts` sibling 파일 신규(58줄, 800줄 정책). 4 신규 필드(`hasFirstDisclosure` + 3 std price). `applyConvertedHousingPriceOverride()`가 `buildGeneralBuildingAssetCards` 진입 시 자동 분기 — 미사용 경로 회귀 0. UI: GeneralBuildingBlock §⑦ 내부 sub-ToggleCard + 환산주택가격 미리보기(useMemo 순수). anchor 6개(F1-1 단위 산식·F1-1b 정확값 110M·F1-2 자산별 안분·F1-4/4b 미사용 회귀·F1-5 0 방어). 전체 2,791 passed 회귀 0.

- **사례 35 — 주택을 상가로 용도변경 완료**. 다주택 + 중과배제기간(2022-05-10 ~ 2024-05-09) 양도 시 LTHD 보유기간 기산일을 변경일로 이동(사전법규재산 2022-684·881, 서울행법 2012구단26961). 신규 필드 3개(`houseToCommercialConversion`/`conversionDate`/`wasMultiHouseAtConversion`) — AssetForm은 `gb*` 접두사. `resolveLTHDStartDate()` 순수 함수를 `transfer-tax-finalize.ts`에 배치(800줄 회피). `TransferTaxResult.lthdStartDate: Date` required 추가 (7 result emit 위치 + UI mock 3 동기화). `SURCHARGE_EXCLUSION_WINDOW` 상수 single source of truth(조특법 시행령 §167조의3, 대통령령 제32672호) — `multi-house-surcharge-helpers.ts:613` 치환. PDF anchor 4종(longTermHoldingDeduction=0 / taxBase=397,500,000 / calculatedTax=133,060,000 / localIncomeTax=13,306,000) + 회귀 anchor 35-2/35-3/35-5/35-7 + resolveLTHDStartDate 단위 3 → 15 passed/1 skipped. 양도세 전체 1015 passed 회귀 0. 14지점 동기화 완료. UI: GeneralBuildingBlock §⑦ 섹션(rose tone — fuchsia는 ToggleCardTone 미지원으로 대체) + 미리보기 카드(useMemo 순수). 후속: 환산취득가 §99-164-10 분기·중과 적용 케이스(35-6 skip)·세대원 자동 판정.

## 2026-05-12

- **부담부증여 Phase 3 — 증여세 통합 완료**. 무상이전분(C−B) 증여세 동시 산출. UI 4필드(관계·세대생략·미성년·신고기한) + Zod + 사이드바 giftFinalTax + anchor 9개(P3-1~4). 전체 2,757/2,758 통과. 후속: 사전증여 합산·외국납부세액공제·F-3 giftTax anchor.
- **부담부증여 Phase 2 — 메뉴 재설계 + 전 부동산 확장**. `transferType` 신설로 부담부증여를 양도 사건으로 분리. propertyType 4종(housing·land·building·general_building). TransferModeBlock 신규 + 사이드바 burdenedGift 메타. 케이스 5-a/12 후속 PR 차단. D-0-2 12억 분모 해석 B 확정. 14지점 sync-checker → ⑬ body.transferType 누락 즉시 수정. 전체 2,738/2,739.

## 2026-05-11

- **일반건물 4가지 조합 확장 (쌍방+쌍방·일방+쌍방·일방+일방)**. `extensionInfo.acquisitionMode` enum + `actualAcquisitionPrice/Expenses`. Step 2/3 분기. UI 서브 라디오 2옵션 + 모드별 필드 + 미리보기 카드 4분기. anchor 68개(§55 누진세율표 자가검증). 사례 31·32·33 회귀 0. 전체 934/935.
- **사례 33 UX 개선 — "쌍방+일방" 라디오 패턴**. acquisitionMethod 4번째 옵션 시각 표시 전용. 기존 3 boolean 조합 매핑으로 백엔드 변경 0. 라벨/hint 동적 + 시나리오 가이드 카드 + 안분 미리보기 카드. 변경 4파일(+158줄). 87/87 anchor 회귀 0.
- **사례 33 증축 건물의 취득 실거래가 환산**. `extensionInfo` 서브객체 5필드 + `actualBundled*` 2필드. `general-building-extension.ts`(299줄) 분리 — 양도가 3-way 안분(§166⑥) + 일괄 취득가 2-way 안분 + 건물2 환산(§176의2②) + 카드 3장. anchor 25개 toBe. 866/867 통과(회귀 0).
- **일반건물 다른 피상속인/증여자 분리 필드**. `buildingDecedent/buildingDonorAcquisitionDate?` 추가, 우선순위 fallback. 비파괴 확장. anchor 9개. 전체 2,589/2,589.
- **일반건물 #7-b 토지 증여이월과세 + 건물 신축 cross-cutting**. §97의2 + §114조의2 cross-cutting. `landCarryoverTaxation` 파이프라인 신규. aggregate→단건 spread로 비교과세 자동 작동. anchor 16개. 전체 2,580/2,580. **분리 PR 후속 표 4건(#4-a·#6·#7-a·#7-b) 모두 완료**.
- **일반건물 #7-a 토지 증여 + 건물 신축 회귀 보호 anchor**. donor 인프라는 #4-a 선행. 엔진 변경 0. anchor 18개. 전체 2,564/2,564.
- **일반건물 #6 토지·건물 모두 상속 회귀 보호 anchor**. 건물 카드 inheritance 보조 필드 매핑. 건물 LTHD 14년 28% = 8,179,704, 가산세 0. anchor 17개. 전체 2,546/2,546.

## 2026-05-10

- **일반건물 #4-a 토지 상속 + 건물 신축 회귀 보호 anchor**. `AssetCardForAggregate`/`buildProperties`에 토지 inheritance 보조 필드 매핑. 사례 32 결과 보존(가산세 13,300,202). anchor 17개. 전체 2,529/2,529.
- **일반건물 토지·건물 취득원인 분리 UX PR**. `gbIsSelfBuilt` boolean 폐지 → 토지·건물 각각 독립 `acquisitionCause` enum. A안(legacy 자동 폐기). anchor +13(2,499→2,512). Playwright 10/10 PASS. **다음 PR 신호**: `transfer-tax-validate.ts` 776줄 — +25줄 시 도메인 분할 선행.
- **신축 건물 단기양도 §114조의2 5% 가산세 사례 32**. 토지·건물 취득일 분리(2008/2018) + `gbIsSelfBuilt`/`gbBuildingAcquisitionDate` 2필드로 §114조의2 ① 5% 가산세(13,300,202) 발동. anchor 28개 + 회귀 51개 + 전체 2,497개. 후속 PR 4건 모두 완료(§176의2 ②정정·toOptionalDate·penaltyBase 승격·addYears).

## 2026-05-08

- **상업용건물·오피스텔 환산취득가 사례 29 + 일반건물 일괄 환산취득가 사례 31**. 신규 `propertyType: "general_building"` + `lib/tax-engine/general-building-valuation.ts`(382줄, §166⑥ + §176의2④ + §163⑥ + §102② 1차 통산 위임). 양도시 건물기준시가 잠금값 20,629,440(BigInt 손계산 함정 주의). anchor 38개 toBe. 2,233/2,233 회귀 보존.

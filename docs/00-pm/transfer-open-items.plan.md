# 양도소득세 — 미결 항목 구현 계획서

**작성**: 2026-07-28 · **기준 master**: `0253fc30` · **선행 완료**: PR #845·#846·#847·#849·#851·#852·#854·#855·#856·#857

이번 세션에서 **확인은 됐으나 처리하지 않은** 항목을 실측 근거와 함께 정리한다.
각 항목은 "왜 지금 안 했는가"를 명시한다 — 미룬 이유가 사라지면 그때가 착수 시점이다.

---

## 0. 우선순위 요약

| # | 항목 | 사용자 영향 | 규모 | 블로커 | 권고 |
|---|---|---|---|---|---|
| ~~R1-a~~ | ~~평가·판정 10종~~ | — | — | — | **✅ 완료 (PR 후속)** |
| ~~R1-b~~ | ~~`splitDetail`·`pre1990LandValuationDetail`~~ | — | — | — | **✅ 완료** |
| ~~R2~~ | ~~초과부담부 가드 구조적 미발동~~ | — | — | — | **✅ 종결 — 가드 정상, 별건 §63 결함 수정** |
| ~~R3~~ | ~~§66 담보채권액 지분 대응분 해석~~ | — | — | — | **✅ 종결 — 현행 정의 유지** |
| R4 | 지분 부담부증여 정식 지원 | 기능 부재 (차단됨) | 대 | — | 🛑 **보류 확정 (2026-07-29 사용자 결정)** |
| R5 | 800줄 초과 파일 9개 → **8개** | 없음 (유지보수) | 중 | 없음 | **부분 완료 — 나머지는 기회주의적 유지** |
| ~~R6~~ | ~~PR #737 집합건물 전유면적~~ | — | — | — | **✅ 실측 완료 + 결함 3건 수정 + 머지** |
| R7 | #591 양도세 감사 백로그 | 미상 | 대 | 항목별 재검증 | **진행 중 — 99 → 6건** |

---

## R1. 일괄(bundled) 결과 상세 카드 — 감면 외 12종

### 현황

PR #857로 **감면·취득가액 24종**을 복구했다(집계 Detail 4 → 25 — 계약 24종 + `penaltyDetail`, 순증 21). 같은 패턴으로 나머지를 넓힌다.

**미전달 16종 중 실제 대상은 12종**이다:

| 제외 | 사유 |
|---|---|
| `mixedUseDetail` · `redevelopmentDetail` · `generalBuildingValuationDetail` | 해당 자산이 **일괄에서 차단**됨(PR #854) → 도달 불가 |
| `amendmentDetail` | 집계 최상위에 **이미 존재**(`transfer-aggregate.types.ts:313`) |

**대상 12종** — 단건 결과뷰 참조 수(= UI 추출 난이도 지표):

| Detail | 단건뷰 참조 | 성격 |
|---|---|---|
| `commercialBuildingValuationDetail` | 2 | 상가 환산 §164⑥ (실측으로 소실 확인) |
| `nonBusinessLandJudgmentDetail` | 2 | 비사업용토지 판정 |
| `multiHouseSurchargeDetail` | 2 | 다주택 중과 |
| `expropriationValuationDetail` | 2 | 공익수용 §164⑨ |
| `housingExpropriationValuationDetail` | 2 | 주택 수용 평가 |
| `auctionValuationDetail` | 2 | 경매 평가 |
| `preHousingDisclosureDetail` | 2 | PHD §164⑤ |
| `rentalHousingExceptionDetail` | 2 | 임대주택 비과세 §155⑳ |
| `familyBusinessDetail` | 2 | 가업상속 §97의2④ |
| `carryoverTaxationDetail` | 3 | 이월과세 §97의2 |
| **`pre1990LandValuationDetail`** | **13** | 1990 토지등급 환산 — 인라인 다수 |
| **`splitDetail`** | **28** | 토지·건물 분리 — 인라인 다수 |

### 방안 — PR #857 패턴 재사용

1. `TransferValuationDetailSource` 계약 타입 신설(`transfer-result.types.ts`)
2. `PerPropertyBreakdown extends` 에 추가
3. `pickValuationDetails()` 단일 주입 지점(`transfer-tax-aggregate.ts`)
4. `BundledAllocationCard`의 `PropertyCard`에서 렌더

**2단 분할 권고**:

- **R1-a (10종, 참조 2~3)**: 각 Detail이 전용 카드 컴포넌트를 이미 가지므로 **거의 배관만**.
  `ReductionDetailCards`처럼 묶음 컴포넌트를 하나 더 만들어 단건·일괄이 공유한다.
- **R1-b (`splitDetail`·`pre1990LandValuationDetail`)**: 단건뷰에 인라인 렌더가 각각
  28·13곳이라 **컴포넌트 추출이 선행**돼야 한다. 추출 자체가 단건뷰 리팩터링이므로 별건.

### 왜 지금 안 했는가

사용자 요청이 "감면 상세부터"였다. 나머지는 **세액에 영향이 없고**(계산은 정상 — anchor로 고정)
배관이 이미 깔려 확장 비용이 낮아졌다.

### 검증 기준

- 계약 타입 ↔ `pickValuationDetails` **1:1 동기화 소스 가드**(PR #857과 동일 — 타입만 넓히고
  헬퍼를 빠뜨리면 값이 조용히 빈다)
- Detail별 행위 anchor 1건씩(라우트 하네스)
- E2E 1건(화면 노출)
- **복원 검증**: 헬퍼를 되돌리면 정확히 실패

---

## R2. 초과부담부 가드가 구조적으로 발동하지 않는다 — ✅ 조사 종결 (가드는 정상 · 별건 결함 1건 수정)

### 조사 결론 (2026-07-28, KoreanLaw 실측)

**가드는 수정 대상이 아니다.** 아래 조문 실측으로 "거의 안 걸리는 것이 정상"이 확정됐다.

| 조문 | 실측 내용 |
|---|---|
| 상증법 §66 | 담보 기준 평가액과 §60 평가액 중 **큰 금액** |
| 상증령 §63①3호 | 근저당 설정 재산 = "평가기준일 현재 당해 재산이 담보하는 **채권액**" |
| 상증령 §63② 전단 | 채권최고액이 채권액보다 **적을** 때만 채권최고액 |
| 상증령 §63② 후단 | 다수 채권(전세금·임차보증금 포함) 담보 시 **합계액** |

⇒ 담보평가 = 임대보증금 + `min(채권최고액, 담보채권액)` ≤ 인수채무액이고,
설정액 ≥ 채무액인 통상의 경우 **정확히 채무액과 같다**. §66의 max가 이를 하한으로 삼으므로
`C ≥ B`가 구조적으로 성립한다. 가드가 유효한 창은 §63② 전단(설정액 < 채무액)뿐이며,
anchor B11이 이미 그 구간을 고정하고 있다.

### 함께 발견된 실제 결함 — 구법 규칙 잔존 (수정 완료)

종전 구현은 `mortgageSetAmount ?? mortgageDebtAmount`로 **설정액을 무조건 우선**했다.
이는 **구** 상속세법 시행령 §5의2 3호(근저당 = **채권최고액**)의 규칙이다.
그 시기 조세심판례(국심1997부0752·국심1994서3198 등 1989~1997년)가 다수 검색되어
현행 근거로 오인하기 쉽다 — 심판례 [325252]가 구 시행령 문언을 명시 인용해 드리프트를 증명한다.

설정액은 통상 채권액의 120%이므로 구법 규칙 적용 시:
증여가액 C 과대 → §159 채무비율 B/C 과소 → 취득가액 과소 → **양도차익 과대(납세자 불리)**.
법령 근거 없는 불리 적용이므로 `feedback_no_unfavorable_application_without_legal_basis` 위반.

**수정**: `computeMortgageValuation()` 단일 산정 지점 신설(`burdened-gift-apportionment.ts`).
`computeSangjeungbeopValuation`·`assertBurdenedGiftEligible` 두 중복 산정부를 이 함수로 통일.
anchor `burdened-gift-63-secured-claim.anchor.test.ts` 9건 + 복원 검증(되돌리면 5건 실패).

### (기록) 조사 전 현황

`assertBurdenedGiftEligible`(`burdened-gift-apportionment.ts`)이 `채무액 > 증여가액`을 차단하는데,
`mortgageSetAmount` 미입력 시 `mortgageDebtAmount`로 fallback되어 **담보평가 = 채무액**이 된다.
→ `giftValuation = max(..., 담보평가)` ≥ 채무액이 **항상 성립**해 가드가 걸릴 수 없다.
설정액 < 실제 잔액인 경우에만 발동한다(anchor B11이 그 구간을 고정).

### 선행 과제 — 가드의 의도 확정

**법령상 이게 정상일 수 있다.** 상증법 §66은 저당권 설정 재산을 "담보채권액과 시가 중 **큰 금액**"으로
평가하므로, 채무액이 평가액의 **하한**이 되는 구조가 조문 취지와 맞을 여지가 있다.
그렇다면 가드는 "거의 안 걸리는 것이 정상"이고 **수정 대상이 아니다**.

→ **KoreanLaw로 §66·상증령 §63 위임 체인을 먼저 확인**한 뒤 판단한다.
코드부터 고치면 법령에 없는 제약을 만들 위험이 있다(`feedback_no_unfavorable_application_without_legal_basis`).

### 왜 지금 안 했는가

본 세션 이전부터 있던 성질이고, 의도 확인 없이 손대면 **잘못된 차단**을 만든다.
상가를 근거 없이 막지 않은 것과 같은 판단이다.

---

## R3. §66 담보채권액의 지분 대응분 해석 (조사) — ✅ 조사 종결 (현행 정의 유지)

### 확보한 해석례

**조심2013서0051** (2013.12.06, 조세심판원 재결 ID 102460) — 1/7 공유지분 취득분에 대한
임대보증금 채무의 부담부증여 여부. 주문이 명시적이다:

> "청구인이 실제로 임대보증금 채무를 인수하였는지 여부 등을 재조사하고, 그 결과에 따라
> **증여재산가액과 임대보증금 채무액 중에서 청구인 지분(1/7)가액을 산출하여**
> 각 증여세 과세표준과 세액을 경정한다."

보강: 국심1998서2062·국심1998서2071(둘 다 **취소**) — 전세보증금 채무액 중 일정부분을
과세관청이 임의로 부담부증여로 본 처분이 뒤집혔다. 국심1994중5434(기각)도 같은 축.

### 결론 — 현행 정의가 맞다

두 축이 함께 읽힌다: **① 실제 인수 여부가 우선**(상증법 §47③, 위 재결이 "실제로 인수하였는지
재조사"를 주문의 조건으로 세움) → **② 인수가 인정될 때 지분 상당액으로 산출**.

현행 구현은 **사용자가 해당 지분의 인수분을 직접 입력**하는 정의다. 이는 ①과 정확히 맞다 —
엔진이 채무를 ×지분율로 자동 분할하면 실제 인수 사실과 무관한 값을 만들고, 과세관청의 임의
안분이 뒤집힌 국심1998서2062의 구도를 코드가 재현하는 셈이다. 자동 안분 fallback 금지 정책과도
같은 방향이다(`feedback_no_silent_apportion_fallback`).

⇒ **UI hint·validate 변경 없음.** 조사 착수 조건이었던 "결과가 현재 정의와 다르면"이 성립하지 않는다.
R4(지분 부담부증여 UI 개방)를 진행하게 되면, hint 문구는 "지분 상당액"이 아니라
**"실제로 인수하는 채무액"**으로 쓸 것 — 위 재결의 우선순위를 반영해야 한다.

---

## R4. 지분 부담부증여 정식 지원 — 🛑 보류 확정 (2026-07-29)

> **결정**: 사용자가 "공유지분 부담부증여는 굳이 복잡하게 수정 안 해도 되니 보류"로 판정했다.
> 대기 중인 제품 결정이 **났다** — 다음 세션에서 다시 후보로 올리지 말 것.
> 현행 차단(단건 `validate-asset.ts:638` · fullFractional `validate.ts:83`)을 **정상 상태로 유지**한다.
>
> 재개 조건: 실제 사용 요청이 들어올 때만. 아래 분석은 그때를 위해 보존한다.

### 현황

- **엔진은 준비됨** — PR #851이 §159의 A·C 지분 축소를 구현하고 23건 anchor로 고정
- **UI는 차단 중** — 단건 지분(`validate-asset.ts:638`) · fullFractional(`validate.ts:86`)

즉 **정확히 계산할 수 있는데 입구가 닫혀 있다.**

### 열려면 필요한 것

지분 분할 모드의 양도가액 모델은 `총계약가 × 지분율`인데, 부담부증여는 `양도가액 = 인수채무액`이라
**두 모델이 비양립**이다(`validate.ts:85` 주석이 이미 지적). 지분 분할 UI에서 §159 경로를
별도 분기로 태우는 재설계가 필요하다.

### 판단 근거

**수요 확인이 선행**돼야 한다. 공유지분을 부담부증여하는 실사용 빈도가 낮다면
현행 차단이 합리적이다(잘못 계산하느니 막는 것이 낫다).

### R2·R3 이후 갱신 (2026-07-28)

두 조사가 R4의 **불확실성을 줄였다** — 열기로 하면 설계가 이전보다 명확하다.

- R3에서 확인된 우선순위(**실제 인수 여부 우선 → 인정 시 지분 상당액**)에 따라
  채무 입력 hint는 "지분 상당액"이 아니라 **"실제로 인수하는 채무액"**으로 쓴다.
  엔진이 ×지분율로 자동 분할해서는 안 된다는 근거도 함께 확보됐다(국심1998서2062 취소례).
- R2에서 담보평가가 `min(설정액, 채권액)`으로 정정돼, 지분 축소 시에도
  **C ≥ B 불변식이 그대로 성립**한다(anchor로 고정). 지분 모드에서 초과부담부 오판 위험이 없다.

여전히 남은 진짜 블로커는 **양도가액 모델 비양립** 하나다(`총계약가 × 지분율` vs `= 인수채무액`).
이건 법령 문제가 아니라 UI 설계 문제이며, **수요가 확인되면** 착수할 수 있다.
수요 미확인 상태에서 대규모 UI 재설계를 하는 것은 추측성 기능이다(Simplicity First).

---

## R5. 800줄 정책 초과 파일 9개

| 파일 | 줄수 |
|---|---|
| `lib/tax-engine/types/inheritance-gift-estate.types.ts` | 1,059 |
| `lib/tax-engine/property-valuation.ts` | 1,025 |
| `lib/tax-engine/legal-codes/transfer.ts` | 840 |
| `lib/tax-engine/general-building-valuation.ts` | 833 |
| `lib/tax-engine/inheritance-tax.ts` | 831 |
| `lib/tax-engine/deductions/inheritance-deductions.ts` | 815 |
| `lib/tax-engine/types/transfer-redevelopment.types.ts` | 803 |
| `lib/tax-engine/transfer-tax.ts` | 803 |
| `lib/api/transfer-tax-schema-sub.ts` | 801 |

**타입 전용 파일 2건**(`*-estate.types.ts` · `transfer-redevelopment.types.ts`)과
상수 파일(`legal-codes/transfer.ts`)은 CLAUDE.md 예외 조항 대상 — 분리 가치가 낮다.

**나머지 6건 중 `property-valuation.ts`(1,025)만 처리했다 (2026-07-28).**

판단 근거 — 나머지 5건은 **801~840 밴드**로 트리거를 1~40줄 넘는다. 이 구간을 쪼개면
CLAUDE.md가 경고한 재분리 thrash(`transfer-tax.ts` 800 직하 착지 → 801 재초과)를 그대로 반복한다.
반면 `property-valuation.ts`는 트리거를 **28% 초과**했고 상속·증여가 공유하는 파일이라
방치 시 회귀 위험이 가장 크다 — 여기서만 분리 전용 작업의 고정비가 정당화된다.

**분할 결과 1,025 → 653줄** (≤700 착지, 데드밴드 확보):

| 파일 | 줄수 | 도메인 |
|---|---|---|
| `property-valuation.ts` | 653 | 공통 헬퍼·부동산·임대환산·디스패처 |
| `property-valuation-special-rights.ts` | 263 | 임대차 환산 §61 · 지상권 §51 · 무체재산권 §64 |
| `property-valuation-financial.ts` | 188 | 현금 §60 · 금융재산 §63 · 가상자산 §65 |

이음매 선정은 **공통 헬퍼 공유 여부를 실측**해서 정했다 — 두 블록 모두
`calcVacantPortionStandardPrice`·`COLLATERAL_DEBT_NOTICE`·`resolveValuationMethod` 참조가 **0건**이라
줄 수를 맞추려는 억지 조각화가 아니라 자연 경계다. **평가 로직은 한 줄도 바뀌지 않았다(순수 이동)**.

공개 API는 `property-valuation.ts`가 전량 re-export해 **기존 import 경로가 유지**된다
(memory `feedback_800line_split_export_preservation`). 전체 11,964 통과·tsc 0.

---

## R6. PR #737 — 집합건물 세대 전유+공용 연면적 자동조회 — ✅ 완료 (머지됨)

### 블로커 해소 — "배포환경 없이 풀 수 없다"는 판단이 틀렸다

종전 기록은 국토부 API가 트래픽 차단 상태라 실 응답을 못 구했다고 적혀 있었다.
2026-07-28 **로컬 `.env.local`의 `MOLIT_RTMS_API_KEY`로 재시도하니 정상 응답**했다.
막힌 것은 환경이 아니라 그 시점의 일시적 차단이었다 — 환경 블로커로 분류된 항목은
**주기적으로 재시도해 볼 값어치가 있다**는 교훈.

### 실측 (은마아파트 `sigunguCd=11680·bjdongCd=10600·bun=0316·ji=0000`)

| 쿼리 | totalCount |
|---|---|
| `dongNm=1` + `hoNm=1410호` | **5** (전유 1행 95.18 + 공용 4행 7.32 = 102.5㎡) |
| `hoNm=1410` (접미사 없음) | 0 |
| `dongNm=1동` / `제1동` | 0 |
| 필터 없음 | 24,066 (그중 **100건만** 반환) |

### 실측이 드러낸 결함 3건 (전부 수정 후 머지)

1. **필터 형식 불일치 — 기능이 죽어 있었다.** 서버 필터는 정확 문자열 일치인데
   폼은 `unitDong="201동"`·`unitHo="3204"`를 그대로 보냈다. 둘 다 0건을 부르므로
   배포했더라도 **항상 수동 fallback**으로 떨어졌을 것이다.
   → `toExposQueryDong`(접미 "동" 제거)·`toExposQueryHo`(접미 "호" **부착**) 신설.
   두 변환의 **비대칭이 실 API의 사실**이라 anchor로 못박았다.
2. **`numOfRows=1000`이 무효** — 서버가 100에서 캡한다. → `EXPOS_MAX_ROWS=100`.
3. **부분 합산 위험** — 필터가 듣지 않으면 24,066건 중 100건만으로 합산해
   "그럴듯하게 작은" 연면적을 무경고로 채운다(기준시가 과소산정).
   → `totalCount > items.length` **절단 가드**로 null 반환(수동 입력 유도).

추가로 동 명칭 드리프트("가"·"A" 등) 대비 **호 단독 1회 재시도**를 넣었다 —
클라이언트 재필터가 동을 다시 대조하므로 안전하다.

anchor 12건 신설(`building-register-expos-query-format.anchor.test.ts`). 전체 11,964 통과.

---

## R7. #591 양도세 감사 백로그 — 진행 중 (99 → 88)

### 재착수 방식 확정 (2026-07-29)

브랜치 `review/transfer-tax`는 **베이스가 2026-07-14, master 대비 504커밋 뒤처져** 있고
테스트뿐 아니라 **엔진 수정 74파일 3,776삽입**을 함께 들고 있다. 통째로 머지하면
그 사이 master가 다르게 고친 부분을 되돌릴 위험이 있다.

⇒ **감사 테스트 26개 파일만 현재 master에 얹어** 무엇이 실제로 실패하는지부터 실측했다.
결과는 **99 실패 / 59 통과 (158건)** — 기록된 99건과 정확히 일치했고, master가 그중
어느 것도 고치지 않았음이 확인됐다.

### 검증 프로토콜 (매 항목 공통)

1. 감사가 인용한 **file:line이 아직 유효한지** 먼저 확인한다. 3개월 드리프트로 죽은 인용이 많다.
   (실례: "route.ts:756이 net + 감면을 주입한다" → 현재 `calculateFilingPenalty` 호출부는
   `transfer-tax-amendment.ts` 한 곳뿐이고 `reductionAmount: 0`을 넘긴다.)
2. **KoreanLaw 원문**으로 판정한다. 감사 기대값도 master 구현도 근거가 아니다.
3. 기존 테스트와 충돌하면 **조문이 이긴다**. 뒤집은 기대값에는 왜 종전이 틀렸는지를 남긴다.
4. 감사 기대값이 **낡았을 수 있다** — master가 이미 다르게(맞게) 고쳐 둔 경우가 실제로 있다.
5. 통과하지 못한 감사 파일은 **master에 넣지 않는다**(pre-push 전체 테스트가 게이트).
   워킹 트리에서 빼고 `review/transfer-tax`에 남겨 둔다.

### 종결 1 — 신고불성실가산세 감면 이중차감 (4건) ✅

`transfer-tax-penalty.ts`의 `penaltyBase`가 `reductionAmount`를 재차감했다.
**국세기본법 §47의3①** 기준은 "과소신고한 **납부세액**"이고 조문이 기준금액에서 제외하라고
명시한 것은 **가산세와 이자 상당 가산액뿐**이다. 납부세액은 감면 반영 후 금액이므로
감면은 이미 1회 반영돼 있다. `FilingPenaltyInput.determinedTax` 주석도 "감면 적용 후"라고
명시하고 있었다 — **주석과 구현이 어긋난 상태**였다.

- 현행 유일 호출자가 `reductionAmount: 0`을 넘겨 **오늘 발동하지는 않았다**(잠재 함정).
- 기존 `T10`이 옛 동작(8,000,000)을 고정하고 있어 **조문 근거로 뒤집었다** — 종전 T10에는
  조문 인용 없이 산식만 적혀 있었다.

### 종결 2 — 상증법 조문 인용 정정 (7건) ✅

| 지적 | 현행 master | 조치 |
|---|---|---|
| `inh_public_interest.lawRef` §48① → §16① | **여전히 §48①** — 상수 `INH_PUBLIC_CONTRIBUTION`(§16①)은 이미 있는데 rule만 미교체 | 수정 |
| `§46` 이재구호금품 2호 → **5호** | 여전히 2호(=우리사주조합 취득이익) | 수정 |
| `§46` 국가유공자 6호 → **9호** | 여전히 6호(=신용보증기금 등). 내용도 "보훈급여금"으로 어긋나 있었다(9호는 **유족이 증여받은 성금·물품**) | 수정 |
| 장애인 신탁 5억 한도 근거 | 본조 §52의2 → **§52의2③**("합산한 금액은 5억원을 한도로 한다") | 상수 신설 |
| `gift_public_trust` → §52 | **이미 §52**(`PUBLIC_TRUST_GIFT`) | 감사 기대값이 낡음 — 상수명만 정정 |
| `gift_disabled_trust` → §52의2 | **이미 §52의2**(`DISABLED_TRUST`) | 동일 |

전부 인용·표시 정정이며 **불산입액·세액 산출은 불변**(anchor로 고정).

### 종결 3 — 재개발 클러스터 (11건) ✅

세 갈래였고 **한 갈래만 세액 변경**이다.

**(a) §166①2호 나목 "또는"을 "및"으로 오구현 — 세액 변경 (3건)**

`redevelopment-housing-contribution.ts`가 환산 모드 인가전 양도차익에서 §163⑥ 개산공제에 더해
실제 인가전 필요경비까지 **합산 차감**해 양도차익·세액이 과소했다. 조문 문언이 결정적이다:

> §166①2호 나목: "… − 법 제97조제1항제2호 및 제3호 **또는** 제163조제6항에 따른 필요경비"

"**또는**"이므로 택일이다 — §166③ 환산취득가를 쓴 경로는 §163⑥ 개산공제 쪽을 택하고
실제 자본적지출·양도비는 별도 가산하지 않는다(§97②2호 가목·나목 택일=max와 같은 구조).
대비로 같은 호 **가목**(인가후)은 §163⑥ 병기가 없어 실제 필요경비만 차감 — 무변경이 맞다.

사례 39: 97,000,000 (종전 88,666,666).

**(b) 신고서 표시 필요경비 합산 (2건)** — 같은 "또는" 위반이 표시 경로 3곳에 있었다.
`preApprovalNecessaryExpense()` 헬퍼로 단일화(환산이면 개산공제, 실가면 실제 필요경비).

**(c) 분기 보유기간 연단위 절사 (4건)** — `lthdHoldingYears × 12`로 재구성해 **잔여월·일수가 소실**
(2007-04-09 → 2014-10-23이 84개월로 표시, 실제 90개월 13일). LTHD 공제율은 만년수만 쓰므로
세액은 맞았고 **신고서 표시만 틀렸다**. 전 세목 공통 `calculateHoldingPeriod`로 교체 —
이 헬퍼가 정확히 `{years:7, months:6, days:13}`을 반환해 감사 기대값과 일치했다(실측).
코드 주석이 이미 "연단위 절사 — 월단위 재산출 필요"라고 스스로 인지하고 있었다.

**(d) 인가전 양도차익 산식에 개산공제 항 누락 (2건)** — `amount`는 정확한데 표시 산식이
개산공제 항을 빠뜨려 산술 결과가 어긋났다(219,218,500 − 141,221,534 − 0 = 77,996,966 ≠ 75,445,917).

### 종결 4 — 다건 집계 (5건) ✅

**(a) 분양권 2년+ 세율군 오분류 — 세액 변경 (4건)**

§104①1호는 "제55조제1항에 따른 세율(**분양권의 경우에는 양도소득 과세표준의 100분의 60**)"이다.
즉 분양권은 **보유기간과 무관하게** 단일세율이다 — 1년 미만 70%(3호) · 1~2년 60%(2호) ·
**2년 이상 60%(1호 괄호)**. 그런데 `classifyRateGroup`의 24개월 게이트를 빠져나간 2년 이상
분양권이 `progressive`로 분류돼, §104⑤2호 그룹 합산에서 누진 자산과 한 그룹이 되고
**그룹 대표세율(60%)이 합산 과세표준 전체에 적용**됐다.

실측: 분양권 차익 3억 + 사업용 토지 차익 2억
→ 올바른 §104⑤ = MAX(합산누진 174,060,000, 자산별합 236,060,000) = **236,060,000**
→ 종전 산출 **300,000,000** (= 5억 × 60%). **63,940,000 과세 초과**.

조합원입주권은 대상이 **아니다** — 1호 괄호가 분양권만 지목하고 2호·3호는 2년 미만 전용이라
2년 이상은 §55① 누진이 맞다. 분양권만 좁게 분기했다.

**(b) 감면 배분 floor 드리프트 (1건)** — 같은 감면 유형 자산들이 각각 독립 floor돼
Σ배분액이 `cappedAggregateReduction`과 최대 (n−1)원 어긋났다. 화면에 "감면 합계"와
"자산별 감면"이 나란히 나오므로 1원도 자기모순으로 보인다. 배분을 map 이전에 선계산하고
**말단 자산이 잔액을 흡수**하게 했다(`feedback_floor_residual_absorption`). 총액 불변 → 세액 불변.

### 종결 5 — 조특법 §99의3 (6건) ✅

**(a) 부동소수 1원 과소산정** — `Math.floor(transferIncome × (numerator/denominator))`.
종전 주석은 "분자·분모 정수 → BigInt 우회 불필요"라고 단정했으나 문제는 크기가 아니라
**중간 비율이 부동소수**라는 점이었다: 70,000,000 ÷ 100,000,000 = 0.7이 2진수로 정확하지 않아
700,000,000 × 0.7 = 489,999,999.99999994 → floor 489,999,999.
`safeMultiplyThenDivide`(곱셈 선행)로 정확값 490,000,000.

**(b) 양도소득금액 상한 클램프 누락** — 5년시점 기준시가 > 양도시 기준시가면 ratio > 1이 되어
감면 대상 소득금액이 실제 양도소득금액을 초과했다. 조특법 §99의3① "5년간 발생한 양도소득금액"
상한이며, **형제 조문이 이미 같은 상한을 둔다**(`new-99.ts:267` · `unsold-98-8.ts:302`) —
내부 정합과 조문 근거가 함께 성립한다.

감사 fixture의 `transferPrice`는 `New993Input`에 없는 필드였다 → `wholePropertyTransferPrice`로 정정.

### 종결 6 — 주식 계열 (32건) ✅

**(a) lot 배분 경로가 정본 세율 함수를 재구현하며 드리프트 (21건) — 세액 변경**

`lot-allocation-tax.ts`가 `applyStockTaxRate`(정본)의 부분집합을 다시 구현해 두 갈래가 틀렸다.

- **중소기업 대주주가 "비대주주" 분기로 떨어졌다.** §104①11 가목 1)의 단기 30%는
  "**중소기업 외**의 법인의 주식등"에만 적용되므로, 중소기업 대주주도 가목이고 2) 누진
  (3억 이하 20% / 초과 25%)이 맞다. 과세표준 5억: 100,000,000 → **110,000,000**.
- **기타자산(`other_asset_*`)이 20% 단일세율**로 계산됐다 → §55 8단계 누진이 맞다.
  과세표준 5천만: 10,000,000 → **6,240,000**(과대였음) / 10억: → **384,060,000**(과소였음).
- **sub-lot마다 누진을 따로 적용**해 3억 구간이 lot 수만큼 반복됐다.
  장기 2 lot·과세표준 4.5억: per-lot 90,000,000 → 집계 **97,500,000**.
  §104⑤2호 단서가 "동일한 호의 세율이 적용되고 그 적용세율이 둘 이상인 경우 **합산**"으로 같은 취지다.

정정은 **재구현 제거 + 정본 위임**이다(`feedback_ui_engine_dual_truth_avoidance`).
단기/장기를 그룹으로 묶어 각각 `applyStockTaxRate`에 넘기고, 안분 잔액은 장기 그룹이 흡수한다.
전량 장기이면 장기 그룹 = 전체라 **단건 경로와 정확히 일치**한다(감사 15건이 이 정합을 고정).

**(b) 주식 그룹 기본공제 한도 초과 (8건) — 세액 변경**

§103②2호 주식 그룹 기본공제는 연 250만원 한도인데, 앞 종목이 한도를 **일부만** 쓴 경우
뒤 종목이 250만원을 다시 공제받았다. 원인은 분기 조건이 `deductThis > 0`이면 무조건
엔진 전량 재계산으로 보냈고, 순수 엔진 `calcBasicDeduction`은 주식 그룹에 **항상**
`min(income, 2,500,000)`을 적용하기 때문이다.
실측: 종목A 1,000,000 사용 → 종목B가 잔여 1,500,000이 아니라 2,500,000 공제 → 그룹 합계
3,500,000(한도 초과) → 과세표준·세액 과소. 0/전액 두 갈래를 없애고 **항상 정확한 잔여액으로 패치**.

**(c) 비상장 평가 — 같은 함수 안에서 서로 다른 법 적용 (3건)**

`acq_face_value_only` 분기만 현행 3:2와 80% 하한을 **하드코딩**해, MAIN 경로(`getValuationWeights`
연혁 모델)와 달랐다. 연혁은 이미 코드에 있었다 — ~1998.12.31. 순자산 단독 /
1999.1.1.~2007.2.27. 3:2 하한 없음 / 2007.2.28.~ 3:2 + 하한. 하드코딩 탓에 2007.2.28. 이전
양도에 하한이 잘못 걸려 양도기준시가↑ → 환산취득가↓ → **세액 과대**였다.
부동산과다보유 가중치 반전의 근거 라벨도 §165⑤(취득후상장)에서 **§165④1**로 정정(세액 불변) —
같은 파일 `:650` 주석이 이미 "§165④1 괄호"로 옳게 적고 있어 내부 불일치였다.

> 🛑 별건 유지: **취득**기준시가 80% 하한 미적용은 여전히 보류다
> ([[project_stock_unlisted_acq_basis_floor80_open]]) — 예규 검증 선행 조건이 그대로다.
> 이번 정정은 **양도**측 연혁 게이팅이라 그 판단과 충돌하지 않는다.

### 종결 7 — 흩어진 개별 항목 (10건) ✅

| 항목 | 성격 | 근거 |
|---|---|---|
| §97의5 시한 fallback에서 등록일 제거 (3건) | 세액 변경 | 조특법 §97의5①1호 — "2018.12.31.까지 **취득**(**매매계약** 포함)하고 **취득일로부터 3개월 이내** 등록". 시한 기준은 취득/계약이며 등록일이 아니다. 등록이 취득 후 3개월 내라 `registrationDate ≥ acquisitionDate`가 항상 성립 → 등록일을 앞에 두면 판정이 **뒤로만 밀려** 시한 내 취득분을 부당 배제했다. §97의3·§97의4의 `registrationDate` 사용은 조문이 **등록 시한**을 직접 규정한 것이라 정당 — 무변경. |
| 부수토지 일체과세 LTHD 3년 게이트 (3건) | 세액 변경 | §95② "…**보유기간이 3년 이상인 것**…". 이 경로만 24개월로 게이팅해 2~3년 구간에 **존재하지 않는 공제**를 부여했다. 같은 파일 `rateForYears`는 이미 `years < 3 → 0`이라 내부 불일치였다. 24개월은 "2년 미만 = 단기세율"이라는 **다른 규칙**을 LTHD 요건으로 착각한 것이다. |
| PHD 분리경로 §97②2호 택일 (3건) | 세액 변경 | §164⑤ PHD 경로가 `환산취득가 + 개산공제 + 자본적지출`을 **전부 합산 차감**해 이중계상했다. 비-PHD 경로는 이미 `applyAssetSwap`으로 가목↔나목 **택일(MAX)**을 구현하고 있었다. 헬퍼를 모듈 스코프로 올려 **한 곳에서만 정의**되게 했다. |
| 환산주택가격 정수 정확도 (2건) | ±1원 | `Math.floor(a * b / c)`가 `a*b > 2^53`에서 틀어졌다 → `safeMultiplyThenDivide`. |

**기존 앵커 1건을 조문 근거로 뒤집었다** — `new-construction-bundled-case-28.test.ts` T-33이
"30개월 = 2년 → 2 × 2% = 4%"로 LTHD > 0을 고정하고 있었는데, 그 산식은 §95②의
**3년 진입요건을 빠뜨린 것**이었다. 세율 판정(누진 vs 단기)은 별개 규칙이라 그 단언은 유지했다.

### 종결 8 — 단건·경계 항목 (8건) ✅

| 항목 | 성격 | 근거 |
|---|---|---|
| §154①2호가목 수용 특례 fail-open | 세액 변경 | `expropriationDate ?? transferDate` fallback 탓에 `transferDate <= transferDate + 5년`이 **항상 참**이라 5년 요건을 검증하지 않고 무조건 보유·거주 면제를 줬다. 5년은 **수용일 기산**이므로 수용일을 모르면 판정 불가 → 특례 미적용이 맞다. 미입력을 유리하게 추정할 근거가 없다. |
| §155① 일시적 2주택 §154① 미검증 | 세액 변경 | 타이밍(요건 A·B)만 보고 비과세를 줬다. §155①은 "…1주택을 소유한 것으로 **보아 제154조제1항을 적용**한다"이므로 종전주택이 **보유 2년 + (취득 당시 조정지역이면) 거주 2년**을 충족해야 한다. 바로 아래 E-3.5(합가 §155④⑤)는 이미 그 요건을 명시하고 있어 E-3만 빠진 내부 불일치였다. 메모리에 🟠Gap B로 기록돼 있던 갭이다. |
| §168의9③7호 상속 임야 3년 윤년 | 세액 변경 | `differenceInDays / 365`가 1,095일을 정확히 3.0으로 만들어 **3년 경과로 오판**했다(2019-03-01 → 2022-02-28). 3년 도래일은 2022-03-01이므로 미경과다. 달력 기준 `addYears`로 교체. |
| §98 "5년 이상 보유" 2/29 롤오버 | 세액 변경 | `setFullYear`가 존재하지 않는 2001-02-29를 만들어 **3/1로 롤오버**시켜 만료일을 하루 밀었다. 민법 §160③(응당일 없으면 말일 만료) → 1996-02-29 + 5년 = 2001-02-28. date-fns `addYears`로 교체. |
| override가 매매사례가액을 우회 | 세액 변경 | `resolveAcquisitionOverride`가 `appraisal`만 무력화하고 동일 성격 추계인 `salesCase`는 뒀다. `calcTransferGain`이 `similarSalesValue ?? acquisitionPrice`를 쓰므로 강제 취득가액이 **완전히 우회**됐다(양도차익 400,000,000 → 300,000,000). |
| 자경 §66⑤ 편입일 안분 (1건) · 금양임야·묘토 §8③ 안분 (2건) | ±1원 | `floor(금액 × 부동소수비율)` → `safeMultiplyThenDivide`. |

### 종결 9 — 임대주택 특례 기본공제 fixture 재구성 (4건) ✅

엔진 정정은 이미 종결 8(PR #868)에서 끝나 있었다 — §103①은 기본공제를 **소득별로 해당 과세기간에
각각 연 250만원** 한도로 규정하는데, 장기임대 거주주택 특례(§155⑳·§161) 조기반환 경로가
연 한도 **전액**을 고정 적용해 같은 해 기사용분을 무시했다(250만원 재차 전액 공제 → 세액 과소).
`max(0, 연한도 − 기사용분)`으로 정정.

남아 있던 것은 **감사 fixture가 검증할 수 없는 상태**라는 점이었다. 감사 브랜치(2026-07-14 베이스)
이후 `RentalUnitInput`이 개편돼 옛 필드명으로는 **특례 경로에 진입조차 못 했다**:

| 감사 fixture | 현행 |
|---|---|
| `registrationDate` | `businessRegistrationDate` + `rentalRegistrationDate` (2분리) |
| `rentalType: "long-8"` | `rentalCategory: "long_general"` |
| — | `isExcluded918Rule` · `hasMinimum2Units` 추가 |

동작이 확인된 `rental-housing-exception/rh-b1-early-exempt-bypass.anchor.test.ts`의 `rentalUnitOk`를
기준으로 맞춰 재구성했다. 본 테스트의 검증 대상은 기본공제이므로 임대 요건은 "전부 충족"으로만 둔다.
⇒ 6건 전부 통과. **복원 검증**: 엔진 정정을 되돌리면 정확히 3건 실패.

이 건은 "감사 지적이 옳아도 **fixture가 낡으면 검증 자체가 불가능**하다"는 사례다 —
엔진과 fixture를 별개 작업으로 분리한 판단이 맞았다.

### 종결 10 — 다주택 tier 혼재 §104⑤2호 본문 적용 (2건) ✅ · 🔴 감사 기대값은 오류

이 파일은 **감사 쪽이 틀린 첫 사례**라 별도로 기록한다. 지적한 결함은 실재하지만
제시한 기대값은 §104⑤에서 도출되지 않는다 — 헤더의 "기대값은 법령·세율표에서 독립 도출"이
성립하지 않는다.

**실재하는 결함**: `multi_house_surcharge` 그룹에 2주택(+20%p)·3주택(+30%p)이 섞이면
**대표자산 단일 tier를 그룹 합산 과세표준 전체에 적용**해 입력 순서에 따라 세액이 달라진다
(3주택 우선 324,060,000 / 2주택 우선 274,060,000). 순서 의존은 그 자체로 정의상 오류다.

**그러나 감사의 "모델 A"(합산 누진 1회 + 자산별 가산분 Σ)도 조문과 다르다.**

§104⑤ — 산출세액 = MAX(1호, 2호)
- **1호**: 과세표준 **합계액**에 §55① 세율 (중과 없음)
- **2호 본문**: **자산별** 양도소득 산출세액 **합계액**
- **2호 단서**: "둘 이상의 자산에 대하여 … **동일한 호**의 세율이 적용되고, 그 적용세율이 둘 이상인
  경우"에만 합산 후 호별 세율 적용 → **큰** 산출세액의 합계

사안의 A는 §104⑦**3호**(3주택 이상), B는 §104⑦**1호**(2주택)로 **호가 다르다** → 단서 미적용,
**2호 본문**(자산별 독립 산출 후 합)이 적용된다.

| 모델 | 값 | 근거 |
|---|---|---|
| 현행 엔진 | 324,060,000 / 274,060,000 | **순서 의존 — 오류** |
| 감사 "모델 A" | 304,060,000 | 합산 누진 174,060,000 + 가산분 130,000,000 — **조문 근거 없는 하이브리드** |
| **§104⑤ 도출값** | **280,120,000** | MAX(1호 174,060,000, 2호 3억누진 94,060,000+90,000,000 + 2억누진 56,060,000+40,000,000 = 280,120,000) |

감사 모델이 큰 이유는 **누진 구간을 합산 기준으로 한 번만 태워** 각 자산의 낮은 구간을 지운 탓이다.
2호 본문은 자산별로 누진을 각각 태우므로 구간이 리셋된다.

**수정 (2026-07-29)**: 감사 기대값 대신 **§104⑤ 도출값 280,120,000**을 목표로 구조를 바꿨다.

`aggregateByGroup`의 누진 호 분기가 `records[idxList[0]]`(입력 첫 자산)의 세율을 그룹 합산
과세표준 전체에 적용하던 것을, **중과 가산율이 갈리면(= 적용 호가 다르면) 자산별 산출세액 합**으로
전환했다. 동일 세율이면 종전대로 합산 1회 floor를 유지한다 — 자산별 floor 합산은 floor 횟수
차이로 ±N원이 어긋나기 때문이다.

`short_term` 그룹은 **이미 같은 판정**(세율 혼재 → 자산별 합)을 하고 있었다. 누진 호 쪽만
빠져 있던 내부 불일치였고, 그 분기의 주석("동일 호 합산이 정확하므로")이 `multi_house_surcharge`에
**여러 호가 섞일 수 있다**는 사실을 놓친 것이 원인이다.

⇒ 순서 무관 확인(정·역순 동일 280,120,000) · 전체 회귀 0 · **복원 검증**: `mixedTier` 판정을
되돌리면 2건 실패. 감사 기대값 304,060,000은 **채택하지 않았고** 테스트 헤더에 왜 틀렸는지 남겼다.

### 종결 11 — 엔진 기능 신설 (4건) ✅

**(a) 납부지연가산세 이자율 기간분할 (2건)** — 국세기본법 시행령 §27의4의 이자율은 개정마다
시행일이 있고 경과조치는 **시행일 이후 기간분에 신율**을 적용하는데, 종전에는
`resolveDailyRate(납부일)` 하나를 전 기간에 곱했다. 개정 전 기간분까지 더 낮은 신율로
계산되어 **가산세가 과소**했다(2021-12-01 기한 → 2022-06-01 납부 182일: 단일율 400,400 vs
구간분할 422,900).

이력 테이블은 이미 있었다(`DAILY_PENALTY_RATE`·`_2019`·`_2016`, 컷오프 2019-02-12·2022-02-15) —
하나만 고르던 것을 구간 분할로 바꿨다. `DELAYED_RATE_PERIODS` 단일 소스를 만들고
`resolveDailyRate`도 같은 값을 쓰게 해 두 곳이 갈리지 않게 했다.
`DelayedPaymentResult.breakdown`(구간별 일수·이자율·금액) 신설, 구간이 둘 이상이면
`steps.formula`도 구간별로 풀어 쓴다(단일 이자율 표기는 자기모순이 된다).
현행 §27의4① "1일 10만분의 22"는 KoreanLaw 원문 확인.

**(b) 다필지 양도차손 통산 (2건)** — 손실 필지를 `Math.max(0, rawGain)`으로 **절사**해
이익 필지와 통산되지 않았다(총차익 과대 → **세액 과대**). §102②·시행령 §167의2는 양도소득금액을
**소득별로 통산**하도록 한다. 다건 집계 경로(`transfer-tax-aggregate`)는 이미 통산하고 있어
**다필지 경로만 어긋난 내부 불일치**였다.

필지별 차익은 부호를 보존하고 합산 단계에서 통산한다. 장기보유특별공제는 §95②상
"**양도차익**에 공제율을 곱한 금액"이므로 차손 필지에는 적용하지 않는다(차손에 공제율을 곱하면
손실이 줄어 통산액이 왜곡된다). 통산 후 합계가 음수면 과세 소득은 0이다.

⇒ 4건 통과 · 전체 회귀 0 · **복원 검증**: 두 정정을 되돌리면 3건 실패.

### 남은 6건 — 성격이 다르다

여기서부터는 **기대값 조정이 아니라 별도 작업**이 필요하다.

| 파일 | 실패 | 왜 남았나 |
|---|---|---|
| `audit-fix-general-building-valuation` | 3 | 무허가 토지에서 전액 0원짜리 **유령 '토지-사업용' 카드** 생성. 카드 조립 구조 변경 필요. |
| `audit-fix-multi-house-surcharge-count` | 2 | **군위군(27720) 광역시 소속 군** 판정 — 저가주택 배제 기준이 REGION이 아니라 VALUE여야 한다. 행정구역 데이터 검증 선행. |
| `transfer-multi-house-tier-model-a` | 2 | 🔴 **감사 기대값 자체가 틀렸다 — 아래 참조** |
| `transfer-tax-penalty-rate-boundary` | 2 | **엔진 기능 신설** — `DelayedPaymentResult.breakdown`(이자율 기간분할) 미구현 |
| `multi-parcel-loss-offset` | 2 | **엔진 기능 신설** — 다필지 손익통산 |


| 파일 | 실패/전체 | 성격 |
|---|---|---|
| `audit-fix-rental-housing-step` | 4/6 | 임대주택 — `registrationDate` 필드 부재 |
| `audit-fix-transfer-tax-exemption`·`-helpers`·`split-gain`·`period-check` 등 | 1~3씩 | 개별 |
| `transfer-tax-penalty-rate-boundary` | 2/2 | **엔진 기능 필요** — `DelayedPaymentResult.breakdown`(이자율 기간분할) 미구현 |

4개 파일은 master에 없는 API를 참조해 **타입 에러**가 난다(위 표의 `new-99-3`·`rental-housing-step`·
`penalty-rate-boundary`·`multi-parcel-loss-offset`) — 이들은 단순 기대값 조정이 아니라
**엔진 변경 제안**이므로 건별로 채택 여부를 판단해야 한다.

## 권고 순서

1. ~~R1-a·R1-b~~ ✅ — 평가·판정 12종 + `nblSurchargeExcluded` 복구(집계 Detail 25 → 37).
2. ~~R2~~ ✅ — 가드는 법령상 정상. 대신 담보평가의 **구법 규칙 잔존**을 §63 기준으로 정정.
3. ~~R3~~ ✅ — 조심2013서0051로 현행 정의(사용자 입력) 타당성 확인. 변경 없음.
4. ~~R6~~ ✅ — 실 API 실측으로 블로커 해소, 결함 3건 수정 후 PR #737 머지.
5. ~~R5(부분)~~ ✅ — `property-valuation.ts` 1,025 → 653. 나머지 5건(801~840)은 thrash 회피로 유지.
6. ~~R4~~ 🛑 — 보류 확정(2026-07-29 사용자 결정). 현행 차단 유지가 정답.
7. **R7 진행 중** — 99 → 6 (93건 종결). 남은 6건: 유령 카드 구조 변경(3)·군위군 행정구역(2)·기타(1).

## 이 계획서의 검증 원칙 (본 세션에서 얻은 것)

- **marker 부재 ≠ 결함** — 산출값(양도차익·필요경비)까지 비교해야 표시 갭과 계산 결함이 갈린다.
  상가에서 정확히 오진할 뻔했다.
- **단건 대조군이 녹색이어야 판별이 성립** — 양쪽 다 실패하면 fixture 미비지 결함 근거가 아니다.
- **"validate 통과"만으로 도달 가능이라 결론 내리지 말 것** — 엔진이 그 경로를 읽는지,
  UI가 그 조합을 제공하는지를 따로 확인한다.
- **차단 테스트만으로는 부족** — 차단이 풀리면 무슨 일이 생기는지를 대조 구조로 고정해야
  왜 막는지가 코드에 남는다.

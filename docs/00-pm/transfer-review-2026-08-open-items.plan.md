# 양도소득세 코드리뷰 2026-08 — 미결 3건 (원 5건 → F35·F16 해소)

> 2026-08 전면 코드리뷰([`docs/reviews/transfer-tax-code-review-2026-08.md`](../reviews/transfer-tax-code-review-2026-08.md))의 확정 결함 44건 중
> **36건은 Wave 1~5로 수정 완료**(`9dd29b62`·`f6225ff2`·`1a65df9f`·`6821ea97`·`f167b70e`)했고,
> 아래는 **정답이 하나로 갈리지 않아 결정 전 착수 금지**로 남긴 것이다.
> 원 5건 **전건 해소** — F35·F16(2026-08-23) + **F03·F08·F23(2026-08-23)**.
> 커밋 `f26f3715`(F23) · `41898c30`(F08) · `2479637e`(F03).
> F35 → `d1eeaf89`·`2d3b8c13`·`b10702fd` · F16 → `ce8fbce5`(Phase 0)·`53783c31`(선행)·`ceafe4b1`(본체)
> 부수 해소: **F15**(배치 누락분) `8cef41cc` · **classifyRateGroup**(F16 조사 중 발견) `53783c31` ·
> **GB 지분 이월과세 미스케일**(F16이 연 별건 9건 중 1건 — 아래 전용 섹션. 🔴 **계획서 Q3 반전 · 재가 대기**)

## 상태 요약

| ID | 제목 | 결함 성격 | 실측 편차 | 결정이 막힌 이유 |
|---|---|---|---|---|
| ~~**F03**~~ ✅ | 다건 자산별 가산세 base | 계산 축 오류 | 실측 **+200,000 (0.85%) 과대** → 해소 | ✅ **§107①·§103②·§47의2① 명문으로 해결** |
| ~~**F08**~~ ✅ | §155⑳ 특례 × 조특법 감면 병용 | 감면 침묵 소실 | 실측 **17,463,000** 반영 | ✅ **세액감면형 계산 + 차감형 고지** (`921f9f92`) |
| ~~F16~~ | ~~컴패니언 이월과세 ⑫ 부재~~ | — | — | ✅ **2026-08-23 해소** — (a) 정식 지원 (`ceafe4b1`) |
| ~~**F23**~~ ✅ | split 자산 §97①3호 양도비 유실 | 진짜 기능 공백 | 양도비 전액(실측 Δ=0) | ✅ **§100② 후문 안분** + 자산 capex 차단 |
| ~~F35~~ | ~~겸용 배율초과 비사토 신고서 표시~~ | — | — | ✅ **2026-08-23 해소** — (나) 채택 (`d1eeaf89`·`2d3b8c13`) |

> ⚠️ **인용 재검증 완료(2026-08-23)** — Wave 1~5가 관련 파일을 수정해 라인이 이동했다.
> 아래 file:line은 커밋 `f167b70e` 기준으로 전수 재확인한 값이다.

---

## ~~F03~~ · ✅ 해소 (2026-08-23 · 커밋 `2479637e`)

**계획서가 걸어둔 블로커가 조문으로 풀렸다.** 「기본공제 배분 로직이 자동 안분 금지와 충돌하는지
판단 선행」이 미결 사유였는데, 배분 **순서가 명문**이다 — 「소득세법」 **§103②**
「먼저 양도한 자산의 양도소득금액에서부터 **순서대로**」. 그리고 **§107①**이 예정신고 산출세액에
**기본공제를 포함**시키고, **§107②**는 2회 이후 합산신고에서 앞선 신고가 기본공제를 소진함을
전제한다. base 자체는 「국세기본법」 **§47의2①**이 「그 신고로 납부하여야 할 세액(**예정신고 포함**)」,
같은 조 **⑤**이 예정신고분·확정신고분 가산세를 **분리**한다 ⇒ 자산별(건별) base가 맞다.

⇒ 채택안은 **(가) 예정신고 축**. `transfer-tax-preliminary-filing.ts` 신설 —
양도일 오름차순으로 단건 엔진을 돌리며 `annualBasicDeductionUsed`를 누적한다.
집계의 `MAX_BENEFIT` 배분(확정신고 축)과 **다른 축**이라 간섭하지 않는다.
⛔ 역안분은 채택하지 않았고 anchor F03-05가 「집계 결정세액 × 20%가 **아님**」을 못박는다.

**실측**: 23,612,000 → **23,412,000**(−200,000 · 0.85%). 안전망은 **0건**이었다(수정 전후
기존 회귀 무변동). anchor 5건 · 구별력 M1 3/5 · M2 1/5.

⚠️ **anchor 함정**: 양도일만 바꾸면 보유기간이 함께 움직여 LTHD가 바뀐다(첫 시도 −600,000).
취득일을 같은 폭으로 옮겨 보유기간을 고정하고 **순서만** 바꿔야 한다.

### (이력) 착수 전 기재

**위치**: `app/api/calc/transfer/multi/route.ts:340-368` (리뷰 시점 `:317` → 라인 이동, 구조 동일)

**결함**: 2-pass 가산세 계산이 **집계 전 자산별 standalone 결정세액**을 가산세 과세표준으로 주입한다.
`baseResult.properties[idx].determinedTax`를 그대로 `filingPenaltyDetails.determinedTax`에 넣는데, 이 값은
손익통산·합산 누진을 거치지 않은 값이라 신고서 전체 결정세액과 어긋난다.

**실측**: 자산 1건 base가 신고서 전체 결정세액의 3.47배. 현행 유지 시 편차 **+200,000(0.93%) 과대**.
`Sum(refDeterminedTax)` = 27,460,000 ≠ 집계 결정세액 31,285,000.

### 선택지

| 안 | 내용 | 비용 | 위험 |
|---|---|---|---|
| **(가)** 예정신고 축 | 「소득세법」 §107① 산출세액(양도차익−LTHD−기본공제)×세율을 자산별로 재산출 | 예정신고 기본공제 250만원을 **시간순 첫 신고에 소진**시키는 배분 로직 신설(aggregate의 MAX_BENEFIT 배분 재사용 불가) | 「자동 안분 금지」 정책과의 충돌 판단 선행 필요 |
| **(나)** 신고단위 집계 축 | 확정신고 기준 집계값을 base로 | 자산별 `penaltyReason`·`filingType` 입력 UI **회수** | 사용자가 이미 넣은 자산별 입력이 사라진다 |
| (다) 현행 유지 | characterization anchor로 고정 | 낮음 | 0.93% 과대가 남는다 |

### 결정이 막힌 이유

**별지84호서식에 자산별 가산세 칸이 존재하지 않는다.** ⑯가산세는 세율구분/합계 축에만 있고,
자산별 명세인 부표1은 ⑱에서 끝난다 — 즉 위임입법이 이 축을 정의하지 않았다.

### ⛔ 금지

`Sum(refDeterminedTax)`나 집계 결정세액을 `taxBaseShare`로 역안분하는 방식은 **채택 금지**다.
실측상 ~1%(200,000) 과대를 **49%(5,760,000 → 8,590,000) 반대 방향 오차**로 바꾼다.
「소득세법」 §107①·「국세기본법」 §47의2⑤에 반한다(예정신고 무신고 base에 차손통산·합산 누진을 넣는 셈).
§104⑤ `refCalculatedTax` 역안분 재제안 금지 항목과 인접하므로 그 방향 확장 금지.

### 착수 전 필수

현재 **안전망 0건**. characterization anchor를 먼저 심어 현행 동작을 고정할 것.

---

## ~~F08~~ · ✅ 해소 (2026-08-23 · `41898c30` 고지 → `921f9f92` 계산)

**병용을 막는 명문이 없다** — 조특법 **§127 전항 verbatim** 확인. ⑦은 「둘 이상의 **감면규정**이
동시 적용되면 하나 선택」일 뿐 §155⑳ 비과세 특례와 감면의 병용을 배제하지 않는다.

**계획서의 미결 사유는 차감형에만 걸린다** ⇒ 두 축을 갈랐다:

| 축 | 처리 | 이유 |
|---|---|---|
| **세액감면형**(§69·§77·§77의2·§97 시리즈) | **계산** | 산출세액 − 감면세액 구조라 이 경로에 그대로 성립. 일반 경로와 **같은 함수·같은 한도 규칙** |
| **차감형·하이브리드**(`ALL_INCOME_DEDUCTION_IDS` 11종) | **고지만** | 양도소득금액 차감인데 §161 안분의 앞/뒤 위치 **명문 부재** |

동반 배관: §133 한도를 `transfer-tax-reduction-cap.ts`로 **추출해 finalize와 공용**(dual-truth 회피) ·
다건 전파(`reductionTypeApplied`·`reducibleIncome` 반환 — 종전 0 반환은 aggregate M-8이 조용히
버렸다) · 가산세 base는 감면 후 결정세액으로 자동 반영.

**실측**: §77 공익수용 **17,463,000** 적용 → 결정세액 116,420,000 → **98,957,000**.
anchor 5건 · 구별력 M1 2/5 · M2 2/5.

> 🆕 **별건 발견 — §77 감면의 농특세가 두 경로 모두 없다.** 「농어촌특별세법 시행령」 §4①1호는
> §77의 농특세 비과세를 **「직접 경작한 토지」로 한정**하므로 주택·비자경 토지에는 농특세
> (감면세액 × 20%)가 붙어야 한다. 실측상 **일반 경로도** `ruralSurtax` 필드 자체가 undefined이고
> 관련 step이 0건이다 ⇒ **두 경로 공통의 별건**이라 F08에서 고치지 않았다(여기서만 고치면 같은
> 감면이 경로에 따라 달라진다).

### (이력) 착수 전 기재 · (2) 고지 단계

**재현**: 같은 자산에서 특례를 끄면 조특법 §77 공익수용 감면 **80,660,250**이 잡히는데, 켜면
감면 0 · 경고 0건 · 감면 관련 step 0건이었다. **UI에도 게이트가 없어 조합은 도달 가능**하다
(§155⑳ 섹션과 감면 섹션이 서로 독립 — `RentalHousingExceptionSection`).

원인은 구조다 — 특례 경로는 `finalizeTransferTax`를 거치지 않아 `calcReductions`가
**아예 호출되지 않는다**.

⇒ 계획서가 「어느 쪽이든 확정」이라 적은 **고지**를 먼저 구현했다(세액 불변 · anchor 4건).
**(1) 실제 감면 계산은 여전히 미결**이다 — 농특세 2-pass·가산세 base·§127⑦ 한도의 aggregate
전파가 동반돼야 하고, 소득금액 차감형을 §161 안분 앞/뒤 어디에 얹을지 명문이 없다.

### (이력) 착수 전 기재

**위치**: `lib/tax-engine/transfer-tax-rental-housing-step.ts:475` (리뷰 시점 `:400` → Wave 4 수정으로 이동)

**결함**: §155⑳ 거주주택 특례 **조기반환 경로가 사용자가 입력한 조특법 감면을
`reductionAmount: 0`으로 하드코딩**해 버린다. 화면 고지도 없다.

### 선택지

| 안 | 내용 | 세액 영향 |
|---|---|---|
| **(1)** 특례 경로도 `calcReductions` 호출 | 조문별 게이트를 두고 실제 감면 계산 | 있음 |
| **(2)** `steps`·`warnings`로 사유만 노출 | 「특례 적용 시 감면은 병용하지 않습니다」 고지 | 없음 |

### 법령 조사 결과 (KoreanLaw 실측)

- **§99·§99의3** — 고가주택 배제 단서가 **명문**이라 0이 정답이다.
- **§77·§97 시리즈·§97의5·§98의8·§99의2** — 배제 문언이 **없어 감면 여지가 실재**한다.
  ⇒ 조문별 게이트가 필요하다. 일괄 0은 이 조문들에 대해 근거가 없다.

### 결정이 막힌 이유

1. 차감형(STEP 4.6)을 「소득세법 시행령」 §161 안분 **前/後 어디에 얹을지 명문이 없다**.
2. **§155⑳ 병용 가부를 직접 판단한 예규·심판례가 부존재**한다.

### 어느 쪽이든 확정된 것

**현행 「무조건 0 + 무고지」는 정당화되지 않는다.** (2)라도 고지는 해야 한다.

### (1) 채택 시 동반 설계

농특세 2-pass · 가산세 base(`:402` `emitPenaltySteps`) · §127⑦ 한도의 aggregate 전파까지 함께 봐야 한다.

---

## F16 · 컴패니언 이월과세 ⑫ 부재

**위치**: `lib/api/transfer-tax-schema-sub.ts:286~` `companionAssetSchema` — `carryoverTaxation` **부재 재확인(2026-08-23)**

**결함**: ⑫에 `carryoverTaxation`이 없어 컴패니언 자산의 이월과세 취득가액이 **0으로 떨어진다**.

### 선택지

| 안 | 내용 | 위험 |
|---|---|---|
| **(a)** 정식 지원 | ⑫스키마 + ⑭매핑 + ④취득가액 3계층 | 일괄 route가 `transferPrice`를 **기준시가 안분값으로 덮은 상태**에서 §97의2② 비교과세(STEP 0.475)가 돌게 된다 |
| **(b)** ⑧ 명시 차단 | SINGLE_ONLY에 carryover 추가 | 술어 범위 결정 필요 |

### (a)의 위험 — 실측 없이 열면 안 된다

`lib/calc/transfer-tax-validate.ts:104-111`이 기록한 부담부증여 **「스케일 충돌 → 표시 필요경비 음수」와 같은 클래스**의 위험을 연다.

### 🆕 (b)의 장애물이 해소됐다 (2026-08-23)

리뷰 시점에는 「`SINGLE_ONLY`가 `some()`이라 **자산 2개인데 primary만 carryover**인 현재 정상 조합까지
막힌다」가 미결이었다. **Wave 5의 F41이 같은 함수 안에 `i > 0` 루프 선례를 만들었다**
(`lib/calc/transfer-tax-validate.ts:169-178` — 컴패니언 매매사례가액 차단).
⇒ (b)를 택한다면 그 패턴을 그대로 따르면 되고, 별도 설계가 필요 없다.

---

## ~~F23~~ · ✅ 해소 (2026-08-23 · 커밋 `f26f3715`)

**전제가 두 번 틀렸다.** ① 첫 프로브는 `propertyType: "commercial"`이라 `calcSplitGain`이
아예 켜지지 않았다(진입 조건은 `housing`·`building`). ② `isSeparateAcquisition`을 넣어
제대로 켠 뒤 재측정하니 원 주장대로 **양도비 Δ=0**이었고, **자산 단위 자본적지출도 함께 Δ=0**이었다
(주장에 없던 것).

**채택**: (가) — §100② 후문이 「공통되는 취득가액과 **양도비용**」을 **명문 열거**하므로 안분이
법정이다. 산식·잔액 규약은 형제 경로(`general-building-swap.ts` `resolvePerPart`)와 **동일**하게
양도가액 비례 floor + 마지막 파트 잔액 흡수.

🔴 `explicitDirect` 판정도 함께 고쳤다 — §97②2호 나목은 「자본적지출 + 양도비」인데 종전 판정이
자본적지출 칸 유무만 봐서 **양도비만 입력하면 환산 파트에서 다시 유실**됐다.

**자산 단위 자본적지출**은 안분하지 않는다(§100② 후문 미열거 — ⛔ 확정). 대신 파트 칸으로
안내하는 validate를 추가했다(`validate-gb.ts` V-8과 동형 · GB 자산은 그쪽이 담당하므로 제외).

**안전망 0건**이었다 — 착수 전 「양도비를 통째로 토지에 얹는」 변이에 613파일 6,756테스트가
전건 통과했다. anchor 2파일 10건 · 구별력 M1 4/10 · M2 1/5 · M3 1/5 · M4 1/5.

### (이력) 착수 전 기재

**위치**: `lib/tax-engine/transfer-tax-helpers.ts:273` 부근

**결함**: split(토지·건물 별개취득) 경로가 **자산-수준 양도비를 통째로 버린다.**
그런데 파트 칸(`landTransferExpense`/`buildingTransferExpense`)은 **저장소 전체 grep 0건**이다
(⚠️ `general-building-swap.ts:204-210`의 동명 지역변수는 GB 전용 내부 계산이라 입력 경로가 아니다).
⇒ **어떤 경로로도 반영이 불가능한 진짜 기능 공백**이다.

### 선택지

| 안 | 내용 | 비용 |
|---|---|---|
| **(가)** 양도비만 안분 구현 | `general-building-swap.ts:196-211` 헬퍼 공유 + 이중 계상 방지 UI | 파트 칸과 자산 칸이 동시 노출돼 **이중 계상 위험**, 배타 안내 신설 필요 |
| **(나)** 자산-수준 입력 차단 + 파트 칸 신설 | 입력 축을 파트로 일원화 | 신규 입력 UI 2칸 + ⑧⑫⑬⑭ 배관 |

### 법령 — (가)의 헬퍼 공유는 적법하다

「소득세법」 §100② 후문이 **「양도비용」을 명시 열거**하므로, `general-building-swap.ts`가 이미 구현한
법정 안분 규칙(자산 가액 비례 + 잔액 흡수)을 공유하는 것은 「자동 안분 fallback 금지」 위반이 아니다.

### ⛔ 확정된 금지 — 자본적지출 안분

자본적지출을 §100② 후문으로 파트 안분하는 것은 **doNotFix 확정**이다. 셋과 정면 충돌한다:
`transfer-tax-acq-cost-swap.engine.design.md:151` 금지 목록 ·
`transfer-tax-validate-gb.ts:344-345`의 조문 논거(§100② 후문은 「공통되는 취득가액과 양도비용」만 열거하고 **자본적지출은 미열거**) ·
「자동 안분 fallback 금지」 정책.

### 함께 결정할 것

`transfer-tax.ts:668` `capitalExpenditureForDisplay` echo를 split에서 억제할지.

---

## ~~F35~~ · ✅ 해소 (2026-08-23)

**커밋**: `d1eeaf89`(characterization anchor 23건) → `2d3b8c13`(수정) → `b10702fd`(800줄 분리)

### 채택안 — (나) 주택분 토지 열의 과세/비과세 분해 정정

**조문이 갈랐다.** 조사 2축이 정반대 결론을 냈고(축 A → (가), 축 B → (나)),
「소득세법」 §104⑤ 후단을 직접 읽어 판정했다:

> 이 경우 **제2호의 금액을 계산할 때** … 한 필지의 토지가 비사업용 토지와 그 외의 토지로
> 구분되는 경우에는 각각을 별개의 자산으로 보아 **양도소득 산출세액을 계산한다**

「각각을 별개의 자산으로 보아」는 **「제2호의 금액을 계산할 때」로 명시 한정된 의제**다 —
산출세액 계산 전용이지 양도차익·필요경비 기재 단계까지 미치지 않는다. 축 A가 이 한정구를
놓치고 일반 자산분할 규칙으로 과독했다. ⇒ 신고서에 비사토 전용 열을 세우는 (가)는 의제를
조문 밖으로 확장하는 방향이므로 채택하지 않았다.

### 이 항목에서 배운 것 (다음 미결 착수 시 재사용)

1. **미결 문서의 전제가 틀릴 수 있다.** 이 문서 초판은 「4열이 법정 서식이면 (가)는 서식 이탈」이라고
   적었는데, 조사 결과 **4열 자체가 법정이 아니었다**. 별지84호 부표1의 열 축은 「세율구분 코드 ×
   자산종류」이고 토지/건물은 **행**이다. 착수 전 전제 재검증이 실제로 값을 했다.
2. **결함이 문서에 적힌 것보다 컸다.** 「비과세 오분류」 하나가 아니라 셋이었다 —
   장특 합계 누락(「합계 ≠ 보유분 + 거주분」)과 「양도소득금액 − 기본공제 ≠ 과세표준」 자기모순이
   함께 있었다. 12억 이하 전액비과세 + 배율초과 조합에서는 **「비과세인데 세금이 나오는」 화면**이었다.
3. **안전망이 문자 그대로 0이었다.** 배율초과 × 신고서 표 픽스처가 저장소 전체에 0건이라
   변이를 넣어도 red가 0건이었다. 「착수 전 anchor 먼저」 조건이 유효했다.
4. **gross/net 비대칭에 주의.** `hp.transferGain`·`hp.landTransferGain`은 비사토를 **포함한 gross**라
   건드리면 이중계상이고, `hp.longTermDeductionAmount`·`hp.incomeAmount`는 비사토 **제외** 후 값이라
   가산이 맞다. 같은 객체 안에서 축이 갈린다.
5. **「4열 합 = 합계」는 정확히 성립하지 않는다.** floor 잔차로 ±2원 어긋나고 기존 anchor도
   ±4원 tolerance로 설계돼 있다 — 판정선은 0이 아니라 4원이다.

## 🆕 F16 작업이 새로 연 별건 (착수 전 확인 필요)

F16 (a) 구현 중 발견됐고 **F16이 만든 것이 아니라 이미 살아 있던** 항목들이다.

> 📋 **잔여 7건의 작업 계획서**: [`transfer-f16-spinoff-items.plan.md`](transfer-f16-spinoff-items.plan.md)
> (2026-08-23 · 인용 전수 재검증 · 배치 B-1~B-5 · Q 4건 · V 8건).
> 🔴 **7건 중 5건이 수치 미측정**이라 착수 전 재현이 선행 조건이다.
> 🔴 **N-2(`splitCompanionIntoTwo`)는 「1줄 수정」이 아니다** — F16이 컴패니언 `carryoverTaxation`을
> 도달 가능하게 만들어, 지금 활성화하면 두 파트에 **2배 계상**이 켜진다.

| 항목 | 실측 | 위치 |
|---|---|---|
| **§97의2②3호 비교 스코프 자산별 → 신고단위** | 조문상 신고단위 확정(§104⑤ 근거). divergence 16/562·4,570,000~39,125,000·**전부 현행 과소**. ⚠️ memory `project_stock_carryover_97_2_necessary_expense_open.md:169-172`의 「갭 없음」과 **상충**(재현 불가) — 착수 시 선결 | `transfer-tax-carryover.ts:585-587` |
| **`splitCompanionIntoTwo`가 dead code** | `resolveCompanionSplit`이 `landNature`를 안 넘겨 `resolveCompanionLandRate`가 **항상 `applied:false`**. §167의5 한도초과 분리가 도달 불가(트리거 조건 충족 픽스처로 실측) | `bundled-split-helpers.ts:462-473` |
| ~~**GB 지분 경로 이월과세 미스케일**~~ | ✅ **2026-08-23 해소** — 아래 §「GB 지분 이월과세 스케일」 참조. 실측 **결정세액 62,914,160원 과소**(60:40 격자). ⚠️ 종전 계획서 **Q3 결정을 반전**했다 — 사용자 재가 대기 | `transfer-tax-api-gb-carryover.ts` |
| **`giftTaxAmount` 지분 스케일 여부** | 미스케일(현행) 유지. 바꾸려면 `OwnershipRatioInput` 안내 문구와 함께 결정. anchor R-1c가 조용한 변경을 막는다 | `transfer-tax-api-carryover.ts` |
| 부담부증여 aggregate **양도가액 열 0** | 수정 전후 모두 열 산술이 깨져 있다(§159 유상분은 `burdenedGiftInfo`에서 파생). ⑧이 마법사 경로를 막지만 **GB route는 aggregate를 거쳐 도달** | `transfer-tax-aggregate.ts` |
| 환산 산식 텍스트 드리프트 | A-9로 이월과세 자산의 `acquisitionPrice`가 채택값이 되어, 이월과세 × 환산에서 인쇄된 피연산자가 결과를 유도하지 않을 수 있다. **표시 전용·수치 미측정** | `DetailedStatementFormulaBuilders.ts:314·317·325` |
| §97②2호 swap 잔여 드리프트 | 환산취득가액이 양도차익에서 차감되지 않는데 표시 열은 환산 재산식을 그린다. 이번 픽스처에서 미발현·**수치 미측정** | `transfer-tax-helpers.ts:250-258` |
| 컴패니언 phd/apd 배관 부재 | ⑫에 `preHousingDisclosure` 필드 자체가 없다. base가 더 과대였으므로 **방향은 나빠지지 않았다**. (a)⑫ 배관 / (b)⑧ 차단 판단 필요 | `transfer-tax-schema-sub.ts` |
| `schema-sub.ts` 706줄 | 착지목표 ≤700을 6줄 초과(hard cap 800 미만) | — |

## GB 지분 이월과세 스케일 (2026-08-23) — 🔴 **확정 결정 반전 · 사용자 재가 대기**

`applyShareScale`이 이월과세 서브객체를 지분율로 줄이지 않아, 지분 양도가액만 × r 되고
증여자 취득가액은 100% 그대로 남았다. 결과는 **음수 양도차익 → 허수 차손 통산**이다.

**실측(60:40 · 40%가 이월과세, route POST)**

| | 결정세액 | `land#1` 양도차익 | 타 지분 차손 잠식 |
|---|---|---|---|
| 수정 전 | **44,549,040** | **−140,000,000** | 47,848,101 × 2 + 44,303,798 |
| 수정 후 | **107,463,200** | +34,000,000 | **0** |

⇒ **62,914,160원 과소.** 지분이 작을수록 커진다(1/4에서 결정세액 **0**까지 관측).

**스케일 판정표** — `PART_SCALE`·`ENGINE_SHAPED_SCALE`·`GIFT_EVENT_SCALE`
(`transfer-tax-api-gb-carryover.ts`). `Record<keyof …, boolean>`이라 **필드가 늘면 tsc가 누락을 잡는다**
— 종전 키-배열 열거가 새 필드를 조용히 빠뜨린 것이 이 결함의 원인이다.

| × r | 미스케일 |
|---|---|
| `donorAcquisitionPrice` · `donorCapitalExpenditure` · `giftDateAssetValue`/`giftDateValuation` | `donorStandardPriceAtAcquisition`(기준시가) · `giftTaxAmount` · `giftTaxCalculated` · `giftTaxBase` |

- 🔴 **기준시가 금지** — 환산 분모가 물건-수준(100%)이고 `ownershipRatio`가 이미 개산공제 base를
  줄인다. 스케일하면 이중 축소(실측: 취득가액 100,000,000 → 40,000,000, 세액 **16,800,000 과대**).
- ⚠️ **`giftTaxAmount` 미스케일** — F16 A-10·anchor R-1c 결정 승계. **뒤집지 말 것.**
- **사건 2필드 미스케일** — 분자만 × r 하면 §163의2② 안분이 정확히 지분 몫이 된다.
  분모까지 줄이면 약분돼 무효, 계수만 줄이면 근거 없이 한 번 더 깎는다.

### 🔴 반전한 결정 — 재가 필요

`docs/00-pm/transfer-gb-carryover-wiring.plan.md` **Q3**(2026-08-10, `14cd5862`)가
「스케일하지 않는다」로 확정하고 anchor **K-15**가 고정하고 있었다. 반전 근거 3건(전부 재확인):

1. ⭐ **Q3가 성립 요건으로 못박은 「3중 일치」의 첫 항목이 끝내 구현되지 않았다.**
   Q3 원문: 「UI 안내 = **이 지분에 대한** 실제 값을 입력하세요」 ·
   실측: `components/calc/transfer/CarryoverGiftBlock.tsx`(305줄)에 `지분`·`100%` **0건**.
2. ⭐ **같은 화면이 정반대를 안내한다.** `OwnershipRatioInput.tsx:129-141` —
   「지분 모드 — 모든 금액을 **100% 기준**으로 입력하세요 … **양도가액·취득가액·필요경비**는
   물건 전체(100%) 기준으로 입력합니다」. 증여자 취득가액 = 취득가액,
   증여자 자본적지출 = §97의2① 필요경비 ⇒ 배너가 이미 포섭한다.
3. ⭐ **미스케일 유지 시 컴패니언 경로와 dual-truth.** 같은 request body 안에서
   top-level `carryoverTaxation`은 × r, GB 지분은 100%가 된다.

> ⚠️ **Q3 원문의 핵심 논거는 살아 있고 구현이 그것을 지킨다.** Q3는 「증여세 산출세액은
> 물건 전체 기준 시세 같은 값이 아니다」라고 했는데, 구현은 증여세 3필드를 **전부 미스케일**로
> 남겼다. 반전한 것은 **취득가액·자본적지출·평가액** 3필드뿐이다.

> 🔑 **되돌리려면 UI부터.** ④만 되돌리면 화면과 계산이 다시 갈린다. 되돌릴 경우
> F16 A-10(컴패니언)·anchor R-1a/R-1b도 함께 되돌려야 한다.

### 🔴 남은 UI 갭 (출하 전 닫아야 함 — 문구 미결정)

배너는 「양도가액·취득가액·**필요경비**는 100% 기준」이라고 하는데, **증여세 상당액도
§97의2① 필요경비**다. 그런데 그 칸은 미스케일이다. 사용자가 배너를 문자 그대로 읽고
100% 기준 값을 넣으면 필요경비가 **1/r 배 과대**가 된다.
⇒ `CarryoverGiftBlock.tsx`의 증여세 상당액·기준시가 두 칸 hint에 예외를 명시해야 계약이 닫힌다.
**UI 문구는 임의로 정하지 않았다.** (동일 갭이 컴패니언 경로에도 있다.)

### 🆕 이 작업이 새로 연 별건

| 항목 | 실측 | 위치 |
|---|---|---|
| 🔴 **legacy 모양 × 환산 이월과세 → 취득가액 0** | ⑫ `carryoverTaxationEngineShape`에 `donorStandardPriceAtAcquisition` **필드가 없어** 조용히 strip된다. `buildEngineShaped`는 「취득가액 0을 피한다(설계 D9-8)」는 주석과 함께 싣는데 ⑫가 버린다. **단건 경로에도 있다**(실측 land acq=0 · gain 430,000,000). F15와 같은 침묵 strip 부류 | `transfer-tax-building-schemas.ts:30-44` |
| 🔴 **GB 실가 경로가 이월과세를 통째로 무시** | `general-building-route-actual.ts`에 `carryoverTaxation` 배선 **0건**. ④는 `transfer-tax-api-gb.ts:645`에서 실가 분기에도 싣는데(주석: 「실가 경로에도 반드시(설계 D1-1)」) 엔진이 안 읽는다. **지분 특유 아님.** ⑧ 차단 여부 미확인 | `general-building-route-actual.ts` |
| 🟠 GB 지분 × 실가에서 세액 `null` + 「경비(NaN」 인쇄 | `general-building-fractional.ts:160`이 `actualAcquisitionPrice`·`actualExpenses`를 안 넘긴다(단건은 `general-building-route-helper.ts:163-164`에서 넘긴다) | `general-building-fractional.ts:160` |
| 🟡 건물 파트 fallback이 토지 값을 복제 | `asset.buildingCarryover ?? c` — 건물 폼이 비면 두 파트가 **같은** 증여자 취득가액을 받아 합계가 증여 전체의 2배. ⑧ 강제 여부 미확인. **지분 축과 독립** | `transfer-tax-api-gb-carryover.ts` |
| 🟡 `giftTaxCalculated`·`giftTaxBase` hint 해석 미확정 | 「증여받은 자산 **전체**」가 「증여 사건 전체」인지 「물건 100% 기준」인지 문구로 안 갈린다. **현행(미스케일) 유지**했고 UI 미접촉. 후자가 정본이면 `giftTaxBase`도 × r 대상 | `CarryoverGiftBlock.tsx` |

**미측정으로 남긴 것**: 건물 파트만 `carryover_gift`인 조합의 route 세액 ·
환산 × 지분 조합의 route 세액 · 지분 3건 이상 · 마지막 지분 잔액 흡수와의 상호작용.

## GB 이월과세 후속 3건 (2026-08-23) — ✅ 해소

직전 GB 지분 스케일 작업이 연 High 2건 + 사용자가 「지금 닫는다」로 결정한 UI 갭.

### A · UI hint 갭 — 어느 칸이 100% 기준인가

배너는 「양도가액·**취득가액·필요경비**는 100% 기준」이라 하는데, 증여세 상당액도
「소득세법」 §97의2① **3호** 필요경비이면서 미스케일이다 ⇒ 문자 그대로 읽으면 **1/r 배 과대**.

⇒ 증여세 3칸(산출세액·과세가액·상당액)에 「지분 모드 예외 — 신고서 금액 그대로」를,
기준시가 섹션에 「두 칸 모두 그대로, 지분율로 나누지 않음」을 **지분 모드일 때만** 노출.

- 지분 판정은 `isFractionalOwnership(asset)` — 배너 `isFractionalRatioStr`와 **같은 하위 술어**.
  `AssetForm`이 이미 지분 2필드를 들고 있어 **prop 신설·미러링 0**.
- `CarryoverGiftBlock`은 GB(토지·건물)·컴패니언 **3곳**이 공유 ⇒ 컴패니언 갭도 함께 닫혔다.
- 스케일되는 3칸(취득가액·자본적지출·평가액)에는 **붙이지 않았다** — 배너가 그대로 맞다.
  anchor가 자본적지출에 예외 문구가 섞이지 않았음을 단언한다.

> 🔴 **법령 인용 오류를 검증에서 잡아 정정했다.** 구현이 증여세 상당액을 「§97의2① **2호**」로
> 적었는데 **3호**다. `legal-codes/transfer.ts` `CARRYOVER_GIFT_TAX_EXPENSE`가
> 「종전 「① 2호 (전단)」은 2023.12.31. 개정 **전** 구조」라며 2026-08-10에 **이미 정정**한 인용을
> 되돌린 것이었다. 같은 파일이 자본적지출을 「① 2호」로 쓰고 있어 한 파일에서 「2호」가
> 두 항목을 가리키는 상태였다. 주석 2곳 정정(사용자 노출 문구엔 호 번호가 없어 오표시는 없었다).

### B · ⑫가 legacy 이월과세의 기준시가를 침묵 strip (High)

`carryoverTaxationEngineShape`에 `donorStandardPriceAtAcquisition`이 **없어** ④가 실은 값을
Zod가 버렸다. ④는 「이 경로에서도 실어야 취득가액 0을 피한다(설계 D9-8)」는 **주석과 함께** 싣는다.

| | 취득가액 | 결정세액 |
|---|---|---|
| 수정 전 | **0** | 204,930,000 |
| 수정 후 | 150,000,000 | **161,460,000** |

⇒ **단건 43,470,000 과대** · 지분(40%) **16,800,000 과대**. K-14 anchor 주석의
「실가 161,460,000 → 환산 204,930,000」과 **원 단위 일치**(독립 재현).

🔑 **⑧이 막기는커녕 강제한다** — `validateGbCarryover`는 환산 모드에서 그 필드를 **필수**로
요구하면서, legacy 분기를 여는 `giftTaxCalculated`·`giftTaxBase`는 요구하지 않는다.
게다가 placeholder가 「증여세 산출세액 (없으면 0)」으로 0 입력을 초대한다.
⇒ 「⑧이 반드시 채우라 한 값을 ⑫가 버리는」 조합. **안전망 0건.**

- ⑫ **2벌 동시 수정**(공유 shape + 단건 인라인 사본) — `:24-28` parity 주석 10→11.
  단건 사본은 오늘 inert지만(mutation 실측 전건 green) parity 보존 + 재발 방어로 넣었다.
- 엔진 타입에 필드를 넣고 `general-building-valuation.ts`의 **인라인 캐스트를 제거**했다 —
  그 캐스트가 타입 부재를 메워 결함을 **가린 장본인**이다.
- Option B(legacy를 part 모양으로 흡수) **기각**: part shape에 `giftTaxAmount`가 없는 것이
  D9-10의 핵심이고, 흡수하면 「🔴 없애지 말 것」(테스트 16건·GBF-27·API 직접 호출자·
  구형 sessionStorage)이 깨진다. 증여세 산출세액 0인 증여는 event 경로로 표현 자체가 불가능하다.

### C · GB 실가 경로가 이월과세를 통째로 무시 (High)

`general-building-route-actual.ts`에 `carryoverTaxation` 배선 **0건**이었다.
데이터는 이미 도착해 있었는데 **타입에 필드가 없고 `as unknown as` 캐스트라 tsc가 침묵**했다.

**재현: 이월과세 유무로 응답이 바이트 동일** — 299,010,000 vs 299,010,000.
같은 서브객체를 환산 모드로 보내면 170,660,000 → 161,460,000 + `carryoverTaxationDetail` 존재
⇒ 픽스처는 유효하고 실가 경로만 미발동.

| 케이스 | 수정 전 | 수정 후 |
|---|---|---|
| 실가 baseline(매매) | 299,010,000 | 불변 |
| 실가 + 토지 이월과세 | **299,010,000** | **225,090,000** |
| 실가 + 건물만 | 299,010,000 | 248,610,000 |
| 실가 + 토지·건물 둘 다 | 299,010,000 | **174,690,000** |
| 환산 경로 2종 (대조군) | — | **불변** |

- NBL 2분할: `splitLandCarryover`로 갈라 60,000,000 + 90,000,000 = **150,000,000**(2배 계상 없음).
- ⚠️ **`applyCarryoverEstimationBasis` 호출은 선택이 아니라 필수** — ⑤·⑧ 어느 쪽도
  `landAcqMode`를 보지 않아 실가 모드에서도 「증여자 취득가액 불명 → 환산」이 열려 있고
  ⑧이 분자를 필수로 요구한다. 빼면 시나리오 A 취득가액이 **0**(P-B와 같은 결함 모양).
- **라우트에 부담부증여 가드를 두지 않았다** — 이월과세는 라우트에서 취득가액을 덮지 않고
  엔진이 시나리오 A/B를 통째로 갈아끼운다. §159와는 ④가 `{}`를 반환해 결합이 안 만들어진다.
  라우트에 두 번째 가드를 두면 ④와 dual-truth가 된다.

### 검증

tsc 0 · lint 0 errors · **test:transfer 605파일 6,711 pass**(base 602/6,681 · +3파일 +30건) ·
전건 `npm test` 1,436파일 15,919 pass · pre-push 하드게이트 3종 통과 · 전 파일 800줄 이내.
구별력 독립 재실측 A **5/15** · B **3/5** · C **6/10** red — **red 0인 anchor 없음**.
교차 오염 없음(mutation C에서 B anchor 전건 green).

### 🆕 이 작업이 남긴 것

| 항목 | 상태 | 위치 |
|---|---|---|
| 🟠 **지분 × 실가 경로가 C의 변경으로 출력이 바뀐다 — anchor 0건** | `general-building-fractional.ts:160-167`이 `gbv`를 통째로 스프레드해 `landCarryoverTaxation`이 지분 경로에도 흘러든다. 실측 `land#1` 취득가액 **0 → 160,000,000**·`adoptedScenario "-" → "B"`. **세액 회귀는 없다**(`determinedTax`가 전후 모두 `null` — 아래 기존 파손 때문). C가 「범위 밖」으로만 적고 **자기 변경이 그 경로 출력을 바꾼다는 사실은 보고하지 않았다** | `general-building-fractional.ts` |
| 🟠 **지분 × 실가에서 세액 `null` + 「경비(NaN」 인쇄** | `:160`이 `actualAcquisitionPrice`·`actualExpenses`를 안 넘긴다(단건은 `route-helper.ts:163-164`에서 넘긴다). **기존 파손** | `general-building-fractional.ts:160` |
| 🟡 술어는 공유하나 **인자가 다르다** | 배너는 원값, hint는 `\|\| "100"` fallback. `(n="40", d="")`에서 갈린다 — 현재 정규화로 **사문**이나 anchor 문구가 코드보다 강하다 | `transfer-tax-api-helpers.ts:163-168` |
| 🟡 GB는 `donorStandardPriceAtTransfer`를 **읽지 않는다** | UI는 두 칸을 받는데 GB 엔진은 분모를 물건-수준 값으로 도출한다. 컴패니언·단건은 `topLevelOverrides`로 실제 사용 ⇒ **자산종류별로 의미가 갈린다** | `transfer-tax-building-schemas.ts` |
| 🟡 환산 분모 floor parity 1원 | 실가 경로 `:257`은 `transferLandPricePerSqm × landArea`에 floor가 없고, 환산 경로는 `general-building-valuation.ts:577`에서 floor한 값을 같은 자리에 넘긴다. C의 호출로 이 값이 **처음 환산 분모가 됐다**. 산술 실측 1원 차 · **route 실측 미수행** | `general-building-route-actual.ts:257` |
| 🟡 부담부증여 × 실가 직접 API 호출 | payload를 손으로 조립하면 이월과세가 발동한다(299,010,000 → 225,090,000). UI로는 도달 불가이고 환산 경로도 같은 동작이라 **경로 간 일관성은 개선**됐다 | — |
| 🟡 개산공제 미리보기 표시 드리프트 | `CarryoverEstimationSection.tsx:145-146` | — |

## 부록 — 이 문서에 없는 것

| 항목 | 상태 |
|---|---|
| F10·F11·F22·F28 | Wave 2~4에서 **사용자 결정 후 적용 완료** |
| **F17**(겸용·일반건물 감면) · **F27**(GB×부담부증여×이월과세) | 사용자가 **「정식 구현」 선택** — 리뷰 수정이 아니라 **신규 기능 트랙**으로 분리 |
| 재개발·입주권 축 | **별도 최종 패스** 대기. 이월 2건: `transfer-tax-redevelopment.ts:210` `calcTax` 4번째 인자 누락(F01과 동일 결함) · `transfer-tax.ts:196`에서 판정하고 `:228`에서 폐기하는 구조 |
| Low 13건 | 표시·일관성 계열. 적대적 검증 미실시 — 수정 시 개별 확인 필요 |
| 미구현 12건 | 리뷰 문서 §7. 결함이 아니라 백로그 |

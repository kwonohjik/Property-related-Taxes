# 국세청 「건물 기준시가 계산서」 공식 양식 재현 — 계획서

> 원본: `/Users/mynote/Documents/기준시가계산서.pdf` (국세청 교재 p.75~80, 2023.1.1. 시행 양식)
> 작성일: 2026-06-11 · 상태: **Plan** (Design·Do 미착수)
> 관련 정책: `besshi-form-replica` · `pdf-case-replica-workflow` · `feedback_pdf_table_row_one_to_one_mapping` · `pre-do-anchor-verification` · `amount-column-align` · `feedback_no_won_suffix`

---

## 1. 목표·범위

PDF의 3개 작성례를 **칸 단위까지 동일한 양식**으로 재현하는 보고서 화면·인쇄 서식을 구현한다.

| # | 작성례 | 페이지 | 세목·시점 | 핵심 특징 |
|---|---|---|---|---|
| (1) | 양도건물에 대한 기준시가 계산 | p.75~76 | 양도소득세 · **양도당시**(2023.1.1) | 층별 복합(근생+단독) + 부속시설 안분, 조정률 미적용 |
| (2) | 최초고시 건물 기준시가 및 취득당시 기준시가 계산 | p.77~78 | 양도소득세 · **취득당시(2000.12.31 이전)** | 2001.1.1 지수표 + ※ 산정기준율 환산(1.016) |
| (3) | 상속건물에 대한 기준시가 계산 | p.79~80 | **상속세**(2023.1.1) | (1)과 동일 건물 + 조정률 번호 기재(0.9(9)·1.2(20)·0.6(24)) |

3개 작성례는 **단일 양식**(섹션 Ⅰ~Ⅵ + ※)의 기재 패턴 차이다. → **컴포넌트 1벌 + 모드 3종**으로 구현한다. 작성례 (1)과 (2)는 한 세트(같은 건물의 양도분·취득분)임에 유의.

비범위(이번 계획 제외): 기계식주차 전용빌딩 서식(별도 산식 — 본 양식 비대상), 공동주택 환산(기존 별도 카드 유지).

---

## 2. 원본 양식 캡처 동결 (섹션·칸 명세)

### Ⅰ. 구분 (작성구분 + 일자)

5열 매트릭스. 양도소득세 아래 「양도당시 | 취득당시(2001.1.1이후 취득 / 2000.12.31이전 취득)」 3분기 + 상속세 + 증여세.

| 작성례 | 양도당시 | 취득(2001이후) | 취득(2000이전) | 상속세 | 증여세 |
|---|---|---|---|---|---|
| (1) | **○ / 2023.1.1** | | **○ / 2000.2.1** | | |
| (2) | "2001.1.1 건물 기준시가"(텍스트) | | **○ / 2000.2.1** | | |
| (3) | | | | **○ / 2023.1.1.** | |

→ 작성구분 행은 ○ 또는 자유 텍스트, 일자 행은 날짜. **(1)처럼 두 칸 동시 표시 가능**.

**○ 위치 파생 규칙(동결)**: 양도 모드 = 양도당시 ○(양도일) + 취득연도 2001 이상이면 「2001.1.1이후 취득」 ○ / 2000 이하면 「2000.12.31이전 취득」 ○(취득일). 취득당시 단독 작성(작성례 2) = 양도당시 칸에 "2001.1.1 건물 기준시가" 텍스트. 상증 모드 = 상속/증여 구분 필드(§4.2)에 따라 해당 칸 ○ + 일자.

### Ⅱ. 건물 기본현황

상단 행: 건물 및 토지 소재지 | 건물구조 | 구조지수① | 건물용도(복수 줄) | 용도지수②(복수 줄) | 토지면적③(**3행 칸**) | ㎡당 공시지가④.
하단 행: 토지가액⑤=③×④ | 평균지가⑤÷③ | 위치지수⑥ | 건물전체 연면적 | 건물전체 층수(지상N층/지하N층) | 내용연수(그룹별)(예 "50년(Ⅰ.Ⅱ.Ⅲ.Ⅳ)") | 건물 신축연도 | 잔가율⑦.

작성례 값(동결): (1)(3) 종로구○○○·철근콘크리트·1.0·근생/단독 1.0/1.0·130.0㎡·2,500,000·⑤325,000,000·평균 2,500,000·⑥1.16·390㎡·지상3/지하1·50년(Ⅰ.Ⅱ.Ⅲ.Ⅳ)·2000·⑦0.586. (2)는 ④2,240,000·⑤291,200,000·⑥1.05·용도지수 0.9/1.0·40년(Ⅰ.Ⅱ.Ⅲ)·⑦0.98.

내용연수 칸의 그룹 목록 괄호는 **평가연도 era에서 파생**(실측: 2001~02 잔가율 3그룹 시대 = `(Ⅰ.Ⅱ.Ⅲ)`, 2003~ = `(Ⅰ.Ⅱ.Ⅲ.Ⅳ)` — era-B 2010·era-C 2023 모두 4그룹 확인). 해당 그룹만 표시 강조(G-4 `residualGroup` echo 사용).

### Ⅲ. 건물의 평가 (주용도 층별)

머리 산식(동결): `＊ ⑧ ＝ ⓐ × ① × ② × ⑥ × ⑦ × ⓧ × ⓨ × ⓩ(천원미만 절사)　⑩ ＝ ⑧ × ⑨`

11열: 층별 | 기준금액ⓐ | 구조지수① | 용도 | 용도지수② | 위치지수⑥ | 잔가율⑦ | **조정률(번호) ⓧ.ⓨ.ⓩ 3칸** | ㎡당 금액⑧ | 건물면적⑨ | 기준시가⑩.
데이터 행 + **빈 행(데이터+빈 합계 9행 고정, 빈 조정률 칸은 `( )` 표기)** + 합계 행(⑨합·⑩합).

| 작성례 | 행 | ⓐ | ② | 조정률 | ⑧ | ⑨ | ⑩ |
|---|---|---|---|---|---|---|---|
| (1) | 지상1 근생 / 지상2·3 단독 | 820,000 | 1.0 | — | 557,000 | 각 100㎡ | 각 55,700,000 / 합 **167,100,000** |
| (2) | 지상1 근생 / 지상2·3 단독 | 400,000 | 0.9 / 1.0 | — | 370,000 / 411,000 | 각 100㎡ | 37,000,000 / 41,100,000×2 / 합 **119,200,000** |
| (3) | 지상1 근생 | 820,000 | 1.0 | **0.9(9)·1.2(20)** | 601,000 | 100㎡ | 60,100,000 |
| (3) | 지상2·3 단독 | 820,000 | 1.0 | — | 557,000 | 각 100㎡ | 합 **171,500,000** |

### Ⅳ. 건물부속(지하 포함) 주차장·기계실·보일러실·대피소·옥탑의 평가

Ⅲ과 동일 11열, 용도 열만 "용도(구분)" — **`주차장(슈퍼귀속)` 식 부속종류+귀속 표기**. 데이터+빈 합계 **7행 동결**(원본 PDF 자체가 p.76=7행·p.78=8행으로 불일치 → 작성례 (1)(3) 기준 7행 채택) + 합계 행(⑨합·⑩합).

| 작성례 | 행(4행) | 조정률 | ⑧ | ⑨ | ⑩ 합계 |
|---|---|---|---|---|---|
| (1) | 주차장(슈퍼 20·주택 40)·보일러실(슈퍼 10·주택 20) | — | 557,000 | 90㎡ | **50,130,000** |
| (2) | 동일 구성 | — | 370,000(슈퍼)/411,000(주택) | 90㎡ | **35,760,000** |
| (3) | 동일 구성 | 슈퍼 0.9(9)·0.6(24) / 주택 0.6(24) | 300,000 / 334,000 | 90㎡ | **29,040,000** |

### Ⅴ. 주용도에 의한 부속시설 건물 면적의 안분계산

머리 산식(동결): `＊ At＝A1＋A2＋……＋A8＋A9　Bi＝Ai÷At　C1＝Ct×B1　D1＝Dt×B1 …… H9＝Ht×B9`

10열: 구분 | 주용도지수 | 해당면적(Ai) | 면적비율(Bi) | 주차장(Ci) | 기계실(Di) | 보일러실(Ei) | 대피소(Fi) | 옥탑(Gi) | (Hi).
행: **계(t) + 1~9** (지수 슬롯 140·130·120·110·100·100|90·60·40·"제외").

작성례 채움: 계(t) At=90㎡·Bi=1.0·Ct=60㎡·Et=30㎡. 행5(지수 100, 단독 귀속) A=60·B=0.67·C=40·E=20. 행6(지수 100 — (2)는 90, 근생 귀속) A=30·B=0.33·C=20·E=10.

**행 의미 판정(사례 대조)**: 미사용 행의 지수 140·130·120·110·60·40·제외는 3개 작성례에서 동일 → 양식 인쇄 가이드 값. 사용 행(5·6)의 지수는 작성례 (2)에서 90으로 달라짐 → **사용 행은 해당 주용도 부분의 실제 용도지수를 기재**, 미사용 행은 가이드 값 유지로 재현한다. Bi는 소수 2자리 표시(0.67·0.33), 안분 면적은 원천 비율로 계산(60×200/300=40 정확).

### Ⅵ. 평가대상 건물 기준시가 및 부속토지 평가액 합계

3칸: 건물 기준시가⑪(=Ⅲ합+Ⅳ합) | 토지가액⑤ | 총합계⑪+⑤.

| 작성례 | ⑪ | ⑤ | ⑪+⑤ |
|---|---|---|---|
| (1) | **217,230,000** | 325,000,000 | **542,230,000** |
| (2) | **154,960,000** | 291,200,000 | **446,160,000** |
| (3) | **200,540,000** | 325,000,000 | **525,540,000** |

### ※ 2000.12.31 이전 취득 시 취득당시 기준시가 계산 (양도소득세 전용)

5칸: 2001.1.1.현재 건물 기준시가(1) | 산정기준율(2) | 취득당시건물 기준시가(3)=(1)×(2) | 토지가액(4) | 합계(3)+(4).
작성례 (2)만 기재: **154,960,000 × 1.016 = 157,439,360** + 234,000,000 = **391,439,360**. (1)(3)은 빈 표 출력(양식 고정부).

---

## 3. Pre-Plan 실측 검증 결과 (probe 완료 — 추정 아님)

현행 엔진 데이터·산식이 PDF 수치를 재현하는지 worktree에서 직접 실행 확인했다(2026-06-11).

| 항목 | PDF | 엔진 실측 | 일치 |
|---|---|---|---|
| 2023 신축가격기준액 ⓐ | 820,000 | `resolveNewBuildingBasePrice(2023)` = 820,000 | ✅ |
| 2023 위치지수 ⑥ @2,500,000 | 1.16 | `resolveLocationIndex` = 116 | ✅ |
| 2023 잔가율 ⑦ (rc·2000신축) | 0.586 | group I, `calcResidualRate(I,23,2023)` = 0.586 | ✅ |
| 2023 용도지수 (단독 #2 / 근생 #41) | 1.0 / 1.0 | 100 / 100 | ✅ |
| 2001 신축가격기준액 | 400,000 | 400,000 | ✅ |
| 2001 위치지수 @2,240,000 | 1.05 | 105 | ✅ |
| 2001 잔가율 (rc·2000신축·1년) | 0.98 | group I(3그룹 시대), 0.98 | ✅ |
| 2001 용도지수 (단독 #1 / 근생 #27) | 1.0 / 0.9 | 100 / 90 | ✅ |
| 산정기준율 (rc=I그룹·신축 2000·취득 2000) | 1.016 | `resolveAcqBaseRate("I",2000,2000)` = 1.016 | ✅ |
| 조정률 번호 9(연면적 1천㎡ 미만)·20(상가 1층)·24(주차장) | 0.9·1.2·0.6 | GROSS_AREA 90·COMMERCIAL_FLOOR 120·ANCILLARY 60 | ✅ |

행 단위 산식 자가검산(수기): (3) 지상1 = 820,000×1.0×1.0×1.16×0.586×0.9×1.2 = 601,995.x → 601,000(천원절사) ×100㎡ = 60,100,000 ✅. ※식 154,960,000×1.016 = 157,439,360 ✅ (원단위 정확).

→ **데이터 레이어는 갭 0**. 갭은 전부 ①엔진 분기 ②입력 필드 ③결과 echo ④서식 컴포넌트에 있다.

---

## 4. 현행 자산 대비 갭 분석 (file:line 검증 완료)

### 4.1 엔진 갭

| # | 갭 | 현행 | 필요 |
|---|---|---|---|
| G-1 | **양도 모드 복합건물 미지원** | `calcCompositeValuation`은 상증 분기에서만 호출(`building-standard-price.ts:173-174` — `if (input.taxType === "inheritance_gift")` 내부). 양도 2시점은 단일 구조·용도만 | 작성례 (1)(2)가 층별 복합 **양도** 케이스. 복합 계산을 시점(연도) 매개변수화해 양도 취득·양도시 각각 적용. 취득연도 ≤2000이면 2001 복합 합계 × 산정기준율(※식). **⚠️ 입력 파급(실측)**: 용도번호 체계가 연도별로 다름 — 근린생활시설 2001=#27 ↔ 2023=#41. 부분 구성(라벨·면적)은 양 시점 공통이되 **부분당 용도(필요 시 구조키)·공시지가는 취득/양도 시점별 입력** 필요. 연도 간 자동 매핑 테이블은 비범위(직접 선택) |
| G-2 | **부속시설 종류 미구분** | `sharedFacilityArea` 단일 합계 + 부분별 `sharedAdjustmentRate`. 공용 echo는 부분당 1행("X 공용") | Ⅳ·Ⅴ는 주차장(Ct)·기계실(Dt)·보일러실(Et)·대피소(Ft)·옥탑(Gt)·기타(Ht) **종류별** 면적·행 분리. 종류별 면적 입력 + 종류×귀속부분 행 echo (단가는 귀속 주용도와 동일 — 금액 합계는 현행과 불변) |
| G-3 | **복합 부분별 조정률 번호 부재** | `CompositePartForm.adjustmentRate`는 %수치만(`building-std-price-form.ts:34`) | (3) Ⅲ·Ⅳ의 `0.9(9)` 표기 — 부분·부속별 조정률 번호(최대 3개 ⓧⓨⓩ) 입력 또는 번호 선택식 입력. **번호→지수 통합 resolver는 현재 부재**(rate 테이블이 구분별 분산: ROOF 1~3·MAX_FLOORS 4~8·GROSS_AREA 9~13 등, 특성 기반 `selectSpecialAdjustment`만 존재) → `resolveAdjustmentRateByNo(no)` 데이터 헬퍼 신설, `ADJUSTMENT_FEATURE_LABEL`과 같은 파일에서 단일 출처 유지 |
| G-4 | **내용연수·잔가율그룹 echo 부재** | `applyNotes.residual`에 그룹 문자열만. 내용연수(50년/40년) 숫자 echo 없음(기계식만 `mechDurableYears`) | Ⅱ 내용연수(그룹별) 칸용 — breakdown에 `durableYears`·`residualGroup` echo 추가 |
| G-5 | ※식 복합 결합 | `calcAcqBaseBreakdown`은 단일 point 전용 | (2)식 = **복합 2001 합계 ⑪ × 산정기준율** → 복합 경로용 ※ 계산 추가 |

### 4.2 입력(폼) 갭 — 신규 필드

| 필드 | 용도 | 비고 |
|---|---|---|
| 작성 일자(양도일/취득일/상속·증여일) | Ⅰ 일자 행 | 현행은 연도만(`acquisitionYear` 등). `DateInput` — 연도는 일자에서 파생(기존 연도 Select와 양방향 단일 필드 통합 검토, `mirror-pattern` 준수) |
| 건물전체 층수(지상/지하) | Ⅱ | 표시 전용 2필드 |
| 토지면적 ③ (최대 3행) | Ⅱ·⑤ | 현행 `landAreaM2` 1행 → 다필지(`landParcels`)와 표기 통합 |
| 부속시설 종류별 면적(주차장·기계실·보일러실·대피소·옥탑·기타) | Ⅳ·Ⅴ | 현행 `sharedFacilityArea` 단일 → 6종 분해(합계는 파생). 기존 단일 입력은 마이그레이션 |
| 부분별 조정률 번호(ⓧⓨⓩ 최대 3) | Ⅲ·Ⅳ 조정률 칸 | 상증 전용. 번호 선택 → 지수 자동(엔진 테이블 import — `ui-engine-dual-truth` 금지) |
| 양도 모드 복합 토글 | (1)(2) 재현 | 현행 `compositeMode`는 상증 전용 게이트(`BuildingStdPriceForm.tsx:158`) |
| 양도 복합 부분별 시점 용도·공시지가 | G-1 파급 | 부분당 취득/양도 용도번호 각각 선택(연도별 체계 상이) + 시점별 공시지가 |
| 상속/증여 구분(표시 전용) | Ⅰ ○ 위치 | 현행 `taxType`은 `inheritance_gift` 통합 — 상속세/증여세 칸 분리 표시용 RadioCardGroup(엔진 영향 0) |

**범위 외 명시**: 양도 모드 다필지(`landParcels`)는 현행도 미지원이며 PDF 3작성례 모두 단일 필지 → 이번 범위에서 제외. ③ 2·3행은 빈 칸 출력(다필지 양도 지원은 후속).

### 4.3 출력 갭

- 현행 인쇄 서식(`BuildingStdPricePrintTable` — 간이 3열 「기준시가의 계산」)은 본 계산서의 부분집합. **결정 D-1**: 공식 계산서로 인쇄 채널 교체(권장) — 간이 서식은 삭제하고 계산서가 모든 모드(단일·복합·산정기준율) 포괄. 단일 건물은 Ⅲ 1행 + Ⅳ·Ⅴ 빈 표로 자연 표현됨.
- 결과 echo 신설: `weightedLandPricePerM2`(기존) + Ⅴ표 데이터(부분별 Ai·Bi·종류별 안분면적) + ※식 4값.

---

## 5. 칸번호 ↔ 변수명 1:1 매핑 (동결 — `feedback_pdf_table_row_one_to_one_mapping`)

| 칸 | 변수/필드 | 소스 |
|---|---|---|
| ① 구조지수 | `structureIndex` (÷100 표시) | breakdown echo(기존) |
| ② 용도지수 | `usageIndex` (÷100 표시) | breakdown echo(기존) |
| ③ 토지면적 | `landAreaRows[i].areaM2` | 폼(신규 통합) |
| ④ ㎡당 공시지가 | `landAreaRows[i].pricePerM2` | 폼 |
| ⑤ 토지가액 | `no5LandValue = Σ floor(③×④)` | 파생(표시 전용 — 엔진 외) |
| 평균지가(⑤÷③) | `avgLandPrice = ⑤ ÷ Σ③` | 파생. **단일 원천 규칙**: ③④는 `landParcels`/`landAreaM2`·`landPrice`와 같은 폼 필드에서 도출(별도 입력 금지). ⑤ 행별 floor로 `weightedLandPricePerM2`(floor 없음)와 1원 미만 차이는 정상 — 표시값 기준 통일은 Design에서 동결 |
| ⑥ 위치지수 | `locationIndex` (÷100) / 다필지 `weightedLandPricePerM2` 경유 | breakdown echo(기존) |
| ⑦ 잔가율 | `residualRate` | breakdown echo(기존) |
| ⓐ 기준금액 | `basePrice` | breakdown echo(기존) |
| ⓧⓨⓩ 조정률(번호) | `adjustmentNos: number[]` + 지수 | 신규 echo(G-3) |
| ⑧ ㎡당 금액 | `pricePerM2` | breakdown echo(기존) |
| ⑨ 건물면적 | `floorArea` | breakdown echo(기존) |
| ⑩ 기준시가 | `standardPrice` | breakdown echo(기존) |
| ⑪ 건물 기준시가 | `no11BuildingTotal = Ⅲ합 + Ⅳ합` — 상증 복합 = `compositeTotal` / **양도 복합 = `acquisitionComposite.total`·`transferComposite.total`(시점별 서식 2벌, 엔진설계 신규 필드)** / 단일 = 해당 시점 `breakdown.standardPrice`(Ⅳ 없음) | 엔진 — 모드 분기 어댑터에서 단일화 |
| Ⅴ Ai·Bi·Ci~Hi | `apportionment.rows[].{areaM2,ratio,parking,machine,boiler,shelter,rooftop,other}` | 신규 echo(G-2) |
| ※ (1)~(4) | `acq2001Total`·`acqBaseRate`·`acqConvertedTotal`·`acqLandValue` | 엔진 확장(G-5) + 폼 |

- 데이터 행 vs 소계 행 구분: Ⅲ·Ⅳ 합계 행은 `합계` 고정 라벨 + ⑨·⑩만 집계(메모리 정책 — 소계 행 변수 혼동 금지).
- testid 동결: `nts-bsp-{section}-{칸}` (예 `nts-bsp-3-row1-no8`, `nts-bsp-6-no11`). Design 단계에서 전수 표 작성.

---

## 6. 구현 단계 (PDCA — `pdf-case-replica-workflow` 준용)

### Phase P (본 문서) → Design

- `docs/02-design/features/building-std-price-nts-report.engine.design.md` — G-1~G-5 타입·산식·echo 명세 + 케이스 매트릭스(3작성례 × 단일/복합 × 조정률 유/무 × ≤2000 취득).
- `docs/02-design/features/building-std-price-nts-report.ui.design.md` — 14 동기화 지점 사전 명세 + testid 전수 표 + 800줄 분할 계획(아래 §8).
- CLAUDE.md 워크플로 준수: **엔진+UI 시니어 단일 메시지 병렬 킥오프** → Do는 시퀀셜(엔진 선행).
- `plan-design-self-review-loop` 적용(검토→정정 2사이클).

### Phase 0 — Pre-Do anchor ✅ 완료 (2026-06-11)

`__tests__/tax-engine/building-standard-price/nts-report-cases.test.ts` 6건 작성·실행 — **전부 통과**.

| anchor | 기대값(원단위) | 실행 결과 |
|---|---|---|
| A-1 작성례(3) 상증 복합 Ⅲ합 | 171,500,000 (지상1 ⑧601,000) | ✅ PASS — 현행 복합 산식 |
| A-2 작성례(3) ⑪(부속 포함) | 200,540,000 | ✅ PASS — **G-2 echo-only 확정**(`sharedFacilityArea` 단일 90으로 정확) |
| A-3math 작성례(1) 양도당시 ⑪ | 217,230,000 | ✅ 산식 PASS / **A-3gap: 양도 모드 compositeParts silent 무시 실증(G-1)** |
| A-4math 작성례(2) 2001 ⑪ | 154,960,000 (지상1 ⑧370,000) | ✅ PASS — inh 경로 |
| A-5 작성례(2) ※ | 157,439,360 | ✅ 데이터 PASS — 1.016 배선만 Do |

**환류(엔진설계 §Pre-Do 검증 결과 반영)**: 복합 산식이 PDF ⑪ 전건을 원단위 재현 → 양도 복합은 산식 신규 0(라우팅 추출만). G-2는 numeric 무관(표시 전용). A-3gap은 silent 과소산출(167,100,000, 부속 누락) — 크래시 아님. Do 범위가 라우팅·echo·※ 배선으로 축소 확정.

### Phase A — 엔진 (G-1~G-5)

1. types 확장: `CompositePartInput.adjustmentNos?`, `AncillaryFacilityInput { kind: "parking"|"machine"|"boiler"|"shelter"|"rooftop"|"other"; areaM2 }[]`, breakdown `durableYears`·`residualGroup`·`adjustmentNos` echo, result `apportionment?`·`acqBase?`(※식 4값).
2. `calcCompositeValuation` → 시점 매개변수화(`calcCompositeForYear(input, year)`), 양도 분기에서 취득·양도 각 1회 호출. 취득 ≤2000: 2001 복합 합계 × `resolveAcqBaseRate`.
3. 부속 종류별 안분: 면적비율(주용도) × 종류별 총면적. 안분은 **면적 차원**(금액 floor 아님) — 면적 `parseFloat(toFixed(2))` 후 단가 곱(기존 규칙 유지), 종류×부분 분리 후 **면적 합 = 입력 종류별 총면적** 가드(toFixed 잔여는 마지막 부분 행이 흡수).
4. 조정률 번호 → 지수 변환은 기존 데이터 테이블 단일 출처. 누적 곱은 정수 분자 / 100^k(기존 `calcSpecialAdjustmentRate` 패턴 재사용).
5. 합계 불변 가드: 종류별 분리 후에도 부분별 공용 합 = 현행 단일 `sharedFacilityArea` 결과와 동일함을 anchor로 고정(회귀 0).

### Phase B — 폼·14 동기화 지점

①폼 상태(작성일자·층수·부속 6종·조정률 번호·양도 복합 토글·**양도 복합 부분별 시점 용도/공시지가·상속/증여 구분**) → ②initial → ③normalize(기존 `sharedFacilityArea` → `ancillary[{kind:"other"}]` 마이그레이션) → ④`toEngineInput` → ⑤UI 위젯(섹션 카드·`DateInput`·`DecimalInput`·`RadioCardGroup`) → ⑥사이드바 해당 없음(독립 도구) → ⑦결과 = 본 계산서 → ⑧validate(부속 종류 합>0이면 귀속 조정률 규칙 — 기존 동기). ⑨~⑭ 해당 없음(API route 미사용 — 클라이언트 직접 호출 도구).

### Phase C — 계산서 컴포넌트 (양식 재현)

- `components/calc/building-std-price/nts-report/` 신설(800줄 정책 사전 분할):
  - `NtsBuildingStdPriceReport.tsx` — 조립(모드 판정 + 데이터 어댑터)
  - `ReportSection1Category.tsx` (Ⅰ) · `ReportSection2Overview.tsx` (Ⅱ) · `ReportEvalTable.tsx` (Ⅲ·Ⅳ 공용 — 11열 표, `variant: "main"|"ancillary"`) · `ReportSection5Apportion.tsx` (Ⅴ) · `ReportSection6Total.tsx` (Ⅵ+※)
- 양식 규칙: Tailwind utility 직접(외부 CSS 금지), 행 수 동결(**Ⅲ 9행·Ⅳ 7행**, 빈 조정률 칸 `( )` 표기), 금액 `text-right font-mono tabular-nums`, **숫자 끝 "원" 금지**, 지수는 소수 표시(116→1.16), 흑백 인쇄 최적화 + `print:block` 채널(`print-only-css-toggle`), 다크모드 흰 배경 강제.
- 화면 출력: 결과 영역에 접힘 카드(▼ 펼침) + 인쇄 시 자동 펼침. 기존 간이 `BuildingStdPricePrintTable`/`PrintView`는 본 계산서로 교체(D-1).

### Phase D — 검증

- anchor: Phase 0의 A-1~A-5 전부 통과 + 행 단위 ⑧·⑩ 원단위 anchor(작성례별 전 데이터 행) + Ⅴ표 Ai·Bi·Ci·Ei anchor(60·0.67·40·20 등) + 자기일관성(⑪ = Ⅲ합+Ⅳ합).
- E2E(`E2E_PORT=3100`): 작성례 (1)(3) 입력 → 계산 → 계산서 섹션 Ⅰ~Ⅵ 값 단언 + `emulateMedia({media:"print"})` 서식 렌더 확인. 기존 12개 spec 회귀.
- 전체 `npm test` + `tsc` + 변경 파일 lint.

---

## 7. 케이스 매트릭스 (Design에서 확장할 골격)

| 축 | 값 | 비고 |
|---|---|---|
| 세목 | 양도(양도당시) / 양도(취득당시 2001+) / 양도(취득당시 ≤2000 = ※식) / 상속 / 증여 | Ⅰ행 ○ 위치 5종 |
| 구성 | 단일(Ⅲ 1행) / 층별 복합(Ⅲ N행) | G-1로 양도에도 복합 허용 |
| 부속 | 없음(Ⅳ·Ⅴ 빈 표) / 종류별 입력 | 6종 × 귀속부분. **부속 입력은 복합 모드 전제** — 단일 건물+부속은 1부분 복합로 표현 |
| 조정률 | 미적용(양도) / 번호 기재(상증) | ⓧⓨⓩ 최대 3 |
| 토지 | 단일 필지 / 다필지(③ 3행 + 가중평균 ⑥) | 기존 `landParcels` |
| 동일연도 §164⑧ | 본 양식 비대상(기존 결과 카드 유지) | 범위 외 명시 |

전 조합 중 PDF 3작성례 = (양도당시·복합·부속·조정률無·단일필지), (취득≤2000·복합·부속·조정률無), (상속·복합·부속·조정률有). 나머지 조합은 Design에서 빈 칸 처리 규칙만 정의.

---

## 8. 리스크·결정 사항

| # | 항목 | 내용 | 권고 |
|---|---|---|---|
| D-1 | 기존 간이 인쇄 서식 처리 | 교체 vs 병행 | **교체로 계획 채택**(§6 Phase C 전제 — 사용자 최종 확인 항목). 공식 계산서가 상위호환. PR#109~110 간이 서식 삭제, E2E "결과 인쇄/PDF 서식" spec 재작성. 공동주택 환산 카드는 현행 PrintView 미출력이므로 교체 영향 없음(실측) |
| D-2 | 연도 입력 ↔ 일자 입력 | Ⅰ 일자는 YYYY-MM-DD, 엔진은 연도 사용 | **(UI 설계에서 갱신)** 연도 Select 유지(기존 E2E·`changeYearWithGuard` 보존) + 일자 `DateInput` **선택 입력**(서식 표기 전용). 일자 입력 onChange에서 연도 동기 set(동일 이벤트 — 미러링 아님). 미입력 시 Ⅰ칸 `{연도}년` 표기 |
| R-1 | Ⅴ표 행 슬롯 의미 | §2 판정(사용 행 = 실제 지수)이 다른 작성례에서 깨질 가능성 | Design에서 국세청 고시 V항 원문 재대조(KoreanLaw/PDF p.본문) 후 동결 |
| R-2 | 종류별 분리 시 floor 1원 오차 | 종류·부분 2축 분리 곱 | 단가 동일(귀속 주용도 기준)이므로 면적 분리 후 동일 단가 곱 — 행 합 = 기존 합 anchor로 가드 |
| R-3 | 기존 복합 사용자 데이터 | `sharedFacilityArea` 단일값 | normalize에서 `{kind:"other"}` 1행으로 이전(③ 마이그레이션) |
| R-4 | 800줄 | 계산서 표 6종 | §6 Phase C 분할을 디자인에 선반영(`feedback_pdca_session_efficiency`) |

---

## 9. 산출물 체크리스트 (Do 완료 정의)

- [ ] engine.design.md + ui.design.md (자가 검토 2사이클)
- [ ] Pre-Do anchor 5건 실행·환류 기록
- [ ] 엔진 G-1~G-5 + 회귀 anchor(기존 176건 + 신규 전부 통과)
- [ ] 폼 ①~⑧ 동기화 + 마이그레이션
- [ ] 계산서 컴포넌트 6분할 + 인쇄 채널 교체
- [ ] PDF 3작성례 원단위 anchor 전 행 통과 (A-1~A-5 포함)
- [ ] E2E 신규 2건 + 기존 12건 회귀 (`E2E_PORT=3100`)
- [ ] `tsc` 0 · `npm test` 전체 통과 · lint 0
- [ ] 결과 숫자 "원" 미표기·금액 우측정렬·내부 id 미노출 grep 자가점검

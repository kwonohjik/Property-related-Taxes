# F16 별건 잔여 7건 — 작업 계획서

> **범위**: F16(컴패니언 이월과세, `ceafe4b1`) 구현 중 드러난 별건 9건 중, 이미 해소된 2건을 뺀
> **잔여 7건**. 원 목록은 [`transfer-review-2026-08-open-items.plan.md`](transfer-review-2026-08-open-items.plan.md)
> §「🆕 F16 작업이 새로 연 별건」.
>
> **전부 F16이 만든 것이 아니라 이미 살아 있던 결함**이다. F16은 그것들을 **보이게** 했을 뿐이다.
> 단, **N-2는 예외** — F16이 컴패니언 `carryoverTaxation`을 도달 가능하게 만들면서
> **위험의 성격이 바뀌었다**(아래 참조).
>
> **이 문서가 아닌 것**: 미결 3건(F03·F08·F23)은 별도 문서 §F03~F23. 재개발·입주권 축은
> 사용자 지시로 **최종 패스** 대기. F17·F27은 신규 기능 트랙.

## 진행 상태

| ID | 항목 | 성격 | 수치 | 안전망 |
|---|---|---|---|---|
| ~~**N-1**~~ ✅ | §97의2②3호 비교 스코프 자산별 → **신고단위** | 계산 축 | ✅재현 **7/300** · 750,000~20,900,000 **과소** → 전건 교정 | anchor 3파일 19건 |
| ~~N-2~~ | ~~`splitCompanionIntoTwo` dead code~~ | ✅ **해소** | **48,200,001 과대**(세율축) · 안분 없이 켰다면 37,800,000 과소 | 0건 → **anchor 8건** |
| ~~N-3~~ | ~~부담부증여 aggregate 양도가액 열 0~~ | 🔵 **재현 불가 = 결함 아님** | 열 산술 **자기일관** | 0건 → **characterization 4건** |
| ~~N-4~~ | ~~환산 산식 텍스트 드리프트~~ | ✅ **해소** — 재현됨 | 인쇄 등식 **차이 100,000,000**(표시 전용) | 0건 → **anchor 4건** |
| ~~N-5~~ | ~~§97②2호 swap 잔여 드리프트~~ | 🔵 **원 주장 기각** + 좁은 잔여만 수정 | 양도차익 산식은 **정확했다** | **anchor 4건** |
| ~~N-6~~ | ~~컴패니언 phd/apd ⑫ 배관 부재~~ | ✅ **해소** — (B) 입력 경로 정리 | **세액 변화 0** (지금도 무시되는 값) | 0건 → **anchor 5건** |
| ~~N-7~~ | ~~`schema-sub.ts` 706줄~~ | ⏸ **분리 안 함** (사용자 결정) | — | — |
| ~~N-0~~ | ~~`giftTaxAmount` 지분 스케일 여부~~ | ✅ **종결** — 아래 §N-0 | — | anchor R-1c |

> 🔴 **7건 중 5건이 「수치 미측정」이다.** 저장소 규칙상 **착수 전 재현이 선행 조건**이며,
> 재현되지 않으면 결함이 아닐 수 있다. 「고쳤는데 아무것도 안 바뀐다」가 이 저장소의 반복 실패 모드다.

---

## 착수 전 확정 사실 (2026-08-23 전수 재검증)

`affbcc80`·`a8ae1583`으로 라인이 이동해 **전 인용을 다시 열어 확인**했다.

| 주장 | 판정 | 근거 |
|---|---|---|
| §97의2②3호 비교가 **자산별**이다 | ✅ | `transfer-tax-carryover.ts:585-586` `determinedTaxA >= determinedTaxB ? "A" : "B"` (원 문서 `:585-587` → **`:585-586`**으로 정정) |
| `resolveCompanionLandRate`가 항상 `applied:false` | ✅ | `appurtenant-land-rate.ts:213-215`가 `landNature !== "appurtenant_to_housing"`이면 즉시 반환. 호출부 `bundled-split-helpers.ts:463-465`는 `{ assetKind: "land", area: companion.areaM2 }`만 넘긴다 — **`landNature` 미전달** |
| `splitCompanionIntoTwo`가 금액을 **5필드만** 분할 | ✅ | `bundled-split-helpers.ts:513-517` — `transferPrice`·`acquisitionPrice`·`expenses`·`capitalExpenditure`·`transferExpense`. 나머지는 `{...base}`(`:521`·`:539`)로 **양쪽에 복제** |
| N-2 안전망 0건 | ✅ | `grep -rln "splitCompanionIntoTwo\|resolveCompanionSplit" __tests__/` → **0파일** |
| 산식이 결과값을 **별도 인자**로 받는다 | ✅ | `DetailedStatementFormulaBuilders.ts:102-113` `buildAllocationFormula(total, numerator, denomParts, resultValue)` — 좌변과 우변이 **독립**이라 산술 불일치가 그대로 인쇄된다. 호출 `:314`(토지)·`:317`(건물)·`:325`(증축) |
| swap 시 환산취득가액 미차감 | ✅ | `transfer-tax-helpers.ts:250-258` `mode: "swap_to_direct"` + 주석 「환산취득가액은 별도 차감하지 않는다」 |
| 컴패니언 ⑫에 PHD 없음 | ✅ | `transfer-tax-schema-sub.ts:576`에 `preHousingDisclosureSchema` **정의만** 있고 `companionAssetSchema`(`:287`)가 **참조하지 않는다**. 단건은 `transfer-tax-schema.ts:347`에서 사용 |
| `schema-sub.ts` 706줄 | ✅ | 착지목표 ≤700 **6줄 초과** · hard cap 800 미달 |
| aggregate 양도가액 열의 출처 | ✅ | `transfer-tax-aggregate.ts:596` `transferPrice: r.singleInput.transferPrice` |
| N-0 UI 문구 배선 | ✅ | `CarryoverGiftBlock.tsx:63`(상수) → `:243`·`:259`·`:275` 3칸 |

---

## N-0 · `giftTaxAmount` 지분 스케일 — ✅ 종결 (2026-08-23)

원 항목은 「미스케일(현행) 유지. **바꾸려면 `OwnershipRatioInput` 안내 문구와 함께 결정**」이었다.
`a8ae1583`이 그 조건을 **반대 방향으로 닫았다** — 미스케일을 유지하되 UI가 그것을 **명시**한다:

> 「지분 모드 예외 — 100% 기준으로 환산하지 말고 증여세 신고서에 적힌 금액을 그대로 입력합니다」

⇒ UI 계약 · ④ 변환 · anchor(R-1c·GBSC-02) **3중 일치 성립**. 더 이상 미결이 아니다.
바꾸려면 세 층을 함께 바꿔야 한다.

---

## N-1 · §97의2②3호 비교 스코프: 자산별 → 신고단위

**위치**: `lib/tax-engine/transfer-tax-carryover.ts:585-586`

**결함(주장)**: 「소득세법」 §97의2②3호의 비교과세는 **결정세액**을 비교하는데, 결정세액은
~~§92②~~ → **§92③2호**의 신고단위 개념이다. 현행은 **자산별**로 A/B를 비교한다.

> 📄 **B-5 착수 계획서 분리**: [`transfer-n1-carryover-filing-unit.plan.md`](transfer-n1-carryover-filing-unit.plan.md) (2026-08-23)
> — 선결 2건 중 **① divergence 재현 완료**(300 격자 **7건**, 전부 현행 과소 750,000~20,900,000),
> **② 안전망은 여전히 미측정**(P-0). 조문 인용 정정(위 취소선) · 예규 **미발견**(검색 6종 0건) ·
> memory 「부동산 다자산 갭 없음」은 **정정 대상**. 착수 전 **Q-N1(해석 채택)·Q-N2(다중 자산 알고리즘)** 확인 필요.

**⚠️ 착수 전 선결 — 2건**

1. 🔴 **memory와 정면 상충한다.** `project_stock_carryover_97_2_necessary_expense_open.md:169-172`가
   같은 축을 「갭 없음」으로 기록했다. F16 조사 축이 divergence **16/562**를 냈으나
   **이번 세션에서 재현하지 않았다.** 둘 중 하나가 틀렸다 — 재현이 선행 조건이다.
2. 🔴 **안전망 미측정.** 착수 전 characterization anchor로 현행을 고정해야 한다.

**보고된 수치(⚠️ 미재현)**: 격자 562 중 16건 불일치 · 4,570,000 ~ 39,125,000 · **전부 현행 과소**.

**참조 축**: F35에서 §104⑤ 후단(「제2호의 금액을 계산할 때 … **각각을 별개의 자산으로 보아**」)이
**산출세액 계산 전용 의제**임을 확인했다. §97의2②3호가 그 의제의 적용 대상인지, 아니면
§92② 신고단위가 그대로 적용되는지 **조문으로 갈라야 한다** — 유추 금지.

**착수 조건**
- [ ] divergence 16/562 재현 (안 되면 **중단**하고 memory 쪽이 옳다고 기록)
- [ ] characterization anchor로 현행 고정 (mutation으로 구별력 실측)
- [ ] §97의2②3호 본문 + §92② KoreanLaw verbatim 확인 — 「결정세액」의 단위를 조문에서 확정
- [ ] 예규·심판례 조회 (부존재면 그 사실을 명시)

**verify**: 신고단위 전환 후 **단건(자산 1건) 경로가 불변**일 것 — 단건에서는 두 스코프가
같아야 한다. 움직이면 전환 방향이 틀렸다.

**⛔ 금지**: `refCalculatedTax` 역안분(F03·§104⑤에서 이미 두 번 금지 확정).

---

## ~~N-2~~ · ✅ **해소 (2026-08-23)** — 아래는 착수 전 기재를 이력으로 남긴다

**결과 요약**

| | 실측 |
|---|---|
| **R-1 조문 조사** | 영 §167의5 = **면적 범위 정의뿐, 안분 명문 없음**(verbatim) · §100② 후문은 **토지·건물 축 + 가액 구분 불분명**이 요건이라 미적용 · 조심 2024서2826은 **면적 기준 분리를 전제**(647㎡ 중 222.83㎡) · 형제 경로 `calcExcessLandRatio`가 이미 **면적 비율** ⇒ **사용자 결정 불요** |
| **결함 크기 (세율 축)** | 초과분에 주택 세율이 그대로 붙었다. primary 단기(5개월) 결정세액 932,050,000 → **883,849,999** = **48,200,001원 과대**. primary 장기는 양쪽 다 누진이라 **세액 동일**(분리만 발생) |
| **2배 계상 (예측 적중)** | 안분 없이 split만 켜면 이월과세 취득가액 100,000,000·증여세 20,000,000이 **양쪽에 전액** 실려 양도차익 합 380,000,000 → 260,000,000, 결정세액 **37,800,000원 과소** |
| **안전망** | 착수 전 **0건**(`splitCompanionIntoTwo`·`resolveCompanionSplit` 참조 테스트 0파일) |

**수정**: `resolveCompanionSplit`이 `landNature`를 전달 + `splitCarryover`로 이월과세 금액 4필드
(`donorAcquisitionPrice`·`donorCapitalExpenditure`·`giftTaxAmount`·`giftDateValuation`) 안분 +
`transferArea`·`auctionPrice` 안분. 기준시가는 미스케일(환산 산식 약분).
800줄 초과로 `bundled-companion-split.ts` 분리(591 + 260).

> 🔑 **지분 축과 규칙이 반대다.** 지분(공유)에서 `giftTaxAmount`는 **미스케일**이지만
> (사용자가 실제 지분 몫을 넣으므로), split은 **한 자산을 둘로 쪼개는** 축이라 하나의 증여세를
> 나눠야 한다. 같은 필드가 축에 따라 갈리므로 한쪽 규칙을 복사하면 조용히 틀린다.

**anchor** `__tests__/api/transfer.route.companion-appurtenant-split-n2.anchor.test.ts` 8건 ·
구별력 M1(landNature 되돌림) **5/8** · M2(carryover 안분 제거) **2/8** · M3(과잉 분리) **1/8**.

> ⚠️ 조사 중 관측한 `scenarioA.acquisitionPrice = 0`은 **결함이 아니었다** — 증여등기 2019-05-01은
> §97의2 적용기간(5년)이 지나 A가 계산되지 않은 정상 동작이다. 2022-05-01로 옮기니 A가 채택된다.

---

### (이력) 착수 전 기재

**위치**: `bundled-split-helpers.ts:463-465` (미전달) · `:499-545` (분할 로직)

**메커니즘(확정)**: `resolveCompanionSplit`은 `:455`에서 `companion.landNature === "appurtenant_to_housing"`을
**이미 확인**하고도, `:463`에서 `resolveCompanionLandRate`에 `{ assetKind: "land", area }`만 넘긴다.
수신부 `appurtenant-land-rate.ts:213`이 `landNature`를 보고 `applied:false`를 반환 ⇒
`:476`에서 `{applied:false}` ⇒ `splitCompanionIntoTwo`가 **한 번도 실행되지 않는다**.
⇒ 영 §167의5 한도초과 분리가 **도달 불가**.

수정 자체는 `landNature: companion.landNature` **1줄**로 보인다. 그러나:

> ### 🔴 이 1줄이 검증되지 않은 코드 경로를 통째로 켠다
>
> `splitCompanionIntoTwo`는 금액 **5필드만** 분할하고 나머지는 `{...base}`로 **양쪽에 복제**한다
> (`:521`·`:539`). **F16이 컴패니언에 `carryoverTaxation`을 도달 가능하게 만들었으므로**,
> 지금 활성화하면 증여자 취득가액·증여세 상당액·증여 당시 평가액이 **두 파트에 각각 전액**
> 들어간다 ⇒ **2배 계상**.
>
> F16 시점에는 「dead code라 F16이 만든 위험은 0」이었다. **N-2를 고치는 순간 그 전제가 사라진다.**
> `carryoverTaxation` 말고도 `{...base}`에 실려 복제되는 금액 필드가 더 있는지 **전수 열거**가 필요하다
> (`reductions`·`burdenedGiftInfo`·`preHousingDisclosure` 등 — **확인 필요**).
>
> **안전망 0건**이라 복제가 켜져도 아무 테스트도 울리지 않는다.

**착수 순서 (강제)**
1. `{...base}`로 복제되는 **금액·이월과세 필드 전수 열거** → 표로
2. 각 필드의 분할 규칙 판정 — 안분인가, 한쪽 귀속인가, 복제가 정답인가.
   ⚠️ 「자동 안분 fallback 금지」에 걸리는 필드가 있는지 확인
3. characterization anchor — **현행(미발동) 상태**를 먼저 고정
4. `landNature` 전달 + 안분 구현을 **함께** 커밋 (1줄만 먼저 넣지 말 것)
5. 활성화 후 세액 격자로 2배 계상 부재 확인

**verify**: 한도 내 + 한도 초과 두 파트의 각 금액 **합 = 분할 전 원값**(floor 잔차 허용치 명시).

---

## ~~N-3~~ · 🔵 **재현 불가 = 결함 아님 (2026-08-23)**

원 기재가 지목한 **바로 그 경로**(GB route → aggregate)를 실제 POST했다. 열은 **자기일관**이었다.

| 취득방법 | | 양도가액 | 취득가액 | 양도차익 | 산술 |
|---|---|---|---|---|---|
| 실가 | land | 2,746,666,666 | 960,279,898 | 1,786,386,768 | ✅ |
| 실가 | building | 1,373,333,334 | 240,069,974 | 1,133,263,360 | ✅ |
| 환산 | land | 1,666,666,666 | 555,555,555 | 1,081,111,111 | ✅ |
| 환산 | building | 833,333,334 | 277,777,778 | 540,555,556 | ✅ |

- `acquisitionMethod` enum은 `"actual" | "converted"` **둘뿐**이라(⑫) 위 둘이 전수다.
- **양도가액 합 = 4,120,000,000 = 채무액**(보증금 10억 + 근저당 31.2억). 즉 §159 유상분이
  정확히 실린다 — 입력 `transferPrice`(25억)와 다른 것이 **정상**이고, 이 차이를 모르면
  「열이 이상하다」는 오해가 생긴다.
- 다른 aggregate 진입점에는 **도달 자체가 불가**하다 — `burdenedGiftInfo`가
  `multi/route.ts`·`transfer-tax-schema-sub.ts`(컴패니언)에 **0건**이라 다자산·컴패니언
  경로는 부담부증여를 받지 않는다.

⇒ **결함 아님으로 종결.** 다만 이 축의 안전망이 **0건**이었기에(그래서 미검증 주장이 문서에
남았다) 현행을 characterization anchor로 고정했다 —
`__tests__/api/transfer.route.bg-aggregate-columns-n3.anchor.test.ts` 4건 ·
구별력 표시 열 무력화 시 **4/4** red.

### F27 흡수 여부 — 불요

F27은 **§159 분기의 §97의2 배선**(계산 축·`legal-codes/burdened-gift.ts` 상수 정정 포함)이고
N-3은 **aggregate 표시 열**이다. 층도 파일도 겹치지 않는다.

---

### (이력) 착수 전 기재

**위치**: `lib/tax-engine/transfer-tax-aggregate.ts:596`

**결함(주장)**: 표시 열이 `r.singleInput.transferPrice`를 그대로 읽는데, §159 부담부증여의
**유상분**은 `burdenedGiftInfo`에서 파생되므로 그 열이 0(또는 총액)이 되어 열 산술이 깨진다.
⑧이 마법사 경로를 막지만 **GB route는 aggregate를 거쳐 도달**한다.

**⚠️ 미재현**: 「0이 된다」를 이번 세션에서 관측하지 않았다. `:596`이 `singleInput.transferPrice`를
읽는다는 것만 확정했다. **착수 전 route POST로 재현할 것** — 재현 안 되면 결함이 아니다.

**착수 전 확인**
- [ ] 「양도가액 열 0」 재현 (GB route 경유)
- [ ] **F27(GB × 부담부증여 × 이월과세) 신규 기능 트랙과 범위가 겹치는지** — 겹치면 그쪽에 흡수
- [ ] 「수정 전후 모두 깨져 있다」 = F16이 만든 것이 아님을 base 커밋에서 확인

**주의**: 표시 계층만 고치고 계산은 건드리지 말 것. 「차감값↔결과 표시 일관성」 원칙상
**표시가 계산을 유도하도록** 만들면 안 된다.

---

## ~~N-4~~ · ✅ **해소 (2026-08-23)** — 재현됐다

**실측**: GB 이월과세 토지 파트에서 인쇄된 등식이 **산술적으로 거짓**이었다.

| | 인쇄된 산식 | 좌변 실계산 | 우변(실제 취득가액) |
|---|---|---|---|
| 이월과세 파트 | `500,000,000 × 100,000,000 / 200,000,000 = 150,000,000` | **250,000,000** | 150,000,000 |

차이 **100,000,000**. 환산·실가 **두 모드 모두** 어긋났다(이월과세면 항상).
원인은 `buildAllocationFormula(total, numerator, denomParts, resultValue)`가 **좌변과 우변을
독립 인자**로 받는데, A-9(`ceafe4b1`) 이후 `p.acquisitionPrice`가 **채택 시나리오 값**이라는 것.

🔑 **단건 경로는 이미 정답을 갖고 있었다** — `buildAcquisitionPriceFormula`에
`carryoverTaxationDetail.adoptedScenario === "A"` 분기가 있다. GB 파트별 경로에만 없어서
같은 사실을 두 화면이 다르게 그렸다. ⇒ 같은 분기를 GB에도 추가(선택지 (가)).

**세액 영향 0** — 표시 전용. 다만 신고서 성격상 검산 가능한 등식이 거짓인 것은 무겁다.

anchor `__tests__/api/transfer.route.gb-carryover-formula-drift-n4.anchor.test.ts` 4건 ·
구별력 분기 무력화 시 **2/4** red(나머지 2건은 대조군 — 비이월과세 파트가 종전 산식을 유지하는지 본다).

---

### (이력) 착수 전 기재

**위치**: `DetailedStatementFormulaBuilders.ts:314`·`:317`·`:325`

**메커니즘(구조 확정 · 수치 미측정)**: `buildAllocationFormula(total, numerator, denomParts, resultValue)`는
**좌변 피연산자와 우변 결과를 독립 인자로** 받아 `A × B / C = D` 문자열을 만든다(`:102-113`).
F16 A-9(`ceafe4b1`)로 이월과세 자산의 `acquisitionPrice`가 **채택 시나리오 값**이 되었으므로,
이월과세 × 환산 조합에서 인쇄된 좌변이 우변을 **유도하지 않을 수 있다**.

⇒ 화면에 **산술적으로 거짓인 등식**이 인쇄된다. 표시 전용(세액 영향 0)이지만 신고서 성격상 무겁다.

**⚠️ 수치 미측정**: 실제로 어긋나는 조합을 **관측하지 않았다**.

**착수 조건**
- [ ] 어긋나는 픽스처를 실제로 찾을 것 (이월과세 × 환산 × 일반건물)
- [ ] 못 찾으면 **결함 아님**으로 종결하고 근거를 남길 것

**선택지**
| 안 | 내용 |
|---|---|
| (가) | 이월과세 채택 시 산식을 **§97의2① 1호 서술**로 교체(「증여자 취득 당시 …」) |
| (나) | 환산 산식은 그대로 두되 **채택 결과를 별도 행**으로 병기 |

**N-5와 같은 배치**로 다루는 것이 낫다 — 둘 다 「표시 산식이 결과를 유도하지 않는다」는 동일 부류다.

---

## ~~N-5~~ · 🔵 **원 주장 기각 (2026-08-23)** — 좁은 잔여만 수정

**원 기재**: 「환산취득가액이 양도차익에서 차감되지 않는데 **표시 열은 환산 재산식을 그린다**」

⇒ **성립하지 않는다.** 양도차익 산식은 **정확했다**. 엔진 step 실측:

| | 양도차익 산식 | 검산 |
|---|---|---|
| swap 발동 | `양도가(1,000,000,000 - 필요경비(자본적지출+양도비 400,000,000` | 600,000,000 ✅ |
| 대조군 | `양도가 … - 취득가(환산 100,000,000 - 경비(개산공제 1,500,000` | 898,500,000 ✅ |

취득가액을 빼지 않음을 **명시**한다. 메인 결과뷰에도 swap 고지 카드가 이미 있다
(`TransferTaxResultView.tsx` — 「필요경비 swap 적용 — 소득세법 §97② 2호 단서」).

### 좁은 잔여 — 이것만 고쳤다

상세명세서(신고서 서식) **취득가액 행**은 `usedEstimatedAcquisition`이면 swap 여부와 무관하게
환산 산식을 인쇄했다. 세 행이 나란히 놓이면 산술이 안 맞아 **보인다**:

> 취득가액 100,000,000 · 필요경비 400,000,000 · 양도차익 600,000,000
> → 10억 − 1억 − 4억 = 5억 ≠ 6억

⇒ 취득가액 행에 **차감 제외 사실만** 덧붙였다. 산식·값은 건드리지 않았다 — 환산취득가액은
실재하는 값이고 나목 채택 여부와 별개로 §97②2호 가목의 구성요소다.

anchor `__tests__/calc/transfer-swap-acq-row-note-n5.anchor.test.ts` 4건 ·
구별력 고지 무력화 시 **2/4** red(나머지 2건은 대조군 — swap 미발동·실가 모드에 노이즈가 없는지).

---

### (이력) 착수 전 기재

**위치**: `lib/tax-engine/transfer-tax-helpers.ts:250-258`

**메커니즘(구조 확정 · 수치 미측정)**: `mode: "swap_to_direct"`일 때 **환산취득가액을 차감하지 않는다**
(양도차익 = 양도가액 − 나목). 엔진 주석이 그렇게 명시하고 **계산은 옳다**.
그런데 표시 열은 환산 재산식을 그린다 ⇒ 인쇄된 산식이 실제 차감 구조와 어긋난다.

**⚠️ 수치 미측정**: F16 픽스처에서 **미발현**이었다.

**착수 조건**
- [ ] 발현 픽스처 확보 (swap 발동 + 표시 열 관측)
- [ ] 못 찾으면 종결

**참조**: memory `feedback_97_2_swap_necessary_expense_max_not_sum`(swap은 **택일(max)**이지 합이 아니다) ·
`feedback_estimated_mode_expenses_pattern`.

---

## ~~N-6~~ · ✅ **해소 (2026-08-23)** — (B) 입력 경로 정리

**R-2 조사 결과 — (a)가 no-op임이 실측으로 증명됐다**

| 지점 | 상태 |
|---|---|
| ⑤ UI | PHD 토글을 **컴패니언에도 렌더**했다 |
| ⑧ validate | `usePreHousingDisclosure`면 **11필드를 필수로 요구**(`:449-480`, 자산 인덱스 무관) |
| ④ 변환 | `buildPreHousingDisclosurePayload(primary, …)` — **primary 전용, 호출 1곳** |
| ⑫ Zod | `companionAssetSchema`에 필드 **자체가 없다** |

⇒ 사용자는 11칸을 **강제로** 채워야 했고 그 값은 통째로 버려졌다.

🔑 **⑫ + ⑭를 모두 배관해도 응답이 바이트 동일**했다(결정세액 419,028,462 불변,
`preHousingDisclosureDetail` 부재). `calcSplitGain`이 **`!input.landAcquisitionDate`면 즉시
`null`**을 반환하는데 `landAcquisitionDate`가 ⑫·⑭ 어디에도 **없다** — 컴패니언은
토지·건물 분리취득 자체를 지원하지 않아 PHD 분기에 **진입조차 못 한다**.
⇒ 최소 배관은 완전한 no-op이고, 정식 지원은 **신규 기능 규모**다.

**채택 (사용자 결정)**: (B) — ⑤에서 토글 대신 안내를 띄우고, ⑧의 11필드 요구를 끈다.
**세액 변화 0**(지금도 무시되는 값이라 기능을 빼앗는 게 아니다). 두 층이 **같은 술어**
(첫 자산 여부)를 보게 해 「화면엔 있는데 검증이 막는다」가 재발하지 않게 했다.

anchor `__tests__/calc/transfer-companion-phd-unsupported-n6.anchor.test.ts` 5건 ·
구별력 ⑧ 게이트 되돌림 시 **2/5** red · 기본값 `false`라 기존 호출부 무변경(N6-05가 고정).

> ⚠️ **TDZ 발견** — `preHousingDisclosureSchema`(`:577`)가 `companionAssetSchema`(`:287`)보다
> **아래**에 선언돼 있어, (a)를 택했다면 참조 순간 런타임 오류가 났다. (A)를 나중에 하려면
> 선언 순서부터 정리해야 한다.

> ✅ **남은 것도 해소 (2026-08-23 `d121ea35`)** — (A) 컴패니언 분리취득 정식 지원.
> ⭐ **착수 조사가 예상을 뒤집었다**: ⑤ UI는 **이미 컴패니언에 렌더되고**(`isSplitable`이 자산
> 인덱스를 안 본다) ④ `buildSplitPayload`도 **자산-무관 공용 함수**였다 — 없던 것은 ④의 **호출**과
> ⑫·⑭ 뿐이었다. 실측 Δ **10,210,000**(장특 63,000,000 → 88,500,000). 단건과 값 일치.
> ⑫는 `splitAcquisitionShape` 한 벌을 단건·컴패니언이 spread하고 ⑭ 타입은 그 shape에서 **파생**한다.
> PHD(§164⑤)는 여전히 컴패니언 미지원 — (B)의 ⑤·⑧ 게이트를 유지한다.

---

### (이력) 착수 전 기재

**위치**: `lib/api/transfer-tax-schema-sub.ts` — `companionAssetSchema`(`:287`)에 `preHousingDisclosure` 없음

**결함(확정 구조)**: `preHousingDisclosureSchema`는 **같은 파일 `:576`에 정의돼 있고** 단건
(`transfer-tax-schema.ts:347`)은 쓰는데, **컴패니언 스키마만 참조하지 않는다.**

🔑 **직전 커밋 `a8ae1583`이 고친 것과 정확히 같은 부류**다 — ⑫가 필드를 모르면 침묵 strip된다.
그때는 「④가 보내는데 ⑫가 버린다」였고, 여기는 「⑫에 칸 자체가 없다」이므로
**④가 보내는지부터 확인해야 한다.**

**⚠️ 미측정**: 「base가 더 과대였으므로 방향은 나빠지지 않았다」는 F16 축의 관측이고
**divergence 수치가 없다**.

**착수 조건 (순서 강제)**
1. [ ] **④가 컴패니언에 PHD를 싣는가?** 안 싣으면 ⑫만 열어도 **no-op**이다
   (「API 트리거만 열면 no-op」 — 이 저장소의 반복 실패 모드)
2. [ ] ⑤ UI가 컴패니언에 PHD 입력을 **렌더하는가?** 없으면 입력 경로 자체가 없다
3. [ ] ⑧이 이 조합을 막는가?
4. [ ] divergence 실측

**선택지**
| 안 | 내용 | 조건 |
|---|---|---|
| **(a)** ⑫ 배관 | 단건과 같은 스키마 재사용 | ④·⑤가 이미 있을 때만 의미 있다 |
| **(b)** ⑧ 차단 | 컴패니언에서 PHD 조합을 명시 거부 | 입력 경로가 없으면 이쪽이 정직하다 |

> ⚠️ **PHD §164⑦은 「자동 안분 fallback 금지」의 유일한 예외**다. 배관을 열 때
> 그 예외가 컴패니언에도 그대로 적용되는지 조문으로 확인할 것 — 유추 금지.

---

## ~~N-7~~ · ⏸ **분리하지 않는다 (2026-08-23 사용자 결정)**

N-6이 (B)로 결론나 `schema-sub.ts`에 필드가 늘지 않는다 ⇒ 파일이 커지지 않으므로
저장소 정책(「700~749에 안정적으로 앉은 파일을 커지지도 않는데 미리 쪼개면 순수 낭비」)대로
**분리하지 않는다**. 706줄 유지(hard cap 800 여유).

⚠️ 나중에 컴패니언 분리취득을 정식 지원(N-6 (A))할 때 **선언 순서(TDZ)와 함께** 정리할 것.

---

### (이력) 착수 전 기재

착지목표 ≤700을 **6줄 초과**, hard cap 800 미달.

> 🔑 **단독으로 하지 말 것.** 「700~749에 안정적으로 앉은 파일을 커지지도 않는데 미리 쪼개면
> 순수 낭비」가 저장소 정책이다. **N-6과 한 배치**로 묶어, N-6이 필드를 더해 파일이 커지는
> 그 김에 **기회주의적으로** 분리한다(고정비인 별도 PR·리뷰·전체 테스트 회피).
>
> 분리 시 **export 보존**에 주의(memory `feedback_800line_split_export_preservation`).

---

## 배치 제안

| 배치 | 항목 | 근거 |
|---|---|---|
| ~~B-1~~ | ~~**N-2**~~ | ✅ **완료 2026-08-23** — 경고가 적중했다. 「1줄」이 아니라 안분 설계가 본체였고, 안분 없이 켰다면 37,800,000원 과소가 켜졌다 |
| ~~B-2~~ | ~~**N-6 + N-7**~~ | ✅ **완료 2026-08-23** — N-6은 (B) 입력 경로 정리, N-7은 파일이 안 커져 **분리 없음** |
| ~~B-3~~ | ~~**N-4 + N-5**~~ | ✅ **완료 2026-08-23** — N-4 재현·수정, N-5 **원 주장 기각** 후 좁은 잔여만. 파일 795줄(위험구간) → `DetailedStatementGbFormulas.ts` 분리(415 + 408) |
| ~~B-4~~ | ~~**N-3**~~ | ✅ **완료 2026-08-23** — **재현 불가로 종결**. F27과 층이 달라 흡수 불요 |
| ~~**B-5**~~ ✅ | **N-1** | 완료 — [`transfer-n1-carryover-filing-unit.plan.md`](transfer-n1-carryover-filing-unit.plan.md) §7 |

**권장 순서**: B-1 → B-2 → B-3 → B-4 → B-5.

- **B-1을 먼저** 하는 이유: F16이 만든 잠재 위험(carryover 복제)을 **가장 먼저 닫는다**.
  이것만 미해결로 두면, 이후 누군가 「1줄이니까」 하고 고쳐 2배 계상을 켤 수 있다.
- **B-5를 마지막**에 두는 이유: 착수 조건(재현·조문 확정·anchor)이 가장 무겁고,
  실패 시 「결함 아님」으로 끝날 가능성도 실재한다.
- **B-3·B-4는 재현 실패 시 종결**될 수 있다 — 착수 비용이 낮으니 먼저 재현만 시도해
  살아남는 것만 본작업으로 넘겨도 된다.

---

## 결정 대기 항목

> ⚠️ **초판이 아래 R-1·R-2를 「사용자 결정」으로 잘못 분류했다.** 둘 다 **조사가 먼저**이고,
> 조사가 답을 내면 질문 자체가 사라진다. 조문·실측을 건너뛰고 물으면 「법 근거 없이 정한 값」이
> 된다 — 이 저장소에서 가장 비싼 실수다.

### R — 착수자가 조사해서 답한다 (갈리면 그때 올린다)

| # | 항목 | 조사 순서 | 사용자에게 올라오는 조건 |
|---|---|---|---|
| **R-1** | **N-2 안분 규칙** — 한도 내/초과 두 파트에 이월과세 금액을 어떻게 나누는가 | ① 영 §167의5에 **한도 내/초과 안분 명문**이 있는가 (KoreanLaw verbatim) ② 「소득세법」 §100② 후문(「공통되는 취득가액과 양도비용」)이 이 축에 미치는가 — ⚠️ F23에서 **자본적지출은 미열거**로 확인됐다. 이월과세 금액이 열거 안에 드는지 별도 판정 ③ 예규·심판례 조회(부존재면 그 사실 명시) ④ **조문이 침묵하면 「자동 안분 fallback 금지」가 이미 답이다** — 임의 안분이 아니라 **검증 오류로 차단**하거나 사용자 입력을 요구 | ①~④를 다 거쳤는데도 **둘 이상의 안이 법적으로 성립**할 때만 |
| **R-2** | **N-6이 (a)⑫배관이냐 (b)⑧차단이냐** | V-7 실측(④가 싣는가 · ⑤가 렌더하는가)이 사실상 답을 정한다 — 입력 경로가 없으면 (a)는 **no-op**이라 (b)뿐이다 | ④·⑤가 **둘 다 존재**해서 (a)가 실제로 동작할 때 — 그때 비용 대비 가치를 물음 |

### Q — 지금 사용자 결정이 필요하다

| # | 질문 | 왜 조사로 답이 안 나오나 |
|---|---|---|
| ~~Q-1~~ | ~~N-3을 F27에 흡수할 것인가~~ | ✅ **조사로 답이 나왔다** — F27은 계산 축(§159 분기의 §97의2 배선), N-3은 표시 열. 층도 파일도 안 겹쳐 **흡수 불요**. 게다가 N-3은 재현 불가로 종결됐다 |
| **Q-2** | **재현 실패 항목의 처리 정책** — N-3·N-4·N-5 중 재현이 안 되는 것은 「결함 아님」으로 종결할 것인가, 더 깊이 팔 것인가 | 7건 중 5건이 미측정이라 이 정책이 **전체 일정을 좌우**한다. 어디서 멈출지는 비용 판단이다 |

---

## V — 착수 전 실측해야 할 것 (전건 미완)

| ID | 대상 | 실패 시 |
|---|---|---|
| ~~**V-1**~~ ✅ | N-1 divergence 재현 | **재현됐다**(7/300 · 격자가 달라 건수는 다름) ⇒ memory 쪽을 정정했다 |
| **V-2** | N-2 `{...base}` 복제 필드 **전수 열거** | 열거 없이 활성화 금지 |
| **V-3** | N-2 활성화 후 2배 계상 부재 | 안분 설계 재검토 |
| **V-4** | N-3 「양도가액 열 0」 재현 | N-3 종결 |
| **V-5** | N-4 어긋나는 산식 픽스처 확보 | N-4 종결 |
| **V-6** | N-5 swap 발현 픽스처 확보 | N-5 종결 |
| **V-7** | N-6 ④·⑤ 존재 여부 | 없으면 (a)는 no-op ⇒ (b) |
| **V-8** | 각 항목 안전망 mutation 실측 | 0건이면 anchor 선행 |
| **V-9** | **R-1 조문 조사** — 영 §167의5 안분 명문 · §100② 후문 포섭 여부 · 예규 | 전부 침묵이면 「자동 안분 금지」 ⇒ 차단·입력 요구 |
| **V-10** | **R-2 입력 경로** — ④가 컴패니언에 PHD를 싣는가 · ⑤가 렌더하는가 | 없으면 (a)는 no-op ⇒ (b) 확정 |

---

## 공통 규율 (전 항목)

- **재개발·재건축·입주권 축은 손대지 않는다** — 사용자 지시로 최종 패스 대기
- 정수 연산 `Math.round()` 금지 · Date는 `lib/api/date-coerce.ts` · 조문 문자열 리터럴 금지
- **자동 안분 fallback 금지**(예외: PHD §164⑦) · UI↔엔진 dual-truth 금지 · `useEffect → store` 미러링 금지
- anchor 파일명·경로에 `transfer` 등 `npm run test:transfer` positional 토큰 **필수**
  (없으면 게이트에서 조용히 빠진다 — 이번 리뷰에서 3번 재발)
- 파일 800줄 hard cap · 착지목표 ≤700
- 게이트: `npx tsc --noEmit` 0 · `npm run lint` error 0 · `npm run test:transfer`
  **base 605파일 6,711 pass**(`a8ae1583` 기준) 이상
- **수정마다 구별력을 실측**한다 — mutation으로 red 건수를 세고, red 0인 anchor는 다시 쓴다
- ⚠️ **직전 커밋들의 확정 결정을 뒤집지 말 것**: Q3 반전(GB 지분 스케일, 사용자 재가) ·
  F16 A-10 스케일 3필드 · `giftTaxAmount` 미스케일 · 기준시가 미스케일

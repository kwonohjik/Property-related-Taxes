# §155 1세대1주택 의제 미구현 4개 항 — ⑦ 농어촌 · ⑧ 수도권밖 부득이 · ⑯ 공공기관이전 · ⑱ 기한예외 — v1.4

> 선행 계획서 [`transfer-surcharge-155-deeming-coverage.plan.md`](./transfer-surcharge-155-deeming-coverage.plan.md)
> §9.6 **D-5**에서 분리. 앞 계획서는 「이미 있는 배제를 올바르게 연결」하는 일이었고,
> 이 문서는 **없는 조문을 새로 구현**하는 일이다.
>
> **작성 원칙**: 법문은 법제처 Open API 원문 인용, 인용 file:line은 실제 파일 대조,
> 수치는 probe 실측. 미확인은 🔶로 명시하고 단정하지 않는다(CLAUDE.md 검증 기준).

---

## 0. 왜 네 항을 묶는가

넷 다 **§155의 1세대1주택 의제**이고, 넷 다 **비과세(§154①)와 중과 배제(영 §167의10①15호·
§167의3①13호)에 동시에** 걸린다. 앞 계획서 Phase B가 `deemedOneHouseBy155`라는 **단일 슬롯**을
만들어 뒀으므로, 각 항은 「비과세 판정 + 그 슬롯 채우기」 한 번으로 양쪽에 반영된다.

따로 구현하면 같은 배선을 네 번 반복하게 된다.

---

## 1. 검증된 법령

「소득세법 시행령」 MST 286211 · 시행 2026-07-01 · 2026-07-31 법제처 Open API 전문 조회.

### 1.1 §155⑯ — 공공기관 지방이전 시 §155① 「3년」→「5년」

> 제1항을 적용(**수도권에 1주택을 소유한 경우에 한정**한다)할 때 수도권에 소재한 법인 또는
> 「지방자치분권 및 지역균형발전에 관한 특별법」 제2조제14호에 따른 공공기관이 **수도권 밖의
> 지역으로 이전**하는 경우로서 법인의 임원과 사용인 및 공공기관의 종사자가 구성하는 1세대가
> 취득하는 다른 주택이 해당 공공기관 또는 법인이 **이전한 시·군 또는 이와 연접한 시·군**의
> 지역에 소재하는 경우에는 제1항 중 **"3년"을 "5년"으로 본다**. 이 경우 해당 1세대에 대해서는
> **종전의 주택을 취득한 날부터 1년 이상이 지난 후 다른 주택을 취득하는 요건을 적용하지 아니한다.**

⇒ 효과 **둘**이다: ① 처분기한 5년 ② **1년 요건 면제**. ②를 빠뜨리기 쉽다.

> ⚠️ 항번호는 법제처 API가 ⑯ 이후를 렌더하지 않아 **순서로 역산**했다(앞 계획서 §1.4 각주와 동일 근거):
> ⑮(다가구) → **⑯**(공공기관 이전) → ⑰(삭제) → ⑱(§89①4호나목 사유) → ⑲(협의분할) → ⑳(장기임대 거주주택).

### 1.2 §155⑱ — §155① 3년 기한의 예외 사유 5종

§155① 본문 괄호가 이를 준용한다: 「… 3년 이내에 종전의 주택을 양도하는 경우
**(제18항에 따른 사유에 해당하는 경우를 포함한다)**」.

> 법 제89조제1항제4호나목에서 "대통령령으로 정하는 사유"란 **다른 주택을 취득한 날부터 3년이
> 되는 날 현재** 다음 각 호의 어느 하나에 해당하는 경우를 말한다.
> 1. 한국자산관리공사에 **매각을 의뢰**한 경우
> 2. 법원에 **경매를 신청**한 경우
> 3. 「국세징수법」에 따른 **공매가 진행 중**인 경우
> 4. 재개발·재건축·소규모재건축사업등의 시행으로 **현금청산금 지급 소송 진행 중** 또는 종료됐으나 **미지급**
> 5. 재개발·재건축·소규모재건축사업등의 시행으로 **수용재결·매도청구소송 진행 중** 또는 종료됐으나 **미지급**

⇒ 판정 기준시점이 **「3년이 되는 날 현재」**로 고정돼 있다. 양도일이 아니다.

### 1.3 §155⑧ — 부득이한 사유로 취득한 수도권 밖 주택

> 재정경제부령으로 정하는 **취학, 근무상의 형편, 질병의 요양, 그 밖에 부득이한 사유**로 취득한
> **수도권 밖에 소재하는 주택**과 그 밖의 주택(일반주택)을 국내에 각각 1개씩 소유하고 있는 1세대가
> **부득이한 사유가 해소된 날부터 3년 이내**에 일반주택을 양도하는 경우에는 국내에 1개의 주택을
> 소유하고 있는 것으로 보아 제154조제1항을 적용한다.

중과 쪽 대응 호는 §167의10①**4호**「제155조제8항에 따른 수도권 밖에 소재하는 주택」이다 —
15호를 거치지 않고 **직접** 배제된다(앞 계획서 F-4 ⑧행에서 「별개 호」로 짚은 것).

### 1.4 §155⑦ — 농어촌주택 + 일반주택

> 다음 각 호의 어느 하나에 해당하는 주택으로서 **수도권 밖의 지역 중 읍지역(도시지역안의 지역을
> 제외한다) 또는 면지역**에 소재하는 주택(**농어촌주택**)과 그 밖의 주택(일반주택)을 국내에 각각
> 1개씩 소유하고 있는 1세대가 **일반주택을 양도**하는 경우에는 국내에 1개의 주택을 소유하고 있는
> 것으로 보아 제154조제1항을 적용한다. 다만, **제3호의 주택에 대해서는 그 주택을 취득한 날부터
> 5년 이내에 일반주택을 양도하는 경우에 한정**하여 적용한다.
> 1. **상속**받은 주택(피상속인이 **취득 후 5년 이상 거주**한 사실이 있는 경우에 한한다)
> 2. **이농주택** — 이농인이 **취득일 후 5년 이상 거주**한 사실이 있는 주택
> 3. 영농·영어 목적으로 취득한 **귀농주택**

부속 조항:

| 항 | 내용 |
|---|---|
| ⑨ | **이농주택** 정의 — 영농·영어 종사자가 전업으로 다른 시·구·읍·면 전출로 거주하지 못하게 된 주택 |
| ⑩ | **귀농주택** 요건 — 2호 취득 당시 **고가주택 아닐 것**(§89①3호) · 3호 **대지 660㎡ 이내** · 4호 영농·영어 목적 · 5호 **세대전원 이사·거주** |
| ⑪ | 귀농은 **귀농 후 최초로 양도하는 1개** 일반주택에 한정 |
| ⑫ | 사후관리 — 귀농일부터 **3년 이상** 영농·영어 미종사 또는 미거주 시 **추징**(2개월 내 신고·납부) |
| ⑬ | 특례적용신고서 제출 의무 |

---

## 2. 현행 상태 (grep 실측)

| 항 | 비과세(`checkExemption`) | 중과 배제 | 입력 필드 |
|---|---|---|---|
| ⑯ | ❌ `resolveTemporaryTwoHouseDeadlineYears`에 분기 없음 | — (①에 종속) | ❌ |
| ⑱ | ❌ `judgeTemporaryTwoHouseTiming`은 3년 초과 시 무조건 false | — (①에 종속) | ❌ |
| ⑧ | ❌ 없음 | ❌ §167의10①4호 대응 필드 없음(`isUnavoidableReason`은 **3호**용) | ❌ |
| ⑦ | ❌ 없음 | ❌ | ❌ |

⇒ 넷 다 **비과세부터 없다.** 중과만 고치는 것으로는 해결되지 않는다.

---

## 3. 설계

### 3.1 `deemedOneHouseBy155` union 확장

```ts
// 현재: "temporary_two_house"
// 확장: | "unavoidable_outside_capital"   // ⑧ — 단, 중과는 §167의10①4호로 직접 배제
//       | "rural_house"                    // ⑦
```

⑯·⑱은 **①의 변형**이므로 새 값을 만들지 않는다 — `temporary_two_house` 판정 안에서 처리한다.

### 3.2 ⑯·⑱ — §155① 판정 내부 확장 (가장 작다)

```
resolveTemporaryTwoHouseDeadlineYears(input, rule):
  + if (input.temporaryTwoHouse?.publicInstitutionRelocation) return 5   // ⑯ 3년→5년

judgeTemporaryTwoHouseTiming(p):
  + oneYearWaived ||= publicInstitutionRelocation                        // ⑯ 후단
  + threeYearMet ||= disposalDelayReason !== undefined                   // ⑱ 기한 예외
```

두 함수는 앞 계획서 Phase A에서 **비과세·중과 단일 소스**가 됐으므로, 여기만 고치면 양쪽에 반영된다.

### 3.3 ⑧ — 별도 의제 경로

비과세: `checkExemption`에 E-3.7 신설. 요건 = 2주택 + 수도권 밖 부득이 주택 보유 + **해소일부터 3년 이내**
+ §154① 충족(「§154①을 적용한다」이므로 앞 계획서 N7과 같은 구조).

중과: `deemedOneHouseBy155`가 아니라 **§167의10①4호**로 직접 배제한다(법문이 15호를 거치지 않는다).
2주택 전용 배제 블록에 추가.

🔶 **미확인 — 사유 미해소 상태의 양도**: 법문은 「해소된 날부터 3년 이내」로 기산점만 정한다.
해소 전 양도가 적용 대상인지 명문이 없다. **명문 부재를 불리하게 해석하지 않는다**
(memory `feedback_no_unfavorable_application_without_legal_basis`) → 해소일 **미입력 = 미해소**로
보아 기한 제한 없이 적용하고, 결과에 근거를 명시한다.

### 3.4 ⑦ — 농어촌주택 (가장 크다)

3유형 × 소재요건 × 유형별 추가요건. 귀농(3호)만 요건이 무겁다.

```
농어촌주택 성립 =
  소재: 수도권 밖 AND (읍(도시지역 제외) OR 면)
  AND 유형별:
    1호 상속   : 피상속인 취득 후 거주 ≥ 5년
    2호 이농   : 이농인 취득 후 거주 ≥ 5년
    3호 귀농   : 고가주택 아님(취득 당시) AND 대지 ≤ 660㎡ AND 세대전원 이사·거주
                 AND 일반주택 양도가 취득일부터 5년 이내 (⑦ 단서)
```

⑪(최초 1개 한정)·⑫(3년 사후관리)는 **엔진이 판정할 수 없다**(과거·미래 양도 이력).
→ 경고(`warnings`)로 노출하고 자기선언 입력에 맡긴다. 앞선 §156의2⑬ 추징 경고와 같은 패턴.

---

## 4. Phase

| Phase | 내용 | 선행 | 규모 |
|---|---|---|---|
| **E-1** | ⑯ 공공기관 이전 — 기한 5년 + 1년 요건 면제 | — | 작음 · ✅ 2026-07-31 |
| **E-2** | ⑱ 3년 기한 예외 5사유 | E-1 | 작음 · ✅ 2026-07-31 |
| **E-3** | ⑧ 수도권 밖 부득이 — 비과세 E-3.7 + §167의10①4호 배제 | E-2 | 중간 · ✅ 2026-07-31 |
| **E-4** | ⑦ 농어촌주택 3유형 | E-3 | 큼 · ✅ 2026-07-31 |

각 Phase는 **엔진 → 14 동기화 지점 → anchor → E2E** 순으로 완결한다.
E-1·E-2는 기존 `temporaryTwoHouse` 입력에 필드를 얹는 것이라 신규 UI 섹션이 필요 없다.

---

## 5. 14 동기화 지점 — 신규 입력 필드

| Phase | 필드 | 위치 |
|---|---|---|
| E-1 | `publicInstitutionRelocation: boolean` | `temporaryTwoHouse` 하위 |
| E-2 | `disposalDelayReason?: "kamco" \| "auction" \| "public_sale" \| "cash_settlement_suit" \| "expropriation_suit"` | `temporaryTwoHouse` 하위 |
| E-3 | `unavoidableOutsideCapitalHouse?: { reason; resolvedDate? }` | top-level |
| E-4 | `ruralHouse?: { kind: "inherited"\|"farm_exit"\|"return_to_farm"; …유형별 }` | top-level |

⑫⑬⑭ 자가 grep 필수 — TypeScript가 잡지 못하는 지점이다.

---

## 6. 리스크

| ID | 리스크 | 완화 |
|---|---|---|
| G-1 | ⑯의 **1년 요건 면제**(후단)를 빠뜨림 | anchor에서 「1년 미경과 + ⑯」 조합을 별도 고정 |
| G-2 | ⑱ 판정 기준시점이 양도일이 아니라 **「3년이 되는 날 현재」** | 자기선언 입력이므로 UI 문구에 기준시점 명시 · 결과에 근거 표시 |
| G-3 | ⑧ 사유 미해소 상태의 해석(§3.3 🔶) | 유리 방향 채택 + 결과에 근거 명시 · 레지스트리 등록 |
| G-4 | ⑦ 귀농 ⑪⑫는 엔진이 판정 불가 | 경고로 노출(자동 안분 fallback 금지 원칙과 구분 — 여기는 **판정 불가**이지 임의 배분이 아니다) |
| G-5 | 넷 다 **배제를 넓히는**(감세) 방향 | before→after 실측을 anchor로 양방향 고정. 미입력 시 완전 불변을 각 Phase 회귀로 고정 |
| G-6 | `deemedOneHouseBy155` union 확장이 겸용 경로까지 파급 | 겸용은 앞 계획서 Phase B2에서 같은 정본을 호출 — anchor로 확인 |

---

## 7. 미검증 레지스트리

| ID | 항목 | 상태 |
|---|---|---|
| W-1 | §155⑧ 사유 미해소 상태 양도의 적용 여부 | 🔶 명문 부재 — 유리 해석 채택(§3.3) |
| W-2 | §155⑯ 「연접한 시·군」 판정 — 자동 판별 가능한가 | ✅ **구현 완료**(매트릭스 생성 포함) → §9.2 |
| W-3 | §155⑦ 「읍지역(도시지역안의 지역을 제외)」 자동 판별 | ✅ **구현 완료** → §9.1 |
| W-4 | §155⑯ 적용 시 조정대상지역 단축 기한(DB 2년)과의 우선순위 | 🔶 법문은 「제1항 중 3년을 5년으로」 — 5년이 덮는 것으로 구현. 조문 해석 확인 필요 |
| W-5 | 조특법 의제(15호 「또는 조세특례제한법에 따라」) 커버리지 | 🔶 앞 계획서 V-6에서 이월 — 미조사 |

---

## 9. W-2·W-3 조사·구현 (2026-07-31)

### 9.1 W-3 ✅ §155⑦ 소재 요건 — **자동 판별 구현**

법문 괄호의 위치가 판정을 가른다:

> 수도권 밖의 지역 중 **읍지역(도시지역안의 지역을 제외한다)** 또는 **면지역**

⇒ 괄호는 **읍지역에만** 걸린다. 면지역은 도시지역이어도 충족이다.

**실측(2026-07-31 · Vworld)** — 이 구분이 실제로 갈리는 사례가 있다:

| 주소 | 용도지역 | 판정 |
|---|---|---|
| 평창군 **평창읍** 하리 100 | **제2종일반주거지역 = 도시지역** | 🔴 읍이지만 **제외** |
| 평창군 **진부면** 하진부리 760 | `uname` 빈값(unknown) | ✅ 면이라 도시지역 불문 **충족** |

**판별 재료가 전부 이미 있었다**:

| 요소 | 출처 |
|---|---|
| 수도권 여부 | `regionCode`(PNU 앞 10) 시도 2자리 — 11·28·41 |
| 읍·면 | Vworld `jibun` 토큰 (풀주소·축약형 혼재 실측 대응) |
| 도시지역 | 기존 `/api/address/land-use-zone` + `classifyUrbanArea` (재산세 도시지역분용으로 이미 존재) |

**구현**: `lib/geo/rural-house-location.ts`(순수) + Step4 주소검색·자동 판정 배지.
읍일 때만 용도지역을 조회한다(면은 불필요한 외부 호출을 하지 않는다).
토글은 조정대상지역과 같은 **touched 가드** 패턴 — 사용자가 만지면 자동 판정이 덮지 않는다.
`unknown`은 **판정 불가**이지 미충족이 아니라 토글을 건드리지 않는다.

> 함정: 「면목동」·「읍내동」처럼 읍·면이 **접두**인 동 이름이 있다 → 토큰 **끝**만 본다(anchor 고정).

anchor 19건(`__tests__/lib/geo/rural-house-location.test.ts`) + E2E 5건.

### 9.2 W-2 ✅ §155⑯ 「연접한 시·군」 — **매트릭스를 생성해 자동 판정**

당초 「데이터 대기」로 종결하려 했으나, 데이터 확보 경로가 계획서 상정보다 훨씬 짧아 그 자리에서 끝냈다.

**계획서가 상정한 수동 단계가 불필요했다**: `docs/00-pm/inheritance-farming-residence-data-infra.plan.md`
§4는 공공데이터포털 `LSMD_ADM_SECT_RGN` **Shapefile ~50MB 수동 다운로드**를 전제로 5~8h를 잡았다.
그런데 **Vworld `LT_C_ADSIGG_INFO`가 전국 256건을 한 번에 반환**한다(실측) — 프로젝트가 이미 쓰는
`VWORLD_API_KEY`를 재사용하므로 새 인증도 없다.

| 산출물 | 내용 |
|---|---|
| `scripts/build-sigungu-adjacency.ts` | Vworld 수집 + turf `booleanIntersects` (bbox 사전 필터로 32,640쌍 → **891쌍**) |
| `lib/geo/administrative-district-adjacency.json` | 시·군·구 **256건** · 인접 관계 **654건** (29KB) |
| `MATRIX_VERSION` | `0000-00-00` → **`2026-07-31`** |

**🔴 코드 체계 드리프트 발견** — 이번 작업의 가장 큰 소득이다.
Vworld는 **전남·광주 통합**(시도코드 `12`)을 이미 반영했고, 구 광주 `29`·전남 `46` 코드는 **없다**.
프로덕션 주소검색이 반환하는 PNU도 같은 체계다(완도군 `1285…` 실측).
⇒ 매트릭스는 현행 PNU와 정합하나, **기존 데이터 파일 2개가 구 코드를 쓰고 있다** — §9.3.

**도서 8건은 인접 0**(완도군·영도구·영종구·강화군·옹진군·울릉군·거제시·남해군).
교량 연결(거가대교·강화대교)은 육지 경계 접함이 아니라 잡히지 않는다 — 계획서 Phase 1-D의
「해상 경계 정책」 미결 사항이며, 현행 동작을 anchor로 명시 고정했다.

**§155⑯ 판정 배선**: 이전 기관·신규주택 소재지 2개를 넣으면 동일/연접을 자동 판정한다.
한쪽만 넣거나 매트릭스에 없는 지역(도서 등)이면 **자기선언을 그대로 신뢰**한다 —
판정 불가를 미충족으로 뒤집지 않는다.

> 계획서 §4-C.3의 **ADJ-4 코드가 틀렸다**(「KOEDB 검증 후 확정」으로 유보돼 있던 것):
> `26290`은 금정구가 아니라 **남구**다(금정구 = `26410`). 실측으로 정정해 anchor에 고정했다.

### 9.3 🔴 별건 발견 — 전남·광주 코드 드리프트 (미수정)

| 파일 | 코드 체계 | 상태 |
|---|---|---|
| Vworld PNU(프로덕션 주소검색) | `12xxx` 통합 | 현행 |
| `lib/geo/administrative-district-adjacency.json` | `12xxx` | ✅ 이번에 생성 |
| `lib/tax-engine/data/population-decline-areas.ts` | **`46xxx`** 구 전남 | 🔴 드리프트 |
| `lib/tax-engine/data/regulated-areas.ts` | **`29xxx`** 구 광주 | 🔴 드리프트 |

⇒ 전남·광주 소재 주택은 `regionCode`가 `12…`인데 판정 집합은 `46…`/`29…`라 **매칭되지 않는다**:
- 인구감소지역 세컨드홈 특례(§167의3①12 다·라목) 미적용 → 주택 수 과다 산입
- 광주 조정대상지역 이력(2020-12-18 ~ 2022-09-25) 자동판별 누락 → 거주요건·중과 미적용

**본 계획서 범위 밖**이며 수정하지 않았다.
→ **후속 조사 완료**: [`sigungu-code-system-drift.plan.md`](./sigungu-code-system-drift.plan.md)
(통합 시행일 2026-07-01 · D-1 광주 조정지역 **−337,386,500 과소과세** · D-2 전남 인구감소
**+388,410,000 과다과세** · `sigungu-codes.ts`는 **10개 시도**가 구 체계).

### 9.4 (구) W-2 보류 사유 — 기록 보존

`lib/geo/administrative-district-adjacency.ts`가 **이미 존재**한다 — 행안부 표준 시·군·구 인접
매트릭스 조회 모듈이다. 그런데 데이터 파일이 `{}`다:

```
lib/geo/administrative-district-adjacency.json  →  3 bytes, "{}"
getAdjacentSigunguCodes(code)  →  항상 []      isMatrixEmpty()  →  true
```

그 파일은 별건 계획서 `docs/00-pm/inheritance-farming-residence-data-infra.plan.md`
**Phase 1-C PR-2**(영농상속공제 인프라)가 채우기로 돼 있다 — 공공데이터포털
`LSMD_ADM_SECT_RGN` Shapefile(~50MB) + turf.js 자동 인접 계산(5~8h 견적).
`@turf/turf`는 이미 설치돼 있고, **JSON 교체만으로 활성**되는 설계다.

**지금 배선하지 않는 이유**: §155⑯에 「이전 기관 소재지」·「신규주택 소재지」 입력 2개를
추가해도 매트릭스가 비어 있어 **항상 「판정 불가」만 보게 된다** — 입력 부담만 늘고 얻는 것이 없다.

**매트릭스가 채워지면 할 일** (연결 지점):
1. `TransferTaxInput.temporaryTwoHouse`에 `relocatedSigunguCode`·`newHouseSigunguCode` 추가
2. 판정: 같은 5자리면 충족, 아니면 `getAdjacentSigunguCodes(relocated).includes(newHouse)`
3. `isMatrixEmpty()`면 종전대로 자기선언 토글로 fallback (회귀 0)

그때까지 §155⑯은 **자기선언 토글**을 유지한다 — UI 문구에 「이전한 시·군 또는 연접한 시·군」
요건을 명시해 두었다.

---

## 8. 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v1.4 | 2026-07-31 | **W-2 구현 완료 — 인접 매트릭스 생성까지 수행.** Vworld `LT_C_ADSIGG_INFO`가 전국 256건을 한 번에 주어 계획서가 상정한 Shapefile 수동 다운로드가 불필요했다. `scripts/build-sigungu-adjacency.ts` 신설 · 매트릭스 256건/654관계 · `MATRIX_VERSION` 갱신. §155⑯ 연접 자동 판정 배선(판정 불가는 자기선언 유지). anchor 6 + 매트릭스 anchor 11 + E2E 2. **계획서 ADJ-4 코드 오류 정정**(26290=남구, 금정구는 26410). **🔴 별건 발견**: 전남·광주 통합 코드(12) vs 기존 데이터 구 코드(46·29) 드리프트 → §9.3 (미수정) |
| v1.3 | 2026-07-31 | **W-3 구현 완료 · W-2 조사 완료.** W-3 — 법문 괄호가 **읍에만** 걸린다는 점이 실측으로 갈렸다(평창읍 하리 = 도시지역 → 제외 / 진부면 = 면이라 불문 충족). 판별 재료(regionCode·jibun·기존 land-use-zone API)가 전부 이미 있어 순수 모듈 + 자동 판정 배지로 구현, anchor 19 + E2E 5. W-2 — `lib/geo/administrative-district-adjacency.ts` **인프라가 이미 존재**하나 JSON이 `{}`라 미작동. 채우는 일은 별건 계획서 Phase 1-C PR-2 소관이며, 비어 있는 상태로 UI를 늘리면 「판정 불가」만 보이므로 **배선 보류** + 연결 지점 기록 |
| v1.2 | 2026-07-31 | **E-3·E-4 구현 완료 — Phase E 전건 종결.** ⑧은 비과세 E-3.7 + 영 §167의10①**4호** 직접 배제(15호 경유 아님) · ⑦은 3유형 판정 + `deemedOneHouseBy155` union에 `"rural_house"` 추가(15호 경유). ⑪⑫는 판정 불가라 경고로 노출(G-4). anchor 21건 · E2E 4건 · 회귀 0. **부수 발견 2건**: (1) `result.warnings`가 **부담부증여 카드에서만** 표시돼 일반 경로 경고(§155⑳ 안내 등)가 화면에 나오지 않았다 → 공용 배너 신설 (2) 겸용 경로에 `householdHousingCount: 0`을 명시 — 중과 주택수와 §155⑦ 「각각 1개씩」 게이트는 축이 다르다 |
| v1.1 | 2026-07-31 | **E-1·E-2 구현 완료.** 정본 2함수(`resolveTemporaryTwoHouseDeadlineYears`·`judgeTemporaryTwoHouseTiming`)에만 손대 비과세·중과에 동시 반영. 14지점 배선(①②③④⑤⑫⑬⑭) · anchor 16건 · E2E 4건 · 회귀 0. **부수 발견**: 부분 비과세(고가주택)일 때 `exemptReason`이 화면 어디에도 표시되지 않았다 — `TransferTaxResultView`에 근거 카드 신설 |
| v1.0 | 2026-07-31 | 최초 작성. 선행 계획서 D-5에서 분리. §155⑦⑧⑯⑱ 법문 전문 확보(MST 286211) · 현행 grep 실측(넷 다 비과세부터 미구현) · Phase E-1~E-4 |

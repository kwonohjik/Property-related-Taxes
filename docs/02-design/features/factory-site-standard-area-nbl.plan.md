# 공장용지 기준면적 초과분 비사업용 토지 중과 — 구현 계획

> 상태: **rev.8 — Phase 0·A·B·C·D·E 전건 완료**
> 작성: 2026-08-05
> 브랜치: `feat/factory-site-standard-area-nbl`
> 선행 작업: [상업용건물 부수토지 기준면적 초과분](commercial-building-appurtenant-land-nbl.plan.md) (PR #1072 머지 완료)

---

## 0. 요약 — 무엇이 없는가

「소득세법」 §104의3①4호**나목**은 「지방세법」 §106①**2호**(별도합산) 또는 **3호**(분리과세) 대상 토지를 비사업용에서 제외한다. 공장용 건축물의 부속토지는 이 두 경로 중 하나로 들어가는데, **양쪽 모두 면적 한도가 있다**. 한도 초과분은 잔여 규정인 §106①1호(**종합합산**)로 떨어져 **비사업용**이 된다.

현행 엔진은 이 한도를 **전혀 적용하지 않는다**. 사용자가 재산세 유형을 "분리과세" 또는 "별도합산"으로 고르면 면적과 무관하게 **전량 사업용**으로 통과한다(`lib/tax-engine/non-business-land/other-land.ts:302`).

이 계획은 그 한도를 넣는다.

---

## 1. 법령 근거 (KoreanLaw 실측 — 2026-08-05 조회)

### 1.1 위임 체인

```
「소득세법」 §104의3①4호나목  (비사업용 제외 = 사업용)
   └→ 「지방세법」 §106①2호 (별도합산)  →  「지방세법 시행령」 §101①1호
   │        한도 = 공장용 건축물 바닥면적 × §101② 용도지역별 적용배율
   │        적용 지역: 특별시·광역시(군 제외)·특별자치시·특별자치도·시지역
   │                  ─ 단, 읍·면지역 / 산업단지 / 공업지역은 제외
   │
   └→ 「지방세법」 §106①3호가목 (분리과세) → 「지방세법 시행령」 §102①1호
            한도 = 「지방세법 시행규칙」 §50 → **별표 6** 공장입지기준면적
            적용 지역: §101①1호 각 목의 지역 = 읍·면지역 / 산업단지 / 공업지역
```

**두 경로는 지역으로 배타 분기한다.** 같은 공장이라도 소재 지역에 따라 한도 산식이 완전히 달라진다.

- 「소득세법」 MST 280405 (시행 2026-07-01) §104의3①4호나목 원문:
  > 나. 「지방세법」 제106조제1항제2호 및 제3호에 따른 재산세 별도합산과세대상 또는 분리과세대상이 되는 토지

- 「지방세법 시행령」 MST 287223 (시행 2026-07-01) §101①1호 원문:
  > 1. 특별시ㆍ광역시(군 지역은 제외한다)ㆍ특별자치시ㆍ특별자치도 및 시지역(다음 각 목의 어느 하나에 해당하는 지역은 제외한다)의 공장용 건축물의 부속토지로서 공장용 건축물의 바닥면적(건축물 외의 시설의 경우에는 그 수평투영면적을 말한다)에 제2항에 따른 용도지역별 적용배율을 곱하여 산정한 범위의 토지
  > 　가. 읍ㆍ면지역
  > 　나. 「산업입지 및 개발에 관한 법률」에 따라 지정된 산업단지
  > 　다. 「국토의 계획 및 이용에 관한 법률」에 따라 지정된 공업지역

- 같은 영 §102①1호 원문:
  > 1. 공장용지: 제101조제1항제1호 각 목에서 정하는 지역에 있는 공장용 건축물(제103조제1항제2호 및 제3호의 건축물을 포함한다)의 부속토지로서 행정안전부령으로 정하는 공장입지기준면적 범위의 토지. 다만, 「건축법」 등 관계 법령에 따라 허가 등을 받아야 하는 건축물로서 허가 등을 받지 않은 공장용 건축물이나 사용승인을 받아야 하는 건축물로서 사용승인(임시사용승인을 포함한다)을 받지 않고 사용 중인 공장용 건축물의 부속토지는 제외한다.

#### ✅ 지역 분기 구조 실증 (Phase 0-1, 2026-08-05)

"제101조제1항제1호 **각 목에서 정하는 지역**"이 §101①1호 본문의 열거 지역(특별시·광역시·특별자치시·특별자치도·시지역) **내부에서도 독립적으로 작동**한다는 것이 조세심판 결정례 2건으로 확인된다:

| 결정 | 소재 | 처리 |
|---|---|---|
| **조심 2025서2489** (2026.04.08) | **부산광역시** 해운대구 — 제2종일반주거지역 | §101①1호 **별도합산** (바닥면적 81,473.36㎡ × 4배 = 325,893.44㎡) |
| 〃 (같은 공장의 다른 필지) | **부산광역시** 해운대구 — **준공업지역** | §102①1호 **분리과세** (연면적 89,865.838㎡ × 100/12 = 748,881.98㎡) |
| **조심 2023지0373** (2023.12.04) | **경기도 시흥시** — **산업단지** | §102①1호 **분리과세** |

⇒ 광역시·시지역이라도 각 목(읍·면 / 산업단지 / 공업지역)에 해당하면 §102①1호로 간다. **같은 공장 안에서도 필지의 용도지역에 따라 두 경로가 갈린다**(2025서2489가 실례).

> ⚠️ **군 지역은 직접 판시한 자료를 찾지 못했다.** 「지방자치법」상 군은 읍·면으로 구성되므로 가목에 해당해 §102①1호로 간다고 해석되고, 위 2건이 보이는 "각 목의 독립 작동"과도 정합한다. 다만 **이 계획의 설계에서는 이 해석이 필요 없다** — §4.3이 지역을 사용자 선택으로 두므로 엔진은 자동판정을 하지 않는다. UI 라벨에 "**군 지역 포함**"을 명시해 오선택만 막으면 된다.

### 1.2 공장입지기준면적 산식 — 「지방세법 시행규칙」 별표 6 (개정 2025.10.31, 실측)

```
                       100
공장입지기준면적 = 공장건축물 연면적 × ─────────────
                    업종별 기준공장면적률
```

**2. 산출기준**

| 항 | 내용 |
|---|---|
| 가 | **공장건축물 연면적** = 공장 경계구역 안 모든 공장용 건축물 연면적(부대시설 포함, **무허가·위법시공 건축물 제외**) + 옥외 기계장치·저장시설의 **수평투영면적** |
| 나 | **업종별 기준공장면적률** = 「산업집적활성화 및 공장설립에 관한 법률」 §8에 따라 산업통상부장관이 고시하는 값 |
| 다 | 2개 이상 업종이면 **업종별로 산출해 합산**. 업종 구분 불가 시 **매출액 최대 업종**의 면적률 적용 |

**3. 추가 인정기준 (초과분이라도 기준면적에 포함)**

| 목 | 내용 |
|---|---|
| 가1 | 「산집법」 §20① 본문 공장 신설 **제한지역** 소재 공장: 산출면적의 **10% 이내**(단, **3,000㎡ 초과분 제외**) |
| 가2 | 그 밖의 지역: 산출면적의 **20% 이내** |
| 나 | 도시관리계획상 녹지지역, 활주로, 철로, **6m 이상 도로**, 접도구역 |
| 다 | 생산공정 특성상 대규모 저수지·침전지 |
| 라 | 경사도 **30도 이상** 사면용지 |
| 마 | 공장 가동 오염피해 인접토지를 소유자 요구로 취득한 경우 → 합산해 기준면적 산정 |
| 바 | 종업원용 체육시설용지(기준면적표 별도 · **공장입지기준면적의 10% 이내**) |

> 바목 체육시설 기준면적표는 종업원 수 구간(100/500/2,000/10,000명)별 운동장·코트·실내시설 수치를 별도로 갖는다. 비고 라: **실내체육시설은 기준면적 × §101② 용도지역별 적용배율**을 합산.

### 1.3 기준공장면적률 데이터 — 「공장입지 기준고시」 별표1 (산업통상부, 공포 20260225)

행정규칙일련번호 `2100000274928`. §3① → 별표1이 KSIC 세세분류(5자리)별 면적률(%).

**전량 확보 완료 (Phase 0-2 ✅, 2026-08-05)** — 법제처 Open API `target=admrul&ID=2100000274928` 단일 호출(419KB)로 전문이 온다. MCP `get_admin_rule`은 50,000자에서 절단되므로 **직접 API를 쓸 것**.

| 항목 | 실측값 |
|---|---|
| KSIC 5자리 고유 행 수 | **480행** |
| 코드 범위 | `10111` ~ `34020` |
| 면적률 분포 | 12%(195) · 15%(109) · 10%(71) · 20%(46) · 3%(28) · 7%(18) · 5%(13) — **7종뿐** |
| 지식산업센터 | §4로 **40% 고정**(별표1 외) |

**개정 이력** (부칙 실측): 1999-12-16 · 2004-09-24 · 2008-07-04 · 2012-04-19 · 2016-03-08 · 2018-09-17 · **2026-02-25**. 27년간 7회, 최근 간격 8년.

> ⚠️ **2026-02-25 개정은 「제11차 한국표준산업분류」 반영이다** (제개정이유 실측: "업종명, 분류번호 등 반영"). 즉 **분류번호 체계가 10차 → 11차로 바뀌었다**. 양도일이 2026-02-25 이전이면 적용 고시는 2018-162호(**10차 코드**)이므로, 현행 데이터셋 하나만 내장하면 과거 양도 건에 **침묵 오류**가 난다. → §4.4 버전 게이트.

> ⚠️ **고시의 산식은 별표6과 방향이 반대다.** 고시 §3②는 `기준공장건축면적 = 공장부지면적 × 기준공장면적률`(면적률을 **곱한다**)이고, 별표6은 `공장입지기준면적 = 연면적 × 100 ÷ 면적률`(**나눈다**)이다. 서로 역함수 관계이며 **모수도 다르다** — 고시는 "공장건축**면적**"(각 층 바닥면적 합계 + 옥외 수평투영), 별표6은 "공장건축물 **연면적**". 두 값은 대체로 같지만 별표6은 무허가·위법시공분을 명시 배제한다. **세법 계산에는 별표6 산식만 쓴다.**

### 1.4 ★ 판정 단위 — **필지가 아니라 「1구의 공장」 전체** (Phase 0-1 실증)

두 결정례가 일치한다:

- **조심 2023지0373** — 시흥시 산업단지 1필지 33,816.2㎡ 중 처분청이 나대지 20,163.83㎡를 종합합산으로 갈랐으나, **"하나의 울타리 내에 소재"** 하고 제조업 특성상 여유 공간이 필요하다는 이유로 **전량 분리과세**로 경정. (참조 조심 2021지2777)
- **조심 2025서2489** — 지목이 '답'인 토지도 "연접한 공장용지와 함께 한 울타리 내 소재"하면 공장 부속토지. 한도 비교의 분모도 **"청구법인 보유 일대 전체 토지면적 199,115㎡"** 였다.

부속토지 해당 여부는 "필지 수나 공부상의 기재와 관계없이 토지의 이용현황에 따라 객관적으로 결정"된다(대법원 1994.12.27. 선고 94누8372, 조심 2023지0373 인용).

#### ⇒ 설계 귀결 — CB와 같은 "약분" 구조

한도 비교가 공장 전체 기준이므로, 양도 대상이 그 공장 토지의 **일부 필지**여도 초과 비율은 전체값으로 결정된다:

```
초과비율 = 1 − (공장 전체 기준면적 S) ÷ (공장 전체 부속토지 면적 L_total)
```

이 비율을 양도 필지에 그대로 적용한다. 필지별로 "어느 필지가 초과분인지" 지정할 필요가 없다 — 구 비업무용부동산 사건에서 **"전체토지면적 중 비업무용부동산의 면적이 차지하는 비율로 안분계산"** 한 처분이 유지된 선례가 있다(국심1993부0467, 1993.05.17. 기각).

> ⚠️ 국심1993부0467은 **구 법인세법상 비업무용부동산** 사건이다. 현행 「소득세법」 §104의3 비사업용 토지와 제도가 다르므로 **안분 방식의 참고**로만 쓰고, 근거 조문으로 인용하지 말 것.

⇒ **선행 CB 작업과 같은 입력 축소가 일어난다.** 사용자는 양도 필지의 면적이 아니라 **공장 전체의 토지면적·건축물면적**을 입력하면 되고, 초과 비율이 `nonBusinessLandAreaRatio`로 주입된다.

### 1.5 단서 — 허가·사용승인 미이행

§102①1호 단서와 §101① 단서는 같은 취지다. **선행 작업에서 이미 확인**했다 — 법제처 법령해석례 **25-0823**(2026.02.03)은 "허가 등"이 「건축법」 §11 신축 허가에 한정되지 않고 **§19② 용도변경 허가 미이행**도 포함한다고 회답했다. 기존 `isUnregistered` 필드 의미가 이미 그렇게 확장되어 있다(`lib/tax-engine/types/commercial-appurtenant.types.ts:46-52`).

> ⚠️ **입증 부담은 과세관청에 있다** — 조심 2025서2489는 "건축물대장이 확인되지 않는다는 이유만으로 허가를 받지 않은 건축물에 해당한다고 단정하기 어렵다"며, **불법 건축물로 보아 행정상 제재조치를 한 사실도 확인되지 않는 점**을 들어 처분청 주장을 배척했다. ⇒ UI 문구를 "공부 확인 불가"가 아니라 **"허가·사용승인을 받지 않은 것이 확인되는 경우"** 로 좁게 쓸 것.

---

## 2. 현행 구현 실측

### 2.1 양도세 NBL — 한도 없음 (이 계획의 대상)

| 지점 | 파일:라인 | 현행 |
|---|---|---|
| 지목 선택지 | `components/calc/transfer/nbl/NblSectionContainer.tsx:32-39` | 6종 — 농지·임야·목장용지·주택부수·별장부수·**기타토지**. **공장용지 없음** |
| 재산세 유형 입력 | `components/calc/transfer/nbl/OtherLandDetailSection.tsx:205-213` | 사용자가 종합합산/별도합산/**분리과세**/비과세 중 직접 선택 |
| 폼→엔진 매핑 | `lib/tax-engine/non-business-land/form-mapper-helpers.ts:238` | `nblOtherPropertyTaxType` → `propertyTaxType`, 기본값 `"comprehensive"` |
| 타입 | `lib/tax-engine/non-business-land/types.ts:267` | `OtherLandUsage.propertyTaxType: PropertyTaxType` |
| 판정 | `lib/tax-engine/non-business-land/other-land.ts:279,302` | `effectiveTaxType !== "comprehensive"` + 기간기준 → **전량 사업용 `buildPass`** |

⇒ **분리과세·별도합산을 고르면 면적 한도 검사 없이 통과한다.** `LandType`에 `factory` 계열이 없으므로 공장은 `other_land`로 들어올 수밖에 없다.

**참고 — 이미 있는 부분 안분 경로**: `other-land.ts:499-541`(Step 3-2 §101①2호나목 바닥면적 carve-out)이 `computeAreaProportioning`으로 부분 비사업용을 산출한다. **공장 한도도 같은 패턴으로 붙일 수 있다.**

### 2.2 재산세 — 사용자 직접입력 (이 계획의 범위 밖, 별건 후보)

| 지점 | 파일:라인 | 현행 |
|---|---|---|
| Zod | `lib/validators/separate-aggregate-input.ts:67-70` | `factoryStandardArea`(㎡) — **사용자가 면적 자체를 입력** |
| 엔진 | `lib/tax-engine/separate-aggregate-land.ts:326-328` | 입력값이 있으면 그대로 `baseArea`로 사용 |
| UI | `components/calc/property/Step2SeparateAggregate.tsx:79-90` | "공장입지기준면적 (㎡)" 숫자 입력 |

⇒ **별표6 산식(연면적 ÷ 면적률)은 재산세에도 구현되어 있지 않다.** 사용자가 어딘가에서 계산해 온 값을 신뢰한다. `rg "기준공장면적률"` → **저장소 전체 0건**.

### 2.3 재사용 가능한 자산 (선행 작업 산출물)

| 자산 | 파일 | 용도 |
|---|---|---|
| §101② 배율 정본 | `lib/tax-engine/local-tax-zone-multiplier.ts:122` `getZoneAreaMultiplier()` | §101①1호 경로(바닥면적 × 배율)에 **그대로 사용** |
| 초과분 판정 헬퍼 | `lib/tax-engine/appurtenant-land-excess.ts:88` `judgeAppurtenantLandExcess()` | 배율·한도·초과비율·단서 처리 일괄. §101①1호 경로에 **그대로 사용** |
| 부분중과 비율 주입 | `nonBusinessLandAreaRatio` (선행 작업 STEP 0.62) | 초과비율 → 부분 중과 |
| 면적 안분 | `computeAreaProportioning` (`utils/area-proportioning.ts`) | NBL 내부 부분 비사업용 산출 |

⇒ **§101①1호(별도합산) 경로는 사실상 신규 코드가 거의 없다.** 신규 구현의 대부분은 **§102①1호(분리과세, 별표6) 경로**다.

---

## 3. 케이스 매트릭스

`L` = **공장 전체** 부속토지 면적(1구 기준 — §1.4), `A` = 공장건축물 **연면적**, `F` = **바닥면적**, `r` = 기준공장면적률(%), `m` = §101② 배율

> ⚠️ **`A`와 `F`는 다른 값이다.** 조심 2025서2489 실례: 같은 공장에서 §101①1호용 81,473.36㎡(바닥면적) vs §102①1호용 89,865.838㎡(연면적). 한 칸으로 받지 말 것.
>
> ⚠️ **`F`는 "각 층 중 최대 바닥면적"이지 건축면적이 아니다**(조심 2025지0451 — 선행 CB 작업에서 확인). 2025서2489 재결문이 "건축물대장상 건축면적의 합계"라 적은 것은 **청구인 제출자료 표현**이며 심판원이 판시한 바가 아니다.

| # | 지역 | 단서 해당 | 한도 산식 | 결과 |
|---|---|---|---|---|
| C-1 | 읍·면 / 산업단지 / 공업지역 | 아니오 | `S = A × 100 ÷ r` (+ 별표6 3호 가산) | `L ≤ S` → 전량 사업용 |
| C-2 | 〃 | 아니오 | 〃 | `L > S` → 초과분 `L−S` 비사업용 (비율 `1 − S/L`) |
| C-3 | 〃 | **예** | — | **전량 비사업용** (§102①1호 단서) |
| C-4 | 시지역 도시지역(그 외) | 아니오 | `S = F × m` | `L ≤ S` → 전량 사업용 |
| C-5 | 〃 | 아니오 | 〃 | `L > S` → 초과분 비사업용 |
| C-6 | 〃 | **예** | — | **전량 비사업용** (§101① 단서) |
| C-7 | ~~어느 쪽이든~~ | — | ~~한도 이내 but 기간기준 미충족~~ | ❌ **도달 불가 — Phase A 실측으로 삭제** |
| C-8 | 2개 이상 업종 | 아니오 | 업종별 `A_i × 100 ÷ r_i` 합산 | 별표6 2호다 |
| C-9 | 업종 구분 불가 | 아니오 | 매출 최대 업종 `r` 단일 적용 | 별표6 2호다 후단 |
| C-10 | 지식산업센터 | 아니오 | `r = 40` 고정 | 고시 §4 |

> 🔴 **C-7은 rev.1의 오해였다** (Phase A 실측으로 삭제). `other_land` 경로는 `meetsPeriodCriteria`에
> **`fullPeriod`(전 보유기간)** 를 사업용 기간으로 넘긴다(`other-land.ts:280`). 그러면 3년·5년·전체
> 모든 창의 비사업용 일수가 0이 되어 `meets`는 **보유기간 길이와 무관하게 항상 true**다
> (`period-criteria.ts:160-189`; 보유 7개월 probe로도 확인). ⇒ 이 경로에서 §168의6 기간기준은
> 판정에 개입하지 않으며, 기존 Step 3-1의 FAIL 분기도 같은 이유로 도달하지 않는다.
>
> ⇒ Step 0.5에 기간기준 분기를 넣지 않는다(도달 불가 코드 금지). `PeriodCriteriaResult`는
> 반환값 조립(`effectiveBusinessDays`·`criteria`)에만 쓴다.

---

## 4. 설계안

### 4.1 방향 — `other_land` 안의 신규 스텝 (권장)

`LandType`에 `factory_site`를 신설하지 **않는다**. 이유:

1. 「소득세법」상 공장용지는 §104의3①**4호**(농지·임야·목장용지 **외의** 토지)로, 현행 `other_land`가 담당하는 바로 그 호다. 별도 `LandType`을 만들면 §168의11①(다목) 호별 면적기준·수입금액비율 등 **4호 공통 로직을 통째로 복제**해야 한다.
2. 이미 §101①2호나목 carve-out이 `other_land` 안에서 부분 안분을 하고 있다(`other-land.ts:499`). 같은 층위다.
3. `LandType` 신설은 `getLandCategoryGroup`·`land-category.ts`·PDF 지목 분류까지 파급된다 — 기존 별건 **A-BS-1**(`building_site`가 주택 배율을 쓰는 문제)과 얽힌다.

⇒ `OtherLandUsage`에 **`factory?: FactoryLandUsage`** 를 추가하고, `judgeOtherLand` 안에 **Step 0.5 "공장 기준면적 한도"** 를 넣는다.

### 4.2 진입 위치 — **판정과 적용을 분리한다** (Phase A에서 정정)

```
Step 0        나대지 간주 (2% / 무건축물)
Step 0.5-판정 ★ 공장 한도 계산 · 이내면 재산세 구분 확정, 초과면 결과만 보관
Step 3-1      비종합합산 + 기간기준 → 전량 사업용
§168의11②     수입금액비율 → 전량 사업용
Step 3-1-1    §168의11① 호별 기준면적 → 사업용 or 호별 안분
Step 0.5-적용 ★ 공장 한도 초과분 안분                  ← 여기서 확정
Step 3-2      §101①2호나목 바닥면적 carve-out
```

**두 지점이 모두 필요하다.**

- **판정을 Step 3-1 앞에** 두는 이유: Step 3-1이 `propertyTaxType`만 보고 `buildPass`로 조기 반환한다(`other-land.ts:302`). 공장이 한도를 초과하면 나목에 해당하지 않으므로 그 통과를 막아야 한다.
- **적용을 뒤에** 두는 이유: 🔴 **「소득세법」 §104의3①4호는 "다음 각 목을 **제외한** 토지"를 비사업용으로 규정한다** — 가·나·다목은 **택일**이다. 나목(공장 한도)에 미달해도 **다목**(§168의11① 호별 기준면적, ② 수입금액비율)에 해당하면 **여전히 사업용**이다. 앞에서 확정하면 그 경로를 차단해 **법 근거 없는 불리 적용**이 된다.

> 🔴 **Phase A 구현 중 실제로 이 결함을 한 번 만들었다.** 초과 시 Step 0.5에서 즉시 `return` 하도록 짰다가, 기존 Step 3-2의 주석(`other-land.ts:571` — "수입금액비율(②)·거주사업관련(① 호별) 우선 경로 이후 — 법 근거 없는 불리 적용 방지")을 읽고 발견했다. **기존 코드의 주석이 새 코드의 검증 기준이 된 사례다.**
>
> 회귀 가드: anchor `ORDER-1`(초과 + 수입금액비율 충족 → 전량 사업용) · `ORDER-2`(초과 + 미달 → 그때 안분, 스텝 순서까지 단언) · `ORDER-3`(초과 + §168의11① 호별 이내 → 사업용).

Step 0.5의 출력:
- 한도 이내 → `factoryTaxTypeOverride`로 재산세 구분 확정 (Step 3-1이 나목으로 통과시킴)
- 초과 → `factoryExcess`에 보관 → 후행 경로 모두 미해당 시 `computeAreaProportioning`으로 부분 비사업용
- 단서 해당 → `standardArea = 0` → 안분 없이 전량 비사업용

### 4.3 지역 분기 입력

`FactoryLandUsage.locationCategory: "eup_myeon_or_complex" | "urban_other"`

| 값 | UI 라벨 | 경로 |
|---|---|---|
| `eup_myeon_or_complex` | **읍·면지역(군 지역 포함) · 산업단지 · 공업지역** | §102①1호 — 별표6 산식 (연면적 `A`, 면적률 `r`) |
| `urban_other` | 그 밖의 특별시·광역시·특별자치시·특별자치도·시지역 | §101①1호 — `judgeAppurtenantLandExcess()` 재사용 (바닥면적 `F`, 용도지역 배율) |

> **지역 자동판정은 하지 않는다.** 산업단지 지정 여부는 현행 입력(`zoneType`)으로 알 수 없고, 자동 안분 fallback 금지 원칙에 따라 **미입력은 검증 오류로 차단**한다. 이 선택이 §1.1의 군 지역 해석 불확실성을 계산에서 제거한다 — 라벨의 "군 지역 포함"이 그 역할을 한다.

### 4.3a 입력 필드 — 「1구의 공장」 전체값 (§1.4 귀결)

| 필드 | 경로 | 비고 |
|---|---|---|
| `totalAppurtenantLandArea` | 공통 | **공장 전체**(하나의 울타리 기준) 부속토지 면적. 양도 필지 면적이 아니다 |
| `totalFloorArea` (`A`) | 읍면·산단·공업 | 경계구역 안 모든 공장용 건축물 **연면적** + 옥외 기계장치·저장시설 수평투영. **무허가·위법시공분 제외**(별표6 2호가) |
| `ratePercent` (`r`) | 〃 | 업종별 기준공장면적률. 지식산업센터 40% 프리셋 |
| `totalFootprintArea` (`F`) | 그 밖 | **바닥면적**(각 층 중 최대 — 건축면적 아님). `A`와 다른 값이다 |
| `zoneType` | 그 밖 | §101② 배율 결정 |
| `isUnregistered` | 공통 | 단서 — 전량 비사업용 |

⚠️ `A`와 `F`를 **한 칸으로 받지 말 것** (§3 매트릭스 경고).

### 4.4 ✅ 기준공장면적률 입력 — **결정 (Phase 0-3 종결, 2026-08-05)**

당초 ㉮(직접입력) / ㉯(데이터셋) / ㉰(면적 직접입력) 3안의 **택일 문제로 세웠으나, ㉮와 ㉯는 배타 관계가 아니다** — 계층이 다르다.

#### 채택 — **엔진은 ㉮로 고정하고, ㉯를 UI 편의 계층으로 얹는다**

```
엔진 계약:  ratePercent: number        ← 이것만 받는다
              ↑
UI 계층:    업종 자동완성 → 면적률 채움 (선택)   또는   사용자 직접 입력
```

**근거**:

1. **엔진은 면적률의 출처를 알 필요가 없다.** 별표6 산식(`연면적 × 100 ÷ r`)과 초과 판정은 `r`이 어디서 왔든 동일하다. 데이터셋 유무가 엔진 설계·타입·테스트에 **영향을 주지 않는다** ⇒ ㉯를 나중에 얹어도 Phase A~C 재작업 0.
2. **㉯의 비용이 당초 추정보다 낮다** — 480행 전량이 API 단일 호출로 확보되고(§1.3), 값은 7종뿐이며, 개정은 8년 간격이다.
3. **프로젝트에 이미 같은 패턴이 있다** — `data/area-standards.ts`·`livestock-standards.ts`가 별표를 내장하고, `other-land.ts`의 `resolveAreaLimit`은 "표에서 자동산출 → 못 찾으면 `standardAreaLimit` 직접입력 fallback"으로 동작한다. 자동완성 UI 선례는 `KiwoomStockNameAutocomplete.tsx`·`ui/address-search.tsx`.
4. **㉰는 기각** — 별표6 산식이 구현되지 않아 이 기능의 목적(초과분 판정)에 미달한다. 재산세 현행 방식(§2.2)이 그 상태다.

#### 🔴 버전 게이트 — 선택이 아니라 필수

2026-02-25 개정이 KSIC를 10차 → 11차로 갈아엎었다(§1.3). 데이터셋을 얹을 때 **반드시** 다음을 건다:

| 양도일 | 동작 |
|---|---|
| ≥ 2026-02-25 | 현행 별표1(11차) 자동조회 |
| < 2026-02-25 | **자동조회 차단 · 직접입력 요구** — 적용 고시는 2018-162호(10차 코드)다 |

추정 배율 금지 원칙(`local-tax-zone-multiplier.ts`의 `residential` 미등재 처리)과 같은 취지다. 2018 고시 데이터셋을 추가로 확보하면 그 구간도 자동화할 수 있다(후속).

#### 구현 순서

- **Phase A~D**: `ratePercent` 직접입력만. 지식산업센터 40% 프리셋(고시 §4)은 포함.
- **후속(별건)**: 별표1 데이터셋 + 업종 자동완성 + 버전 게이트.

### 4.5 별표6 3호 추가 인정기준 — Phase 0에서 범위 결정

가1·가2(10%/20% 가산)는 **산식이 명확하고 파급이 작다** → 포함 권고.
나~바(녹지·활주로·철로·도로·접도구역 / 저수지 / 사면용지 / 오염피해 인접 / 종업원 체육시설)는 **각각 별도 면적 입력**이 필요하다 → 단순 가산 면적 1칸(`additionalRecognizedArea`)으로 받고 근거를 사용자가 판단하게 할지, 항목별로 나눌지 결정 필요.

> 마목(오염피해 인접토지)은 NBL에 **이미 유사 입력이 있다** — `unconditional-exemption.ts:126` `isFactoryAdjacent`(§168의14③5호). **다른 조문이므로 혼동 금지**: §168의14는 그 인접토지 자체를 무조건 사업용으로 보는 규정이고, 별표6 마목은 공장입지기준면적 **산정 시 합산**하는 규정이다. Phase 0에서 상호작용 확인 필요.

---

## 5. Phase 분할

### Phase 0 — 착수 조건 (법령·설계 검증)

| # | 항목 | 산출 | 블로커 |
|---|---|---|---|
| 0-1 | ✅ **종결** — 지역 분기 구조 실증(조심 2025서2489·2023지0373) · 판정 단위 = 1구 공장 전체 · 군 지역은 자동판정 미실시로 무쟁점화 | §1.1·§1.4 | — |
| 0-2 | ✅ **완료** — 별표1 전량 확보(480행·7종 요율·개정 이력) | §1.3 | — |
| 0-3 | ✅ **종결** — 엔진 계약 ㉮ 고정 + ㉯는 UI 계층 후속 + 버전 게이트 | §4.4 | — |
| 0-4 | 별표6 3호 나~바 구현 범위 결정 | 입력 필드 명세 | 아니오(축소 가능) |
| 0-5 | ✅ **완료(Phase E에서 수행)** — 초과비율별 세액 실측표 | §6 Phase E | — |
| 0-6 | §168의14③5호 `isFactoryAdjacent`와 별표6 마목 상호작용 확인 | 이중적용 여부 판정 | 아니오 |

> 0-5는 선행 작업에서 **§104⑤ 비교과세로 낮은 초과비율에서는 세액이 변하지 않는 구간**이 실측됐다. 공장도 같은지 확인해야 anchor 기대값을 잘못 세우지 않는다.

### Phase A — 엔진 (별표6 산식 + Step 0.5) ✅ **완료 (2026-08-05)**

| 파일 | 내용 |
|---|---|
| `non-business-land/factory-land-standard-area.ts` (신설 187줄) | 별표6 산식 순수 함수 `computeFactoryStandardArea` + 경로 분기 `judgeFactoryLandExcess`. 다업종 합산(2호다)·3호가 추가인정(10%/3,000㎡·20%)·3호나~바 직접입력·단서 처리 |
| `non-business-land/land-usage.types.ts` (신설 240줄) | `types.ts` 800줄 초과로 지목별 사용현황 타입 분리 — `FactoryLandUsage`·`FactoryIndustrySegment` 포함 |
| `non-business-land/types.ts` (842 → 638줄) | 분리 + 전량 재수출(소비처 import 경로 불변) |
| `non-business-land/other-land.ts` (606 → 674줄) | **Step 0.5** 삽입 (Step 3-1 앞) |
| `legal-codes/transfer.ts` | `NBL.FACTORY_LAND_SEPARATE`(§102①1호+별표6) · `NBL.FACTORY_LAND_AGGREGATE`(§101①1호) |
| `legal-verification/citation-parser.ts` | `LAW_ALIAS`에 「지방세법 시행규칙」 등재 |
| `legal-verification/manifest/additions-local-rule.ts` (신설) | 시행규칙 §50 검증 규칙 + `verifier-manifest.ts` 배럴 등록 |
| `__tests__/.../factory-land-standard-area.anchor.test.ts` (신설) | anchor **30건**(순서 가드 3건·입력 가드 4건 포함) |

**verify 결과**: `npx tsc --noEmit` 0건 · 신규 anchor 30건 GREEN · NBL 335건 GREEN · **전체 13,556건 GREEN(실패 0)** · `npm run lint` 0 errors(신규 파일 경고 0) · `npm run verify:legal` **338건 통과·실패 0**.

> 🔴 **입력 누락 침묵 오작동 1건을 잡았다** — `segments`(연면적·면적률)나 `totalFootprintArea`가
> 비면 기준면적이 0이 되어 부속토지 **전량이 비사업용**으로 떨어지고, `other-land.ts`가
> 그것을 단서(허가 미이행)로 오인해 **사유까지 틀리게** 표시했다. 두 경로 모두 `throw`로 막고
> 단서 판별을 `standardArea === 0` 추론에서 **명시 플래그 `isUnregisteredException`** 으로
> 바꿨다. 회귀 가드 `GUARD-1~4`.

> 🟠 **법령 검증 게이트가 즉시 잡았다** — 새 상수가 「지방세법 시행규칙」을 인용하자
> `legal-verification-unverifiable.test.ts`가 "모수 밖 인용"으로 빨개졌다(전체 테스트 1건 실패).
> CLAUDE.md가 "두 번 재발했다"고 적은 바로 그 갭이다. 법제처 API로 조회 가능한 법령이므로
> `UNVERIFIABLE`이 아니라 `LAW_ALIAS` + manifest에 등록해 해소했다.
>
> ⚠️ **§50 규칙의 한계** — §50 본문은 "별표 6에 따른다"는 위임 한 줄이고 산식은 별표에 있다.
> 법제처 조문 API는 별표를 반환하지 않으므로 **이 규칙은 위임 구조 변경만 잡고
> 별표 6 자체의 개정(직전 2025.10.31)은 못 잡는다.** 파일 헤더에 명시했다.

**외부 기준점 검증(EXT-1~4)** — 조심 2025서2489 수치를 그대로 재현한다:

| # | 입력 | 기대 | 결과 |
|---|---|---|---|
| EXT-1 | 연면적 89,865.838㎡ ÷ 12% | 748,881.98㎡ | ✅ 소수 2자리 일치 |
| EXT-3 | 바닥 81,473.36㎡ × 일반주거 4배 | 325,893.44㎡ | ✅ 일치 |
| EXT-4 | 연면적 ≠ 바닥면적 | 두 기준면적이 다름 | ✅ |

> 🔴 **Phase A에서 확정한 것** — `factoryTaxTypeOverride`: 한도 이내면 공장 판정이 재산세 구분을
> 확정한다(사용자가 `propertyTaxType`을 종합합산으로 두었어도 `special_sum`/`separate`로 승격).
> 공장은 §101①**1호**라서 §101①2호나목의 2% 나대지 룰(`isBareLand`)이 적용되지 않는다 —
> 조문이 "제1호에 따른 공장용 건축물은 제외한다"고 명시한다.

### Phase B — API 배관 (⑫⑬⑭) ✅ **완료 (2026-08-05)**

| 지점 | 파일 | 내용 |
|---|---|---|
| ⑫ | `lib/api/transfer-tax-schema-nbl.ts` | `nblFactory*` **8필드** + `nblFactorySegmentRawSchema`(다업종 배열) |
| ⑬ | — | **변경 없음.** `buildNonBusinessLandRaw`가 필드를 나열하지 않고 **prefix-pick**(`k.startsWith("nbl")`)으로 운반한다 ⇒ 필드명이 `nbl`로 시작하면 자동 통과 |
| ⑭ | `non-business-land/form-mapper-helpers.ts` | `buildFactory()` 신설 + `buildOtherLand`에 연결 |
| ⑭ | `non-business-land/form-mapper.ts` | `buildOtherLand(..., zoneType)` — 자산 `nblZoneType`을 공장 판정에 승계 |
| — | `__tests__/.../factory-land-plumbing.anchor.test.ts` (신설) | 라운드트립 anchor **9건** |

> 🔴 **이 경로의 실질 관문은 ⑫뿐이다.** ⑬이 prefix-pick이라 이름만 맞으면 통과하므로,
> Zod 등록을 잊으면 **컴파일도 되고 다른 테스트도 다 통과하는데 엔진에만 값이 안 간다**
> (한도 판정 자체가 사라져 초과분 중과가 조용히 미발동). `STRIP-1~3`이 그 관문을 지킨다.
>
> ⚠️ **필드 계약 개수 가드**(`STRIP-2`) — `nblFactory*` 8개를 정렬 배열로 고정했다.
> 필드를 늘리면 이 목록도 갱신해야 한다.

**용도지역은 별도 필드를 두지 않았다** — 자산의 `nblZoneType`을 승계한다(단일 소스). §101①1호
배율은 양도 대상 필지의 용도지역으로 정하며, 이는 조심 2025서2489가 같은 공장의 필지를
각자의 용도지역으로 판정한 것과 정합한다.

**verify 결과**: `tsc` 0건 · 배관 anchor 9건 GREEN · NBL+calc 1,440건 GREEN.

### Phase C — 폼·validate (①②③⑧)

- `AssetForm` 필드 + `calc-wizard-asset-migrate.ts` 기본값(`nblFactoryEnabled: false` 등)
- `lib/calc/transfer-tax-validate-asset.ts` — 미입력 차단

#### 🔴 ⑧이 막아야 할 throw 조건 — **전수 (실측)**

엔진 예외는 `route.ts:432`에서 **HTTP 500**이 된다(인라인 필드 오류가 아니다). 토글을 켜고
값을 비워두면 사용자는 원인 모를 500을 본다 ⇒ validate가 **다섯 개 전부** 선차단해야 한다.

| # | 조건 | 던지는 곳 |
|---|---|---|
| 1 | `totalAppurtenantLandArea` ≤ 0 | `judgeFactoryLandExcess` 진입부 |
| 2 | `locationCategory`가 두 리터럴이 아님 | 〃 (조용한 분기 방지) |
| 3 | 별표6 경로: `segments` 비었거나 `floorArea`·`ratePercent` ≤ 0 | 〃 |
| 4 | §101①1호 경로: `totalFootprintArea` ≤ 0 | 〃 |
| 5 | §101①1호 경로: `zoneType` 미입력·§101② 표 미등재 | `judgeAppurtenantLandExcess` |

> 5번은 자산의 `nblZoneType`을 승계하므로, 세분 전 `residential`을 고른 사용자는 공장 토글을
> 켜는 순간 막힌다 — validate 메시지에 "세분된 용도지역을 선택하세요"를 포함할 것.

- **verify**: `tsc` 0건 + 14지점 self-grep + validate 5조건 anchor

#### ✅ 완료 (2026-08-05)

| 지점 | 파일 | 내용 |
|---|---|---|
| ① | `stores/calc-wizard-asset-nbl-other.ts` | `NblOtherFormSlice`에 `nblFactory*` 8필드 + `NblFactorySegmentFormItem` |
| ② | `stores/calc-wizard-asset-factory.ts` | 기본값(토글 `false` → 기존 동작 불변) |
| ③ | `stores/calc-wizard-asset-migrate.ts` | 이력 복원 시 신규 필드 채움 |
| ⑧ | `calc/transfer-tax-validate-nbl-other.ts` | `validateNblFactory()` — throw 5조건 전수 선차단 |
| — | `__tests__/calc/factory-land-validate.anchor.test.ts` (신설) | **18건** — validate ↔ 엔진 **양방향 대칭** |

> 🔴 **대칭 테스트를 넣은 이유** — validate가 **덜 막으면** 사용자가 원인 모를 500을 보고,
> **더 막으면** 엔진은 계산 가능한데 UI가 막는다(UI 통과 ↔ validate 차단 모순).
> `COND-1~5`는 각 조건을 validate가 막는지 **그리고** 그 입력을 엔진에 직접 흘리면 실제로
> 던지는지를 둘 다 단언한다 — 한쪽만 고치면 깨진다. `PASS-1~6`은 과차단을 막는다
> (경로별로 안 쓰는 필드는 비어 있어도 통과해야 한다).

> ⚠️ **segment 배열에 `id`를 넣으며 배관 테스트가 깨졌다** — 폼 배열은 React key용 `id`를
> 갖는데(`NblParcelFormItem` 선례) Zod 스키마에는 없어 `parse`가 실패했다. 스키마에 `id`를
> 추가해 해소. **폼 배열 필드를 추가할 때는 raw 스키마도 같은 모양이어야 한다.**

**verify 결과**: `tsc` 0건 · validate anchor 18건 GREEN · NBL+calc+stores 1,469건 GREEN.

**14지점 self-grep**: ①②③⑧⑫⑭ ✅ · ④⑬은 prefix-pick이라 명시 나열 없음(정상) ·
⑥ 해당 없음(면적 입력이라 사이드바 합계 대상 아님) · ⑤⑦은 Phase D 대상.

### Phase D — UI (⑤⑦) ✅ **완료 (2026-08-05)**

| 지점 | 파일 | 내용 |
|---|---|---|
| ⑤ | `components/calc/transfer/nbl/FactoryLandSection.tsx` (신설 311줄) | 토글 → 지역 라디오 → **경로별** 필드 + 미리보기 |
| ⑤ | `components/calc/transfer/nbl/OtherLandDetailSection.tsx` | 마운트 1줄 (685줄 → 800 정책상 분리) |
| ⑦ | `components/calc/NonBusinessLandResultCard.tsx` | 안분 근거 조문 열거에 공장 경로 추가 |
| — | `__tests__/components/factory-land-section.anchor.test.tsx` (신설) | **16건** |
| — | `e2e/transfer-nbl-factory-land.spec.ts` (신설) | **6건** |

**설계 결정**

- **미리보기는 엔진과 같은 순수 함수**(`computeFactoryStandardArea`)를 호출한다 — 화면 숫자와
  판정 값이 갈릴 수 없다(`feedback_ui_engine_dual_truth_avoidance`). `PREVIEW-1`이 엔진 산출과
  DOM 텍스트를 직접 대조한다.
- **경로별 필드 노출** — 별표6은 연면적, §101①1호는 바닥면적으로 **다른 값**을 요구하므로
  한 화면에 같이 띄우지 않는다(`UI-3`·`UI-4`).
- **값이 모자라면 미리보기를 띄우지 않는다** — 추정 표시 금지(`PREVIEW-6`·`PREVIEW-7`).
- 문구 가드(`COPY-1~3`): "양도하는 토지 면적이 아닙니다" · "건축면적이 아닙니다"(조심 2025지0451)
  · "받지 않은 것이 확인되는 경우에만"(입증부담 — 조심 2025서2489).

> 🔴 **E2E가 처음에 6건 전부 실패했는데 원인은 코드가 아니었다.** `reuseExistingServer: !CI`가
> 포트 3000에 떠 있던 **다른 워크트리의 dev 서버**를 잡아, 이 브랜치 코드가 아닌 것을 테스트했다.
> `E2E_PORT=3131`로 격리하니 전건 통과. **워크트리에서 E2E를 돌릴 때는 `E2E_PORT`를 반드시 줄 것**
> (playwright.config.ts:13 주석이 같은 취지).

> ⚠️ **`data-testid`는 하이픈 속성이라 TS가 통과시킨다.** `ToggleCard`는 props를 spread하지 않아
> 붙여도 DOM에 나오지 않는데 컴파일 오류가 없다 — 죽은 prop이 남는다. 토글 셀렉터는 프로젝트
> 관례대로 `getByRole("switch", { name })`를 쓴다.

**verify 결과**: `tsc` 0건 · lint 0 errors · 톤/폰트 pre-push 게이트 통과 ·
컴포넌트 anchor 16건 · **공장 E2E 6건** · **NBL E2E 전체 13건 GREEN**(회귀 0).

### Phase E — 상호작용 회귀 ✅ **완료 (2026-08-05)**

`__tests__/tax-engine/transfer/factory-land-interaction.anchor.test.ts` (신설) — **16건**.

#### 🔴 §104⑤ 비교과세 — 낮은 초과비율에서는 세액이 **전혀** 안 변한다 (실측)

픽스처: 토지 양도 10억 · 취득 3억 · 2013-06-01 취득 → 2020-06-01 양도. 기준세액 **221,730,000원**.

| 공장 전체면적 | 초과비율 | 산출세액 | 증분 |
|---|---|---|---|
| 12,000㎡ | 한도 이내(사업용) | 221,730,000 | — |
| 13,000㎡ | 7.69% | 221,730,000 | **0** |
| 15,000㎡ | 20% | 221,730,000 | **0** |
| 20,000㎡ | 40% | 221,730,000 | **0** |
| 22,000㎡ | **45.45%** | 221,826,860 | **+96,860** ← 여기서 뒤집힌다 |
| 24,000㎡ | 50% | 224,195,000 | +2,465,000 |
| 30,000㎡ | 60% | 231,422,000 | +9,692,000 |
| 40,000㎡ | 70% | 238,784,000 | +17,054,000 |
| 120,000㎡ | 90% | 260,142,000 | +38,412,000 |

「소득세법」 §104⑤가 **그룹별 합산세액과 전체 일반 누진세액 중 큰 것**을 택하므로, 초과비율이
낮으면 일반세액이 채택되어 중과 효과가 **법령상 소멸**한다. ⇒ "초과분이 잡혔는데 세액이 그대로"는
결함이 아니다. 판정(`ratio`)과 세액을 **따로** 단언한다.

> ⚠️ **한도 이내면 `areaProportioning` 자체가 생성되지 않는다**(전량 사업용). `ratio === 0`을
> 기대하면 실패한다 — `E104-1a`로 분리했다.

#### 부칙 §9270호 §14① (2009.3.16~2012.12.31 취득 → +10%p 배제)

> 🔴 **검증에는 세액이 실제로 오르는 비율을 써야 한다.** 초과 40% 이하는 §104⑤ 때문에 원래
> 안 오르므로, 그 구간으로 테스트하면 "배제됐다"와 "원래 안 오른다"를 구분하지 못해 **공허하게
> 통과**한다. ⇒ 초과 **70%**(+17,054,000 확인 구간)로 검증했다.
>
> `E-CRISIS-1`: 2010-06-01 취득 · 초과 70% → 판정은 `ratio 0.7`로 정상이나 세액은 대조군과 동일(204,090,000).
> `E-CRISIS-2`: 같은 70%라도 2013-06-01 취득이면 238,784,000으로 오른다.

#### swap·수용과의 독립성

| 조합 | 대조군 | 공장(70%) | 증분 |
|---|---|---|---|
| 기본 | 221,730,000 | 238,784,000 | +17,054,000 |
| §97②2호 swap | 163,140,000 | 173,698,500 | +10,558,500 |
| 공익수용 | 221,730,000 | 238,784,000 | +17,054,000 |

swap은 **양도차익**에, 공장 초과분은 **세율**에 작용해 서로 독립이다. swap 시 증분이 작은 것은
과세표준이 줄어서이지 중과가 사라져서가 아니다(`E-SWAP-2`가 그 대소관계를 고정).

**단서**(`E-PROVISO-1`): 면적만 보면 한도 이내(12,000㎡)여도 허가·사용승인 미이행이면
`isNonBusinessLand = true` · `areaProportioning` 없음(안분이 아니라 전량) · 세액 상승.

---

## 6. 14 동기화 지점 (착수 전 체크리스트)

| # | 지점 | 대상 |
|---|---|---|
| ① | 폼 상태 | `AssetForm.nblFactory*` |
| ② | initial | `calc-wizard-asset-migrate.ts` |
| ③ | normalize | 동상 |
| ④ | API 변환 | `lib/calc/transfer-tax-api-helpers.ts` |
| ⑤ | UI 위젯 | `OtherLandDetailSection.tsx` |
| ⑥ | 사이드바 합계 | 해당 없음(면적 입력·금액 아님) |
| ⑦ | 결과 카드 | NBL 판정 스텝 표시 |
| ⑧ | validation | `transfer-tax-validate-asset.ts` |
| ⑨⑩ | Zod enum | `transfer-tax-schema-nbl.ts` |
| ⑪ | `acquisitionDate` fallback | 해당 없음 |
| ⑫ | **Zod 입력 객체 정의** | `factory` 오브젝트 — **침묵 strip 위험** |
| ⑬ | **body spread** | `callTransferTaxAPI` |
| ⑭ | **Route 엔진 input 매핑** | `engine-input.ts` |

---

## 7. 리스크

| # | 리스크 | 대응 |
|---|---|---|
| R-1 | ✅ 해소 — 지역 분기는 사용자 선택이므로 군 지역 해석이 계산에 개입하지 않는다 | UI 라벨 "읍·면지역(**군 지역 포함**) / 산업단지 / 공업지역" |
| R-1b | 사용자가 **양도 필지 면적**을 "공장 전체 면적"으로 잘못 입력 | §1.4가 요구하는 값은 **1구 공장 전체**다 — 필드 라벨·hint에 명시하고, 양도 필지 면적과 다를 수 있음을 안내 |
| R-2 | 별표1 데이터셋을 얹을 때 **KSIC 차수 드리프트**로 과거 양도 건에 침묵 오류 | §4.4 버전 게이트 — 2026-02-25 이전 양도는 자동조회 차단·직접입력 요구 |
| R-3 | 별표6 3호 가산을 모두 넣으면 **입력 필드가 10개를 넘는다** | 0-4에서 범위 축소. 단일 "추가 인정면적" 1칸 + 근거 링크가 유력 |
| R-4 | 낮은 초과비율에서 §104⑤ 비교과세로 **세액이 안 변해** anchor가 무의미해질 수 있다 | Phase 0-5 실측 후 anchor 설계 |
| R-5 | 재산세 경로와 산식이 **두 벌**이 된다(재산세는 직접입력) | Phase A 헬퍼를 세목 공용 위치에 두고, 재산세 전환은 별건으로 기록 |
| R-6 | `other_land` 파일이 606줄 → Step 0.5 추가로 **800줄 트리거 접근** | Phase A에서 공장 로직은 별도 파일로 분리(오케스트레이션 호출 1줄만 추가) |

---

## 8. 범위 밖 (기록만)

- **재산세 §102①1호 산식 미구현** — `factoryStandardArea` 직접입력 유지(§2.2). Phase A 헬퍼 완성 후 별건으로 전환 검토.
- **A-BS-1 `building_site` 주택 배율 오적용** — `land-category.ts:41`이 `building_site`를 `housing` 그룹으로 보내 「소득세법 시행령」 §168의12(주택 배율)를 적용한다. 현재 UI 도달 불가(`LAND_TYPE_OPTIONS` 6종에 없음). 이 계획과 파일은 인접하나 **조문이 다르다**(§168의12 vs §101②) — 별건 유지.
- **「지방세법 시행규칙」 별표6 3호 바목 체육시설 기준면적표** — 종업원 수 구간별 표. 0-4 결과에 따라 포함/제외.

---

## 9. 참고

### 법령 (실측 — 2026-08-05 KoreanLaw 조회)

| 근거 | 식별자 | 시행 |
|---|---|---|
| 「소득세법」 §104의3①4호나목 | MST 280405 | 2026-07-01 |
| 「지방세법 시행령」 §101①1호·§101②·§102①1호 | MST 287223 | 2026-07-01 |
| 「지방세법 시행규칙」 §50·별표6 | MST 287031 | 2026-07-01 (별표6 개정 2025.10.31) |
| 「공장입지 기준고시」 §3·§4·별표1 | 행정규칙일련번호 2100000274928 | 공포 2026-02-25 |
| 법제처 법령해석례 25-0823 (허가 등의 범위) | 일련번호 342727 | 2026-02-03 |

### 조세심판 결정례 (실측 — 전문 확인)

| 결정 | 의결일 | 이 계획에 쓰이는 논점 |
|---|---|---|
| **조심 2025서2489** | 2026.04.08 | 같은 공장에서 용도지역에 따라 §101①1호↔§102①1호가 갈린 실례 · 연면적(89,865.838)≠바닥면적(81,473.36) · 화학섬유 12% 실례 · 단서 입증부담은 과세관청 |
| **조심 2023지0373** | 2023.12.04 | 판정 단위 = **1구(하나의 울타리) 공장 전체** — 필지 내 나대지도 포함 (참조 조심 2021지2777) |
| 국심1993부0467 | 1993.05.17 | 초과분 **면적비율 안분** 선례 (⚠️ 구 법인세법 비업무용부동산 — 참고용) |
| 국심1994서5582 | 1995.08.17 | 공장경계 내 **녹지지역 제외** 후 기준면적 적용 (별표6 3호나 관련) |
| 1998-0639 | 1998.11.28 | **사면용지** 취급 (별표6 3호라 관련) |
| 국심1998중2634 | 1999.09.22 | **업종 적용**의 당부 · 도로·야적 면적의 건물면적 산입 여부 |
| 1994경2196 | 1996.09.14 | **옥외 기계장치 수평투영면적** 산입 (별표6 2호가 관련) |

> 아래 4건(1994~1999)은 **제목만 확인**했고 전문은 읽지 않았다. Phase 0-4에서 별표6 3호 범위를 정할 때 필요한 것만 전문 확인할 것.

### 코드

- `lib/tax-engine/non-business-land/other-land.ts` (606줄) — `judgeOtherLand`
- `lib/tax-engine/appurtenant-land-excess.ts:88` — `judgeAppurtenantLandExcess`
- `lib/tax-engine/local-tax-zone-multiplier.ts:122` — `getZoneAreaMultiplier`
- `lib/tax-engine/separate-aggregate-land.ts:326` — 재산세 `factoryStandardArea` 소비 지점

---

## 10. 검토 이력

### rev.8 (2026-08-05) — Phase E 완료 · **전 Phase 종료**

- 상호작용 anchor 16건. 세액은 전부 **실측값**(throwaway probe로 얻은 뒤 고정)
- 🔴 **§104⑤ 비교과세로 초과 40%까지 세액이 전혀 변하지 않는다** — 선행 CB 작업과 같은 현상이
  공장 경로에서도 확인됐다. 경계는 **40% ↔ 45.45%** 사이(+96,860원이 첫 증분)
- 🔴 **부칙 §9270호 §14① 검증에는 세액이 오르는 비율을 써야 한다** — 40% 이하로 테스트하면
  "배제됐다"와 "원래 안 오른다"를 구분 못 해 공허하게 통과한다. 초과 70%로 검증했다
- swap·수용과 독립임을 증분 대소로 고정(`E-SWAP-2`)
- ⚠️ **한도 이내면 `areaProportioning`이 아예 없다**(`ratio === 0`이 아니다). 처음에 0을 기대해
  실패했고 `E104-1a`로 분리했다
- Phase 0-5(세액 영향 실측)를 여기서 함께 종결

### rev.7 (2026-08-05) — Phase D 완료 (UI)

- ⑤ `FactoryLandSection.tsx` 신설(311줄) — `OtherLandDetailSection`이 685줄이라 800 정책상 분리
- ⑦ 결과 카드의 **안분 근거 조문 열거**에 공장 경로 추가. 스텝·`areaProportioning`은 범용 렌더라
  자동 표시되지만, 이 열거만 하드코딩이라 빠질 뻔했다(`feedback_engine_result_display_drift`)
- 컴포넌트 anchor 16건 + E2E 6건 + NBL E2E 전체 13건 GREEN
- 🔴 **E2E 6건 전부 실패 → 원인은 코드가 아니라 포트였다.** `reuseExistingServer`가 3000에 떠 있던
  **다른 워크트리 dev 서버**를 잡아 이 브랜치가 아닌 코드를 테스트했다. 실패 스냅샷에 섹션 텍스트가
  3회 보여 잠깐 헷갈렸는데 **전부 테스트 소스 에코**였다. `E2E_PORT=3131`로 격리하니 전건 통과
- ⚠️ **`data-testid`가 조용히 죽는 경우**: 하이픈 속성이라 TS가 통과시키는데 `ToggleCard`는
  props를 spread하지 않아 DOM에 안 나온다. 죽은 prop 3개를 제거하고 `getByRole("switch")` 관례로 전환
- UI 표준 준수: 인라인 색상 클래스 대신 `<ToneCard>`·`TONE` 토큰 사용(CLAUDE.md) —
  `check-tone-classes.sh`·`check-font-sizes.sh` 통과

### rev.6 (2026-08-05) — Phase C 완료 (폼·validate)

- ①②③ 폼 슬라이스·기본값·마이그레이션. **토글 기본 `false`라 기존 이력의 판정 결과는 불변**
- ⑧ `validateNblFactory()` — 엔진이 던지는 **5조건 전수** 선차단(§6 Phase C 표). 엔진 예외가
  HTTP 500이 되는 구조라(`route.ts:432`) 인라인 오류로 바꾸려면 여기서 막는 수밖에 없다
- **양방향 대칭 anchor 18건**: `COND-1~5`는 "validate가 막는다" + "그 입력을 엔진에 직접
  흘리면 실제로 던진다"를 **둘 다** 단언한다(덜 막음 방지). `PASS-1~6`은 과차단 방지 —
  경로별로 안 쓰는 필드(별표6 경로의 바닥면적, §101①1호 경로의 업종)는 비어도 통과해야 한다
- ⚠️ segment 배열에 React key용 `id`를 넣자 Zod `parse`가 깨졌다(스키마에 `id` 부재).
  **폼 배열 필드는 raw 스키마와 모양이 같아야 한다** — `NblParcelFormItem` 선례대로 정정

### rev.5 (2026-08-05) — Phase B 완료 (API 배관) · **Phase A 계약 결함 정정**

🔴 **Phase A가 판정 단위를 잘못 구현하고 있었다.** 계획 §1.4는 한도 비교를 「1구의 공장 전체」로
한다고 적어 놓고, 엔진은 `input.landArea`(= `asset.acquisitionArea`, **양도 대상 토지 면적**)를
비교 대상으로 썼다. 양도분이 공장 일부일 때 초과비율이 통째로 틀어진다.

- Phase B 배관을 설계하며 "이 면적이 어디서 오는가"를 추적하다 발견했다
  (`form-mapper.ts:70` — `landArea = asset.acquisitionArea`)
- ⇒ `FactoryLandUsage.totalAppurtenantLandArea`(**필수**) 신설. 한도 비교는 전체값으로 하고
  거기서 나온 **비율**을 양도분에 적용한다 — 선행 CB 작업의 지분 약분과 같은 구조
- `judgeFactoryLandExcess(usage, context)` — `landArea` 인자 제거(전체값을 usage가 들고 있다)
- 회귀 가드 `PART-1~3`. 특히 **PART-2는 정정 전 구현이라면 통과했을 케이스**다
  (양도분 5,000㎡만 보면 한도 12,000㎡ 이내 → 「전량 사업용」 오판정)

**추가로 막은 조용한 분기 오류 1건** — `locationCategory`가 빈 문자열이면 삼항 연산자가
말없이 `aggregate_taxation`으로 흘려 **다른 산식**(바닥면적 × 배율)이 적용됐다. 두 리터럴이
아니면 던지도록 바꿨다(`E2E-3`).

**Phase B 자체**: ⑫ Zod 8필드 추가, ⑬은 prefix-pick이라 무변경, ⑭ `buildFactory` 신설.
라운드트립 anchor 9건으로 폼→Zod→mapper→엔진 도달을 실증했다(§6 Phase B 표).

### rev.4 (2026-08-05) — Phase A 완료 (엔진)

**🔴 구현 중 스스로 만든 결함 1건을 잡았다 (§4.2)** — 공장 한도 초과 시 Step 0.5에서 즉시
비사업용으로 확정하도록 짰는데, 「소득세법」 §104의3①4호는 가·나·다목 **택일**이라 나목(공장
한도)에 미달해도 다목(수입금액비율·§168의11① 호별)에 해당하면 사업용이다. 기존 Step 3-2의
주석(`other-land.ts:571`)이 같은 이유로 뒤에 놓여 있다는 것을 읽고 발견했다.
⇒ **판정(앞)과 적용(뒤)을 분리**하고 회귀 가드 `ORDER-1~3`을 추가했다.


- `factory-land-standard-area.ts`(208줄) 신설 — 별표6 산식 + 두 경로 분기. `urban_other`는 선행 CB 작업의 `judgeAppurtenantLandExcess()`를 그대로 위임한다(신규 코드 0)
- `other-land.ts`에 **Step 0.5** 삽입 — Step 3-1이 `propertyTaxType`만 보고 조기 반환하므로 반드시 그 앞
- anchor **23건** GREEN · NBL 전체 328건 GREEN · `tsc` 0건
- ✅ **외부 기준점 통과**: 조심 2025서2489의 748,881.98㎡·325,893.44㎡를 소수 2자리까지 재현
- 🔴 **C-7(기간기준 미충족)을 매트릭스에서 삭제** — rev.1의 오해였다. `other_land` 경로는
  `meetsPeriodCriteria`에 **전 보유기간**을 사업용 기간으로 넘겨(`other-land.ts:280`)
  `meets`가 항상 true다(보유 7개월 probe로 확인). 도달 불가 분기를 넣지 않았고,
  기존 Step 3-1의 FAIL 분기도 같은 이유로 죽어 있다는 것을 주석으로 남겼다
- 🟠 **부수 작업**: `types.ts`가 내 추가로 800줄을 넘겨(778→842) `land-usage.types.ts`로
  분리했다(842→638·240). 전량 재수출이라 소비처 import 경로는 불변
- 🟠 **미해결(기존)**: `legal-codes/transfer.ts`는 내 변경 **전에 이미 862줄**로 800 초과였다.
  내 기여는 +5줄이라 이번 범위에서 분리하지 않았다 — 별건

### rev.3 (2026-08-05) — Phase 0-1 종결 · Phase 0 전건 완료

**조심 2025서2489**(2026.04.08) 전문 확인이 이 계획의 여러 미확정을 한 번에 해소했다.

- **지역 분기 구조 실증**: 같은 부산 해운대구 공장에서 **제2종일반주거지역 필지 → §101①1호(별도합산)**, **준공업지역 필지 → §102①1호(분리과세)** 로 갈렸다. 경기 시흥시 산업단지 사례(조심 2023지0373)와 합쳐, "각 목에서 정하는 지역"이 본문 열거 지역 **내부에서도 독립 작동**함이 확인된다
- **군 지역 쟁점 해소 — 다만 확인이 아니라 무쟁점화다**: 직접 판시 자료는 못 찾았으나, §4.3이 지역을 **사용자 선택**으로 두므로 엔진이 자동판정을 하지 않는다 ⇒ 해석이 계산에 개입하지 않는다. UI 라벨에 "군 지역 포함"만 명시하면 된다. **블로커에서 제거**
- 🔴 **판정 단위 = 「1구의 공장」 전체** (§1.4 신설) — 필지가 아니다. 조심 2023지0373은 1필지 33,816.2㎡ 중 나대지 20,163.83㎡도 "하나의 울타리 내"라는 이유로 전량 분리과세로 경정했다. ⇒ **선행 CB 작업과 같은 약분 구조**가 성립해, 사용자는 양도 필지가 아니라 **공장 전체 면적 2개**를 입력하면 초과 비율이 확정된다
- 🔴 **연면적 ≠ 바닥면적 실증**: 같은 공장에서 §101①1호용 **81,473.36㎡**(바닥), §102①1호용 **89,865.838㎡**(연면적). 한 칸으로 받으면 틀린다
- ✅ **데이터셋 교차검증 성공**: 재결례가 인용한 화학섬유(합성섬유) **12%** 가 rev.2에서 추출한 별표1의 KSIC `20501` 값과 일치하고, `89,865.838 × 100 ÷ 12 = 748,881.98㎡` 가 재결례 수치와 **소수 둘째 자리까지 일치**한다 ⇒ 파싱·산식 해석·실무 계산이 모두 검증됨
- **단서 입증부담**: "건축물대장이 확인되지 않는다는 이유만으로 무허가로 단정 불가"(2025서2489) ⇒ UI 문구를 좁게 쓸 것
- 별표6 3호(녹지·사면용지·도로·옥외 기계장치) 관련 재결례 4건을 **제목만** 수집 — 전문 미독, Phase 0-4에서 필요분만 확인

⇒ **Phase 0 블로커 전건 해소.** 남은 0-4·0-5·0-6은 블로커가 아니다(범위 축소로 통과 가능).

### rev.2 (2026-08-05) — Phase 0-2·0-3 종결

- **0-2 완료**: 「공장입지 기준고시」 별표1 **전량 확보** — 법제처 `target=admrul&ID=2100000274928` 단일 호출(419KB). **480행**(KSIC `10111`~`34020`), 요율은 **7종**(12%가 195행으로 최다). MCP `get_admin_rule`은 50,000자 절단 — **직접 API를 쓸 것**
- 개정 이력 실측: 27년간 7회, 최근 간격 8년. 2026-02-25 개정은 **KSIC 11차 반영**(제개정이유 실측) — 요율 정책이 아니라 **분류번호 체계 교체**
- **0-3 종결**: ㉮/㉯를 택일 문제로 세운 것이 오설정이었다 — **계층이 다르다**. 엔진은 `ratePercent: number`만 받으므로 데이터셋 유무가 엔진 설계에 무관하고, ㉯를 나중에 얹어도 Phase A~C 재작업이 0이다 ⇒ **엔진 ㉮ 고정 + ㉯ UI 후속**
- ⇒ **블로커에서 0-3 제거**. 남은 블로커는 0-1(군 지역 귀속) 1건
- 🔴 **버전 게이트 신설**: KSIC 10차↔11차 드리프트 때문에 데이터셋 자동조회는 **양도일 ≥ 2026-02-25**로 한정해야 한다. 그 이전은 직접입력 요구(추정 금지)

### rev.1 (2026-08-05) — 초안

- 법령 위임 체인 4단(소득세법 → 지방세법 → 시행령 → 시행규칙 별표6 → 산업통상부 고시 별표1) KoreanLaw 실측 확보
- **지역에 따라 한도 산식이 배타 분기**한다는 것이 이 기능의 핵심 구조임을 확인 — 선행 CB 작업 자산은 그중 §101①1호 경로에만 재사용된다
- 현행 갭 실측: `other-land.ts:302`가 재산세 유형만 보고 전량 사업용 통과. 저장소 전체에 `기준공장면적률` 0건
- 미확정 3건을 Phase 0 블로커로 분리(군 지역 귀속 · 면적률 입력 방식 · 별표6 3호 범위)

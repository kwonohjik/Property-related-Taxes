# 일반건물 — 「구분양도/일괄양도」 축(`saleSplitMode`) 도입 계획서

- **작성**: 2026-08-06
- **출처**: `general-building-part-major-acquisition.plan.md` §9 **O-2**(「본 계획 범위 밖 — 일반건물은 §166⑥ 안분이 전제」)
- **대상**: `assetKind === "general_building"`

> **이 문서의 모든 인용 file:line은 2026-08-06 작업 트리 기준 실측이다.** 조문은 KoreanLaw 원문
> 조회(`소득세법` mst=280405 · `소득세법 시행령` mst=286211 · `부가가치세법 시행령` mst=283641),
> 수치는 throwaway probe 실측값이다. 확인하지 못한 것은 §9에 「확인 필요」로 명시한다.

---

## §0 결론 먼저 — 이 계획은 **선행 조건**을 제시한다

**도입 자체는 조문에 부합한다.** 오히려 현행이 조문과 어긋난다(§2.1).

그러나 **§100③(30% 의제 규칙)이 코드베이스에 전무**하고(§3.1), 그 가드 없이 구분양도를 열면
납세자가 구분 가액 선택만으로 세액을 움직일 수 있다 — **실측 최대 28,644,000원**(§3.2).
그리고 그 결함은 **이미 split(주택·건물) 경로에 존재**한다.

⇒ 권고는 **안 C — §100③ 가드를 먼저 구현하고 그 다음 도입**(§4.3).
가드는 일반건물 전용이 아니라 **split과 공유하는 순수 함수**로 만든다.

---

## §1 용어 정리

| 축 | 값 | 뜻 |
|---|---|---|
| `saleSplitMode` | `"apportioned"` | **일괄양도** — 총 양도가액 1건, 토지·건물은 기준시가 비율로 안분 |
| | `"actual"` | **구분양도** — 계약서에 토지·건물 양도가액이 각각 기재돼 그대로 사용 |

취득가액 축(`landAcqMode`/`buildingAcqMode`)과 **별개**다. 취득 축은 이미 일반건물에 도입됐다
(`general-building-part-major-acquisition.plan.md`). 이 계획은 **양도가액 축**만 다룬다.

---

## §2 조문 근거

### 2.1 원칙은 「각각 구분 기장」이고, 안분은 **예외**다

「소득세법」 제100조 제2항 **전문**:

> 제1항을 적용할 때 양도가액 또는 취득가액을 실지거래가액에 따라 산정하는 경우로서 토지와 건물 등을
> 함께 취득하거나 양도한 경우에는 이를 **각각 구분하여 기장**하되 토지와 건물 등의 가액 **구분이
> 불분명할 때에는** 취득 또는 양도 당시의 기준시가 등을 고려하여 대통령령으로 정하는 바에 따라
> 안분계산(按分計算)한다.

「소득세법 시행령」 제166조 **제6항**:

> 법 제100조제2항의 규정을 적용함에 있어서 토지와 건물 등의 가액의 구분이 **불분명한 때에는**
> 「부가가치세법 시행령」 제64조제1항에 따라 안분계산 하며 …

⇒ **안분은 「구분이 불분명한 때」의 처리**다. 구분이 분명하면 그 값을 쓰는 것이 본문이다.

**현행 일반건물은 안분을 무조건 적용한다** — 이것이 O-2의 실체다:

| 경로 | 지점 | 동작 |
|---|---|---|
| 환산 | `lib/tax-engine/general-building-valuation.ts:193` `allocateBundledTransferPrice(input)` | 조건 없이 호출 |
| 실가 | `app/api/calc/transfer/general-building-route-actual.ts:188·197` `Math.floor(totalTransferPrice * landRatioNum)` | 조건 없이 안분 |

일반건물 payload에는 **토지·건물 양도가액 필드 자체가 없다**(`GeneralBuildingInput`·
`GeneralBuildingActualPricePayload` 모두 `totalTransferPrice` 단일).

### 2.2 안분 방법은 부가령 §64①이다 — **감정평가 우선 단서**가 있다

「부가가치세법 시행령」 제64조 제1항 제1호:

> 토지와 건물 또는 구축물 등 …에 대한 「소득세법」 제99조에 따른 기준시가가 **모두 있는 경우**:
> 공급계약일 현재의 기준시가에 따라 계산한 가액에 비례하여 안분계산한 금액. **다만, 감정평가가액
> … 이 있는 경우에는 그 가액에 비례하여 안분 계산한 금액으로 한다.**

⇒ 기준시가 비율은 **감정평가가액이 없을 때의** 방법이다. 현행 코드는 기준시가 비율만 구현하고
**단서를 구현하지 않았다**(§3.3 — 별건 갭).

같은 조 제2호·제3호는 기준시가가 없는 경우(감정평가 → 장부가액 → 국세청장이 정하는 바)를 정한다.
일반건물은 기준시가 2필드가 필수 입력이라 1호 경로가 전제된다.

### 2.3 🔴 §100③ — 구분 가액이 안분 가액과 **30% 이상** 차이나면 「불분명」으로 **의제**

「소득세법」 제100조 **제3항**:

> 제2항을 적용할 때 토지와 건물 등을 함께 취득하거나 양도한 경우로서 그 토지와 건물 등을 **구분
> 기장한 가액**이 같은 항에 따라 **안분계산한 가액과 100분의 30 이상 차이가 있는 경우**에는 토지와
> 건물 등의 가액 구분이 **불분명한 때로 본다**. 다만, 다른 법령에서 정하는 바에 따라 가액을 구분한
> 경우 등 대통령령으로 정하는 사유에 해당하는 경우는 제외한다.

단서의 「대통령령으로 정하는 사유」 = 「소득세법 시행령」 제166조 **제8항**:

1. 다른 법령에서 정하는 바에 따라 토지와 건물 등의 가액을 구분한 경우
2. 토지와 건물 등을 함께 취득한 후 건물 등을 **철거하고 토지만 사용**하는 경우

> 부가령 §64②가 같은 2사유를 「실지거래가액을 공급가액으로 한다」로 규정한다 — 두 법의 예외가
> 문언까지 일치한다.

⇒ 구분양도를 허용하려면 **이 의제 규칙이 함께 있어야** 한다. 없으면 30% 넘는 임의 구분이 그대로
통과한다.

---

## §3 현상 실측

### 3.1 🔴 §100③ 30% 규칙이 코드베이스에 **전무하다**

`lib/tax-engine`·`lib/calc` 전역에서 「100분의 30」·`0.3` 임계·「불분명」 판정을 찾지 못했다.
검색으로 걸린 30%는 전부 무관하다 — LTHD 30% 캡(`transfer-tax-lthd.ts:109`) · 세대생략 할증
(`gift-simultaneous.ts:9`) · 저가양수 §35 시가 30%(`bargain-transfer.ts:25`) · 재산세 세율.

**split(주택·건물) 경로에도 없다.** validate는 `saleSplitMode !== "actual"` early-return
(`lib/calc/transfer-tax-validate-split.ts:286`) 후 **합 ≤ 총액**(overflow)만 검사한다(`:300-307`).
30% 비교는 어디에도 없다.

⇒ §100③ 미구현은 **일반건물 도입의 선행 조건**이면서 **split 경로의 기존 결함**이다.

### 3.2 세액 노출 폭 — 실측 28,644,000원

probe: 주택 · 양도 2024-06-01 15억 · **토지 취득 2014-06-01 / 건물 취득 2016-06-01**(보유기간 상이)
· 취득가액 파트 4억+4억 · 양도시 기준시가 토지 9억 / 건물 6억(⇒ 안분 비율 토지 60%) · `makeMockRates()`.

| 신고 방식 | 토지 / 건물 양도가액 | 총세액 |
|---|---|---|
| 일괄양도(안분) | 9억 / 6억 (기준시가 비율) | **228,195,000** |
| 구분양도 · 안분과 동일 비율 | 9억 / 6억 | **228,195,000** (동일 — 일관성 확인) |
| 구분양도 · 토지 몰아주기 | 14억 / 1억 | **199,551,000** |
| 구분양도 · 건물 몰아주기 | 1억 / 14억 | **218,031,000** |

**최대−최소 = 28,644,000원.** 두 몰아주기 모두 안분보다 세액이 낮다 — 파트별 보유기간이 달라
장기보유특별공제가 갈리기 때문이다(토지가 2년 먼저 취득).

> ⚠️ 이 수치는 **가드 부재의 노출 폭**이지 「현행이 틀렸다」는 뜻이 아니다. 구분 기장이 실제
> 계약을 반영하면 그 값이 정답이다. 문제는 **30% 초과 구분을 걸러내지 못한다**는 것이다.

### 3.3 부가령 §64①1호 **단서(감정평가 우선) 미구현** — 별건 갭

`calcSaleApportionRatio`(`lib/tax-engine/transfer-tax-split-gain.ts:259-269`)는 기준시가 비율만
산출한다. 감정평가가액이 있을 때 그 비율을 쓰는 단서 분기가 없다. 일반건물도 동일
(`general-building-valuation.ts:92-105` `allocateBundledTransferPrice`).

일반건물 UI는 감정평가가액 입력 축을 갖고 있지 않으므로(취득 축의 `appraisal` 모드는 **취득가액**
용도다) 현재 도달 불가다. **범위 밖으로 두고 §9에 기록**한다.

### 3.4 ⚠️ `saleSplitMode`는 엔진 입력 타입에 있으나 **엔진이 읽지 않는다**

| 계층 | 지점 | 사실 |
|---|---|---|
| 폼 타입 | `lib/stores/calc-wizard-asset.ts:433` | `"apportioned" \| "actual"` |
| 폼 기본값 | `lib/stores/calc-wizard-asset-factory.ts:153` | `"apportioned"` |
| ④ API 게이트 | `lib/calc/transfer-tax-api-split.ts:77` | `saleDirectActive = isSplitActive && saleSplitMode === "actual"` — 이 게이트가 파트 양도가액 **전송 여부**를 결정 |
| 엔진 입력 타입 | `lib/tax-engine/types/transfer.types.ts:608` | `saleSplitMode?: "actual" \| "apportioned"` 선언됨 |
| 엔진 본체 | `lib/tax-engine/transfer-tax-split-gain.ts` | **`saleSplitMode` 참조 0건** |
| 엔진 실제 스위치 | 같은 파일 `:483-484` | `input.landTransferPrice` / `input.buildingTransferPrice`의 **유무** |

`transfer-tax-api-split.ts:74`의 주석은 「엔진은 saleSplitMode를 명시 입력으로 소비하므로(…"죽은
모드" 재발 방지)」라고 적었으나 **실제로는 소비하지 않는다** — 주석↔구현 드리프트다
(메모리 `feedback_engine_comment_vs_impl_drift`).

**설계에 미치는 영향**: 일반건물에 축을 도입할 때 「모드 플래그를 엔진에 넘긴다」가 아니라
**「파트 양도가액 2필드를 넘기고, 있으면 쓴다」**가 실제 계약이다. 모드는 UI·API·validate 계층의
게이트다. 이 사실을 모르면 엔진에 플래그만 추가하고 값이 안 흘러 「모드가 죽는」 현상이 재발한다.

### 3.5 일반건물 전용 제약 — split과 다른 점

| 항목 | 일반건물 | split |
|---|---|---|
| 양도가액 필드 | **총액 1개**(`totalTransferPrice`) | 총액 + 파트 2필드 |
| 카드 수 | 토지(사업용·비사업용 분할 가능) + 건물(본체·증축) → **최대 4장** | 2파트 |
| 안분 소비처 | 양도가·취득가·필요경비·환산 분모 **모두** `landRatioNum` 1개로 파생 | 축별 분리 |
| 응답 형태 | `mode: "bundled"` | `mode: "single"` |

⇒ 일반건물은 안분 비율이 **여러 축에 동시 사용**된다. 구분양도를 도입하면 **양도가액 축만**
비율을 쓰지 않게 되고, 환산 분모·필요경비 안분은 여전히 기준시가 비율을 쓴다. 이 경계를
명확히 하지 않으면 「양도가액은 구분인데 환산 분모는 안분」이 뒤섞여 조용한 오답이 된다.

---

## §4 설계 안

### 4.1 안 A — 지금 바로 도입 (가드 없이)

일반건물 payload에 `landTransferPrice`/`buildingTransferPrice`를 추가하고, 있으면 안분을 건너뛴다.

- ✅ 구현 최소. split의 UI 컴포넌트(`LandBuildingSaleSplitSection`)를 재사용할 여지.
- ❌ **§100③ 가드 부재를 새 자산유형으로 확대**한다. §3.2의 28,644,000원 노출이 일반건물에도 생긴다.
- ❌ 「법 근거 없이 불리 적용 금지」의 반대 방향 — 근거 없이 **유리** 적용을 열어주는 것도
  법령 정확성 위반이다(CLAUDE.md 「법령 정확성 최우선」).

### 4.2 안 B — 도입하지 않음 (현행 유지)

- ✅ 회귀 0. 작업량 0.
- ❌ **§100② 전문에 반한다** — 계약서에 구분 기재된 자산에도 안분을 강제한다. 부가가치세법
  §29⑨(토지·건물 함께 공급 시 건물분 공급가액)이 건물분 부가세 산정을 요구하므로 구분 기재가
  이루어지는 거래 형태가 존재하고, 그 경우 「구분이 불분명」하지 않다.
- ❌ 일반건물이 상업용 건물 중심 자산유형인데, 그 거래에서 구분 기재가 가장 흔하다
  (⚠️ **실무 빈도는 미확인** — §9).

### 4.3 안 C — **§100③ 가드 선행 후 도입** ⭐ 권고

**Phase 1**: §100③ 의제 규칙을 **공유 순수 함수**로 구현하고 **split 경로에 먼저 적용**한다.

```
// lib/tax-engine/sale-split-deemed-unclear.ts (신규)
//   §100③ — 구분 기장 가액이 안분 가액과 30% 이상 차이나면 「불분명」으로 의제
isDeemedUnclearSplit({
  declaredLand, declaredBuilding,     // 구분 기장 가액
  apportionedLand, apportionedBuilding, // 안분 가액(부가령 §64①)
  exemptReason,                        // §166⑧ 1호·2호 해당 여부
}): boolean
```

- 적용 결과가 「불분명」이면 **안분 가액으로 되돌린다**(구분 입력을 무시). 차단이 아니라 **의제**다
  — 조문이 「불분명한 때로 **본다**」이므로 계산을 진행해야 한다.
- §166⑧ 예외 2사유는 **사용자 입력**으로 받는다(자동 판정 불가 — 「다른 법령에 따라 구분」은
  계약 근거, 「철거 후 토지만 사용」은 사실관계).
- 이 Phase만으로 **split 경로의 기존 결함이 해소**된다.

**Phase 2**: 일반건물에 축을 도입하고 Phase 1 함수를 **그대로 호출**한다.

- ✅ 조문 준수 · 노출 차단 · split과 단일 소스.
- ⚠️ Phase 1이 **기존 split 경로의 세액을 바꾼다** — 30% 초과 구분으로 신고하던 케이스가
  안분으로 되돌아간다. 회귀 테스트가 그 케이스를 담고 있으면 anchor 갱신이 필요하다(§7 R-1).

---

## §5 케이스 매트릭스 (Phase 2 — 일반건물)

> 🔴 **2026-08-06 정정 — 아래 서두는 Q-1 확정(§11) 전에 쓰였다.**
>
> 종전 서술: 「30% 판정은 파트별이 아니라 구분 기장 가액 **전체**를 안분 가액과 비교한다.
> 아래 비율은 **토지분 기준**이다.」
>
> **확정된 규칙은 그 반대다** — 토지·건물 **양쪽 모두**를 각각 안분값과 비교하고, **한쪽이라도**
> 30% 이상 벗어나면 발동한다(§11.1). 「한쪽만 검증하고 나머지는 차액으로 결정」이 실무 자료가
> 지적한 **실수유형**이다(§11.3). 아래 표의 「안분 대비 차이」는 **더 크게 벗어난 파트** 기준으로
> 읽는다 — 작은 파트가 실질 제약이다(분모가 작아 같은 차이 금액이 큰 비율이 된다).

| # | 구분 기재 | 안분 대비 차이 | §166⑧ 예외 | 기대 동작 |
|---|---|---|---|---|
| S-1 | 없음(총액만) | — | — | 안분 (현행 동일 — **회귀 0**) |
| S-2 | 있음 · 안분과 동일 | 0% | — | 구분 값 사용 |
| S-3 | 있음 | 29% | — | 구분 값 사용 |
| S-4 | 있음 | **30%** | — | **안분으로 의제** (경계 — 「이상」이므로 30%도 포함) |
| S-5 | 있음 | 55% | — | 안분으로 의제 |
| S-6 | 있음 | 55% | 1호(다른 법령) | **구분 값 사용**(단서) |
| S-7 | 있음 | 55% | 2호(철거·토지만) | 구분 값 사용(단서) |
| S-8 | 한쪽만 입력 | — | — | ~~차단~~ → **잔액 도출 + 도출된 파트도 판정 대상**(Q-3 §11.3 · 1-C 엔진이 이미 그렇게 동작한다 — `transfer-tax-split-sale-price.ts` `declared`) |
| S-9 | 구분 + 토지 NBL 분할 | 임의 | — | 구분 값을 **면적비로** 사업용·비사업용 안분(O-4 §14와 같은 순서) |
| S-10 | 구분 + 증축(건물 2장) | 임의 | — | 건물 구분 값을 본체·증축에 어떻게 나눌지 **미결**(§9) |
| S-11 | 구분 + 부담부증여 | — | — | **차단** — §159가 채무비율로 자동 산정(취득 축 V-4와 같은 이유) |
| S-12 | 구분 + 환산 파트 | 임의 | — | 양도가액은 구분 값, **환산 분모는 기준시가 비율 유지**(§3.5 경계) |

---

## §6 14 동기화 지점 (Phase 2)

| # | 지점 | 작업 |
|---|---|---|
| ① 폼 상태 | `AssetForm.saleSplitMode` — **이미 존재**(`calc-wizard-asset.ts:433`) · `landTransferPrice`/`buildingTransferPrice`도 이미 존재 | 신규 없음 |
| ② initial | `factory.ts:153` `"apportioned"` — **이미 존재** | 신규 없음 |
| ③ normalize | `calc-wizard-asset-migrate.ts:339-341` legacy `landSplitMode` 이전 — **이미 존재** | 신규 없음 |
| ④ API 변환 | `lib/calc/transfer-tax-api-gb.ts` — 파트 양도가액 2필드 + §166⑧ 예외 플래그 전송 (환산·실가 **양 경로**) | **신규** |
| ⑤ UI 위젯 | 일반건물 블록에 구분/일괄 토글 + 파트 양도가액 2칸 + 예외 사유 체크 | **신규** |
| ⑥ 사이드바 | `computeTransferSummary` 양도가액 합계 — 구분 값 우선 | **확인 필요** |
| ⑦ 결과 카드 | 안분 표에 「구분 기재」/「30% 의제로 안분」 표시 | **신규** |
| ⑧ validation | `transfer-tax-validate-gb.ts` — 한쪽만 입력 차단 · 부담부증여 조합 차단 · 합 ≤ 총액 | **신규** |
| ⑨⑩ Zod enum | `saleSplitMode`·예외 사유 enum | **신규** |
| ⑪ 자산-수준 fallback | 해당 없음 | — |
| ⑫ Zod 입력 객체 | `lib/api/transfer-tax-building-schemas.ts`에 `landTransferPrice`·`buildingTransferPrice`·예외 플래그 | **신규** |
| ⑬ body spread | `transfer-tax-api-gb.ts` 반환 객체 | **신규** |
| ⑭ Route 매핑 | `general-building-route-helper.ts` 실가 경로는 **스프레드**라 자동 흐름(#1088) · 환산 경로도 `gbPayload` 스프레드 | **확인만** |

> ⑭가 「확인만」인 것은 #1088에서 명시 나열을 스프레드로 바꿔 두었기 때문이다. 그 전이라면
> 4필드를 목록에 추가해야 했고, 빠뜨리면 침묵 strip이 됐다.

---

## §7 리스크

| ID | 리스크 | 대응 |
|---|---|---|
| **R-1** | Phase 1이 **기존 split 경로의 세액을 바꾼다**(30% 초과 구분 → 안분 의제) | 기존 anchor 전수 확인 후 갱신. 갱신 시 **법령 정합 우선**(메모리 `feedback_anchor_correction_legal_priority`) |
| **R-2** | 한쪽만 입력된 경우 잔액 도출(총액 − 입력분)을 허용할지 | split의 `splitPair`(`split-gain.ts:117-137`)는 한쪽만 있으면 **잔액을 도출**한다(`:125-126`). 일반건물도 같게 할지 **확정 필요** — 잔액 도출분은 「구분 기장」이 아니라 계산 결과이므로 30% 판정 대상인지 함께 판단 |
| **R-3** | §3.5 경계 혼재 — 양도가액만 구분, 환산 분모·필요경비는 안분 | 축별로 어떤 비율을 쓰는지 **표로 고정**하고 anchor로 못박는다 |
| **R-4** | S-10 증축(건물 2장)에 건물 구분 값 배분 근거 없음 | Phase 2에서 **증축 조합 차단**(취득 축 V-3과 동일 선례). 별건으로 승격 |
| **R-5** | §166⑧ 예외를 사용자 자기신고로 받으면 남용 가능 | 예외 선택 시 근거 입력을 요구하고 결과 화면·신고서에 **명시 표시**. 자동 판정은 불가 |
| **R-6** | 800줄 정책 — `transfer-tax-validate-gb.ts` **376줄** · `transfer-tax-api-gb.ts` **316줄**(2026-08-06 실측) | Phase 2 착수 시 재측정. validate가 376이라 §5 매트릭스 12케이스 검증을 더하면 여유가 크지 않다 |

---

## §8 Phase 계획

```
Phase 0  법령 확정 — §100③ 「안분계산한 가액」의 비교 기준 확정        → verify: 예규·심판례 조회
Phase 1  §100③ 공유 함수 + split 경로 적용 (기존 결함 해소)          → verify: anchor + 기존 anchor 전수
Phase 2  일반건물 축 도입 (④⑤⑦⑧⑨⑩⑫⑬)                            → verify: 케이스 매트릭스 S-1~S-12
Phase 3  E2E — 구분/일괄 토글 → 계산 → 결과 표시                      → verify: 실플로우 1건
```

**Phase 0이 선행이다.** §100③은 「구분 기장한 가액이 안분계산한 가액과 100분의 30 이상 차이」인데,
비교 대상이 **토지분끼리인가 · 건물분끼리인가 · 어느 한쪽이라도인가**가 문언만으로 확정되지 않는다.
§5 매트릭스는 「토지분 기준」으로 잠정 기술했으나 **확정 전 구현 금지**
(메모리 `feedback_unverified_authority_blocks_tax_change`).

---

## §9 미결 (착수 전 확정 필요)

| ID | 항목 | 상태 |
|---|---|---|
| ~~Q-1~~ | §100③ 30% 비교의 **대상** | ✅ **확정 — 토지·건물 「양쪽 모두」**(§11). 실무 자료 4개 사례를 규칙으로 재현 검증 |
| ~~Q-2~~ | 「30% 이상 차이」의 분모 | ✅ **확정 — 안분값**(§11). 적정범위 = 안분값 × (0.7, 1.3) 개구간 |
| **Q-3** | 한쪽만 입력 시 잔액 도출 허용 여부(R-2) | 🟢 **부분 확정** — 도출은 허용되나 **잔액 쪽도 30% 검증 대상**이다(§11.3 실수유형). 「입력 축을 어디에 둘지」만 UI 판단으로 남는다 |
| ~~Q-4~~ | S-10 증축 조합 — 건물 구분 값을 본체·증축에 배분하는 근거 | ✅ **종결 — 근거 없음 확인 ⇒ 차단 확정**(§19.1). 명문·판례·해석례 모두 답하지 않는다. 「불분명하니 안분」은 해석일 뿐이라 세액 변경 방향으로 쓰지 않는다 |
| **Q-5** | 부가령 §64①1호 **단서(감정평가 우선)** 미구현(§3.3) | 🟡 **별건**. 일반건물은 감정평가가액 입력 축이 없어 현재 도달 불가 |
| ~~Q-6~~ | 일반건물 거래에서 토지·건물 구분 기재의 **실무 빈도** | ✅ **종결**(§19.4) — 법령·판례로 답할 문항이 아니고, Phase 2가 도입을 마쳐 더 이상 의사결정에 영향을 주지 않는다 |
| **Q-7** | `transfer-tax-api-split.ts:74` 주석↔구현 드리프트(§3.4) | 🟢 **정정만 필요** — 엔진은 `saleSplitMode`를 읽지 않는다. Phase 1에서 주석 수정 |

> ~~Q-1·Q-2는 세액이 바뀌는 판정이라…~~ → **2026-08-06 확정**(§11). **Phase 1 착수 가능**.

---

## §10 Phase 0 사전 조회 결과 (2026-08-06)

Q-1·Q-2를 답하려고 **미리** 조회했다. 결론: **직접 답하는 자료를 찾지 못했다** — Phase 0은 그대로 남는다.

| 조회 | 결과 |
|---|---|
| 조세심판원 「토지 건물 가액 구분 불분명」 | **106건**. 대부분 1995~2009년 사안으로, §100③ 신설 **전** 「불분명」을 개별 판단한 것이다(예: 조심2009서0080 · 조심2008서1390·1391 — 매매계약서 구분가액이 불분명하다고 보아 기준시가 비율 안분한 처분을 **기각**). 30% 기준 자체를 다루지 않는다 |
| 조세심판원 「구분기장 100분의 30」 | **0건** |
| 조세심판원 「토지 건물 구분 기장 안분 30」 | **0건** |
| 국세청 해석 「토지 건물 구분 기장 30」 | **0건** |

주목할 1건: **국심2005광2738** 「매매계약서에 **구분 기재된** 토지·건물의 가액이 **불분명한 것으로
볼 수 있는지** 여부(경정)」 — 쟁점 구조가 §100③과 같으나 **2006.03.07 의결로 §100③ 신설 전**이다.
본문을 읽으면 「구분 기재를 불분명으로 되돌리는」 판단 기준의 연혁적 근거가 될 수 있다.

⇒ **Phase 0의 작업 내용을 구체화**한다:
1. §100③의 **신설 시기**를 확인하고(연혁 조회) 그 이후 재결례로 범위를 좁혀 재검색
2. 국심2005광2738 본문 조회 — 신설 전 판단 기준 파악
3. 그래도 Q-1·Q-2가 확정되지 않으면 **보수적 해석**을 채택하고 그 사실을 결과 화면에 명시
   (어느 쪽으로 해석해도 세액이 달라지므로, 임의 선택은 금지 —
    메모리 `feedback_unverified_authority_blocks_tax_change`)

> ⚠️ 「검색 0건」은 「규칙이 없다」가 아니라 **「이 검색으로는 찾지 못했다」**다.
> 법제처 API는 공백 키워드를 AND로 처리하므로 키워드가 많으면 결과가 사라진다 —
> Phase 0에서 키워드를 나눠 재조회할 것.

---

## §11 Q-1·Q-2 확정 — 30% 규칙 (2026-08-06)

> **출처**: 사용자 제공 실무 자료(§100③ 「실수유형」 해설 + 사례 4건).
> ⚠️ **예규 번호·발간 주체는 자료에 표시되지 않았다** — 조문 문언과 내적으로 일치하고 사례 4건이
> 아래 규칙으로 **정확히 재현**되므로 확정으로 채택한다. 인용이 필요한 문서에서는 §100③ 조문과
> 이 재현 검증을 근거로 들 것.

### 11.1 확정된 규칙

자료 본문:

> 구분한 토지와 건물 또는 구축물 등의 가액이 감정가액 등에 따라 안분계산한 금액과 100분의 30 이상
> 차이가 있는 경우로 규정되어 있어 **토지와 건물가액 모두 ±30% 미만 범위내이어야 함**에 유의

⇒ 세 가지가 동시에 확정된다.

| 논점 | 확정 |
|---|---|
| **Q-1 비교 대상** | **토지·건물 양쪽 모두**. 한쪽이라도 벗어나면 부적합 |
| **Q-2 분모** | **안분값**. 적정범위 = 안분값 × 0.7 초과 ~ × 1.3 미만 (**개구간**) |
| **경계** | 「30% **이상** 차이」가 부적합이므로 **정확히 30%도 부적합**. 자료도 「이상이므로 7.8억보다 작고 4.2억보다 **큼**에 유의」로 명시 |

### 11.2 재현 검증 — 자료 사례 4건이 test vector다

총거래가액 10억(VAT 제외) · **감정가액** 건물 6억 / 토지 4억
→ 안분값 건물 **6억** · 토지 **4억** → 적정범위 건물 (4.2, 7.8) · 토지 (2.8, 5.2)

| 사례 | 계약서 건물 | 계약서 토지 | 건물 차이 | 토지 차이 | 자료 | 규칙 재현 |
|---|---|---|---|---|---|---|
| 1 | 4.3 | 5.7 | 28.3% | **42.5%** | 부적합 | 부적합 ✓ |
| 2 | 7.7 | 2.3 | 28.3% | **42.5%** | 부적합 | 부적합 ✓ |
| 3 | 7.1 | 2.9 | 18.3% | 27.5% | 적합 | 적합 ✓ |
| 4 | 4.9 | 5.1 | 18.3% | 27.5% | 적합 | 적합 ✓ |

**4건 전부 일치.** Phase 1의 anchor는 이 4건을 그대로 고정한다.

### 11.3 🔴 실수유형 — 「한쪽만 검증하고 차액으로 결정」

자료가 경고하는 함정이 Q-3(R-2)의 답이다.

- 사례1: 건물을 최저한계 4.3억으로 잡고 토지를 잔액(10−4.3=5.7억)으로 두면 → **토지가 5.2억 상한을 넘는다**
- 사례2: 건물을 최고한계 7.7억으로 잡고 토지를 잔액(2.3억)으로 두면 → **토지가 2.8억 하한을 밑돈다**
- 사례3·4: **작은 쪽(토지)**을 한계값으로 잡고 큰 쪽을 잔액으로 두면 양쪽이 모두 범위에 든다

⇒ 잔액 도출 자체는 허용되나 **잔액 쪽도 반드시 30% 검증 대상**이다. 자료 결론:
「반드시 토지와 건물을 **각각** ±30% 미만 범위 내인지 여부를 검토하여야 함」.

> 수학적으로 당연하다 — 차이 금액은 양쪽이 같은데(합이 총액으로 고정) 분모가 작은 쪽의 **비율이
> 항상 크다**. 그래서 **작은 파트가 실질 제약**이 되고, 큰 쪽만 보면 반드시 놓친다.

### 11.4 ⚠️ 안분 basis가 **감정가액**이다 — §3.3 갭의 격상

자료 예시의 안분 근거는 기준시가가 아니라 **감정가액(건물 6억, 토지 4억)**이다. 이것이 부가령
§64①1호 **단서**(「감정평가가액이 있는 경우에는 그 가액에 비례하여 안분」)의 적용례다.

⇒ §3.3에서 「별건 갭·현재 도달 불가」로 분류한 항목을 **격상**한다. 30% 판정의 기준선이 안분값이고
그 안분값이 감정가액으로 정해질 수 있으므로, **감정가액 입력 축이 없으면 30% 판정 자체가 틀린
기준선으로 이루어진다**. Phase 1 설계에 포함 여부를 판단해야 한다(§9 Q-5 재분류).

### 11.5 구현 주의 — 부동소수 **bound** 금지 (2026-08-06 실측 정정)

⚠️ 이 절의 초판은 「검증 스크립트에서 `6 × 1.3 = 7.800000000000001`이 나왔다」를 근거로 들었으나
그 값은 **억 단위** 스크립트의 것이었다. **원 단위로 다시 측정**하니 위험의 위치가 달랐다.

brute-force 실측 (원 단위 1천만~20억, 10만원 간격):

| 형태 | 부정확 건수 |
|---|---|
| `안분값 × 1.3` (상한) | **0건** |
| `안분값 × 0.7` (하한) | **3,821건** ← 여기가 위험하다 |
| `\|구분값−안분값\| / 안분값 >= 0.3` (비율) | **0건** — 정수식과 결과가 갈리지 않았다 |

⇒ 진짜 위험은 **하한 bound**다. 판정이 실제로 뒤집히는 입력이 존재한다:

```
안분 토지 167,800,000 · 구분 토지 117,460,000  (정확히 −30%)
  float 하한 = 167_800_000 * 0.7 = 117459999.99999999
  ⇒ `구분값 < 하한`이 false → bound 방식은 「적합」으로 **오판**
  ⇒ 정수식은 |117,460,000 − 167,800,000| × 100 = 5,034,000,000,000
              ≥ 167,800,000 × 30 = 5,034,000,000 × … → 발동(정답)
```

**곱셈만 쓰는 정수식**으로 구현한다(금액은 원 단위 정수 — CLAUDE.md 정수 연산):

```
발동 ⟺ |구분값 − 안분값| × 100 ≥ 안분값 × 30
```

비율 형태(`/ a >= 0.3`)도 실측상 정수식과 일치했으나, 정수식은 **증명 부담이 없어** 그쪽을 쓴다.
anchor U-3가 이 케이스를 고정하고, float bound로 바꾸면 4건이 실패한다(인과 검증 완료).

### 11.6 Phase 0 종결 · Phase 1 착수 가능

§8의 Phase 0(법령 확정)은 이로써 종결이다. 남은 판단은 **구현 정책**이다.

| # | 남은 판단 | 성격 |
|---|---|---|
| D | 30% 초과 시 차단 vs 안분으로 계산 진행 | 조문이 「불분명한 때로 **본다**」이므로 **계산 진행**이 문언에 맞다 |
| E | §166⑧ 예외 2사유를 사용자 체크로 받는 방식 | 자동 판정 불가 — 근거 입력 요구 + 결과 명시 |
| Q-5′ | 감정가액 안분 basis 지원 범위(§11.4) | Phase 1 포함 여부 판단 필요 |

---

## §12 Phase 1 상세 설계 — §100③ 가드 (2026-08-06 착수 결정)

### 12.1 확정된 정책

| # | 결정 | 근거 |
|---|---|---|
| **D** | 30% 초과 시 **차단하지 않고 안분으로 계산 진행** + 화면·신고서에 「30% 초과로 안분 적용」 명시 | 조문이 「불분명한 때로 **본다**」(의제)다. 차단하면 신고 자체가 불가능해진다 |
| **E** | §166⑧ 예외 2사유는 **사용자 체크 + 근거 입력**, 결과·신고서에 명시 | 자동 판정 불가 — 「다른 법령에 따라 구분」은 계약 근거, 「철거 후 토지만 사용」은 사실관계 |
| **Q-5′** | **감정가액 안분 basis를 Phase 1에 포함** | 30% 판정의 기준선이 안분값이므로, 감정가액이 있는데 기준시가로 안분하면 **틀린 기준선으로 판정**한다(§11.4) |

### 12.2 판정 함수 — 엔진에 두고 순수 함수로

```
lib/tax-engine/sale-split-deemed-unclear.ts (신규)

export interface DeemedUnclearInput {
  declared:    { land: number; building: number };  // 구분 기장 가액
  apportioned: { land: number; building: number };  // 안분 가액 (§12.3 basis로 산출)
  /** §166⑧ 예외 — 해당하면 30% 판정을 건너뛴다 */
  exemption?: "other_law" | "demolished_land_only";
}

export interface DeemedUnclearResult {
  /** 「불분명한 때로 본다」 발동 여부 */
  deemedUnclear: boolean;
  /** 실제 적용할 가액 — 발동 시 안분값, 아니면 구분값 */
  applied: { land: number; building: number };
  /** 표시용 — 파트별 이탈률(만분율 정수) · 어느 파트가 벗어났는지 */
  detail: {
    landDeviationBp: number;      // basis point (1% = 100)
    buildingDeviationBp: number;
    landOver: boolean;
    buildingOver: boolean;
    exemptionApplied?: "other_law" | "demolished_land_only";
  };
}
```

**정수 연산**(§11.5). 부동소수 비교를 쓰지 않는다:

```
over(declared, apportioned) ⟺ |declared − apportioned| × 100 ≥ apportioned × 30
deviationBp = floor(|declared − apportioned| × 10000 / apportioned)
```

`deemedUnclear = !exemption && (landOver || buildingOver)` — **양쪽 모두** 검사(§11.1 Q-1).

### 12.3 안분 basis 3단 서열 — 부가령 §64① (Q-5′)

| 호 | 조건 | basis |
|---|---|---|
| 1호 본문 | 토지·건물 기준시가가 **모두 있음** | **기준시가** 비율 |
| 1호 **단서** | 위 + **감정평가가액이 있음** | **감정평가가액** 비율 ← 우선 |
| 2호 본문 | 어느 하나/모두 기준시가 없음 + 감정평가가액 있음 | 감정평가가액 비율 |
| 2호 단서 | 감정평가가액도 없음 | 장부가액(없으면 취득가액) 비율 → 기준시가 있는 자산은 그 합계액을 다시 기준시가로 안분 |
| 3호 | 1·2호 적용 불가·곤란 | 국세청장이 정하는 바 |

⇒ **서열: 감정평가가액 > 기준시가 > 장부가액/취득가액 > 국세청장 고시.**

**Phase 1 지원 범위**: 1호(기준시가) + 1호 단서·2호 본문(감정평가가액)까지. 2호 단서(장부가액)·3호는
입력 축이 없고 도달 빈도가 낮아 **범위 밖**으로 두고 §9에 기록한다.

⚠️ **감정평가가액에는 시기 요건이 있다**. 부가령 §64①1호 괄호:

> 공급시기(중간지급조건부 또는 장기할부판매의 경우는 최초 공급시기)가 속하는 과세기간의 **직전
> 과세기간 개시일부터** 공급시기가 속하는 과세기간의 **종료일까지** 「감정평가 및 감정평가사에 관한
> 법률」에 따른 감정평가법인등이 평가한 감정평가가액

양도소득세에 준용할 때 「공급시기」를 **양도시기**로 읽는 것은 **유추**다(§166⑥이 부가령 §64①을
차용하는 구조상 자연스러우나 명문은 아니다). ⇒ **감정일자를 입력받아 이 창을 검증**하고, 창을
벗어난 감정은 basis로 쓰지 않는다. 유추임을 주석과 화면 안내에 남긴다.

선례: `partialApportionBasis`(`lib/stores/calc-wizard-asset-partial-area.ts:34`)가 이미
「기준시가 / 감정가액」 2택 축을 갖고 있다. **필드 구조와 UI 패턴을 그 선례에 맞춘다** — 다만 그
축은 취득 당시 값이고 엔진 미전송(UI 전용)인 반면, 이 축은 **양도 당시** 값이고 **엔진 전송**이다.

### 12.4 어디서 판정하나 — 엔진이 정본, validate는 같은 함수를 재사용

| 계층 | 역할 |
|---|---|
| **엔진** | 판정 + 적용 + `detail` 산출 → **정본** |
| validate | 같은 함수를 import해 **사전 안내**(차단 아님 — D). 안분값 산출이 필요하므로 엔진의 basis 함수도 공유 |
| UI | validate 결과를 표시 |

validate가 30% 판정을 **재구현하면 dual-truth**가 된다(메모리 `feedback_ui_engine_dual_truth_avoidance`).
안분값 산출도 엔진 함수(`calcSaleApportionRatio` 확장)를 그대로 쓴다.

### 12.5 표시 계층 (D)

| 위치 | 내용 |
|---|---|
| 결과 요약 | 30% 초과 시 배지 「토지·건물 구분가액 30% 초과 → 안분 적용(§100③)」 |
| 안분 명세 표 | 구분 기재값 · 안분값 · **파트별 이탈률** · 적용값 4열 |
| 신고서 | 적용값 기준(이미 그렇다). 다만 「구분 기재를 안분으로 의제」한 사실을 각주로 |
| 예외 적용 시 | 「§166⑧ ○호 예외로 구분가액 인정」 + 사용자 입력 근거 |

⚠️ **금액이 틀리는 것보다 표시가 틀리는 것이 더 오래 숨는다** — 이 세션에서 반복 확인한 패턴이다.
`detail`을 결과에 실어 표시가 엔진 판정을 **그대로 읽게** 한다(재계산 금지).

### 12.6 §166⑧ 예외 입력 (E)

```
saleSplitExemption: "" | "other_law" | "demolished_land_only"   // 신규 폼 필드
saleSplitExemptionNote: string                                   // 근거 (필수)
```

- `""`(미선택)이 기본 — 3-state 패턴(`partialAcqDistinct` 선례).
- 선택 시 **근거 입력을 필수**로 한다(빈 값이면 validate 차단). 남용 억제 + 신고서 각주 재료.
- 2호(철거 후 토지만 사용)는 건물 양도가액이 0에 가까운 것이 정상이므로, 선택 시 UI에서 그 취지를
  안내한다.

### 12.7 🔴 R-7 — 구분양도에 **양도시 기준시가가 필수**가 된다 (실측)

30% 판정은 안분값을 요구하고, 안분값은 양도시 기준시가(또는 감정가액)에서 나온다.
**그런데 현행은 구분양도 + 두 파트 실가 조합에서 기준시가 칸을 아예 열지 않는다** —
`saleStdPlacement`(`lib/calc/transfer-tax-split-acq-mode.ts:223-231`)가 구분양도에서는 **환산 파트만**
기준시가를 노출한다.

**fixture 실측**(블록 단위 스캔):

| 항목 | 건수 |
|---|---|
| 파트 양도가액 2필드를 가진 fixture | **47** |
| 그중 양도시 기준시가 2필드가 **없는** fixture | **26** (파일 19개) |

⇒ 기준시가를 필수화하면 최대 26개 fixture가 영향받는다. 그중 몇 건이 「구분양도 + 둘 다 실가」
조합인지는 Phase 1에서 확정한다(상한값이다).

**대안 검토 — 「기준시가 없으면 판정 건너뛰기」는 채택하지 않는다.** 사용자가 칸을 비워 두면 가드를
우회할 수 있어 **가드의 의미가 사라진다**. 우회 가능한 가드는 가드가 아니다.

⇒ `saleStdPlacement`·`needsSaleStdPart`를 **구분양도에서도 양쪽 요구**로 바꾸고, UI 노출과 validate
요구를 **같은 술어**로 유지한다(dead-end 금지 — 메모리 `feedback_ui_gate_removes_sole_input_path`).

### 12.8 R-1 재측정 — 세액 anchor 회귀는 사실상 없다

첫 스캔에서 13건으로 보였으나 **파일 단위 인덱스 매칭 오탐**이었다(S2 fixture의 양도가액과 S1
fixture의 기준시가를 짝지음). 블록 단위로 다시 세니:

| 항목 | 건수 |
|---|---|
| 4값이 **같은 블록**에 있는 fixture | 15 |
| 그중 30% 초과 | **1** — `__tests__/calc/transfer-tax-validate-split.test.ts` (토지 6억 / 건물 **9,999,999,999**) |

그 1건은 **overflow 가드 극단값 테스트**로 세액 anchor가 아니다.

⇒ **R-1은 작다. 진짜 비용은 R-7이다.** 첫 수치를 그대로 계획에 옮겼다면 규모를 4배 이상
부풀렸을 것이다 — 스캐너 결과도 검증 대상이다.

> 🔴 **1-C 실측 정정 (2026-08-06) — 스캐너는 반대 방향으로도 틀렸다.**
>
> 엔진 통합 후 전체 스위트를 돌리니 **스캐너가 찾지 못한 파일**이 걸렸다:
> `__tests__/calc/split-housing-separate-acq-part-std.test.ts` (테스트 3건). 4값이 한 블록에
> 있는데도 놓친 이유는 스캐너가 `baseTransferInput({...})` 형태를 전제했고 이 파일은 **plain
> object literal + `as TransferTaxInput`**이라서다.
>
> | | 스캐너 | 실측(전체 스위트) |
> |---|---|---|
> | 30% 초과 fixture | 1건 (`transfer-tax-validate-split.test.ts`) | **1파일 3테스트** (위 파일) |
> | 스캐너가 찾은 1건 | — | **깨지지 않았다** (validate 테스트 — ⑧ 배관은 1-E) |
>
> ⇒ 스캐너는 **과대(13→1)와 누락(0→1)을 동시에** 냈다. 최종 수치는 **전체 스위트 실행**으로만
> 확정할 것 — 정적 스캔은 착수 규모 감각에만 쓴다.

### 12.9 14 동기화 지점 (Phase 1 = split 경로)

| # | 지점 | 작업 |
|---|---|---|
| ① 폼 | `saleSplitExemption`·`saleSplitExemptionNote` · 감정가액 basis 필드(감정가액 2 + 감정일자) | **신규** |
| ② initial | 위 필드 기본값(`""`) | **신규** |
| ③ normalize | 구 세션 복원 방어 | **신규** |
| ④ API 변환 | `transfer-tax-api-split.ts` — 신규 필드 전송 | **신규** |
| ⑤ UI | 구분양도 섹션에 예외 체크 + 근거 · 감정가액 basis 선택 · **양도시 기준시가 상시 노출**(R-7) | **신규** |
| ⑥ 사이드바 | 양도가액 합계는 **적용값** 기준 | **확인 필요** |
| ⑦ 결과 카드 | §12.5 표시 4항목 | **신규** |
| ⑧ validate | 예외 선택 시 근거 필수 · 기준시가 필수화(R-7) · 30% 사전 안내 | **신규** |
| ⑨⑩ Zod enum | 예외 사유 · basis enum | **신규** |
| ⑫ Zod 입력 | 신규 필드 5개 | **신규** |
| ⑬ body spread | 전송 객체 | **신규** |
| ⑭ Route 매핑 | split 경로는 `buildTransferEngineInput` 경유 — 명시 나열 여부 확인 | **확인 필요** |

### 12.10 anchor 계획

| ID | 내용 | 값 |
|---|---|---|
| **U-1** | 자료 사례 4건 재현 | §11.2 표 그대로 원단위 고정 |
| **U-2** | 경계 — 정확히 ±30%는 **부적합** | 안분 6억 → 7.8억·4.2억 둘 다 발동 |
| **U-3** | 정수 연산 — 부동소수로는 갈리는 입력이 결정적으로 판정된다 | `6억 × 1.3` 케이스 |
| **U-4** | 한쪽만 벗어나도 발동(양쪽 검사) | 사례1·2 |
| **U-5** | §166⑧ 예외 시 발동하지 않음 | 사례1 + 예외 |
| ~~U-6~~ → **B-1** | basis 서열 — 감정가액이 있으면 기준시가를 쓰지 않는다 | ✅ 1-B 완료. 두 basis가 갈리는 fixture(토지 4억 vs 6억) |
| ~~U-7~~ → **B-2·B-3** | 감정 시기 요건 이탈 시 감정가액을 basis로 쓰지 않는다 | ✅ 1-B 완료. 창 = [(양도연도−1)-01-01, 양도연도-12-31] |
| **B-8** | 창 산출은 **UTC 기준** — 로컬 연도와 섞으면 KST에서 1년 밀린다 | ✅ 1-B 완료 (신설) |

> ⚠️ **anchor ID 충돌**: 1-A anchor 파일이 U-6을 「방어적 입력」에 먼저 썼다. 1-B는 **B-** 접두어를
> 쓰고 위 표에 대응을 남긴다.
| **U-8** | 발동 시 세액이 안분 결과와 **일치**한다 | §3.2 probe의 228,195,000 |
| **U-9** | 표시 `detail`이 엔진 판정을 그대로 읽는다(재계산 없음) | 이탈률 bp 일치 |

### 12.11 Phase 1 세부 단계

```
1-A  판정 함수 + U-1~U-5 anchor (Pre-Do 실패 확인)        → verify: 사례 4건 재현     ✅
1-B  안분 basis 서열 확장(감정가액) + U-6·U-7             → verify: basis 분기        ✅
1-C  엔진 통합(split) + U-8                                → verify: 발동 시 안분 일치 ✅
1-D  R-7 — 기준시가 필수화 + fixture 정리(상한 26건)       → verify: 전체 회귀     ✅ §14
1-E  ④⑧⑨⑩⑫⑬ 배관 + ⑤ UI + ⑦ 표시 + U-9                → verify: 14지점 자가점검 ✅ §15
1-F  E2E 1건 (구분양도 → 30% 초과 → 안분 표시)             → verify: 실플로우      ✅ §15.7
```

> ✅ **Phase 1 종결**(2026-08-06). Phase 2(일반건물 `saleSplitMode` 축 도입 — §5 매트릭스
> S-1~S-12 · §6 14지점)와 Phase 3(토글 E2E)은 착수 전이다.

**1-D가 가장 무겁다.** 1-A~1-C는 순수 함수·엔진이라 회귀가 좁고, 1-D에서 기존 fixture가 걸린다.
1-D 착수 전에 「구분양도 + 둘 다 실가」 조합의 정확한 건수를 먼저 센다.

### 12.12 이 문서로 답이 안 되는 것 (Phase 1 착수 후 판단)

| # | 항목 | 상태 (2026-08-06 조사 §19) |
|---|---|---|
| Q-8 | 부가령 §64①**2호 단서**(장부가액 → 재안분)·**3호**(국세청장 고시) 지원 여부 | ✅ **범위 밖 유지 확정** — 일반건물은 기준시가 2필드가 필수라 1호 경로가 전제된다(§19.4) |
| Q-9 | 「공급시기 → 양도시기」 유추의 타당성 | 🟢 **현행 유지** — 대법원 판례(법제처 ID `618857`)가 「안분계산하는 **방법**을 준용한다는 의미」라고 판시. ⚠️ 본문 미확보라 확정은 보류(§19.2) |
| Q-10 | 다필지·증축 등 **3파트 이상** 자산에서 30% 판정 단위 | ✅ **종결 — 차단 유지**. Q-4와 같은 결론이다(§19.1) |

---

## §13 Phase 1-C 완료 보고 — 엔진 통합 (2026-08-06)

### 13.1 배선 지점 — 「모드 플래그」가 아니라 「값의 유무」다

§3.4에서 확인한 사실을 그대로 따랐다. `saleSplitMode`는 **엔진이 읽지 않는다** — 실제 스위치는
`landTransferPrice`/`buildingTransferPrice`의 **유무**다. ⇒ 플래그 분기를 새로 만들지 않고
**값이 있으면 판정**하는 계약으로 배선했다(「죽은 모드」 재발 방지).

| 파일 | 역할 |
|---|---|
| `lib/tax-engine/transfer-tax-split-sale-price.ts` (신규 124줄) | 양도가액 축 **단일 정본** — basis 해석 + §100③ 판정 |
| `lib/tax-engine/transfer-tax-split-gain.ts` | `resolveTransferPriceSplit(input)` 1회 호출로 교체 (773 → 769줄) |
| `lib/tax-engine/types/transfer-split-gain.types.ts` | `SaleSplitJudgmentDetail` + `SplitGainResult.saleSplitJudgment?` |
| `lib/tax-engine/types/transfer.types.ts` | `TransferTaxInput.saleSplitExemption?` (엔진 계약만 — 배관은 1-E) |

**`calcSaleApportionRatio`를 제거**했다. 양도가액 안분이 이제 `resolveSaleApportionBasis`
단일 정본이며, 그쪽은 **비율이 아니라 금액 쌍**을 낸다 — §100③이 「안분계산한 **가액**」과의 차이를
보므로 비교 대상이 금액이어야 하고, 부가령 §64①1호 단서(감정 우선) 서열도 함께 담긴다. 비율 함수를
남기면 dual-truth가 된다. 옛 이름을 가리키던 주석 6곳을 새 이름으로 갱신했다.

### 13.2 🔴 1-B 정정 — basis 종류에 따라 usability 기준이 다르다

1-B의 `usableAsBasis`는 두 basis 모두 「양쪽 다 양수」를 요구했다. 엔진 통합에서 **기존 fixture
1건이 차단**됐다 — `pre-housing-disclosure.test.ts` D-11-1이 **양도시 건물 기준시가 0**으로
「토지 100% 안분」을 만들고 있었다.

두 값은 성질이 다르다:

| basis | 0의 의미 | 판정 |
|---|---|---|
| **기준시가** | 고시·산정값이라 **0도 값일 수 있다**(철거 예정 건물 등) | 합계 > 0이면 채택 |
| **감정평가가액** | 「감정평가법인등이 **평가한** 가액」 ⇒ 0 = **미평가** | 양쪽 > 0이어야 채택 |

부가령 §64①1호는 「기준시가가 **모두 있는 경우**」라 하지만, **0으로 입력된 것**을 「없는 것」으로 볼
근거(예규·심판례)를 확인하지 못했다. ⇒ 종전 동작을 **보존**한다 — 법령 문언 해석만으로 세액을
바꾸지 않는다(메모리 `feedback_unverified_authority_blocks_tax_change`). anchor **B-9** 신설.

> ⚠️ 이 비대칭은 **의도된 것**이다. 「일관성」을 이유로 한쪽에 맞추면 D-11-1이 다시 깨지거나
> (기준시가를 엄격하게) 감정 미입력이 basis로 채택된다(감정을 느슨하게).

### 13.3 U-8 실측 — 세액이 안분값으로 되돌아온다

§3.2 probe 재현값과 가드 도입 후 값:

| 신고 방식 | 토지 / 건물 | 도입 전 | 도입 후 |
|---|---|---|---|
| 일괄(안분) | 9억 / 6억 | 228,195,000 | 228,195,000 |
| 구분 · 범위 안 | 10억 / 5억 (11.1% · 16.7%) | 226,347,000 | 226,347,000 |
| 구분 · 토지 몰아주기 | 14억 / 1억 | 199,551,000 | **228,195,000** |
| 구분 · 건물 몰아주기 | 1억 / 14억 | 218,031,000 | **228,195,000** |
| 구분 · 건물 정확히 −30% | 10.8억 / 4.2억 | 224,868,600 | **228,195,000** |
| 구분 · 1원 안쪽 | 1,079,999,999 / 420,000,001 | 224,868,600 | 224,868,600 |
| 구분 · §166⑧ 예외 | 14억 / 1억 + `other_law` | 199,551,000 | 199,551,000 |

🔴 **경계 fixture 2건은 도입 전 세액이 같다**(224,868,600 — 과세표준 절사가 1원을 흡수한다).
도입 후 **갈라지는 것**이 경계가 정확히 30%에서 물린다는 증거다. anchor 16건.

### 13.4 인과성 검사 (mutation probe)

| 무엇을 깨뜨렸나 | anchor 실패 |
|---|---|
| 가드 제거 (구분값 항상 사용) | **9건** |
| 경계를 개구간으로 (`>=` → `>`) | 1건 |
| §166⑧ 예외 무시 | 3건 |
| 토지만 판정 (건물 미검사) | 1건 |
| 잔액 도출분을 판정에서 제외 | 2건 |

### 13.5 기존 fixture 회귀 — **1파일 3테스트**

`__tests__/calc/split-housing-separate-acq-part-std.test.ts`의 구분 기재(토지 6억 / 건물 4억)가
같은 fixture의 양도시 기준시가 비율(3억 : 1억 → 안분 7.5억 / 2.5억)에서 **건물 60% 벗어나** 있었다.
§100③ 하에서는 애초에 성립하지 않는 신고다.

그 anchor의 주제는 **취득시 기준시가 축**이므로 양도가액을 안분값(7.5억 / 2.5억)과 일치시켜 두 축의
간섭을 없앴다. 기대값은 건물 환산 480,000,000 → **300,000,000**(파트 독립) · 1,200,000,000 →
**750,000,000**(역산 경로)로 갱신했다 — **2.5배 대비는 그대로**라 역산 회귀 판별력은 보존된다
(H3 mutation probe로 확인).

### 13.6 게이트

`npx tsc --noEmit` **0** · `npm run lint` **0 errors** · `npm test` **13,891 passed**
(13 skipped · 1 todo · 1,244 파일).

### 13.7 1-D로 넘기는 것

- **⏳ U-8-7 한시 계약**: 안분 basis(양도시 기준시가·감정가액)가 없으면 **판정하지 않는다**.
  §12.7이 「기준시가 없으면 건너뛰기」를 영구 설계로 **기각**했으므로, 1-D에서 이 anchor의 계약을
  **「차단」으로 뒤집는다**(describe를 지우지 말 것).
- `saleSplitExemption`은 엔진 계약만 있고 **폼·API·validate 배관이 없다**(1-E ①②③④⑧⑨⑫⑬).
- `transfer.types.ts`가 **821줄**로 800을 넘었다. 다만 **로직 없는 타입 전용 파일**이라 루트
  File Size Policy의 예외 항목이다(내 변경 이전에도 807줄로 이미 초과). 1-E에서 이 파일을 또 열게
  되므로 그때 분리를 판단한다.

---

## §14 Phase 1-D 완료 보고 — 양도시 기준시가는 「항상 축 A」 (2026-08-06)

커밋 `4b00f4a7` — 31파일 `+471/−313`.

### 14.1 배치가 **불변**이 됐다 — 판정 인자가 결과를 가르지 않는다

§12.7(R-7)이 요구한 「구분양도에서도 양쪽 요구」를 적용하니, `saleStdPlacement`의 판정 인자
(`saleSplitMode`·파트 모드·`selfOwns`)가 **전부 결과를 가르지 않게** 됐다. ⇒ 인자를 제거했다.

| 대상 | 종전 | 1-D |
|---|---|---|
| `saleStdPlacement(ctx)` | 모드·파트·소유 3축으로 축 A / 파트 섹션 분기 | `saleStdPlacement()` — 항상 `{saleAxis:true, landPart:false, buildingPart:false}` |
| `needsSaleStdPart(part, ctx)` | 위 판정 소비 | `needsSaleStdPart(part)` — 파생 형태(`saleAxis || 파트`)는 유지 |
| 엔진 `resolveTransferPriceSplit` | basis 없으면 `return declared`(한시) | **`TaxCalculationError` throw** |
| 건물 기준시가 런처 | 2시점 통합 버튼 1개 | 양도시 / 취득시 **분리 버튼** |

**상수를 함수로 남긴 이유**는 두 가지다 — ① UI 노출과 validate 요구가 **같은 값 하나**에서
나오는 구조를 유지해 조건이 되살아날 때(Q-10: 3파트 이상 자산의 판정 단위) 한 곳만 고친다.
② `landPart`/`buildingPart`를 반환 형태에 남겨 파트 섹션 경로를 보존한다.

### 14.2 U-8-7 — 한시 계약을 **차단으로 뒤집었다**

§13.7이 예고한 대로 describe를 지우지 않고 계약만 반전했다. 1-C에서는 basis가 없으면 판정을
건너뛰어 세액 199,551,000이 그대로 나왔으나, 그러면 **칸을 비워 두는 것만으로 가드를 우회**할 수
있다. 1-D가 칸을 상시 노출·필수로 만들었으므로 이제 차단이 정답이다.

anchor 3건으로 재작성: ① 양쪽 미입력 → 차단 · ② **한쪽만 입력해도 차단**(비율은 양쪽이 있어야
성립한다) · ③ 일괄양도는 **종전 메시지**로 차단(별개 경로임을 고정).

> ⚠️ 정상 경로에서는 validate(`needsSaleStdPart`)가 먼저 막는다. 엔진 throw는 validate를 우회한
> 경로(직접 API 호출 등)에 대한 **backstop**이다.

### 14.3 테스트 조합 공간의 **소멸** — 커버리지 축소가 아니다

`__tests__` diff 실측: `it` **추가 31 / 삭제 31**(순증감 0) · `describe` **추가 6 / 삭제 7**(−1).

삭제된 7개 중 6개는 이름만 바뀐 대응물이 있다. 대응물이 없는 1개는
**「소유 여부 — 비소유 파트의 양도차익은 폐기되므로 분모도 불요」**이고, A8의 **전수 96조합**
루프도 함께 사라졌다(`split-sale-std-placement.test.ts` `−91/+33`).

⇒ 이는 커버리지를 줄인 것이 아니라 **함수가 인자를 받지 않아 그 조합을 만들 수 없게 된 것**이다
(타입 레벨 소멸). 대신 A8은 「항상 축 A」와 불변식 `saleAxis && (landPart || buildingPart)` 부재를
직접 단언한다.

### 14.4 게이트 (2026-08-06 14:03 실측)

`npx tsc --noEmit` **0** · `npm run lint` **0 errors**(286 warnings — 기존치) ·
`npm test` **1,244파일 13,891 passed**(13 skipped · 1 todo · 189초).

1-C 보고(§13.6)와 **같은 13,891건**이다 — 위 순증감 0과 일치한다.

### 14.5 🔴 후속 — **도달하지 않는 경로가 생겼다**

`landPart`/`buildingPart`가 항상 `false`이므로 아래는 렌더되지 않는다:

| 위치 | 상태 |
|---|---|
| `LandBuildingSplitSection.tsx:440` · `:509` 분기 | 조건 상수 false |
| `TransferLandStdPartCard` · `TransferBuildingStdPartCard` (`TransferStdPriceCards.tsx`) | 도달 불가 |

**제거하지 않았다.** 기능 영향이 0이고, Q-10(3파트 이상 자산의 판정 단위)이 파트별 배치를 되살릴
수 있다. 제거 여부는 **Q-10 판단과 함께** 결정한다 — 그전에 지우면 되살릴 때 다시 쓰게 된다.

> 🔴 **1-D는 vitest만 반전하고 E2E를 놓쳤다 — CI가 PR #1101부터 빨간불이었다**(2026-08-06 사후 확인).
>
> `e2e/split-mode-gating.spec.ts` **5건**이 구 배치(파트 섹션 분산)를 그대로 단언해 E2E 샤드 3/4가
> 실패했다. 1-D 커밋(`4b00f4a7`)은 `e2e/`를 **한 줄도 건드리지 않았다**.
>
> | | 1-D가 갱신했나 | 결과 |
> |---|---|---|
> | vitest 컴포넌트 테스트 | ✅ (`split-std-price-colocation.test.tsx` 등) | 통과 |
> | E2E spec | ❌ | **PR #1101·#1102 연속 빨간불** |
>
> ⚠️ **계약을 반전할 때 계층별 테스트를 전수로 세라.** vitest가 초록이어도 E2E는 별개 게이트이고,
> **E2E는 pre-push에 없어 CI에서만 드러난다**(루트 CLAUDE.md). master에 브랜치 보호가 없어
> 빨간불인 채로 머지가 계속됐다.
>
> ⇒ 1-E 후속으로 5건을 **삭제가 아니라 새 계약으로 반전**했다(U-8-7·A8과 같은 방식). 40건 전건 통과.

### 14.6 1-E로 넘기는 것 (grep 실측)

- **`saleSplitExemption` 배관이 전무하다.** 엔진 2파일(`transfer.types.ts` ·
  `transfer-tax-split-sale-price.ts`)에만 있고 폼·`transfer-tax-api-split.ts` ·
  `route.ts` · validate는 **0건**이다. §12.9의 ①②③④⑧⑨⑩⑫⑬가 통째로 남았다.
- `saleSplitExemptionNote`(근거)는 **아직 어느 계층에도 없다**(§12.6).
- **감정평가가액 basis 축**(가액 2필드 + 감정일자)도 폼·API에 없다. 엔진 서열 판정은 1-B에서
  완성됐고 `resolveBasis`가 **인자 형태로 자리를 비워 두었다**
  (`transfer-tax-split-sale-price.ts:40-42`) — 배관만 붙이면 된다.
- ⑦ 표시 4항목(§12.5) + **U-9** anchor.
- `transfer.types.ts` **821줄** 분리 판단(§13.7에서 이월).

---

## §15 Phase 1-E 완료 보고 — 배관·입력·표시 (2026-08-06)

### 15.1 무엇을 채웠나

1-B가 만든 서열 판정과 1-C가 만든 엔진 계약은 **값이 도달하지 않아** 실제로는 동작하지 않았다.
1-E는 그 사이를 잇는다.

| 계층 | 내용 |
|---|---|
| 엔진 | `TransferTaxInput`에 감정 3필드 · `resolveBasis` 배관 · `SaleSplitJudgmentDetail.appraisalRejected` |
| ④ | 감정 3필드는 **일괄·구분 양쪽** 전송 · 예외는 구분양도 게이트 · 근거는 **미전송**(UI 전용) |
| ⑧ | V8 감정 3필드 all-or-nothing · V9 예외 선택 시 근거 필수 |
| ⑫⑭ | Zod 4필드 + Route 매핑(**감정일자 `Date` 변환**) |
| ⑤ | 감정 카드(모드 무관) · 예외 카드(구분양도 전용) — 순서는 엔진 로직 순서 그대로 |
| ⑦ | 판정 블록 — 구분값·안분값·이탈률·적용값 4행 + basis 종류 + 배제/예외 사유 |

**Q-7 종결**: 엔진은 `saleSplitMode`를 **읽지 않는다**(타입 선언 1곳뿐). `transfer-tax-api-split.ts`
주석이 반대로 적혀 있어 정정했다 — 그 결과 양도가액 전송 게이트의 성격이 바뀐다. "사용자 의도
보호"가 아니라 **유일한 방어선**이다(잔존 입력값이 통과하면 엔진이 구분 기재로 보고 판정까지 태운다).

### 15.2 ⑥ 사이드바는 **손대지 않았다** (§12.9의 「확인 필요」 해소)

`computeTransferSummary`의 양도가액 합계는 **자산 총액(`actualSalePrice`)의 합**이고 토지·건물로
나누지 않는다(`calc-wizard-store.ts:528-533`). §100③은 총액을 **나누는 방식**만 바꾸므로 합은
불변이다 ⇒ 변경 불필요.

### 15.3 근거 텍스트를 엔진에 보내지 않은 이유

`saleSplitExemptionNote`는 계산에 쓰이지 않는 서술 텍스트다. 엔진 입력에 넣으면 Zod·Route·엔진
타입 3곳이 계산과 무관한 문자열을 나르게 된다 ⇒ **UI 전용**으로 두었다(`addressDong`·`addressHo`
선례). 화면 표시는 예외 **사유(호)**까지만 하고, 사용자가 적은 근거 문구는 **신고서 각주**와 함께
후속으로 다룬다(§15.6).

### 15.4 게이트 (2026-08-06 14:57 실측)

`npx tsc --noEmit` **0** · `npm run lint` **0 errors**(286 warnings — 기존치) ·
`npm test` **1,250파일 14,007 passed**(13 skipped · 1 todo · 173초).

신규 anchor **43건**: E-1 감정 배관 인과성 9 · E-2 5단 배관 14 · ⑤ 입력 위젯 11 · U-9 표시 9.

### 15.5 🔬 이번에 실측으로 확인한 함정 3종

| 함정 | 실측 |
|---|---|
| **⑫⑭ 짝지어 누락** | ⑫만 빼면 tsc가 잡지만(⑭가 읽으므로), **둘 다 빼면 프로덕션 코드에서는 침묵**한다. 그리고 신규 필드는 그 둘이 같은 작업 단위라 **짝지어 빠진다** |
| **`data-testid` 침묵 소실** | `ToggleCard`·`ToneCard` 모두 DOM으로 흘리지 않는다. TS는 `data-*`를 JSX 특례로 통과시켜 **타입만 보고 붙이면 조용히 사라진다** ⇒ 감싸는 div에 붙인다 |
| **표시 반올림이 부동소수에 걸린다** | `(5555/100).toFixed(1)`이 `"55.5"`(내부값 55.549999…). 세액엔 영향 없지만 **반올림이 우연에 좌우**된다 ⇒ 십분율 정수에서 반올림 |

> Base UI `Switch.Root`는 `role`도 노출하지 않는다 — 토글 조작은 `getByLabelText(title)`
> (선례 `redev-exemption-toggle-tri-state.anchor.test.tsx:54`).

### 15.6 Phase 1-F 완료 — E2E 실플로우 (2026-08-06)

`e2e/sale-split-30pct-deemed-unclear.spec.ts` **2건 통과**(4.2초).

각 계층 anchor는 계층을 따로 검증하지만, 이 spec은 **폼에 값이 있을 때 판정이 화면까지
도달하는가**를 한 번에 통과시킨다 — 어느 계층이 끊겨도 여기서 드러난다.

- 토지 몰아주기(14억/1억) → 발동 + **적용 가액이 안분값**(9억/6억) + basis 표시
- 적정범위(10억/5억) → **발동하지 않고** 구분 기재 가액 유지

> 대조군을 함께 두는 이유는 「항상 뜨는 배너」가 아니라 **판정 결과**임을 증명하기 위해서다.
> 발동 케이스만 있으면 블록을 무조건 렌더해도 통과한다.

**시드 방식**: 마법사를 클릭으로 완주하지 않고 `sessionStorage`에 폼을 심고 마지막 단계로
점프한다(`general-building-97-2-swap.spec.ts` 선례). 입력 경로 자체는 ⑤ anchor가 이미 잡으므로
여기서 되풀이하면 느리고 깨지기 쉬운 spec이 된다.

### 15.8 후속으로 넘기는 것

- ~~**신고서 각주 + 근거 문구 표시**(§12.5 3·4항목의 나머지)~~ → ✅ **완료**(§20)
- `transfer.types.ts` **839줄**(1-E에서 +18) — 여전히 **타입 전용 파일**이라 File Size Policy
  예외 항목이다. 로직이 들어가면 그때 분리한다.
- §14.5의 도달 불가 경로(파트 std 카드)는 **그대로 둔다** — Q-10 판단과 함께.

---

## §16 Phase 2 설계 보강 — 실측 (2026-08-06 착수)

### 16.1 🟢 엔진 구조가 유리하다 — **안분 소비 지점이 하나다**

§3.5는 「안분 소비처가 양도가·취득가·필요경비·환산 분모 **모두**」라 경계 혼재(R-3)를 우려했다.
**실측 결과 그 우려는 구조적으로 해소된다** — `allocation`(Step 1 결과)의 소비처 전수:

| `general-building-valuation.ts` | 소비 |
|---|---|
| `:193` | `allocateBundledTransferPrice(input)` — **유일한 생성 지점** |
| `:199` | `calculateConvertedAcquisition(input, allocation)` — 환산취득가 **분자** |
| `:281` · `:343` · `:371` | 자산 카드 `transferPrice` (토지·토지분할·건물) |
| `:297` · `:323` | NBL 사업용/비사업용 **면적비 분할**(S-9) |
| `:452` · `:464` | 단위 테스트용 gain 헬퍼 |

⇒ **`allocateBundledTransferPrice` 한 곳만 구분값을 내면** 양도가액·환산 분자·NBL 분할이 전부
따라온다. 개산공제(`calculateEstimatedDeduction`)는 `input`만 받으므로 **영향이 없다**.

🔴 **환산 분모는 자동으로 안전하다** — `calculateConvertedAcquisition`에서 `allocation`은
**분자에만** 등장한다(`general-building-converted-acquisition.ts:68`·`:74`). 분모
(`landStdTotalForValuation`)는 `input`의 기준시가에서 만들어진다(`:44-49`).

⇒ S-12의 「양도가액은 구분값, 환산 분모는 기준시가 비율 유지」가 **코드 변경 없이 성립**한다.
환산취득가 = **구분 양도가액** × (취득시 기준시가 ÷ 양도시 기준시가)이 되며, 이것이 법령상
맞는 형태다 — 구분 기재가 인정되면 그 파트의 「양도 당시 실지거래가액」이 곧 구분값이다.

### 16.2 산식이 이미 같다 — 통일해서 dual-truth를 없앤다

| | 산식 |
|---|---|
| `allocateBundledTransferPrice` (`:102-106`) | `floor(safeMultiplyThenDivide(총액, 토지std, 합계std))` · 건물 = 총액 − 토지 |
| `resolveSaleApportionBasis`의 `apportion` | **완전히 동일** |

⇒ 일반건물 안분을 1-B가 만든 `resolveSaleApportionBasis` 호출로 교체한다. 값이 바뀌지 않아야
하며(회귀 0), 그 대가로 **부가령 §64① basis 서열(감정 > 기준시가)이 일반건물에도 따라온다**.

### 16.3 차단 대상

| 케이스 | 근거 | 지점 |
|---|---|---|
| **S-10 증축**(R-4) | 건물 구분값을 본체·증축에 배분할 근거 없음(Q-4 미확정) | `:181-183` `extensionInfo` 분기 — 3-way 경로에 구분값이 오면 차단 |
| **S-11 부담부증여** | §159가 채무비율로 자동 산정 | ⚠️ **일반건물 × 부담부증여 조합 가능 여부 미실측** — 구현 시 확인 |

### 16.4 감정평가가액 basis — 엔진은 지원, UI는 범위 밖

16.2의 교체로 엔진은 감정가액을 받을 수 있게 된다. 다만 §9 Q-5가 지적한 대로 **일반건물에는
입력 축이 없다** ⇒ Phase 2는 **엔진 지원까지만** 하고 UI는 넣지 않는다. 1-E에서 만든
`SaleAppraisalBasisCard`를 재사용하면 되므로 후속 비용은 작다.

### 16.5 R-6 파일 크기 재측정 (2026-08-06)

| 파일 | 줄 |
|---|---|
| `lib/api/transfer-tax-building-schemas.ts` | **387** |
| `lib/calc/transfer-tax-validate-gb.ts` | **376** |
| `app/api/calc/transfer/general-building-route-helper.ts` | **326** |
| `lib/calc/transfer-tax-api-gb.ts` | **316** |

전부 800 미만이고 400 이하라 **Phase 2 분량(각 +30~60줄)으로는 트리거에 닿지 않는다**.

### 16.6 단계

```
2-A  §5 매트릭스 갱신 + 이 설계 보강                      → verify: 낡은 전제 제거   ✅
2-B  Pre-Do anchor (일반건물 구분양도 → 30% 판정)         → verify: 현행에서 실패
2-C  엔진 — 입력 필드 + allocate 교체 + 증축 차단          → verify: 회귀 0 + 판정 발동
2-D  배관 ④⑧⑨⑫⑬⑭                                       → verify: 5단 전 구간 anchor
2-E  UI ⑤ + 결과 ⑦                                        → verify: 컴포넌트 anchor
2-F  E2E + 전체 회귀                                       → verify: 실플로우
```

---

## §17 Phase 2 완료 보고 (2026-08-06)

### 17.1 결과

일반건물이 **구분양도 축 + §100③ 30% 의제**를 갖췄다. §5 매트릭스 S-1~S-9·S-12는 동작하고,
S-10(증축)·S-11(부담부증여)은 **차단**으로 확정했다(Q-4 미확정 · §159 자동 산정).

`allocateBundledTransferPrice`를 Phase 1 함수 조합(`resolveSaleApportionBasis` +
`judgeDeemedUnclearSplit`)으로 교체했다. 산식이 종전과 동일해 **회귀 0**이고, 부가령 §64①
basis 서열이 함께 따라왔다(감정가액 입력 축은 UI가 없어 미도달 — §16.4).

### 17.2 🔴 E2E가 **표시 no-op**을 잡았다

⑦ 판정 블록을 `GeneralBuildingValuationDetailCard`에 붙였는데, **그 카드는 일반건물 화면에
뜨지 않는다**. 일반건물은 자산 카드 2장을 만들어 **aggregate 경로**로 흐르므로 실화면이
`MultiTransferTaxResultView` → `BundledAllocationCard`다. 그 카드는 `TransferTaxResultView`
(단건 뷰) 전용이었다.

| | 붙인 곳 | 실제 화면 |
|---|---|---|
| 처음 | `GeneralBuildingValuationDetailCard` | ❌ 렌더되지 않음 |
| 정정 | `BundledAllocationCard`(안분 카드 아래) | ✅ |

⚠️ **컴포넌트 anchor는 이것을 잡지 못한다** — 컴포넌트를 직접 렌더해 검증하므로 「그 컴포넌트가
실제로 화면에 오르는가」는 범위 밖이다. 계산은 맞는데 화면에 안 보이는 상태를 **E2E만** 드러냈다
(메모리 `feedback_api_trigger_without_input_path_is_noop`와 같은 계열 — 트리거만 열고 경로가 없는 것).

> `GeneralBuildingValuationDetailCard`에 넣은 산식 분기(구분 기재 시 안분식 대신 구분값 표시)는
> **그대로 두었다** — 그 카드가 뜨는 경로가 따로 있다면 올바르게 동작하고, 없다면 무해하다.
> 도달 여부 확정은 별건이다.

### 17.3 🔴 validate가 **엉뚱한 필드**를 볼 뻔했다

처음에 「토지+건물 합 = 총액」 검증을 `transfer-tax-validate-gb.ts`에 넣고 총액을
`asset.actualSalePrice`로 읽었다. **단건 일반건물의 총 양도가액은 폼-전역
`contractTotalPrice`다**(`transfer-tax-api.ts:232-238`) — `actualSalePrice`는 일괄 모드의
자산별 가액이라 단건에서는 비어 있다.

⇒ 검증을 **엔진으로 옮겼다**. `allocateBundledTransferPrice`는 `input.totalTransferPrice`를
확실히 알고, validate는 자산 하나만 받아 총액을 알 방법이 없다.

### 17.4 게이트 (2026-08-06 16:40 실측)

`npx tsc --noEmit` **0** · `npm run lint` **0 errors** ·
`npm test` **1,254파일 14,048 passed**(13 skipped · 1 todo · 186초) ·
`npx playwright test e2e/gb-sale-split-30pct.spec.ts` **3 passed**.

신규 anchor **46건**: 엔진 G-1~G-6 **13** · 배관 ④⑧⑫ **16** · ⑤ 위젯 **7** · 표시 재사용 **9**(1-E) ·
E2E **3**. Pre-Do로 먼저 작성해 4건 실패를 확인한 뒤 구현했고, 판정 분기를 무력화하는
mutation probe로 인과성을 확인했다.

### 17.5 이번에 확인한 함정

- **거짓 통과**: 증축 차단 테스트가 `toContain("증축")`이었는데 필수 필드 미입력 시 뜨는
  「증축일을 입력하세요」에도 매칭돼 새 규칙에 도달하지 않고도 통과했다. 필수 필드를 채우고
  단언을 `toContain("구분 기재할 수 없습니다")`로 좁혔다.
- **고아 import**: 안분을 공용 함수로 옮기면서 `safeMultiplyThenDivide`가 미사용이 됐다
  (lint warning 286 → 287). 내 변경이 만든 것만 제거했다.

### 17.6 남은 것

- **Phase 3** — 구분/일괄 토글 E2E는 2-F가 실질적으로 덮었다. 남은 것은 **UI 조작 경로**
  (라디오 클릭 → 칸 등장 → 계산)로, ⑤ 컴포넌트 anchor 7건이 조작을 이미 검증한다.
- ~~일반건물 **감정평가가액 입력 축**(§16.4)~~ → ✅ **완료**(§18)
- Q-4(증축 배분 근거)가 확정되면 S-10 차단 해제 — 그때 `buildGeneralBuildingAssetCardsWithExtension`
  3-way 경로에 판정을 배선한다.

---

## §18 일반건물 감정평가가액 basis 축 (2026-08-06)

§16.4에서 「엔진 지원까지만, UI는 범위 밖」으로 남겼던 축을 **입력부터 결과까지** 연결했다.

### 18.1 무엇을 붙였나

| 지점 | 내용 |
|---|---|
| 엔진 | `GeneralBuildingInput` 감정 3필드 + `resolveSaleApportionBasis`에 `appraisal` 전달 |
| ④ | `saleAppraisalFields` — **모드 게이트 없음**(일괄의 basis이자 구분의 비교 대상) |
| ⑧ | 3필드 all-or-nothing — **구분양도 블록 밖**에 둔다(일괄에서도 검사해야 한다) |
| ⑫ | Zod 3필드 (`nonnegative` — 0은 「미평가」라는 뜻이라 배제 사유로 기록돼야 한다) |
| ⑭ | **감정일자 `Date` 변환** — `coercedGbRaw`에 추가 |
| ⑤ | 1-E `SaleAppraisalBasisCard` **그대로 재사용** — 폼 필드가 같아 신규 위젯이 없다 |

### 18.2 🔬 ⑭ Date 변환은 vitest가 닿지 않는다 — E2E가 유일한 감시다

배관 anchor는 Zod parse 결과를 엔진에 **직접** 넣으므로 route helper를 거치지 않는다. 감정일자는
JSON을 지나며 문자열이 되는데, `Date`로 바꾸지 않으면 엔진의 창 비교(`getTime()`)가 런타임에
깨진다 — **타입도 통과하고 vitest도 통과한다**.

mutation probe로 확인했다: `coercedGbRaw`에서 변환을 빼면 **E2E만 실패**한다.

### 18.3 차단 조합에서는 감정 카드도 숨긴다

증축(3-way)에서는 건물을 본체·증축으로 나누는데 감정가액은 **토지·건물 2필드**뿐이라 배분 근거가
없다 — 구분 기재를 막은 것과 같은 이유다. 부담부증여는 §159가 양도가액을 자동 산정하므로 basis
자체가 쓰이지 않는다.

### 18.4 게이트 (2026-08-06 17:00 실측)

`tsc` **0** · `lint` **0 errors** · `npm test` **1,254파일 14,062 passed** ·
`e2e/gb-sale-split-30pct.spec.ts` **4 passed**.

신규 anchor **14건**: 엔진 G-7 **5**(감정 우선·일자 없음·창 밖·구분 기재와 결합·배제 사유) ·
배관 ④⑫ **3** · ⑧ **3** · ⑤ **3** · E2E **1**.

---

## §19 미결 문항 조사 결과 (2026-08-06)

법제처 Open API(`lib/korean-law/client.ts`)로 조회했다. **본문은 API가 제공하지 않아 제목(요지)까지만**
확인했으므로, 아래 인용은 요지 수준이며 판단 근거로 쓸 때 그 한계를 함께 적는다.

> ⚠️ 조회한 식별자는 **법제처 내부 판례 ID**다(사건번호 아님). 사건번호가 필요하면 별도 확인이 필요하다.

### 19.1 Q-4 · Q-10 — **근거를 찾지 못했다 ⇒ S-10 차단 유지 확정**

「증축이 있는 건물에서 계약서상 **건물 양도가액 하나**를 본체·증축에 어떻게 배분하는가」를 다룬
명문·판례·심판례를 **찾지 못했다**.

| 조회 | 결과 |
|---|---|
| 판례 "증축" | 10건 — 전부 **다른 쟁점**(무허가 옥탑 1주택 판정 `619603`·`613875`·`330782` / 증축에 환산취득가 적용 불가 `418872` / 증축비용 증명책임 `346146`·`335654` / 증축 vs 신축 `331530`) |
| 판례 "증축 취득시기" · "건물 증축 양도소득" | **0건** |
| 법령해석례 "건물 증축 양도가액 안분" | **0건** |

**조문도 답하지 않는다.** 「소득세법」 제100조 제2항은 「**토지와 건물 등**의 가액 구분」을,
「부가가치세법 시행령」 제64조 제1항은 「**토지와 건물 또는 구축물 등**」의 안분을 규율한다 —
둘 다 **건물 내부(본체·증축) 배분을 대상으로 하지 않는다**.

> 「건물 내부 구분이 불분명하니 §100② 본문에 따라 안분하면 된다」는 **논리적으로는 성립한다**.
> 그러나 그것은 해석이고, 차단을 푸는 것은 **새 계산 경로를 여는 세액 변경**이다. 명문·예규 없이
> 그 방향으로 가지 않는다(메모리 `feedback_unverified_authority_blocks_tax_change`).

⇒ **S-10(증축) 차단을 유지한다.** Q-10(3파트 이상 판정 단위)도 증축이 그 실례이므로 같은 결론이다.
사용자에게는 「일괄양도로 계산하세요」라는 대안을 함께 안내한다(이미 구현됨 — `blockedReason`).

### 19.2 Q-9 — 준용 범위에 관한 판시를 찾았다 (현행 구현과 부합)

법제처 판례 ID **`618857`**:

> 소득세법상 토지와 건물 등의 가액의 구분이 불분명한 경우 **안분계산하는 방법은 부가가치세법상
> 방법을 준용한다는 의미로 해석될 뿐임**

⇒ §166⑥의 준용이 **계산 방법**에 관한 것이라는 취지다. 그렇다면 부가령 §64①1호 괄호의
「공급시기」를 양도소득세 맥락에서 **양도시기**로 읽는 현행 구현
(`sale-split-apportion-basis.ts` `appraisalWindowOf`)이 그 취지에 어긋나지 않는다.

⚠️ **다만 본문을 확인하지 못했다** — 이 판시가 무엇을 **부정하기 위해** "~일 뿐"이라고 했는지가
본문에 있을 것이므로, 준용 범위를 좁히는 취지일 가능성도 배제할 수 없다. ⇒ Q-9는
**「현행 유지 + 유추임을 화면·주석에 명시」**를 그대로 두고, 확정은 본문 확보 후로 미룬다.

### 19.3 §100③ 30% 규칙 자체를 다룬 판례 2건 (참고 — 기존 구현 재확인)

| ID | 요지 |
|---|---|
| `616337` | 토지와 건물 등을 함께 양도한 경우로서 **건물분의 공급가액이** 기준시가로 안분계산한 가액과 **100분의 30 이상 차이**가 있는 경우 … |
| `615021` | 사업자가 실지거래가액으로 구분한 토지와 건물 또는 구축물 등의 가액이 … **안분계산한 금액과 100분의 30 이상 차이**가 있는 … |

두 건 모두 **구분 기재 ↔ 안분값 비교**라는 구조를 확인해 준다(Phase 1에서 확정한 규칙과 같은 구조).
세부 판시는 본문 미확보로 인용하지 않는다.

### 19.4 Q-6 · Q-8 — 종결

- **Q-6**(일반건물 거래에서 구분 기재의 실무 빈도): 법령·판례로 답할 수 있는 문항이 아니다.
  계획서 §4.2가 이미 「조문상 정당성은 §2.1로 성립하므로 착수 블로커가 아니다」라고 정리했고
  Phase 2가 도입을 마쳤으므로 **더 이상 의사결정에 영향을 주지 않는다** ⇒ 종결.
- **Q-8**(부가령 §64① 2호 단서 장부가액 · 3호 국세청장 고시): 입력 축이 없고, 일반건물은
  기준시가 2필드가 **필수 입력**이라 1호 경로가 전제된다 ⇒ **범위 밖 유지**.

---

## §20 신고서 각주 + §166⑧ 예외 근거 문구 (2026-08-06)

§12.5 표시 4항목 중 남아 있던 **3(신고서 각주)·4의 근거 문구** 부분을 채웠다.

### 20.1 무엇을 표시하나

| 조건 | 표시 |
|---|---|
| §100③ **발동** | 「신고서 양도가액은 **안분가액**(토지 … · 건물 …)으로 기재됩니다 — **계약서상 구분 기재 금액과 다릅니다**」 |
| §166⑧ **예외 적용** | 사유(호) + **사용자가 적은 근거 문구** + 「신고서에 그대로 적용한 사유로 위 내용을 기재하세요」 |
| 미발동 | 각주 없음 — 구분값이 그대로 신고되므로 다를 것이 없다 |

발동 각주가 필요한 이유는 **화면 값과 신고서 값이 갈리기 때문**이다. 사용자는 계약서에 적은
금액을 입력했는데 신고서에는 안분가액이 올라간다 — 그 사실을 말하지 않으면 「왜 내가 넣은 값이
아니지」가 된다.

### 20.2 근거 문구는 **엔진을 거치지 않는다** — 호출부가 폼에서 읽어 넘긴다

`saleSplitExemptionNote`는 계산에 쓰이지 않는 서술 텍스트라 API로 보내지 않기로 했다(§15.3).
그래서 판정 결과(`SaleSplitJudgmentDetail`)에는 **사유(호)만** 있고 문구는 없다 ⇒ 표시 컴포넌트가
`exemptionNote` prop으로 받는다.

**전달 경로 3갈래**(자산별로 다르므로 `assetKind`와 같은 층위로 흘린다):

| 경로 | 체인 |
|---|---|
| 단건 split | `TransferTaxResultView` → `SplitGainDetailSection` |
| 일반건물 | `BundledAllocationCard` → `SaleSplitJudgmentBlock` |
| 다자산 split | `BundledAllocationCard` → `PropertyCard` → `ValuationDetailCards` → `SplitGainDetailSection` |

세 번째 경로는 `formData.assets.find(a => a.assetId === p.propertyId)`로 **자산별** 근거를 찾는다 —
`formData.assets[0]` 고정 참조는 다자산에서 틀린 문구를 낸다.

### 20.3 게이트 (2026-08-06 17:49 실측)

`tsc` **0** · `lint` **0 errors** · `npm test` **1,257파일 14,088 passed** ·
E2E(`gb-sale-split-30pct` + `sale-split-30pct-deemed-unclear`) **6 passed**.

신규 anchor **5건**(U-9-4) — 근거 표시 · 근거 없을 때 미표시 · 예외 안내 · 발동 각주 · 미발동 시 각주 없음.

> ⚠️ **`<strong>`이 문장을 쪼개면 `getByText` 정규식이 걸리지 않는다** — `getByText`는 요소 하나의
> 텍스트를 보지 자식으로 나뉜 문장을 잇지 않는다. 단일 텍스트 노드 기준으로 단언한다.

### 20.4 §12.5 표시 4항목 — **전건 완료**

| # | 항목 | 상태 |
|---|---|---|
| 1 | 결과 요약 배지(30% 초과 → 안분 적용) | ✅ §14·§17 |
| 2 | 안분 명세표 4행(구분 기재·안분·이탈률·적용) | ✅ §14 |
| 3 | 신고서 각주 | ✅ §20 |
| 4 | 예외 사유 + 사용자 입력 근거 | ✅ §20 |

---

## §21 Q-9 확정 — 감정평가 **시기 요건은 판정하지 않는다** (2026-08-06 사용자 결정)

### 21.1 결정

> 이 프로젝트에서는 감정평가서 요건을 검토하지 말고, 사용자가 감정평가액으로 안분하겠다고
> 선택하고 감정평가액을 입력받아 그 기준으로 안분하도록 구현한다. **모든 책임은 사용자에 있다.**

### 21.2 왜 이 방향이 맞나

부가령 §64①1호 괄호의 기간 제한은 「**공급시기**가 속하는 과세기간의 직전 과세기간 개시일부터
… 종료일까지」다. 이를 양도소득세에 적용하려면 「공급시기」를 「양도시기」로 치환해 읽어야 하는데
**명문이 없다**(§19.2 — 대법원 판례는 「안분계산하는 **방법**을 준용한다는 의미」라고만 판시했고
본문은 확보하지 못했다).

⇒ 엔진이 **유추 위에서 사용자 입력을 배제**하는 것은 근거가 약하다. 요건의 존재를 **알리고**
판단은 사용자에게 맡긴다.

### 21.3 무엇이 바뀌었나

| 계층 | 종전 | 지금 |
|---|---|---|
| 엔진 | 유효 창 계산 → 벗어나면 `out_of_window` 배제 → 기준시가 후퇴 | **시기 무관 채택**. `appraisalWindowOf`·`transferDate` 인자·`appraisalWindow` 반환 제거 |
| `AppraisalRejectReason` | `out_of_window` \| `incomplete` | **`incomplete`만** |
| validate | 감정가액 입력 시 **감정일자 필수** | 일자는 **선택**. 「양쪽 모두」만 요구 |
| UI | 유효 창 기간 안내 | **요건 존재 + 미검증 사실** 안내 · 일자 라벨에 「참고용」 |

⚠️ **「양쪽 모두 입력」은 그대로 요구한다** — 이것은 법령 판단이 아니라 **비율을 내려면 두 값이
필요하다**는 산술적 필요조건이다. 성격이 다른 규칙을 함께 걷어내지 않았다.

### 21.4 반전된 계약 (삭제가 아니라 대상 소멸)

| anchor | 처리 |
|---|---|
| B-2 (시기 이탈 배제) | **반전** — 「시기와 무관하게 채택」 |
| B-3 (창 경계 포함) · B-8 (창 산출 UTC) | **소멸** — 창을 계산하지 않으므로 검증 대상이 없다 |
| E-1-2 (일자 없으면 미채택) | **반전** — 일자 없이도 채택 |
| E-1-3 · G-7 (창 밖 후퇴) | **반전** — 오래된 감정도 채택 |
| validate 2건 (일자 필수) | **반전** — 비워도 통과 |
| UI 2건 (창 기간 안내 · 배제 사유 표시) | **반전** — 미검증 사실 안내로 |

§14.3의 「조합 공간 소멸」과 같은 성격이다 — 검증을 줄인 것이 아니라 **검증할 대상이 없어졌다**.

### 21.5 게이트 (2026-08-06 19:47 실측)

`tsc` **0** · `lint` **0 errors** · `npm test` **1,259파일 14,105 passed** · E2E **6 passed**.

# §104 「해당 호」 판정 단일화 — `rateClause` 승자 오염 + 비사토 축 누락 — v2.2

작성 2026-08-02 · 선행 [[transfer-104-5-proviso-mixed-use-rate-gaps.plan.md]] (v3.0 · P12 전 단계 완료)

> **표기 규약** — 이하 **§**는 「소득세법」, **영 §**는 「소득세법 시행령」을 가리킨다.
> 다른 법령은 매번 법령명을 병기한다. (memory `feedback_law_citation_must_name_statute_and_tier`)

> **📌 결함 요약**
>
> | ID | 결함 | 방향 | 실측 규모 | 상태 |
> |---|---|---|---:|---|
> | **E-1** | `rateClauseKeyOf`에 **§104①8호(비사업용 토지) 축이 없다** → `short_term` 그룹에서 비사토와 그 외 자산이 같은 키로 묶여 **입력 순서에 따라 세액이 달라진다** | **순서 의존**(과대 +20,000,000 / 과소 −4,060,000) | **24,060,000** | ✅ **해소 Q2** |
> | **E-1ⓑ** | `uniformRate`가 **적용세율**만 봐, 비사토가 §104① 후단에서 단기세율로 이기면 사업용과 세율이 같아져 후보 집합이 달라도 합산 | 순서 의존 | **14,060,000** | ✅ **해소 Q2**(Do 중 발견) |
> | **E-1ⓒ** | 키가 하나라도 다르면 **그룹 전체**가 자산별 합으로 붕괴 — 같은 호끼리의 합산까지 끊김(누진 호 분기의 D-12와 동형) | 과소 | **20,000,000** | ✅ **해소 Q2**(Do 중 발견) |
> | **E-1ⓓ** | 위기취득 비사토(부칙 §9270호 §14①)의 호 표기가 `104-1-8`이라 **가산 0인 자산이 일반 비사토와 같은 버킷** | 순서 의존 | **53,000,000** | ✅ **해소 Q2**(§9 등록 갭) |
> | **E-2** | 파트 그룹핑 키가 `rateClause`(**승자**)라 ⓐ「해당 호는 같은데 승자만 갈린」 파트가 **나뉘고**, ⓑ「해당 호는 다른데 승자만 같은」 파트가 **합쳐진다** | **양방향** | ⓐ **+15,000,000** · ⓑ **−13,760,000**(단건) · **−10,440,000**(다건) | ✅ **해소 Q3** |
>
> **E-1이 우선이다**(✅ Q2 완료). 이어 **E-2는 조합 구성에 성공했다** — 계획서가 「4중 조건이라
> 좁다」고 본 것과 달리 **단일 자산 안에서 2중 조건**으로 성립한다(§4 재작성).

---

## 0. 발단

P12(§104⑤ 호별 합산) 3단계를 마치며 남긴 한계 —
「split 자산의 주택 파트가 2년 미만 + 조정 다주택이면 `rateClause`가 §104⑦ 후단 승자로
오염된다」 — 를 별건으로 착수하려고 조사하다가, **더 크고 명확한 결함(E-1)**을 발견했다.

**조사에서 바뀐 것 3가지**:
1. 오염 축이 **하나가 아니라 둘**이다 — §104⑦ 후단(다주택)뿐 아니라 **§104① 후단(비사업용
   토지)**도 승자에 따라 호가 갈린다.
2. 호 판정이 **3곳**에 흩어져 있다(dual이 아니라 **triple-truth**).
3. 그중 `rateClauseKeyOf`(P9가 D-11을 고치며 만든 것)에 **비사토 축이 통째로 없다** — 이것이
   실측 가능한 결함 E-1이다.

---

## 1. 검증된 법령 (법제처 Open API · MST 280405 · 시행 2026-07-01)

### 1.1 「해당 호」가 둘 이상일 수 있는 근거

- **§104① 후단**: 「하나의 자산이 다음 각 호에 따른 세율 중 **둘 이상에 해당**할 때에는
  해당 세율을 적용하여 계산한 양도소득 산출세액 중 **큰 것**을 그 세액으로 한다」
- **§104⑦ 후단**: 「해당 주택 보유기간이 2년 미만인 경우에는 [중과세율] … 과 제1항제2호 또는
  제3호의 세율을 적용하여 계산한 양도소득 산출세액 중 **큰 세액**을 … 산출세액으로 한다」

⇒ **자산 하나가 여러 호에 「해당」하고, 그중 하나가 「적용」된다.**
「해당」과 「적용」은 다르며, §104⑤2호 단서는 그 구분 위에 서 있다.

### 1.2 §104⑤2호 단서가 「해당 호」를 요구한다

> 다만, 둘 이상의 자산에 대하여 §104① 각 호, ④ 각 호 및 ⑦ 각 호에 따른 세율 중
> **동일한 호의 세율이 적용되고, 그 적용세율이 둘 이상인 경우** … 합산한 것에 대하여
> **각 해당 호별 세율**을 적용하여 산출한 세액 중에서 **큰 산출세액**의 합계액으로 한다.

효과절이 「**각 해당 호별** … 중 **큰** 것」이므로 **자산이 복수 호에 해당함**을 전제한다.
⇒ 그룹핑 키는 **승자**가 아니라 **「해당 호 집합」**이어야 한다.

**본문의 계산 단위**는 예규가 확정했다 — 「"자산별"에서 "자산"의 의미는 §104 **각 호별로
합산한 자산**」(「기획재정부 재산세제과-536」 2018.6.19. · 국세청
「기준-2018-법령해석재산-0098」[법령해석과-1715] 2018.6.21.).
선행 계획서 §1.6-A 참조.

---

## 2. 현행 호 판정 지도 — **3곳에 흩어져 있다** (grep 실측)

| # | 위치 | 판정 방식 | 문제 |
|---|---|---|---|
| ① | `transfer-tax-rate-calc.ts` `calcTax`의 `rateClause` (반환 11곳) | **승자 기준** | §104① 후단·⑦ 후단이 고른 **승자**를 낸다 → 「해당 호 집합」을 표현하지 못한다 |
| ② | `transfer-tax-aggregate-helpers.ts:427` `rateClauseKeyOf` | **입력 기반** — 단기 밴드 + 다주택 밴드 | **비사업용 토지(§104①8호) 축이 없다** → E-1 |
| ③ | `transfer-tax-split-rate.ts:299` `clauseKey` | ①의 `rateClause` 기반 | ①의 오염을 그대로 상속 |

### 2.1 ① `calcTax` 반환 11곳 분류 (실측)

**승패에 따라 호가 갈리는 곳 — 2쌍**:

| 축 | 승자별 반환 |
|---|---|
| **§104① 후단** (비사업용 토지) | `:391` 단기 승 → `shortTermClause(...)` ↔ `:403` 비사토 승 → `"104-1-8"` |
| **§104⑦ 후단** (다주택 중과) | `:460` 중과 승 → `surchargeClause(...)` ↔ `:472` 단기 승 → `shortTermClause \| "104-1-1"` |

**단정(승패 무관)**: `:218` 미등기 `104-1-10` · `:279` 부수토지 일체과세 누진 `104-1-1` ·
`:294`·`:307` `unifiedShortTermClause(...)`(수동 오버라이드는 `undefined`) ·
`:496` T-3 중과(2년 이상이라 후단 없음) · `:509` 기본 누진 `104-1-1`.

> 🔴 **v1.1 정정 — 반환은 11곳이지만 `rateClause`를 내는 곳은 10곳이다.**
> `:335`(**조특법 §98①1호 20% 단일세율** · `forceFlatRate20`)는 「§104①에도 불구하고」 세율을
> 대체하므로 **§104 각 호 밖**이다 — `rateClause`가 없는 것이 정확하고, 후보도 `undefined`가
> 맞다(「호 불명 = 묶지 않음」이 안전측). Q1 anchor A-5가 이를 고정한다.

### 2.2 ② `rateClauseKeyOf`의 축 (실측)

```ts
const shortBand = holdingMonths < 12 ? "104-1-3" : holdingMonths < 24 ? "104-1-2" : "-";
const surchargeBand = /* 유예·부칙배제 아니고 주택류 + 조정 + 2주택↑ */ "104-7-1" | "104-7-3" | "-";
return `${shortBand}|${surchargeBand}`;
```

⇒ **§104①8호(비사업용 토지) 축이 없다.** 비사토 여부가 키에 반영되지 않는다.

---

## 3. 결함 E-1 — `short_term` 그룹의 **입력 순서 의존** (실측 완료)

### 3.1 메커니즘

`classifyRateGroup`(`:72`)이 **2년 미만 자산을 전부 `short_term`으로** 보낸다.
비사업용 토지가 2년 미만이면 그 그룹에 들어가는데, `rateClauseKeyOf`가 비사토 축을 보지
않으므로 **사업용 토지와 같은 키**가 된다 → `sameRateClause` 참 → **합산 1회**:

```ts
const tr = calcTax(groupTaxBase, parsedRates, records[idxList[0]].correctedSingleInput);
//                                            ^^^^^^^^^^^^^^^^^ 입력 목록의 **첫 자산**
```

⇒ 첫 자산이 비사토면 그룹 전체에 §104①8호가, 사업용이면 §104①2호가 적용된다.

### 3.2 실측 (2026-08-02 · throwaway probe)

**구성**: 둘 다 2025-01-01 취득 · 2026-06-01 양도(**17개월** → §104①2호 구간) · 기본공제 소진

| 자산 | 성격 | 과세표준 | 해당 호 | 자산 단독 세액 |
|---|---|---:|---|---:|
| **N** | **비사업용** 토지 | 300,000,000 | **{①8호, ①2호}** | **124,060,000** (누진 94,060,000 + 10% 30,000,000 > 40% 120,000,000 → ①8호 승) |
| **P** | 사업용 토지 | 200,000,000 | {①2호} | **80,000,000** (40%) |

| 입력 순서 | 그룹 세액 | 판정 |
|---|---:|---|
| **A. `[N, P]`** (비사토 먼저) | **224,060,000** | 5억 전체에 §104①8호 |
| **B. `[P, N]`** (사업용 먼저) | **200,000,000** | 5억 전체에 §104①2호 40% |
| **차이** | **24,060,000** | 🔴 **입력 순서에 따라 세액이 달라진다** |

### 3.3 도출값 — **자산별 합**(2호 본문)

N과 P가 **공유하는 호는 ①2호뿐**이고 **①8호는 N만** 해당한다.
합산하면 어느 쪽이든 법문에 어긋난다:
- ①8호로 합산 → P에 없는 비사업용 가산이 붙는다(**과대 20,000,000**)
- ①2호로 합산 → N의 비사업용 가산이 사라진다(**과소 4,060,000**)

⇒ **「동일한 호의 세율이 적용되고」 요건 불충족 → 단서 미적용 → 2호 본문(자산별 합)**.
R7 감사(2026-07-29 · `docs/00-pm/transfer-open-items.plan.md:554-575`)가
`multi_house_surcharge`의 ⑦1호/⑦3호 혼재를 **같은 논리로** 고쳤다.

| | 세액 |
|---|---:|
| 2호 본문 = N 124,060,000 + P 80,000,000 | **204,060,000** |
| 1호 = 전체 합산 누진(base 500,000,000) | 174,060,000 |
| **§104⑤ MAX** | **204,060,000** |

⇒ 현행 A는 **20,000,000 과대**, B는 **4,060,000 과소**다.

### 3.4 발현 범위

- **필요 조건**: 같은 과세기간에 **비사업용 토지**와 **그 외 자산**을 **둘 다 2년 미만** 보유로
  양도. 두 자산이 같은 단기 구간(둘 다 1년 미만 또는 둘 다 1~2년)이어야 같은 키가 된다.
- 🔶 **미측정**: 「비사토 승 vs 단기 승」이 자산마다 갈리는 경우(N₁은 ①8호 승, N₂는 ①2호 승)도
  같은 키가 되어 합산된다. E-2와 얽힌 국면이라 착수 시 함께 실측한다.

---

## 4. 결함 E-2 — 파트 묶음 키가 **승자**다 (✅ 조합 구성·실측 완료 2026-08-02)

`calcTax`의 `rateClause`는 §104① 후단·§104⑦ 후단이 고른 **승자**다. 이것을 파트 묶음 키로
쓰는 곳은 **③ `computeSplitPartTax`**(자산 내부)와 **② 누진 호 분기**(다건)다.

### 4.1 🔴 v2.1 정정 — 발현 조건은 「4중」이 아니라 **자산 내부 2중**이다

종전 §4는 발현 조건을 「누진 호 그룹 + split 주택 자산 + 파트가 2년 미만 + 조정 다주택 +
같은 호 집합인 파트/자산이 **또** 있어야」로 보고 **조합 구성을 선결 과제**로 남겼다.
**틀렸다.** ③은 **단건 엔진에서도 도는 경로**이고, 한 자산의 **토지 파트와 건물 파트**만으로
조건이 채워진다:

1. split 주택 자산 — **토지를 나중에 취득**(`resolveAppurtenantLandRateBasisDate`의 `max`)
2. **두 파트가 같은 호 집합**인데 과세표준 차이로 **승자가 갈릴 것**

다른 자산도, 다건도, 누진 호 그룹도 필요 없다.

### 4.2 ⓐ 「해당 호는 같은데 승자만 갈려」 나뉜다 — **과소 15,000,000**

**구성**: 주택 · 건물 2024-08-01(22개월) · 토지 2025-01-01(**17개월**) · 조정 3주택 ·
토지 파트 과세표준 300,000,000 · 건물 파트 150,000,000

| 파트 | 해당 호 | 승자 | 단독 세액 |
|---|---|---|---:|
| 토지 | **{①2호, ⑦3호}** | ⑦3호 (누진 94,060,000 + 30% 90,000,000 = 184,060,000 > 60% 180,000,000) | 184,060,000 |
| 건물 | **{①2호, ⑦3호}** — **같다** | ①2호 (60% 90,000,000 > 누진 37,060,000 + 30% 45,000,000) | 90,000,000 |

- **현행**: 승자 키가 `"104-7-3"` ↔ `"104-1-2\|0.6"`로 갈려 **따로 계산** → **274,060,000**
- **Q3 후**: 후보 키가 `"104-1-2+104-7-3"`로 같아 **합산 450,000,000** →
  누진 154,060,000 + 30% 135,000,000 = **289,060,000**(> 60% 270,000,000)

⇒ **과소 15,000,000.** 이것은 **교재 사례2(D-11)를 파트 단위로 옮긴 것과 정확히 같다** —
「B·C **별도로** 비교하는 것이 아니라 **합산한 금액**에 중과세율을 적용해 단기세율 산출세액과
비교한다」.

### 4.3 ⓑ 「해당 호는 다른데 승자만 같아」 합쳐진다 — **과대 13,760,000**

**구성**: 주택 · 건물 2015-01-01(**11년**) · 토지 2025-01-01(17개월) · 조정 3주택 ·
토지 파트 300,000,000 · 건물 파트 50,000,000

| 파트 | 해당 호 | 승자 |
|---|---|---|
| 토지 | **{①2호, ⑦3호}** | ⑦3호 |
| 건물 | **{⑦3호}** — 2년 이상이라 단기 호에 해당하지 않는다 | ⑦3호 |

- **현행**: 승자가 같아 **합산 350,000,000**, 대표가 토지(17개월)라 §104⑦ 후단까지 발동 →
  **219,060,000**
- **Q3 후**: 후보 키가 달라 **분리** → 184,060,000 + 21,240,000 = **205,300,000**

⇒ **과대 13,760,000.** 11년 보유 건물분에 **토지 파트의 단기 비교**가 얹혀 있었다.

⭐ **ⓐ와 ⓑ가 한 쌍이라는 것이 E-2의 본질이다.** 구조가 같은 두 자산이 **과세표준 크기에 따라**
합쳐지기도 하고 나뉘기도 한다 — 순서 의존이 아니라 **승자 의존**이다.

### 4.4 ② 다건 누진 호 분기에서도 발현 — **13,760,000 → 10,440,000**

위 ⓑ 자산 2건(토지 300,000,000/100,000,000 · 건물 각 50,000,000)을 같은 과세기간에 양도:
`multi_house_surcharge` 그룹의 파트 묶음이 승자 기준이라 **314,060,000** →
Q3 후 토지 파트끼리·건물 파트끼리 묶여 **303,620,000**(**−10,440,000**).

### 4.5 🔴 v2.1 정정 — 「비사토 축은 구조적 발현 불가」의 **근거가 틀렸다**

종전 §4는 「한 필지는 취득일이 하나뿐이라 자산이 2년 이상이면 파트도 2년 이상이다」라고 했다.
**split 자산에는 해당하지 않는다** — 배율 초과분 파트는 `resolveLandStatutoryAcquisitionDate`가
낸 **토지 기산일**을 쓰므로(`transfer-tax-split-rate.ts` seeds), 건물이 11년이어도 토지가
17개월이면 그 파트는 2년 미만이다.

결론(자산 내부에서 8호 묶음은 항상 단일)은 **다른 이유로** 성립한다 — 비사토 파트가 있으면
토지 파트가 `isNonBusinessLand: false`로 강제되어(같은 seeds) **8호를 가진 파트가 하나뿐**이기
때문이다. 코드 주석은 이미 그렇게 적혀 있었고 계획서만 어긋나 있었다.

🔶 **다건(②)의 비사토 축은 여전히 미측정**이다 — split 자산 **2건**이 각각 2년 미만 비사토
파트를 가지면 그 파트끼리 묶일 수 있다. §9에 등록한다.

---

## 5. 설계 — `calcTax`가 「해당 호 **후보 목록**」을 반환한다

### 5.1 핵심

```ts
interface CalcTaxResult {
  rateClause?: RateClause;              // 기존 — 실제 **적용**된 호(표시·기록용, 유지)
  candidateClauses?: RateClause[];      // 신설 — 그 자산/파트가 **해당**하는 호 전부(정렬)
}
```

- `:391`·`:403`(§104① 후단) → `["104-1-8", shortTermClause(...)]`
- `:460`·`:472`(§104⑦ 후단) → `[surchargeClause(...), shortTermClause(...)]`
- 단정 지점은 후보가 하나 → `[rateClause]`

**그룹핑 키 = 정렬된 `candidateClauses` 조인.** 승패와 무관해진다.

### 5.2 세 판정을 후보 목록으로 통일

| 현행 | 변경 |
|---|---|
| ② `rateClauseKeyOf` (입력 기반 재구성) | **폐기** → `candidateClauses` 사용. 비사토 축 누락(E-1)이 자동 해소된다 |
| ③ `clauseKey`(split-rate) | `finals[i].rateClause` → `finals[i].candidateClauses` |
| P12 2단계 파트 묶음 | `p.rateClause` → 파트의 `candidateClauses` (`SplitRatePart`에 필드 추가) |

⇒ **triple-truth가 단일 소스로 수렴한다.** `calcTax`가 호를 아는 유일한 지점이 된다.

### 5.3 ⚠️ 합산 시 세율 적용 — 「각 해당 호별 세율 중 큰 것」

후보가 둘 이상인 그룹을 합산하면 **합산액에 각 후보 호를 적용해 큰 것**을 취해야 한다(단서).
현행은 `calcTax(mergedBase, 대표 rateInput)`이 그 MAX를 **내부에서** 수행한다
(§104① 후단·⑦ 후단이 `calcTax` 안에 있다) ⇒ **신규 세율 로직이 필요 없다.**
D-11(P9)이 같은 성질을 이용했다(선행 계획서 §4.7).

### 5.4 범위 밖

- **1호(전체 합산 누진)** — 과세표준 합계액이라 호와 무관. 불변.
- **기본공제 배분**(§103②) — `allocateBasicDeduction`은 자산 단위 `idx` 기반. 별개 최적화.
- **차손 통산** — §102①은 §94①1·2·4호 / 3호 / 5호 / 6호의 **소득 구분 4개**이지 §104① 각 호가
  아니다(원문 실측). 무관.

---

## 6. Phase

| Phase | 내용 | 세액 변경 | 선행 |
|---|---|---|---|
| **Q1** ✅ | `calcTax`에 `candidateClauses` 추가 (`rateClause`를 내는 **10곳**) + `SplitRatePart`에 전달 | **없음**(정보 추가만) | — · **완료 2026-08-02 §6-R** |
| **Q2** ✅ | ② `rateClauseKeyOf` **폐기** → `short_term`을 **호별 버킷 합산**으로(누진 호 분기와 동일 규약) → **E-1 ⓐⓑⓒⓓ 해소** | **있음** — 순서 의존 제거 | Q1 · **완료 2026-08-02 §6-R2** |
| **Q3** ✅ | ③ `computeSplitPartTax` · ② 누진 호 분기가 `clauseBucketKey`에 넘기는 인자를 `[rateClause]`(승자) → `candidateClauses`(해당 호 집합)로 교체 → **E-2 해소** | **있음 · 양방향** — ⓐ +15,000,000 / ⓑ −13,760,000 / ② −10,440,000 | Q1·Q2 · **완료 2026-08-02 §6-R3** |

> 🔴 **v2.1 정정 — 「인자 교체 1줄」이 아니다.** ②는 `ClausePart` 타입에 `candidateClauses`가
> 없어 **타입 1줄 + flatMap 두 분기 2줄 + 키 호출 1줄 = 4줄**, ③이 1줄이다(실측: 임시 적용해
> tsc 통과 확인). 여전히 작지만 「1줄」은 틀렸다.

> ✅ **Q3는 기존 anchor를 하나도 깨지 않는다** — 임시 적용 상태에서 **812파일 9,828건 통과·회귀 0**
> 실측. 바뀌는 조합은 **어떤 기존 anchor도 덮고 있지 않다** ⇒ R-5(「anchor가 현재 동작 고정이
> 되어 검증력이 없다」)는 §4.2~§4.4 조합으로 해소된다.

**Q1은 세액 불변**이라 안전하게 끊어갈 수 있다(P12 1단계와 같은 성질).

---

### 6-R Q1 구현 결과 ✅ (2026-08-02)

**엔진 2파일**(`transfer-tax-rate-calc.ts` 737줄 · `transfer-tax-split-rate.ts` 607줄).
**세액 변경 0** — 정보만 추가했다.

| | 변경 |
|---|---|
| `CalcTaxResult` | **`candidateClauses?: RateClause[]`** 신설. `rateClause`(승자)는 표시·기록용으로 유지 |
| `clauseSet(...)` 헬퍼 | 중복 제거 + **문자열 오름차순 정렬**. 후보 0개면 `undefined`(묶지 않음 = 안전측) — R-3 대응 |
| **후단 비교 2쌍** | 각각 **공유 변수**로 묶어 승자와 무관하게 같은 배열을 낸다 (`nblCandidates` · `stCandidates`) |
| 단정 분기 6곳 | `clauseSet(그 호)` 부착 |
| `SplitRatePart` | `candidateClauses` 필드 + split·부분 비사토 파트 **양쪽** 전달 |

**Do 중 정정 2건**:
1. 🔴 **계획서가 「반환 11곳」이라 했으나 `rateClause`를 내는 곳은 10곳**이었다 —
   `:335`(조특법 §98①1호)는 §104 밖이라 후보도 `undefined`가 맞다(§2.1 정정).
2. 다주택 축의 `surchargeInfoST`·`historicalRateST`를 `if` 블록 밖으로 빼자 **타입 narrowing이
   풀려** tsc 오류가 났다(`effectiveSurchargeType`에 `"none"` 포함). `stSurchargeType` 변수로
   narrowing을 옮겨 해소.

🔶 **Do 중 발견 — §9에 등록**: `nblCrisisExcluded`(부칙 §9270호 §14①)이면 가산이 0이라 실질
§104①1호인데 호 표기는 `"104-1-8"`이다. 일반 비사토와 합산되면 대표 입력에 따라 갈릴 수 있어
**E-1과 같은 유형의 잠재 갭**이다. Q1은 현행 표기를 유지했고 Q2에서 실측한다.

**anchor 12건 신설**(`rate-clause-candidates.anchor.test.ts`) — **분기별 전수**를 고정한다
(R-1 대응). 핵심은 **A-1a↔A-1b · A-3a↔A-3b**: 승자가 갈려도(`rateClause`가 다름)
**후보 배열은 동일**함을 고정한다.

**검증**: `tsc` 0 · `lint` 0 · 양도+calc **173파일 1,692건** · 전체 **1,155파일 12,970건 · 회귀 0**.

### 6-R2 Q2 구현 결과 ✅ (2026-08-02)

**엔진 3파일** + **호 축 신설 1파일**. **세액 변경 있음** — 아래 4가지 순서 의존이 사라진다.

#### 무엇을 바꿨나

`short_term` 분기의 **전부-아니면-전무** 판정(`uniformRate || sameRateClause`)을 폐기하고,
누진 호 분기(P12)와 **같은 「호별 버킷 합산」** 규약으로 통일했다.

```
buckets = groupBy(자산, clauseBucketKey(candidateClauses, appliedRate, n))
버킷 1건  → 자산 세액 그대로
버킷 2건+ → calcTax(Σ 버킷 과세표준, 대표 자산 입력)   ← §104①·⑦ 후단 MAX는 calcTax 내부
```

`clauseBucketKey`(신설 · `transfer-tax-rate-clause.ts`)가 **세 소비처의 단일 키 함수**다 —
`short_term` 분기 · 누진 호 분기 · `computeSplitPartTax`. triple-truth가 여기서 수렴한다.

#### Do 중 실측한 누수 3건 (계획서에 없던 것)

| # | 누수 | 실측 |
|---|---|---:|
| ⓑ | `uniformRate`가 **적용세율**만 봤다 — 비사토가 §104① 후단에서 **단기세율로 이기면** 사업용과 40%로 같아져 후보 집합이 달라도 합쳐졌다. §3.4가 「미측정」으로 남긴 국면의 실체다 | `[N2,P]` 174,060,000 ↔ `[P,N2]` 160,000,000 = **14,060,000** |
| ⓒ | 키가 하나라도 다르면 **그룹 전체**가 자산별 합으로 붕괴 — 같은 호끼리의 합산까지 끊겼다. 누진 호 분기의 **D-12와 동형**이다 | `[N,N2,P3]` 328,060,000 ↔ 280,000,000 · 도출 **304,060,000** |
| ⓓ | **위기취득 비사토**(§9 등록 갭) — 가산이 0인데 호 표기가 `104-1-8`이라 일반 비사토와 같은 버킷 | `[CB,CnB]` 186,660,000 ↔ 239,660,000 = **53,000,000** |

⇒ **키 교체만으로는 E-1이 해소되지 않았다.** ⓑ는 키와 무관한 별도 게이트였고, ⓒ는 버킷 구조
자체의 문제였다. Pre-Do 실측이 아니었으면 「E-1 해소」를 잘못 선언할 뻔했다.

#### ⓓ의 근거 — 해석이 아니라 정본 인용

「소득세법」 부칙(제9270호) §14①은 정본이 이미 확정해 두었다 —
`legal-codes/surcharge-transition.ts:42` 「중과세율 배제(**→§104①1호 기본세율**, 보유 2년
미만이면 §104①2·3호 단기)」(「기획재정부 재산세제과-1422」 2023.12.26. ·
서울행정법원 2024구단72950). ⇒ `rateClause`·후보를 `"104-1-1"`로 정정했다.

내부 정합 근거도 있다 — `classifyRateGroup:103`은 **다주택 축**에서 이미 같은 부칙을
`progressive`로 분류하고 있었다. 비사토 축만 빠져 있던 좌우 불일치다
(memory `feedback_sibling_path_already_implements_rule`).

#### ⚠️ 그룹 세액 ≠ 결정세액

anchor 작성 중 **Q-8을 한 번 틀렸다** — 버킷 분리로 2호가 114,800,000이 됐는데도 결정세액은
120,460,000이었다. 그 위에 **§104⑤ MAX(1호 합산누진, 2호)**가 한 번 더 걸리기 때문이다.
버킷 분리를 anchor로 고정하려면 **2호가 이기는 조합**을 써야 한다(Q-8이 그 조합, Q-8d가 1호 승).

#### 800줄 정책 — 기회주의적 분리

`transfer-tax-rate-calc.ts`가 **776줄**(위험구간)에 도달해, 이번에 연 김에 **호 축**을
`transfer-tax-rate-clause.ts`(94줄)로 분리했다 → **707줄**. 종전 import 경로는 재수출로 보존한다.
세액을 계산하지 않는 순수 분류 축이라 응집도가 다르고, Q1~Q3가 반복해서 여는 구역이다.

**검증**: `tsc` 0 · `lint` 0 · 신규 anchor 12건 + GREEN 조건 44건 = **56건** · 양도+calc **812파일 9,827건 회귀 0**.

### 6-R3 Q3 구현 결과 ✅ (2026-08-02)

**엔진 2파일.** 묶음 키 인자를 `[rateClause]`(승자) → `candidateClauses`(해당 호 집합)로 교체.
⇒ **triple-truth가 완전히 수렴했다** — 세 소비처(`short_term` 분기 · 누진 호 분기 ·
`computeSplitPartTax`)가 **같은 키 함수 + 같은 인자 축**을 쓴다.

| 경로 | 변경 |
|---|---|
| ③ `computeSplitPartTax` | 키 호출 1줄 |
| ② 누진 호 분기 | `ClausePart` 타입에서 `rateClause` → `candidateClauses` 교체 + flatMap 두 분기 + 키 호출 |

**Do 중 추가 정리 2건**(내 변경이 만든 것만 — Surgical):
1. `assetTaxOf`의 `rateClause` 반환이 **고아**가 됐다(마지막 소비처가 ②였다) → 제거.
2. P12가 남긴 경고 주석 「⚠️ 이 분기에만 적용한다 — `rateClause`를 그룹 키로 쓰면 D-11이
   회귀한다」가 **전제를 잃었다**. Q3가 키를 후보 집합으로 바꿔 승패 오염 자체가 없어졌고,
   `short_term`도 Q2에서 같은 규약으로 옮겼다 → 주석 정정.

**anchor 8건 신설**(`split-part-clause-bucket.anchor.test.ts`) — 세액 5건 + **구조 3건**.
구조 anchor(E-2a-1·E-2b-1)가 「후보 집합은 같은데 승자가 다르다」·「후보 집합이 부분적으로만
겹친다」를 직접 고정한다 — 세액만 고정하면 규칙이 바뀐 이유가 남지 않는다.

**검증**: `tsc` 0 · `lint` 0 · 신규 8건 + GREEN 조건 56건 = **64건** · 전체 회귀 0.

---

## 7. Pre-Do anchor (착수 전 · memory `feedback_pre_anchor_verification`)

### 7.1 🔴 GREEN 조건 — **먼저 통과시켜야 한다**

세액을 바꾸는 Phase(Q2·Q3)는 **이 프로젝트가 이미 재현해 둔 authoritative 사례**가 살아
있어야 한다. (선행 계획서 §4.9-R 교훈 — 새 anchor만 GREEN인 채로 머지했다가 되돌린 전례.)

| 대상 | 위치 |
|---|---|
| 교재 사례1·2 (§104⑤2호 단서) | `aggregate-same-clause-104-5.anchor.test.ts` B-28·B-29 |
| R7 감사 도출값 280,120,000 유형 | 〃 B-29c |
| 예규(호별 합산) | `aggregate-progressive-clause-104-5.anchor.test.ts` B-36~B-42 |
| P12 호별 합산 | `aggregate-clause-merge-104-5.anchor.test.ts` B-43~B-47 |
| 파트 참고세액 | `aggregate-ref-tax-parts.anchor.test.ts` A-1~A-3b |
| **Q2 호별 버킷**(E-1 ⓐⓑⓒⓓ) | `aggregate-clause-bucket-short-term.anchor.test.ts` Q-1~Q-8d |

### 7.2 신규 anchor 시나리오

| ID | 케이스 | 현행 | 도출값 |
|---|---|---:|---:|
| **Q-1** | E-1 `[N(비사토 3억·17개월), P(사업용 2억·17개월)]` | 224,060,000 | **204,060,000** |
| **Q-2** | E-1 **순서 반전** `[P, N]` | 200,000,000 | **204,060,000**(동일) |
| Q-3 ✅ | 비사토 2건만(같은 해당 호 집합) | 224,060,000 | **224,060,000**(합산 유지 — 승자가 갈려도 같은 호) |
| **Q-6** ✅ | 🆕 ⓑ `[N2(비사토·단기 승 40%), P(사업용 40%)]` | 174,060,000 ↔ 160,000,000 | **160,000,000** |
| **Q-7** ✅ | 🆕 ⓒ `[N, N2, P3]` — 버킷 2개 | 328,060,000 ↔ 280,000,000 | **304,060,000** |
| **Q-8** ✅ | 🆕 ⓓ 위기취득 `[CB, CnB]`(2호 승 조합) | 186,660,000 ↔ 239,660,000 | **202,620,000** |
| Q-4 | 회귀 — 사업용 2건만 | 불변 | 불변 |
| Q-5 | 회귀 — 교재 사례2(①2호 + ⑦3호 동시 해당) | 불변 | 불변 |

**Q-1·Q-2가 같은 값이 되는 것**이 이 작업의 핵심 성공 기준이다(순서 의존 제거).

### 7.3 Q3 anchor 시나리오 (✅ 전부 실측 — 임시 적용해 측정)

| ID | 케이스 | 현행 | Q3 후 | 차 |
|---|---|---:|---:|---:|
| **E-2a** | ③ 같은 호 집합·승자 갈림 (건물 22개월 + 토지 17개월 · 조정 3주택 · 3억/1.5억) | 274,060,000 | **289,060,000** | **+15,000,000** |
| **E-2b** | ③ 다른 호 집합·승자 같음 (건물 **11년** + 토지 17개월 · 3억/5천만) | 219,060,000 | **205,300,000** | **−13,760,000** |
| **E-2c** | ② 위 E-2b 자산 2건 다건 | 314,060,000 | **303,620,000** | **−10,440,000** |
| E-2d ✅ | 회귀 — 호도 승자도 다른 파트 쌍(`[Y]` 단독) | 81,240,000 | 81,240,000 | 0 |

⚠️ **ⓑ·②는 세액이 줄어든다.** 「법 근거 없이 불리 적용 금지」와 반대 방향이라 근거를 분명히
둔다 — 11년 보유 건물분에 **토지 파트의 §104⑦ 후단 단기 비교**가 얹히던 것이 제거되는 것이고,
**Q2가 이미 anchor로 고정한 규칙**(해당 호 **집합**이 다르면 묶지 않는다 — E-1ⓐ의 N/P)을
파트에 그대로 적용한 결과다. 새 해석이 아니다.

### 7.4 ⚠️ Q3 착수 전 확정해야 할 해석 1건

**「해당 호 집합」이 부분적으로 겹칠 때 어느 버킷인가** — 토지 {①2호, ⑦3호} · 건물 {⑦3호}는
**⑦3호를 공유**한다. 두 가지 읽기가 가능하다:

- **(A) 집합 일치**(Q2가 채택·anchor로 고정) — 집합이 다르면 묶지 않는다.
- (B) 공유 호로 묶고 토지 파트만 ①2호 비교를 추가로 받는다 — **파트 과세표준이 두 버킷에
  중복 계상**되므로 성립하지 않는다.

⇒ **(A)가 유일하게 정합적**이다. 근거: §104⑤2호 단서의 효과절이 「합산한 것에 대하여 **각 해당
호별** 세율을 적용하여 산출한 세액 중 **큰** 것」이라, 합산 대상 전원이 **같은 집합**이어야 그
「각 해당 호별」이 정의된다. {①2호,⑦3호}와 {⑦3호}를 묶어 ①2호를 적용하면 **11년 보유 자산에
단기세율**을 물리게 되어 법문에 정면으로 어긋난다.

이 판단은 **Q2에서 이미 세액으로 확정**됐다(E-1ⓐ: N{①8호,①2호} vs P{①2호} 불합산 · anchor Q-1·Q-2).
Q3는 같은 규칙을 파트로 확장할 뿐이다.

---

## 8. 리스크

| # | 리스크 | 완화 |
|---|---|---|
| R-1 ✅ | `calcTax` 반환 지점(rateClause를 내는 **10곳**)을 모두 고쳐야 한다 — 한 곳이라도 빠지면 후보가 불완전해 **묶음이 조용히 갈린다** | Q1에서 **반환 지점 전수 표**를 만들고 anchor로 각 분기의 `candidateClauses`를 고정 |
| R-2 ✅ | `rateClauseKeyOf` 폐기가 **D-11(P9) 회귀**를 부를 수 있다 | GREEN 조건 44건을 착수 전·후 모두 통과 확인(회귀 0). 교재 사례2는 후보 집합 `{①2호, ⑦3호}`가 같아 **버킷 1개**로 합산된다 — 승자 세율을 키에 넣지 않는 규약이 이를 보장한다 |
| R-3 | 후보 목록이 **정렬 불안정**하면 키가 흔들려 순서 의존이 재발한다 | 키 생성 시 **명시적 정렬**(`RateClause` 문자열 오름차순) + anchor |
| R-4 ✅ | 단일세율 호는 「호\|세율」로 묶는 규약(P12)이 있는데 후보 목록과 **어떻게 결합**할지 미정 | **Q2에서 확정** — `clauseBucketKey(후보, 세율, id)`: 후보 없음→`solo-{id}` · **누진 호 포함→후보 조인만**(승자 세율은 무의미 — 합산액에 호별 세율을 다시 적용해 MAX를 취하므로) · **단일세율 호만→`후보\|세율`**(P12 규약 승계) |
| R-5 ✅ | E-2 조합을 끝내 구성하지 못하면 Q3의 anchor가 「현재 동작 고정」이 되어 검증력이 없다 | **조합 구성 성공**(§4.2~§4.4) — ⓐ·ⓑ·②의 세 조합이 각각 세액을 바꾸므로 anchor가 실질 검증력을 갖는다. 🛑 보류 선택지는 **철회** |

---

## 9. 미검증 레지스트리

| 항목 | 상태 |
|---|---|
| E-2 발현 조합 | ✅ **구성 완료**(§4.2~§4.4) — 자산 내부 **2중 조건**이면 성립한다. 종전 「4중」 판단은 틀렸다 |
| E-1의 「비사토 승/단기 승이 자산마다 갈리는」 국면 | ✅ **Q2에서 실측·해소** — `uniformRate` 누수 ⓑ가 그 실체였다(14,060,000) |
| `unifiedShortTermClause`가 `undefined`를 내는 경로(수동 오버라이드)의 후보 목록 | 🔶 미확정 — 「호 불명」은 묶지 않는 것이 안전측이나 후보 목록에서 어떻게 표현할지 미정 |
| 🆕 **위기취득 비사토의 호 표기** | ✅ **Q2에서 실측·해소** — 순서 의존 **53,000,000**. `rateClause`·후보를 `"104-1-1"`로 정정(정본 `surcharge-transition.ts:42` 인용) |
| §104④(지정지역 비사업용 토지) | ✅ **엔진에 분기 없음**(선행 계획서 v1.2에서 speculative로 폐기 확인) — 후보 목록 대상 아님 |
| 🆕 **다건(②)의 비사토 축 E-2** — split 자산 **2건**이 각각 2년 미만 비사토 파트(배율 초과분)를 가지면 그 파트끼리 묶일 수 있고 승자가 갈릴 수 있다. 자산 내부에서는 8호 파트가 항상 단일이라 발현하지 않는다(§4.5) | 🔶 **미측정** — 배율 초과분 2건 조합 필요 |
| 🆕 **`short_term` 그룹의 파트 단위 버킷** — split·부분 비사토 자산은 Q2에서도 **자산 단위로 solo** 처리한다(`isAssetLevelClause5`). 누진 호 분기는 P12가 **파트를 버킷 멤버로** 풀었는데(D-7·D-12) `short_term`은 아직 자산 단위다 | 🔶 **미측정** — 「2년 미만 split 자산 + 같은 호 다른 자산」 조합이 필요하다. Q2는 **그룹 전체 붕괴**만 고쳤다(자산 하나만 solo로 빠진다) |
| 🆕 **`uniformRate` 폐기의 잔여** — 후보가 `undefined`인 자산(조특법 §98①1호 20% · 부수토지 수동 세율 ≠ 0.6/0.7)은 이제 **각자 계산**한다. 종전에는 세율이 같으면 합산 1회였다 | ✅ **회귀 0 확인**(전체 테스트). 단일세율이라 차이는 floor 횟수뿐이고, 자산별 floor가 **안전측**이다 |

---

## 10. 이력

- **v1.0** (2026-08-02) — 최초 작성. 선행 P12가 남긴 「§104⑦ 후단 승자 오염」 한계를 별건으로
  착수하려 조사하던 중 **더 크고 명확한 결함 E-1을 발견**했다.
  - 오염 축이 **둘**임을 확인(§104⑦ 후단 + **§104① 후단**) — `calcTax` 반환 11곳 전수 분류.
  - 호 판정이 **3곳**에 흩어져 있음을 확인(triple-truth).
  - **`rateClauseKeyOf`에 §104①8호 축이 통째로 없다** — `short_term` 그룹에서 비사토와
    사업용 토지가 같은 키로 묶여 **입력 순서 의존 24,060,000** 실측
    (`[N,P]` 224,060,000 vs `[P,N]` 200,000,000 · 도출값 **204,060,000**).
  - E-2(승자 오염)는 발현 조건이 4중이라 **조합 구성이 선결**임을 명시하고 🔶 미실측으로 등록.

- **v2.0** (2026-08-02) — **Q2 완료**(PR 예정). `rateClauseKeyOf` 폐기 + `short_term`을
  **호별 버킷 합산**으로 통일. Do 중 **키 교체만으로는 E-1이 해소되지 않음**을 실측으로 확인하고
  누수 3건(ⓑ `uniformRate` 14,060,000 · ⓒ 그룹 전체 붕괴 20,000,000 · ⓓ 위기취득 비사토
  53,000,000)을 함께 해소했다. 키 함수를 `clauseBucketKey` 하나로 통일해 **triple-truth가 수렴**
  ⇒ Q3는 인자 교체 1줄이 됐다. 호 축을 `transfer-tax-rate-clause.ts`로 분리(800줄 정책 776→707).

- **v2.1** (2026-08-02) — **Q3 착수 전 계획서 재검토**. 🔴 정정 4건:
  - **E-2 발현 조건이 「4중」이 아니라 자산 내부 2중**이다 — ③은 단건 엔진에서도 도는 경로라
    한 자산의 토지·건물 파트만으로 성립한다. **조합 구성 성공 + 3케이스 실측**
    (ⓐ +15,000,000 · ⓑ −13,760,000 · ② −10,440,000).
  - **E-2는 양방향**이다 — 「같은 호인데 승자가 갈려 나뉜다」(ⓐ)와 「호가 다른데 승자가 같아
    합쳐진다」(ⓑ)가 한 쌍이다. 순서 의존이 아니라 **승자 의존**.
  - **「인자 교체 1줄」이 틀렸다** — ②는 `ClausePart` 타입에 `candidateClauses`가 없어 4줄이다.
  - **「비사토 축 구조적 발현 불가」의 근거가 틀렸다** — split 자산의 배율 초과분 파트는 **토지
    기산일**을 쓰므로 자산이 2년 이상이어도 파트는 2년 미만일 수 있다. 결론은 다른 이유
    (토지 파트 `isNonBusinessLand: false` 강제)로 성립하며, **다건(②)은 여전히 미측정**이다.
  - R-5(anchor 검증력) **해소** · 🛑 보류 선택지 **철회**.
  - ✅ Q3 임시 적용 상태에서 **기존 anchor 회귀 0**(812파일 9,828건) 실측.

- **v2.2** (2026-08-02) — **Q3 완료**. 묶음 키 인자를 승자 → 해당 호 집합으로 교체해 **E-2 해소**.
  세 소비처가 같은 키 함수·같은 인자 축을 쓰게 되어 **triple-truth 완전 수렴**.
  Do 중 고아 필드 1건 제거 + P12 경고 주석 1건 정정(전제 소멸).
  ⇒ **E-1·E-2 모두 종결.** 남은 것은 §9의 🔶 2건(다건 비사토 축 · `short_term` 파트 단위 버킷).

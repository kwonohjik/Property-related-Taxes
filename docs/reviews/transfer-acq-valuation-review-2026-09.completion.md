# 양도세 취득가액·평가 축 코드리뷰 — 결과 요약

> 원 리뷰: [`transfer-acq-valuation-review-2026-09.md`](transfer-acq-valuation-review-2026-09.md) (1,020줄, PR #1412)
>
> ⚠️ **원 리뷰 문서는 착수 전 시점 그대로다.** §6의 12배치 표는 「예정」으로 읽히지만
> 실제로는 5개 PR로 처리됐다. **처리 결과는 이 문서가 정본이다.**
>
> ✅ **2026-09-03 2차 — 미해소 3건(A06·A10·A23)까지 법령 조사 후 전건 종결.** §12 참조.

---

## 1. 결과

확정 결함 **21건** 중 **18건 처리 완료**, **3건 미해소**(전부 법령 판단 선행 필요).
추가로 리뷰 밖에서 **인용 오류 2클러스터**를 발견해 1건 정정·1건 마커화했다.

| | 건수 | CI |
|---|---|---|
| 리뷰 문서 (#1412) | 결함 21건 확정 | **0회** (`paths-ignore`) |
| PR-1 (#1416) | 조기반환·배관 8건 | 1회 ✅ |
| PR-2 (#1419) | 평가 산식·가업상속·표시 6건 | 1회 ✅ |
| PR-3 (#1423) | ⏸ 미결 4건 확정 | 1회 ✅ |
| PR-4 (이 문서) | 인용 정정 + 죽은 필드 + 미해소 마커 | 1회 |

**CI 4회로 18건.** 원 리뷰의 12배치를 그대로 냈다면 12회였다.
배치를 묶을 수 있었던 근거는 **CI 비용 = PR 개수**라는 실측이다 — CI는 `pull_request`에서만
돌고 job이 `draft == false`에 걸려 있어, 커밋 수·파일 수는 비용에 영향을 주지 않는다.

**anchor 107건**을 신설했고 **전부 mutation probe로 포착을 실증**했다.

---

## 2. 세액이 실제로 틀어졌던 것 (실측)

| ID | 증상 | 편차 |
|---|---|---|
| A01 | 일부양도에서 pre1990 §164④ 취득기준시가가 「양도분 면적」이 아니라 「취득 전체면적」 | 154,704,000 |
| A02 | 미등기 다필지에서 장기보유특별공제가 그대로 적용 + 개산공제 3%(→0.3%) | 필지별 |
| A03 | 상증법 평가액을 비우고 §164 기준시가만 채우면 §163⑨ payload 자체가 미전송 | 전액 |
| A04 | 취득원인을 상속에서 바꿔도 `familyBusinessInheritance`가 걸러지지 않음 | 71,242,600 |
| A07 | 지분 모드에서 ①은 안분되는데 ②(§164④ 기준시가)는 100% 물건 값 | 지분 역수배 |
| A15 | 첫 필지에 「환지처분확정일 익일」을 켜면 기산일이 양도일로 붕괴 | 75,779,000 |
| A20 | 다필지 조기반환이 상류 STEP의 판정·평가 상세를 전부 버림 | 표시 전량 |

---

## 3. PR 매핑

| PR | 발견 ID | 성격 |
|---|---|---|
| **#1416** | A01 · A02 · A03 · A04 · A09 · A15 · A19 · A20 | 조기반환·배관. **8건 중 8건의 뿌리가 술어 중복**이었다 |
| **#1419** | A07 · A08 · A17 · A18 · A21 · A22 | 평가 산식 지분 안분 · 가업상속 · 결과 카드 표시 |
| **#1423** | A05 · A11 · A12 · A13 · A16 | ⏸ 미결 — 사용자 결정에 따라 전부 **명시 차단**으로 확정 |
| **#1431**(이 PR) | (신규) A23 · 인용 정정 · `inheritanceMode` 제거 | 인용 드리프트 · 죽은 필드 · 미해소 마커 |

**PR-1의 8건은 개별 가드가 아니라 단일 소스 술어 추출로 고쳤다** —
`transfer-pre1990-land-gate.ts` · `transfer-fb-gate.ts` 신설, `parcelEffectiveAcquisitionDate` 공용화.
원인이 「게이트 누락」이 아니라 **「같은 판정이 여러 곳에 복제되며 갈라진 것」**이었기 때문이다.

---

## 4. 🔴 리뷰의 처방을 실측으로 뒤집은 것 4건

### 4-1. A06 — 「양도비 축만 착수」가 구조적으로 불가능했다

리뷰 §6 배치는 「양도비 축만 착수, 자본적지출 축은 ⏸」이었다. 실측 결과 **분리가 안 된다**:
swap 자격 게이트(`|| landTransferExpense > 0`)는 비-PHD와 **공유**되는데, 두 경로가 나목 후보를
**다르게 구성**한다 — 비-PHD는 파트 칸 직접입력만 읽고, PHD는 자산 단위 `expenses`를 안분한다.
게이트를 열면 **안분된 자본적지출이 딸려 들어와** 90,660,639원이 움직인다(문서 실측 4~10M의 **10배**).
그건 ⏸로 남긴 A06-2를 조용히 채택하는 것이므로 **되돌리고 ⏸ 유지**했다.

### 4-2. A11 — 「Sum_A === Sum_F이면 항상 틀린다」가 참이 아니다

「소득세법 시행규칙」 §80①**2호** 구간(최초공시일 > 취득연도 다음 연도 말일)에서는 **비율 1이
법령상 정답**이다. 무조건 차단하면 새 오류를 만든다. ⇒ `classifySameAdjustmentPeriod`의
`clause_1` 구간에서만 차단하고, 그 경계를 **과차단 뮤테이션**으로 실증했다.

### 4-3. A22 — 범위를 넓혀야 했다

안분 지점 3종(주택·상가·토지)이 같은 객체의 같은 구조라 **하나만 맞추면 자산 종류별로 다른
규약**이 된다. 세 곳을 함께 `applyRatio`로 감쌌다.

### 4-4. A05 — 리뷰가 붙인 44,586,668원은 이 결함의 크기가 아니다

컴패니언 경로의 세액 영향은 **0원**이다(`calcSplitGain`이 그 앞에서 죽는다).
원 suggestedFix(2필드를 ④⑫⑭에 추가)는 **실측 no-op**이라 채택하지 않고, ⑤⑧ 차단으로 갔다.

---

## 5. 리뷰 밖에서 나온 것 — 인용 드리프트 2클러스터

리뷰가 「별건」으로 미뤄둔 §162 인용을 정정하다가 **같은 조문 축에서 두 번째 오류**를 찾았다.
memory `feedback_citation_drift_replicates_across_repo`(인용은 전역 복제된다)의 실례가 하나 더 늘었다.

### 5-1. ✅ 정정 — §162①6호 → §162①9호 단서 (6곳)

리뷰는 4곳으로 기록했으나 실제로는 **6곳**이었다 — `multi-parcel-transfer.ts`의
**:13(파일 헤더)과 :48(`ParcelInput.useDayAfterReplotting` JSDoc) 둘 다** 누락돼 있었다.
:48은 1차 정정에서도 놓쳤고 **역방향 grep으로 뒤늦게 잡혔다**.

조문 본문(KoreanLaw, 시행 2026-07-01) 확인 결과 **두 겹으로 틀렸다**:

| | |
|---|---|
| §162①**6호** 실제 | 「민법」 §245①에 의한 소유권 취득 = **점유를 개시한 날** (점유취득시효 — 환지와 무관) |
| §162①**9호 본문** | 환지처분으로 취득한 토지의 취득시기 = **환지 전의 토지의 취득일** |
| §162①9호 **단서** | 교부면적이 권리면적보다 **증감된 경우** 그 증감분 = **환지처분의 공고가 있은 날의 다음날** |

⇒ 저장소 주석 「환지처분 **확정일** 다음날을 취득일로 보는 특례」는 ①조문 번호가 틀렸고
②날짜 표현이 법문과 다르며(「공고가 있은 날」) ③**단서를 본칙처럼** 적어놨다.
상수를 `"소득세법 시행령 §162 ① 9호 단서"`로 바꾸고 본칙/단서 구조를 주석에 명시했다.

> ⚠️ **UI 라벨 「환지처분확정일」은 이번에 건드리지 않았다.** 법문은 「공고가 있은 날」이라
> 사용자가 입력할 날짜 자체가 달라지는 **의미 변경**이고, 필드명·검증 메시지·E2E·anchor
> 21곳에 파급된다. 별건으로 남긴다.
>
> ⚠️ `ParcelListInput`의 토글은 **필지 전체**에 익일 규칙을 적용하는데, 단서의 적용 범위는
> **증감 면적분**이다. 같은 저장소의 `CompanionAssetCardReplot.tsx:178`은 그 범위를
> 정확히 구현하고 있다(「증환지: 권리면적 초과분은 … 별도 취득」). **범위 불일치 가능성 —
> 미판정으로 남긴다.**

### 5-2. 🔴 A23 (신규·미해소) — §162의2는 「지하수개발ㆍ이용권」이다

`EXCHANGE_LAND_REDUCTION: "소득세법 시행령 §162의2"`(감환지 면적 산정)의 인용이 틀렸다.
§162의2의 실제 제목·본문은 **「지하수개발ㆍ이용권 등의 양도가액」**으로, 환지와 무관하다.
같은 저장소의 legal-verification manifest가 그 조문 키워드를 `["지하수개발ㆍ이용권", "토사석의
채취허가", "임목"]`으로 등록해 두고 있어 **파일이 자기 자신과 모순**돼 있었다.

- **오인용은 확정**(조문 본문 확인). **올바른 근거는 미확인**.
- 엔진이 적용하는 산식은 `종전토지면적 × 교부면적 / 권리면적`인데, §152(환지등의 정의)·
  §162①9호 어디에도 대응 명문이 없다. `legal_research`는 무관한 법령을 반환해 **검색 실패**.
- ⚠️ **추측으로 다른 조문을 넣지 않았다.** 상수에 마커만 달았다.
- 🔴 이 값은 `legalBasis`로 **결과 화면에 노출**되고 `LawArticleModal`이 법령 원문으로
  **링크까지 건다** — 누르면 「지하수개발ㆍ이용권」이 뜬다.
- 같은 오인용이 **UI·테스트 6곳에 복제**돼 있다(`ParcelListInput.tsx:292` ·
  `CompanionAssetCardReplot.tsx:41` · `AssetAreaSection.tsx:82` 주석 ·
  `multi-parcel-transfer.test.ts` 2곳 · `transfer-housing-area-basic-info.spec.ts` 주석).

### 5-3. ✅ 정정 — `phd-helper.ts`의 §164⑤ 자기인용 → §164⑦

조문 본문으로 산식이 갈렸다:

| | 산식 |
|---|---|
| **§164⑤** (건물) | 최초 고시 기준시가 **× 국세청장이 고시한 기준율** |
| **§164⑦** (주택) | 최초 공시 주택가격 **× (취득당시 가목+나목 합계 / 최초공시당시 가목+나목 합계)** |

`phd-helper`가 구현한 `P_A_est = P_F × Sum_A / Sum_F`는 **§164⑦**이다.
§164⑤가 등장하는 지점은 §164⑦ **후단의 준용**뿐이고 그 발동요건은 「나목의 가액이 없는 경우」다
(A13에서 이미 확정).

---

## 6. 미해소 3건 — 전부 법령 판단 선행

| ID | 내용 | 막힌 지점 | 마커 |
|---|---|---|---|
| **A06** | 자본적지출 안분 정본 | 저장소가 두 규약으로 갈림 — validate-split은 **차단**, 겸용 PHD 엔진은 **실제 안분**(F18 anchor P7-1·P7-2가 9억을 588,432,521/311,567,479로 동결) | `transfer-tax-validate-split.ts` |
| **A10** | 필지 순서가 세율 기산일 결정 (84,722,000원) | §104②·§104⑤ verbatim 확인했으나 **「한 자산 내 여러 필지」 명문 부재**. 현행은 1호도 2호도 아닌 제3의 값 | `transfer-tax-api.ts` |
| **A23** | §162의2 오인용 | 오인용은 확정, **올바른 근거 미확인** | `legal-codes/transfer.ts` |

**세 건 모두 소스에 ⏸/🔴 마커를 달았다** — 발견 ID로 역방향 grep이 걸린다.
(NBL 리뷰에서 확립한 규칙: 미해소 항목도 마커를 남긴다.)

### 사용자 결정으로 종결된 4건 (PR-3)

| ID | 선택 |
|---|---|
| A05 · A12 | ⑧에서 명시 차단 (컴패니언 미지원 조합) |
| A13 | ⑧에서 차단 (「선택」 라벨 → 필수) |
| A11 | 1차는 **탐지 + ⑧ 차단만** (B·D 입력 신설은 보류) |
| A16 | **미입력만** 차단 (명시 `"0"`은 허용) |

---

## 7. 리뷰 문서 자체의 정정

### 7-1. 「§164⑥·⑦의 괄호 단서는 조문 API로는 재현되지 않는다」 — **틀렸다**

원 리뷰 §10은 산식이 이미지라 법제처 `lsInfoR.do`의 `<img alt>`를 봐야 한다고 기록했다.
2026-09-03 재확인 시 **KoreanLaw `get_law_text`가 괄호 단서를 본문 텍스트로 그대로 반환**했다:

> 「…(취득당시의 가액과 최초로 공시한 주택가격 공시당시의 가액이 동일한 경우에는
> **제8항의 규정을 준용**한다)」

⇒ A11의 근거는 이제 **조문 API 단독으로 재현 가능**하다. 오프라인 아카이브·img alt 우회는 불필요.

### 7-2. 「§162①6호 오인용 4곳」 → **6곳**

`multi-parcel-transfer.ts` :13·:48이 누락돼 있었다.
⇒ 지금 `§162①6호`가 남은 곳은 `legal-codes/transfer.ts:224` 하나뿐이며,
그것은 「종전 표기가 왜 틀렸는지」를 설명하는 **의도된 참조**다.

### 7-3. A05의 44,586,668원

이 결함의 크기가 아니다(§4-4). 원 리뷰 §1-①이 이미 정정했으나 §0 요약 표에는 남아 있다.

---

## 8. 처리 완료된 별건 항목

| 항목 | 처리 |
|---|---|
| §162①6호 오인용 6곳 | ✅ §162①9호 단서로 정정 |
| `phd-helper.ts` §164⑤ 자기인용 | ✅ §164⑦로 정정 |
| 죽은 필드 `inheritanceMode` | ✅ 제거 (타입·factory·마이그레이션 2곳·읽기 1곳). 분기는 `computeMode`가 유일 산출 지점 |
| `transfer-tax-validate-asset.ts:62` 주석 어긋남 | ✅ PR-3에서 해소 |
| `multi-transfer-tax-api.ts:30` 주석 어긋남 | ✅ PR-1에서 해소 |
| 컴패니언 `standardPricePerSqmAtAcquisition` 채널 부재 | ✅ PR-3이 ⑤⑧에서 차단해 도달 불가. **채널 신설은 별건** |
| §162의2 오인용 | 🔴 A23으로 승격 — 미해소 |
| 일반 환산(비-pre1990) partial ⑤ 바인딩 | ✅ **§13에서 정정** — ④ 경유만으로는 **안전하지 않았다**(분자가 총액이라 ⑤가 결정한다) |

---

## 9. 교훈

1. **「양도비 축만」처럼 축을 쪼갠 처방은 게이트가 공유되면 성립하지 않는다.** A06은 게이트를
   여는 순간 보류하기로 한 축이 딸려 들어왔다. **처방을 배치로 쪼갤 때 게이트 공유 여부를 먼저 본다.**
2. **「항상 틀린다」류의 판정식은 반대 구간을 찾아봐야 한다.** A11은 §80①2호 구간에서 현행이
   정답이었다. 과차단 뮤테이션이 그 경계를 지킨다.
3. **인용 오류는 단독으로 존재하지 않는다.** §162①6호를 고치러 들어가 §162의2·§164⑤ 두 건이
   더 나왔다. 한 조문 축에서 하나를 찾으면 **인접 상수를 전수 대조할 것.**
4. **manifest가 이미 정답을 들고 있었다.** §162의2 오인용은 legal-verification manifest의
   키워드(`지하수개발ㆍ이용권`)와 legal-codes 주석(`환지 감환지`)을 나란히 놓자마자 드러났다.
   **두 소스가 같은 조문을 다르게 설명하면 그 자체가 신호다.**
5. **틀린 걸 확인했다고 아무거나 넣지 않는다.** A23은 오인용이 확정됐지만 올바른 근거를 못 찾아
   마커만 남겼다. 추측 인용은 원래 오류보다 나쁘다 — 다음 사람이 검증된 값으로 오인한다.
6. **검증자 사망을 「반증됨」으로 처리하면 결함이 조용히 사라진다.** 워크플로가
   `votes.length === 0`을 기각으로 분기해 A20이 탈락했다가 메인 루프가 복구했다.
7. **mutation probe 축은 에이전트별 격리 워크트리가 필요하다.** 87 에이전트를 한 워크트리에서
   돌려 소스가 실시간으로 바뀌었다(원 리뷰 §10 「워크트리 오염」).

---

## 10. 안전망

원 리뷰의 가장 중요한 부수 발견은 **probe 18회 중 15회가 반응 0건**이었다는 것 —
이 축에는 안전망이 사실상 없었다. 그래서 수정마다 anchor를 함께 세웠다.

| anchor 파일 | 건수 |
|---|---|
| `pre1990-land-gate.anchor.test.ts` | 12 |
| `family-business-cause-gate.anchor.test.ts` | 14 |
| `parcel-replotting-acq-date.anchor.test.ts` | 9 |
| `fb-imputed-capex-display.anchor.test.tsx` | 8 |
| `multi-parcel-unregistered-and-detail-echo.anchor.test.ts` | 7 |
| `sec163-9-clause-b-only.anchor.test.ts` · `inherited-acq-detail-dropped.anchor.test.ts` | 각 6 |
| `pre1990-partial-area` · `auction-clause2-path-gate` · `gift-deemed-bargain-price-required` · `phd-164-8-proviso-gate` · `companion-unsupported-gates` | 각 5 |
| `sec163-9-clause-b-ownership-ratio.anchor.test.ts` | 4 |
| **합계** | **107** |

### 기존 테스트가 결함을 고정하고 있던 사례 2건

계약을 고쳐야 했던 것들이다 — 그대로 뒀다면 수정이 빨간불이 됐다.

- `inherited-acquisition-reported-value-always.anchor.test.ts` L-04 — §164 경로 미완성을 동결
- `reduction-phd-helper.test.ts` — A13의 fallback과 A11의 §164⑧ 누락을 **동시에** 봉인

---

## 11. 이 리뷰가 닿지 않은 곳

원 리뷰 §10을 그대로 승계한다. 다음 리뷰 후보:

- **기준시가 산정 본체 무감사** — `building-standard-price.ts`(497) + helpers(693)
- **가산세·수정신고 축 0건**
- **재개발·입주권 본체** 약 2,300줄 (A19가 호출 지점만 봄)
- **겸용주택 9파일** — A06의 정본 판정에 직접 걸린다
- **일반건물(GB) 파일군** — A04가 GB 경로 71,242,600원을 발견했으나 파일군 자체는 미정독
- **E2E 0건 · 브라우저 수동 확인 0건** — 도달성 판정은 전부 코드 경로 추적 + 엔진 직접 호출
- **본문 미확인 조문 3건** — 「소득세법」 부칙(법률 제12169호) §12 · 「상속세 및 증여세법」
  §18의2① · 「소득세법 시행령」 §166


---

## 12. 미해소 3건 종결 (2026-09-03 · 법령 조사 후)

§6에서 「법령 판단 선행」으로 남겼던 3건을 KoreanLaw 조문·예규 조사로 확정했다.
**A06은 ⏸ 사유였던 전제 자체가 틀렸다.**

### 12-1. 🔴 A06 — 「저장소가 두 규약으로 갈려 있다」가 사실이 아니었다

원 리뷰 §9-4는 「`validate-split`은 **차단**하는데 겸용 PHD 엔진은 **실제 안분**한다」를
⏸ 사유로 들었다. **넷 다 확인해 보니 성립하지 않는다**:

| 검증 | 결과 |
|---|---|
| 「소득세법」 §100② 후문 verbatim | 「공통되는 **취득가액과 양도비용**」 — **자본적지출 미열거** |
| 시행령 §166⑥ | 「토지와 건물 등의 **가액의 구분이 불분명한 때**」의 안분만 — 자본적지출 위임 아님 |
| **F18 anchor의 실체** | P7-2 = 「필요경비 합계 == **나목**(자본적지출+양도비)」 · P7-3 = 「자본적지출·양도비 **어느 쪽이든 성립**」 ⇒ **§97②2호 나목 총액을 겸용 2파트에 배분**한 것이지 §100② 안분이 아니다 |
| `split-gain`의 자본적지출 안분 | **deprecated `expenses`**(legacy `directExpenses`) 경로에서만 발동. 신규 `capitalExpenditure`는 항상 0이고, UI도 legacy 필드를 **stale 세션에서만** 렌더한다 |

게다가 저장소는 **이미 근거를 화면에 달아 두고 있었다** — `AssetSectionExpense.tsx`가
「공통되는 필요경비는 자산 가액에 비례하여 안분 (§100② 후문 **유추** · **국심2005구1458**)」을 표시한다.

국세청 예규 **법인46012-2439**도 현행 규약과 같은 순서를 명시한다:
> 「**자본적지출액**이 있는 경우 … 어느 하나의 개별필지에 **귀속되는 것이 분명한 경우에는 해당필지**에
> 가산하고, 그 **귀속이 불분명한 경우에는** … 개별필지의 면적에 비례하여 **안분**계산한 금액을 가산」

⇒ **현행(「파트 칸에 직접 입력하라」)이 정답**이고, 정본 확정은 필요 없었다.

**구현한 것 — 양도비 축만**(사용자 결정): PHD split 경로가 `input.transferExpense`를
**읽지 않아 통째로 유실**되던 것을 비-PHD 경로와 같은 규약(양도가액 비례 + 잔액 흡수)으로 안분한다.
근거는 §100② 후문의 **명문 열거**다.

**실측**(Excel 정본 픽스처): 양도비 6억 → 토지분 swap 발동, 양도차익 **139,089,851 → 85,682,451**.
9억 → 양쪽 swap, **670,558,309 + 229,441,691 = 900,000,000 정확히 보존**.
자본적지출만 6억 → **무변동**(안분 대상 아님 — A06-4가 범위를 고정).

> ⚠️ **「변화 없음」이 「미배선」이 아니다.** §97②2호는 가목(환산+개산공제) ↔ 나목 **택일**이라
> 나목이 가목을 넘기 전까지는 양도비를 제대로 읽어도 세액이 안 움직인다. 작은 금액으로
> 검증하면 수정이 먹혔는지 알 수 없다 — anchor는 swap이 실제로 뒤집히는 구간에서 단언한다.

### 12-2. ✅ A10 — 순서 의존 제거 (사용자 결정)

- **§104②** verbatim: 보유기간 = 「**해당 자산의** 취득일부터 양도일까지」
- **§104⑤ 후단** verbatim: 「**한 필지의 토지**가 비사업용 토지와 그 외의 토지로 구분되는 경우」만
  별개 자산으로 본다 ⇒ **「한 자산 내 여러 필지」에 관한 명문은 여전히 없다**
- 예규 **재일46014-2353**: 취득시기가 다른 1필지 내 토지는 **부분별로 달리** 산정
- 예규 **법인46012-2439**: 취득시기 다른 필지 합병 시 **세율 구분적용을 위해** 필지별 안분
  (둘 다 구법·타법 — **유추**이지 직접 근거가 아니다)

⇒ 명문이 없는 「필지별 세율 구분」은 건드리지 않고, **입력 순서 의존만** 제거했다.
`representativeParcelAcquisitionDate`가 순서 무관하게 **가장 이른 실효 취득일**을 고른다.
늦은 날을 쓰면 보유기간이 짧아져 납세자에게 불리한데 **그렇게 볼 명문이 없기 때문**이다.

🔴 **기존 anchor 2건이 순서 의존을 함께 봉인하고 있었다.** A15-4는 `[환지, 일반]` → 2003-01-01을,
A15-6은 `[일반, 환지]` → 2005-04-10을 단언했다 — **같은 필지 집합을 순서만 바꿔 다른 값**을
고정한 것이다. A15-6을 순서 무관 anchor(A10-1·A10-2)로 정정했다.

### 12-3. ✅ A23 — 올바른 근거 확정 (§162①9호 단서)

국세청 예규 **재일46014-1813**(1998.09.22), **관련 법령란이 「소득세법 시행령 제162조」**:
> 「환지처분으로 환지면적이 권리면적에 미달하여 환지청산금을 교부받는 부분은 토지가
> **유상으로 이전되는 것이므로 양도에 해당**되며, 이 경우 **양도시기는 소득세법 시행령 제162조**의
> 규정에 의하는 것이고 양도가액은 구획정리 후 양도당시 기준시가 또는 실지거래가액으로 하는 것임.」

⇒ 상수를 **「소득세법 시행령 §162 ① 9호 단서」**로 정정하고, 복제된 오인용을 **전수 정정**했다
(상수 1 + 엔진 4 + UI 3 + store 2 + validate 1 + schema 1 + 테스트 2 + E2E 1 = **15곳**).

🔑 **저장소가 정답을 두 곳에 갖고 있었다** — legal-verification manifest는 §162의2를
`["지하수개발ㆍ이용권","토사석의 채취허가","임목"]`으로 등록했고,
`stock-transfer/lot-allocation.ts:15`는 「**시행령 §162의2는 지하수개발권 조문**」이라고
적어 두었다. 그런데 양도세 축은 같은 조문을 「환지 감환지」로 불렀다.
**두 소스가 같은 조문을 다르게 설명하면 그 자체가 신호다.**

### 12-4. 안전망

신규 anchor **`phd-transfer-expense-apportion.anchor.test.ts` 5건** + A15 파일 정정 2건.
뮤테이션 probe로 포착을 실증했다 — PHD 경로 되돌림 → A06 **3건 RED**(A06-3·A06-4는 기준선·범위
고정이라 GREEN이 정상), `parcels[0]` 복원 → A10 **2건 RED**, §162의2 복원 → MP-9 **1건 RED**.

> ⚠️ **뮤테이션을 첫 occurrence에 넣었다가 오판할 뻔했다.** 비-PHD와 PHD 경로가 같은 변수명을
> 쓰기 때문에 `replace(..., 1)`이 **비-PHD를 쳤고** A06 anchor는 GREEN으로 남았다.
> 「anchor가 반응하지 않는다」를 곧바로 「anchor가 약하다」로 읽지 말 것 —
> **뮤테이션이 의도한 지점에 실제로 들어갔는지 먼저 확인**한다.

### 12-5. 별건으로 남긴 것

- **`stock-transfer-exempt-result.ts:81`의 「의제취득일 §162의2」** — 주식 도메인의 별개 오인용으로
  보인다(의제취득일은 §162⑥·⑦ — 법률 제4803호 부칙 §8 위임). **이 축의 담당 범위 밖이라 보고만 한다.**
- **UI 라벨 「환지처분확정일」** — 법문은 「공고가 있은 날」. 입력 날짜의 의미가 바뀌고 21곳 파급.
- **`ParcelListInput` 토글의 적용 범위** — 단서는 증감 면적분 한정인데 필지 전체에 적용한다.
  `CompanionAssetCardReplot.tsx:178`은 범위를 정확히 구현하고 있어 **불일치 가능성** — 미판정.
- **A06-2 · 필지별 세율 구분** — 둘 다 명문 부재로 착수하지 않았다(위 근거 참조).


---

## 13. §9-7 종결 (2026-09-03) — 「④가 덮는다」는 전제가 틀렸다

### 13-1. 결함

⑤ `CompanionAcqPurchaseBlock`의 **취득시** `StandardPriceInput`이
`area={props.acquisitionArea}`(취득 **전체** 면적)를 곱해 총액을 파생했다.
**양도시** 칸은 `transferArea`(양도분)를 곱하므로 일부양도에서 **분자만 부풀었다**.

**엔진 실측**: 총세액 **27,827,432 → 79,199,706 = 51,372,274원 과소과세**
(전체 200㎡ 중 100㎡ 양도 · 취득단가 100만/㎡ · 양도단가 300만/㎡ · 양도가액 5억).

### 13-2. 🔴 왜 남아 있었나 — 계획서가 「고쳤다」고 기록하고 있었다

`transfer-partial-area-apportionment.plan.md` §1.1은 이 경로를
「`land` 일괄 (축 A 토지) … ✅ **B4-1 정정**」으로 표기했다. **틀렸다**:

| | |
|---|---|
| B4-1이 고친 것 | 엔진 `acquisitionArea` (`resolveAcqAreaForStdPrice`) |
| 그 값을 소비하는 곳 | **split 경로뿐** — `transfer-tax-split-gain.ts:54` (`perSqm × acquisitionArea`) |
| 비-split **일괄** 경로의 환산 분자 | **총액** `standardPriceAtAcquisition` — ⑤ 위젯이 만든다 |

**④ body 실측(정정 전)**: `standardPriceAtAcquisition=200,000,000`(전체 200㎡) ·
`standardPriceAtTransfer=300,000,000`(양도분 100㎡) · `acquisitionArea=100`(B4-1 ✅)
⇒ **면적은 고쳐졌는데 총액은 안 고쳐진** 상태였다.

### 13-3. 계획서가 기각했던 방향이 사실은 정답이었다

계획서 §165는 「**채택하지 않은 안**: UI 파생(표시·계산 이원화를 만든다)」이라 적었다.
그 기각은 **④가 이 경로를 덮는다는 전제** 위에 있었다. 전제가 틀렸으므로 기각도 성립하지 않는다 —
⑤가 곱하는 면적을 ④와 **같은 술어**로 맞추면 표시와 계산이 오히려 **일치**한다(이원화가 아니라 해소).

⇒ 술어 `usesTransferAreaForAcqStdPrice`를 `resolveAcqAreaForStdPrice`에서 추출해
**④와 ⑤가 공유**하게 했다. 갈릴 수 없는 구조로 만든 것이 수정의 핵심이다.

### 13-4. 안전망 — 자매 anchor 2종이 필요했다

| 파일 | 건수 | 무엇을 막나 |
|---|---|---|
| `partial-acq-std-price-ui-binding.anchor.test.ts` | 6 | 술어 · 산술 · 세액 규모 |
| `partial-acq-std-price-area-binding.anchor.test.tsx` | 3 | **JSX 바인딩 자체** |

> 🔑 **산술 anchor만으로는 부족했다.** 정작 결함이던 것은 `area={...}` **한 줄**이고,
> 그 줄을 되돌려도 산술 anchor는 **전부 통과**한다. 컴포넌트 anchor를 붙여 실측으로 확인했다 —
> 바인딩 되돌림 → **2건 RED**, 술어 무력화 → **8건 RED**.

> ⚠️ **S97-3은 뮤테이션에 반응하지 않는다** — ④와 ⑤를 같은 술어로 비교하므로 술어가
> **통째로 틀려도 일관되게 통과**한다. 그 테스트의 목적은 「발산 탐지」이지 「정합성 증명」이 아니다.
> 이 한계를 테스트에 명시했다.

> ⚠️ **셀렉터 함정** — `same` 모드에서는 취득시·양도시 위젯이 **둘 다** 「면적 (㎡)」 라벨을
> 렌더해 `getByText`가 strict mode 위반을 낸다. 「취득시 기준시가」 섹션으로 범위를 좁혔다.

### 13-5. 교훈

**「어느 층을 고쳤다」가 「그 층이 소비된다」를 뜻하지 않는다.**
B4-1은 실제로 `acquisitionArea`를 정확히 고쳤다 — 다만 그 값을 읽는 경로가 split뿐이었다.
정정 후에는 **그 값을 실제로 읽는 지점**까지 따라가 확인해야 한다.
계획서의 ✅ 표기를 신뢰해 넘겼다면 이 51,372,274원은 계속 남았을 것이다.


---

## 14. 최종 결산 (2026-09-03)

리뷰 §9의 ⏸ **7건 전건 종결**.

| 항목 | 처리 | PR |
|---|---|---|
| A05 · A12 · A11 · A13 · A16 | 사용자 결정 → 명시 차단 | #1423 |
| **A06** | ⏸ 사유가 **전제 오류**로 소멸 · 양도비 축 구현 | #1433 |
| **A10** | 순서 의존 제거 (필지별 세율 구분은 명문 부재로 미착수) | #1433 |
| **A23**(신규) | §162의2 오인용 → §162①9호 단서 (15곳) | #1433 |
| **§9-7** | ⑤ 바인딩 정정 — 51,372,274원 과소과세 | #1436 |

**소스 ⏸ 마커 잔존 0건** — A06 마커는 #1436 이후에도 「미해소」로 남아 있어 별도 정정했다
(문서는 종결인데 소스는 미해소라 **자기모순**이었다 — 이 리뷰가 반복해 지적한 패턴이 스스로에게도 났다).

### 두 번 반복된 실패 패턴

이번 2차 처리에서 **⏸ 사유 4건 중 2건이 「전제가 틀렸다」**로 끝났다:

| | ⏸ 사유였던 전제 | 실제 |
|---|---|---|
| **A06** | 「저장소가 두 규약으로 갈려 있다」 | F18 anchor는 §100② 안분이 아니라 **§97②2호 나목 배분** — 충돌 없음 |
| **§9-7** | 「④가 덮으니 안전하다」 | 비-split 일괄의 분자는 **총액**이라 ⑤가 결정 — ④는 닿지 않는다 |

⇒ **⏸으로 미루기 전에 그 전제부터 실측할 것.** 두 건 다 「확정할 것이 없었다」로 끝났고,
미룬 동안 §9-7의 51,372,274원은 계속 살아 있었다.

### 안전망 총계

| PR | 신규 anchor |
|---|---|
| #1416 · #1419 · #1423 | 107 |
| #1433 (A06) | 5 |
| #1436 (§9-7) | 9 (산술 6 + **JSX 바인딩 3**) |
| **합계** | **121** |

전부 mutation probe로 포착을 실증했다. 다만 두 번 다 **처음 세운 anchor가 결함을 못 봤다**:

- A06 — 뮤테이션이 비-PHD 경로에 들어가 anchor가 GREEN (**뮤테이션 위치 오류**)
- §9-7 — 산술 anchor는 JSX 한 줄 되돌림에 반응하지 않음 (**anchor 층위 오류**)

⇒ **「anchor가 반응하지 않는다」를 곧바로 「결함이 없다/anchor가 약하다」로 읽지 말 것.**
먼저 ①뮤테이션이 의도한 지점에 들어갔는지 ②anchor가 그 층을 실제로 관측하는지를 확인한다.

# 비사업용 토지(NBL) 코드리뷰 — 결과 요약

> 원 리뷰 [`nbl-code-review-2026-09.md`](nbl-code-review-2026-09.md) · 배치 계획 [`../00-pm/nbl-review-fix-batching.plan.md`](../00-pm/nbl-review-fix-batching.plan.md)
> 리뷰 기준 `master` **a30833f3** (2026-09-02) → **종결 `01015ee2`** (2026-09-03)

## ✅ 종결 (2026-09-03)

**확정 결함 78건 · 별건 2건 전건 처리 · 보류 3건도 2026-09-14에 전건 종결**([§6](#6-근거-미확보로-남긴-3건)).
레거시 임계 0.8의 지목 한정(부칙이 아니라 구법 본문이 갈랐다) · 강원·전북특별자치도의 군
(제주법 §10① 실측) · **V5-d 임야 편입유예 미상 기본값**(원칙/예외 구조로 확정).

**종결 판정의 근거는 「배치」가 아니라 「구현」이다** — 리뷰 문서의 `^### <ID>` 헤더 **87건**을
뽑아 `lib components app __tests__ e2e`에서 역방향 grep한 결과 **미참조 0건**이다.

```bash
grep -o '^### [A-Z0-9-]*' docs/reviews/nbl-code-review-2026-09.md | sed 's/^### //' | sort -u \
  | while read id; do
      [ "$(grep -rl "$id" lib components app __tests__ e2e 2>/dev/null | wc -l)" -eq 0 ] && echo "미참조: $id"
    done
```

> 🔴 **최초 판은 「확정 78건 전건 처리」라고 적었으나 그때 12건이 미해소였다.** 근거로 삼은 것이
> 「어느 PR에 **배치**됐는가」였기 때문이다. 위 역방향 측정을 처음 돌렸을 때 그 12건이 나왔고,
> 그래서 **처리한 자리마다 발견 ID를 주석으로 남기는 것**이 이 문서의 규약이 됐다 — 주석이
> 없으면 다음 사람도 같은 착각을 반복한다([§1](#1-결과) 대조표 · [§8](#8-교훈) 7·8번).

---

## 1. 결과

| | 건수 | 상태 |
|---|---|---|
| 확정 결함 | **78** | ✅ 처리 78 — 1차 66 + 2차 12([§9](#9-미해소-12건--재측정과-처리-2026-09-02)) |
| 기각 (1차 3 + 2차 7) | 10 | 재제안 금지로 문서화 |
| 미확정(UNCERTAIN) | 2 | COV-10 **종결** · V5-d **결론** |
| 별건(설계상 후속) | 2 | ✅ [§10](#10-e2-09-후속--결격-과세기간을-실제로-차감한다-1429) 조특령 §66⑭ · [§11](#11-별표6-3호바-표까지-엔진이-산출한다-1435) 별표6 3호바 표 |
| **근거 미확보 보류** | **2** | ⏸ 착수 조건 미충족 — [§6](#6-근거-미확보로-남긴-3건). 종전 3건 중 **레거시 임계 0.8은 2026-09-14 해소** |

**PR 17개**(1차 10 + 정정·2차 5 + 별건 2) · 신규 테스트 **28파일**(anchor 27 + E2E 1).

> ⚠️ **두 측정은 서로 다른 것을 잰다.**
>
> | 측정 | 무엇을 재나 | 결과 |
> |---|---|---|
> | 계획서 ID ↔ 리뷰 ID 차집합 | 발견이 **어느 PR에 배치됐는가** | 미배치 2건(COV-10 · U1-02) |
> | 리뷰 ID ↔ 코드·테스트 grep | 발견이 **실제로 구현됐는가** | 미참조 15건 → 개별 확인 후 **미해소 12건** |
>
> 최초 판은 앞의 측정만 하고 「전건 처리」라고 적었다. **배치는 구현이 아니다** — 한 PR에
> 여러 ID를 묶으면 그중 일부가 손대지 않은 채 남고, ID 대조는 그것을 잡지 못한다.
>
> ✅ **지금은 뒤의 측정이 통과한다** — 리뷰 헤더 ID **87건 전건이** 코드·테스트에서 참조된다
> (`for id in …; do grep -rl "$id" lib components app __tests__ e2e; done` → 미참조 0).
> 처리한 자리마다 **발견 ID를 주석으로 남기는 것**이 이 측정을 가능하게 한다.

---

## 2. 세액이 실제로 틀어졌던 것 (실측)

| 결함 | 실측 세액차 | 방향 | PR |
|---|---|---|---|
| **A1-01** 기타토지 재산세 분류 미선택이 매퍼에서 조용히 「종합합산」으로 | 76,725,000원 | 과대 | #1392 |
| **V7-b** 목장 §104의3①3호 단서를 축산 영위 시 건너뜀 | 76,548,532원 | 과대 | #1394 |
| **V10-f** 지분분할 일괄양도에서 NBL 플래그 미승계 | 74,661,400원 | 과소 | #1392 |
| **A3-01 · V9-a** ⑧ NBL 검증이 조기 return 뒤에 있어 도달 불가 | 65,550,000원 | 과대 | #1392 |
| **V2-b** 기간 배열 빈 행 침묵 drop (농지·목장·별장) | ±57,150,000원 | 양방향 | #1392 |
| **H-2** 2016.1.1. 전 양도분에 장특공제 적용 (구 §95② 배제 미구현) | 44,000,000원 | 과소 | #1396 |
| **E6-01** 일괄양도에서 상가 부수토지 초과분 중과 소실 | 11,683,750원 | 과소 | #1396 |
| **V10-a** 컴패니언 자산 NBL 중과 미전송 | 4,263,600원~ | 과소 | #1392 |

**COV-1**(구법 임계 0.8이 프로덕션 경로에서 도달 불가)은 판정 자체를 뒤집으나 금액을 산출하지 않았다 — #1403.

---

## 3. PR 매핑

| PR | 머지 | 내용 | 발견 |
|---|---|---|---|
| [#1392](https://github.com/kwonohjik/Property-related-Taxes/pull/1392) | `ca601966` | ⑧ 검증 게이트 + `lib/calc` 배선 (**엔진 무변경**) | 23 |
| [#1394](https://github.com/kwonohjik/Property-related-Taxes/pull/1394) | `881cee8f` | 지목 판정 엔진 + 대응 UI | 46 |
| [#1396](https://github.com/kwonohjik/Property-related-Taxes/pull/1396) | `64d8b6b0` | §95② 연혁 + 일괄양도 중과 | 15 |
| [#1398](https://github.com/kwonohjik/Property-related-Taxes/pull/1398) | `265c10bf` | 재촌 일반구 — **양도세·상속세 동시** | 4 |
| [#1400](https://github.com/kwonohjik/Property-related-Taxes/pull/1400) | `7f70ed72` | 도시지역 지역열거 + 보전녹지 (신규 입력 축) | 2 |
| [#1403](https://github.com/kwonohjik/Property-related-Taxes/pull/1403) | `b2f3b49c` | 안전망 — 구법 임계 도달성 · 시드 · E2E | 5 |
| [#1407](https://github.com/kwonohjik/Property-related-Taxes/pull/1407) | `3cbcba19` | 신고서 ③세율구분 코드 + ⑨세율 행 | COV-10 |
| [#1409](https://github.com/kwonohjik/Property-related-Taxes/pull/1409) | `b3cd10f0` | 복원 자산 `assetId` · rowOrder 가드 · CI 함정 | 파생 |
| [#1413](https://github.com/kwonohjik/Property-related-Taxes/pull/1413) | `bd178a63` | 이력 복원 ↔ factory 격차 제거 (COV-6 근본) | 파생 |
| [#1417](https://github.com/kwonohjik/Property-related-Taxes/pull/1417) | `22a0fbbb` | U1-02 거리 스냅샷 + V5-d 정책 명시 | 2 |
| [#1420](https://github.com/kwonohjik/Property-related-Taxes/pull/1420) | `35e6e622` | 이 요약 문서 (초판) | — |
| [#1422](https://github.com/kwonohjik/Property-related-Taxes/pull/1422) | `ea2a1c67` | 🔴 **「전건 처리」 정정** + 미해소 12건 확정·배치 | — |
| [#1424](https://github.com/kwonohjik/Property-related-Taxes/pull/1424) | `da2377c1` | 표시·인용·안내 (엔진 무변경) | 7 |
| [#1426](https://github.com/kwonohjik/Property-related-Taxes/pull/1426) | `43afb5a2` | 배관·게이트 (엔진 무변경) | 2 |
| [#1427](https://github.com/kwonohjik/Property-related-Taxes/pull/1427) | `49539464` | 별표6 3호바 10% 상한 — **세액 이동** | 1 |
| [#1428](https://github.com/kwonohjik/Property-related-Taxes/pull/1428) | `0a3dc6d0` | 발견 ID 마커 + 문서 갱신 — 역방향 grep 87건 통과 | 4 |
| [#1429](https://github.com/kwonohjik/Property-related-Taxes/pull/1429) | `c731bfb0` | 조특령 §66⑭ 결격 과세기간 입력 — **엔진 차감** | 별건 |
| [#1435](https://github.com/kwonohjik/Property-related-Taxes/pull/1435) | `01015ee2` | 별표6 3호바 **표** 자동 산출 (비고 2-나·다·라) | 별건 |

**배치 순서의 근거**: PR-1은 엔진 무변경이라 실패 위험이 가장 낮고, 잘못된 입력이 차단되기 시작해 PR-2의 anchor 작성이 쉬워진다. PR-3은 지목 판정이 안정된 뒤라야 합산·장특 축의 회귀 원인을 가릴 수 있다. PR-4(상속세 동반)·PR-5(14지점 신규 필드)는 **회귀 축이 달라** 합치면 CI가 빨개졌을 때 원인을 가릴 수 없다.

---

## 4. 🔴 리뷰 자체가 틀렸던 것

리뷰 산출물을 그대로 채택했다면 **새 결함을 만들었을** 지점들이다.

### 4-1. 배경 전제 오류 — 「비사업용 토지는 장기보유특별공제 배제」

리뷰 착수 시 배경 규칙으로 제시했으나 **현행법과 어긋난다**. §95②의 제외 열거는 **미등기양도자산(§104③)과 §104⑦ 각 호(다주택 중과)뿐**이고, 비사업용 토지는 §104①8호라 열거에 없다 — **표1 공제가 그대로 적용**된다.

2차 검증 에이전트가 잡아냈고 메인 루프가 조문 본문으로 재확인했다. 파급:

- 1차 발견들의 세액영향에 계상된 「장특 배제분」은 **과대 계상**이었다(+10%p 부분만 유효).
- 반대로 **과거 양도분에는 배제가 실재했고 그것이 미구현**이었다 → H-2(44,000,000원).

### 4-2. 처방의 판별식이 틀림 — PR-4 일반구 판정

리뷰 처방은 일반구를 `code[3:5] !== "00"`으로 가르라고 했다. 실측하니 **256건 중 238건**이 걸려 서울 종로구(11110)·의정부시(41150)까지 오탐한다. 대안 「끝자리 ≠ 0」도 12건 오탐.

정본은 **명칭**이었다 — `fullName`이 3토큰(도 + 시 + 구)인 것이 정확히 **39건 · 13개 시**, 반례 0. 그대로 구현했다면 자치구까지 접혀 **반대 방향의 과대적용**이 됐다. 원 리뷰 문서에 정정 블록을 남겼다.

### 4-3. 착수 조건이 미해소인 채였던 것 — COV-1

처방에 「⚠️ 0.8 적용 여부는 개정 전 §168의6 본문을 확인한 뒤 결정 — **확인 전 numeric 변경 금지**」가 걸려 있었고, 리뷰는 그 조회에 실패한 상태였다. 시행일별 MST로 직접 받아냈다:

| 시행본 | §168의6 각 호 다목 |
|---|---|
| 2014.03.11. (MST 151843) | 「소유기간의 **100분의 20**」 |
| 제26067호 [시행 2015.02.03] (MST 168102) | 「**100분의 40**」 |

⇒ 레거시 0.8이 **법적으로 옳다**는 것이 확인돼 배관을 고칠 수 있었다. 같은 조회에서 **경계 상수가 하루 어긋난 것**(`2015-02-02` strict `<` → 구법이 적용될 하루를 신법으로)도 함께 드러났다.

---

## 5. 리뷰 밖에서 나온 것

작업 도중 발견해 함께 처리했다. 원 리뷰에 없던 항목들이다.

| 발견 | 내용 | PR |
|---|---|---|
| 세율 표시 이중 계상 | 상세명세서가 `appliedRate + surchargeRate`로 찍어 비사업용 토지 실효 **55%를 65%로** 표시. `appliedRate`는 이미 중과 포함(`baseRate + additionalRate × ratio`) | #1407 |
| 복원 자산 `assetId` 누락 | 중복 판정 `other.assetId !== a.assetId`가 `undefined !== undefined` → **false** → 서로 다른 자산을 같은 자산으로 취급 | #1409 |
| `migrateAsset` ↔ factory 격차 | factory 462키 중 복원 결과에 없는 것 **147개**. COV-6(16필드)·`assetId`가 모두 이 뿌리 | #1413 |
| rowOrder 복제 | 합산 빌더가 별도 배열을 가져, 단건만 고치면 **합계 단언이 공허하게 통과** | #1409 |
| `gh pr checks --watch` exit 0 | PR 생성 직후 부르면 큐 등록 전이라 감시 대상이 없어 성공 종료. 그 시점 롤업은 conclusion이 **전부 빈 문자열** | #1409 (CLAUDE.md) |

---

## 6. 근거 미확보로 남긴 3건

셋 다 **조문 문언만으로는 갈리지 않아** 멈춘 것이었고, **2026-09-14에 전건 종결**됐다.
셋 중 둘은 착수 조건으로 적어둔 자료가 **애초에 답을 가진 문서가 아니었다**.

| 항목 | 현행 | 바꾸는 방향 | 착수 조건 |
|---|---|---|---|
| ~~**V5-d** 임야 편입유예 미상 기본값~~ | — | — | ✅ **2026-09-14 확정** — 아래 |
| ~~레거시 임계 0.8의 **지목 한정**(농·임·목만)~~ | — | — | ✅ **2026-09-14 해소** — 아래 |
| ~~**강원·전북특별자치도의 군**이 §104의3① 지역열거 대상인지~~ | — | — | ✅ **2026-09-14 해소** — 아래 |

### ✅ 레거시 임계 0.8의 지목 한정 — 해소 (2026-09-14)

착수 조건이던 **제26067호 부칙**을 법제처 원문으로 확보했다. 그런데 판정을 가른 것은 부칙이
아니라 **구법 본문**이었다.

| 확인한 것 | 결과 |
|---|---|
| [시행 2015.01.01. 대통령령 제24356호] §168의6 | 각 호 다목(3호는 나목) 「소유기간의 **100분의 20**」 · **지목 구분 없음** |
| [시행 2015.02.03. 대통령령 제26067호] §168의6 | 같은 자리 「소유기간의 **100분의 40**」 · **지목 구분 없음** |
| 제26067호 **부칙 전문**(제1조~제23조) | §168의6 개별 적용례·경과조치 **없음** ⇒ 제2조②만 적용 |

§168의6은 법 §104의3① **각 호 외의 부분**의 「대통령령으로 정하는 기간」을 정의하는 조문이라
애초 지목별로 갈릴 자리가 아니다. 부칙 제2조②(「이 영 중 양도소득에 관한 개정규정은 이 영
시행 이후 최초로 양도하는 분부터 적용한다」)가 지배하므로 **경계는 시행일 2015-02-03 하나**다.

⇒ `getThresholdRatio`에서 `categoryGroup` 매개변수를 **제거**했다(남겨두면 같은 분기가 다시
들어온다). 실측 영향:

- **실질 관문은 별장 하나다.** `meetsPeriodCriteria`를 부르는 judge는 5개지만 기타토지는
  `fullPeriod`(전 보유기간)를 넘겨 비율이 항상 1이라 임계와 무관하게 통과하고, 농·임·목은
  종전에도 0.8이었다.
- 별장 비별장기간 **75%·2014-06-01 양도**: 종전 REDIRECT(다른 지목 재판정) → **별장 확정**.
  85%는 구법 임계를 넘어 종전대로 REDIRECT(회귀 가드 LT-5).
- anchor `legacy-threshold-all-categories.anchor.test.ts` 5건. 뮤테이션(종전 동작 재현)에서
  LT-1·LT-3 RED 확인.
- ⚠️ **기존 테스트 1건이 결함을 지키고 있었다** — `period-criteria.test.ts`의
  「기타 지목은 2015.2.3. 전이어도 0.6」이 근거 없는 분기를 고정하고 있어 법령 정합으로 정정했다.

### ✅ 강원·전북특별자치도의 군 — 해소 (2026-09-14)

「특별자치도 전환 관련 해석」을 착수 조건으로 걸어두었으나, **해석례를 기다릴 문제가 아니었다** —
관련 법률 3건의 본문이 그대로 답이었다.

| 확인 (법제처 직접 조회) | 내용 |
|---|---|
| 법 §104의3①1호나목 (mst 280405, 시행 2026.1.1.) | 괄호는 「**제주**특별자치도 … §10②에 따라 설치된 **행정시의 읍·면지역**」만 제외 |
| 제주특별자치도법 §10① (mst 283901) | 「제주자치도는 … 그 관할구역에 **지방자치단체인 시와 군을 두지 아니한다**」 |
| 강원특별자치도법 §7② (mst 286505) | 「관할구역은 **종전의 강원도의 관할구역**으로 한다」 |
| 전북특별자치도법 §8② (mst 286593) | 「관할구역은 **종전의 전라북도의 관할구역**으로 한다」 |

열거의 「특별자치도」 괄호가 **행정시의 읍·면지역만** 제외한 이유는, 그 조문이 상정한
특별자치도(제주)에 **군이 존재할 수 없기** 때문이다 — 군을 열거에 넣겠다는 의사를 표시한 적이
없다. 강원·전북은 종전 도의 관할구역·시군 체계를 그대로 승계했으므로 그 안의 군은 **종전
「도의 군」 그대로**다. 명칭 변경만으로 지역기준 대상이 되어 불리해질 근거가 없다.

⇒ **세액 변동 0.** `urban-region-scope.ts:52`의 `endsWith("군")`이 이미 시도를 가리지 않고
대상 밖으로 처리하고 있었다 — 바뀐 것은 **그 동작이 확정됐다는 사실**과 안전망이다.

- anchor 5건 추가(leaf 3 + judge 2 — 강원 홍천군 51720·전북 완주군 52710).
- **뮤테이션**: 군 분기에서 시도코드 51·52를 빼면(= 특별자치도의 군을 열거 안으로) 신규 **3건 RED**,
  기존 2,985건은 **전원 통과**했다 ⇒ 종전엔 이 축을 지키는 안전망이 **0건**이었다.
- 구별력 짝으로 「같은 특별자치도라도 **시**(춘천 51110)는 읍·면/동으로 갈린다」를 함께 고정했다.

### ✅ V5-d 임야 편입유예 미상 기본값 — 확정 (2026-09-14)

「§168의9①2호 단서의 **입증책임 배분** 판례·예규」를 착수 조건으로 걸어두었으나, 이건
앞의 두 건과 성격이 달랐다 — **조문이 정하지 않은 것**이라 본문을 더 읽어도 안 닫힌다.
⇒ 자료를 더 기다리는 대신 **원칙/예외 구조에서 배분을 도출**했다.

| 조문 층위 (mst 280405 실측) | 역할 |
|---|---|
| 법 §104의3①2호 **본문** 「임야」 | **원칙 = 비사업용** |
| 같은 호 **가목** | 예외를 연다(공익·산림 보호육성) |
| 영 §168의9①2호 | 그 예외의 **내용**(산지 안의 임야 + 시업 중 등) |
| 그 **단서** 「편입된 날부터 N년이 경과한」 | 예외의 **한계** |

⇒ **사업용(= 예외)을 주장하는 쪽이 그 요건사실을 뒷받침한다.** 편입일은 예외가 유지되는지를
정하는 요건사실이므로 **미상이면 예외를 세울 수 없어 원칙인 비사업용으로 확정**한다.
농지·목장과 결과는 같지만 **경로가 다르다** — 농지·목장은 「유리한 예외 미입증」, 임야는 「원칙 복귀」.

실무 실증 — **조심 2022서8116**(2023.9.12., 양도): 쟁점 자체가 「비사업용 토지에 해당하지
않는다는 **청구주장**의 당부」로 구성되고, 청구인이 산림경영계획인가서·산림청 회신·임대차계약서를
대어 §168의9①2호가목 예외를 세웠다(경정청구 거부처분 취소).
⚠️ 이 재결례는 **단서(N년 경과)의 입증책임을 정면으로 다루지 않는다** — 구조와 실무를 보여줄
뿐이므로 그 이상으로 인용하지 말 것.

⇒ **세액 변동 0.** 코드는 이미 그렇게 동작하고 있었고(`checkForestIncorporationGrace`),
바뀐 것은 「근거 확보 전 변경 금지」 → **⛔ 재검토 금지**라는 상태 표지다.
**뮤테이션**: 미상 기본값을 `isApplied: true`로 뒤집으면 **4건 RED**(V5-d anchor 2 + 공용 유닛 1 +
목장 판정 1) ⇒ 이 확정은 실제로 안전망이 지킨다.

### V5-d 배경 상세

리뷰는 「임야만 §168의9①2호가 단서로 과세측 재포섭 구조」라고 지적했다. 확인 결과:

- **계산은 공유가 맞다** — 2014 시행본에서 §168의8⑥ 「2년」 / §168의9①2호 단서도 「편입된 날부터 **2년**이 경과한」이었고, 제26067호에서 셋 다 3년으로 **함께** 개정됐다. 문언 방향은 반대지만(「지나지 아니한」 제외 / 「경과한」 제외) 귀결이 「N년 이내 → 사업용」으로 같다.
- **갈리는 곳은 미상 기본값 하나**다. 농지·목장은 날짜가 **납세자 유리 예외**라 미입증 시 예외 미적용이 자연스럽지만, 임야는 「N년 경과」가 **과세를 되살리는 적극적 사실**이다.

⇒ `checkForestIncorporationGrace` 래퍼로 분리해 현행 기본값이 **의도된 선택**임을 못 박고, 공용 함수 주석에 임야가 이 경로를 쓴다는 사실과 연혁 실측 근거를 적었다. 실무 노출은 좁다 — ⑧ 검증이 도시지역 임야에 편입일을 **필수로 차단**하므로 미상 조합은 API 직접 호출·구 이력 복원에서만 생긴다.

---

## 7. 재발 방지 장치

리뷰가 지적한 **안전망 부재**(착수 시 NBL 전용 테스트 40파일 447건이 전원 통과하던 상태)에 대한 조치다.

| 장치 | 무엇을 막나 |
|---|---|
| `__tests__/db/nbl-judgment-rules-reachability.anchor.test.ts` | DB/시드 경로가 판정 규칙 그룹을 떨어뜨리는 것 · 중과율 키 소실 |
| `e2e/transfer-nbl-surcharge-amount.spec.ts` | UI 토글 → raw → Zod → route → 엔진 게이트 전 구간에서 +10%p 소실 |
| `__tests__/lib/stores/migrate-factory-parity.anchor.test.ts` | factory에 필드를 추가하고 migrate를 잊는 것 |
| `__tests__/components/filing-form-roworder-parity.anchor.test.ts` | 두 rowOrder 중 한쪽에만 행을 추가하는 것 |
| `__tests__/lib/calc/nbl-land-division-plumbing.anchor.test.ts` | ⑫Zod·⑭엔진 매핑 침묵 stripping |

**뮤테이션으로 구별력을 확인했다** — 각 anchor마다 해당 로직을 되돌려 실제로 실패하는지 검증했고, 그 결과를 각 PR 본문에 남겼다.

### 기존 테스트가 결함을 고정하고 있던 사례

PR-2에서 4건이 나왔다. 예: `nbl-result-card-render.test.tsx`가 **별표 1의3에 없는 축종**(`"hanwoo"`)을 써서 자동산출이 0을 반환하는 데 의존해 초과분을 만들고 있었다. 전부 조문 인용을 붙여 정정하고 음성 대조군을 추가했다 — **단언을 약화시키지 않았다**.

---

## 8. 교훈

1. **리뷰 산출물은 그 자체가 검증 대상이다.** 배경 전제 1건·처방 판별식 1건이 틀렸고, 그대로 채택했으면 새 결함이 됐다. 특히 **처방에 적힌 판별식은 실측으로 재현**해야 한다.

2. **「착수 금지」 조건은 넘어선 뒤에도 살아 있다.** COV-1은 리뷰가 조회에 실패해 조건이 미해소인 채였다. 그 조회를 다시 시도해 해소한 뒤에야 손댈 수 있었다.

3. **배치 누락은 ID 대조로만 잡힌다.** U1-02는 🔴 등급인데 계획서에 없었다. 사람 눈으로는 78건 중 1건이 빠진 것을 알 수 없다.

4. **한쪽만 고치면 조용히 통과한다.** rowOrder를 단건만 고쳤을 때 합계 단언이 공허하게 통과했고, 타입도 테스트도 잡지 못했다. **양쪽에 같은 구조가 있으면 parity 테스트**를 둔다.

5. **표시 결함은 계산 결함과 같은 뿌리를 갖는다.** `refCalculatedTax`의 중과 이중 계상을 PR-3에서 고쳤는데, **표시 쪽에 같은 식이 남아** 있어 PR #1407에서야 드러났다. 하나를 고칠 때 같은 식을 쓰는 곳을 전부 grep할 것.

6. **근거가 없으면 멈추는 것도 결과다.** 3건을 미확인으로 남겼고, 각각 착수 조건과 세액 방향을 코드에 적었다. 근거 없이 움직였다면 셋 중 둘은 납세자에게 불리한 방향이었다.

7. 🔴 **「배치했다」를 「처리했다」로 적지 말 것.** 이 문서의 최초 판이 그렇게 적었다. 계획서 ID 대조는
   **누락된 배치**를 잡는 도구이지 **구현 여부**를 재는 도구가 아니다. 완료 선언의 근거는
   **리뷰 ID ↔ 코드·테스트 역방향 grep + 미참조 건 개별 확인**이어야 한다. 이 세션에서
   그 역방향 측정을 처음 돌리자 12건이 나왔다 — 4번 교훈(「한쪽만 고치면 조용히 통과한다」)의
   문서판이다.

8. **처리한 자리에 발견 ID를 주석으로 남기면 역방향 측정이 가능해진다.** 재측정에서
   미참조로 나온 15건 중 **4건은 이미 고쳐져 있었고**(E2-07·U3-06·E5-04·E6-04-V·U3-03-V)
   ID 주석이 없어 잡히지 않은 것뿐이었다. 반대로 주석이 있었으면 「12건 미해소」를
   **리뷰 종료 시점에** 알 수 있었다. ID 주석은 장식이 아니라 **완료 판정의 계측 지점**이다.

9. **한 발견을 고치면 인접 안전망이 「무엇을 고정하고 있었는지」가 드러난다.** E2-06을 고치다
   목장 주요 케이스 4건이 전부 **프로덕션에서 도달 불가능한** `standardArea` 직접입력만
   고정하고 있었음을 알았다. 테스트가 통과한다는 것이 실제 경로가 검증된다는 뜻은 아니다.

---

## 9. 미해소 12건 — 재측정과 처리 (2026-09-02)

리뷰 문서의 `^### <ID>` 헤더 **73건**을 뽑아 `lib components app __tests__ e2e` 전수 grep →
**58건 참조 / 15건 미참조**. 미참조 15건을 코드로 개별 확인해 **3건은 이미 수정됨**(ID 주석만 없음),
**12건 미해소**로 확정했다.

**이미 수정돼 있던 3건** — E2-07(`pasture.ts` 사업용 비율 역전) · U3-06(결과 카드 「원」 접미사) ·
E5-04(목장 종중 분기 도달 불가 — `isRelatedPasture`가 무조건 의제 블록 밖으로 이동).

2차 검증 항목 **E6-04-V · U3-03-V**(`longTermDeductionExcluded`가 양도일과 무관하게 true)도
H-2와 함께 이미 해소돼 있었다 — `engine.ts:314`가 `isNblLthdExclusionEra(input.transferDate)`로
게이팅한다. 넷 다 **ID 주석이 없어** 역방향 grep에 잡히지 않았을 뿐이다. #1428에서 마커를 달았다.

### 미해소 목록과 처리

| 발견 | 등급 | 내용 | 처리 |
|---|---|---|---|
| **U3-05** | 🟡 | 무조건 의제 `legalBasis` 9종에 법령명 누락 (「시행령 §168조의14③1호」) | `NBL.UNCONDITIONAL_*` 상수로 이관 · #1424 |
| **E5-05** | 🟡 | 임야·목장 종중 의제가 **농지 전용 조문**을 인용 | 지목별 분기 — 농지 §168의8③6호 / 임야 §168의9③8호 / 목장 §168의10②2호 · #1424 |
| **E5-06** | 🟡 | `grace-period.ts` 전체 미사용 — 폐기된 「일수 합산」 모델 | `@deprecated` + 「일수 합산 금지」 (삭제 안 함 — Surgical) · #1424 |
| **E2-05 · U1-04** | 🟠 | 농지전용 허가일이 매퍼·엔진에 미도달 + 라벨 「(3년 이내)」가 §168의8③4호에 없는 요건 | 라벨을 법문대로 교체 + `nblFarmlandConversionDate` 5지점 제거 · #1424 |
| **E2-06 · U1-05** | 🟡 | 목장 안내문이 **존재하지 않는** 「기준면적 직접입력」 필드를 지칭 | 안내문 교체 + **자동산출 경로로 안전망 이전** · #1424 |
| **E2-08** | 🟡 | 사육두수 입력에 별표 1의3 제2호 **법정 산정방법 3종**이 안내되지 않음 | FieldCard hint + 자동산출 warning 양쪽에 명시 · #1424 |
| **E2-09** | 🟡 | §168의8② 후단이 준용하는 **조특령 §66⑭**(3,700만원 결격 과세기간) 미안내 | ① 1호·2호 기준 안내 #1424 → ② **결격 과세기간(연도) 입력·엔진 차감** #1429 |
| **E5-03** | 🟠 | `nblOwnershipRatio`에 범위 검증이 없어 `50` 입력 시 UI는 verbatim 곱하고 엔진은 1로 정규화 | ⑧에 `0 < ratio ≤ 1` 차단 + hint 강화 · #1426 |
| **U2-02** | 🟡 | 휴양시설 「기준면적 직접입력」 게이트가 바닥면적을 빠뜨려 UI↔validate 비대칭 | 게이트를 validate와 같은 4요소로 · #1426 |
| **E4-06** | 🟡 | 지방세법 시행규칙 [별표 6] 3호**바**목의 「기준면적의 100분의 10 이내」 한도 미강제 | 바목 전용 입력 분리 + clamp — **양도세·재산세 동시** · #1427 |

**등급 분포** — Critical·High 0건. 🟠 3건(E2-05·U1-04·E5-03)은 **배관·검증 부재**,
나머지 9건은 **표시·안내·dead code**였다. 세액을 직접 틀리게 산출하는 것은
**E4-06 하나**(공용 leaf `computeFactoryStandardArea` — 양도세 NBL + 재산세 분리과세 동시)뿐이었고,
방향은 납세자 유리(비사업용 면적 과소)였다.

### E4-06 세액 실측

읍·면 공장, 연면적 2,000㎡ · 기준공장면적률 20%(산출 10,000㎡), 부속토지 30,000㎡,
종업원용 체육시설 5,000㎡ 입력:

| | 기준면적 | 비사업용 면적 | ratio |
|---|---|---|---|
| 종전 | 17,000㎡ | 13,000㎡ | 0.4333 |
| 현행 | **13,200㎡** | **16,800㎡** | **0.56** |

**3,800㎡(전체의 12.7%p)** 과소 산출이 해소된다.

10%의 분모는 **바목을 제외한 공장입지기준면적**(1호·2호 + 가 + 나·다·라)으로 읽었다 —
조문이 가목에서는 「제1호 및 제2호에 따라 산출된 면적」, 바목에서는 「공장입지기준면적」으로
**표현을 구분**하기 때문이다. 바목 자신을 포함하면 순환하고, 「1호·2호만」으로 읽으면 분모가
작아 납세자에게 불리하다.

### 2차 안전망

| 파일 | 건수 | 뮤테이션 |
|---|---|---|
| `legal-basis-citation.anchor.test.ts` | 15 | 종전 문자열로 되돌리면 **7 RED** |
| `nbl-farmland-pasture-section-render.test.tsx` | 7 | 두 섹션을 렌더하는 첫 테스트 |
| `pasture.test.ts` `[E2-06]` | +5 | 종전 케이스가 **도달 불가 분기**만 고정하던 것을 실제 경로로 이전 |
| `nbl-ownership-ratio-resort-gate.anchor.test.tsx` | 17 | 두 수정을 되돌리면 **7 RED** |
| `factory-employee-sports-cap.anchor.test.ts` | 10 | clamp 제거 시 3파일 47건 중 **8 RED** |
| `property-factory-separate-limit` · `factory-land-plumbing` | +6 | 재산세 경로 · ⑫⑭ 라운드트립 |

### 기존 테스트 정정 4건

`factory-land-section`(COPY-4·COPY-5) · `property-separated-factory`(PTF-10) ·
`factory-land-plumbing`(STRIP-2) — 라벨이 「나·다·라·바」에서 「나·다·라」로 갈리고 바목이
별도 칸이 된 데 따른 것. **단언은 약화하지 않고 강화했다**(바목 칸 존재를 새로 단언).

---

## 10. E2-09 후속 — 결격 과세기간을 실제로 차감한다 (#1429)

#1424는 「해당 과세기간은 제외하고 입력하세요」라는 **안내만** 두었다. 사용자가 자경 기간 행을
손으로 쪼개야 했고, 쪼개지 않으면 자경기간이 과대 인정돼 §168의6 기간기준을 잘못 통과했다
(**과소과세** 방향). 별건으로 남겼던 「과세기간별 결격 플래그 입력」을 여기서 구현했다.

⇒ 결격 **과세기간(연도)** 을 직접 받고 엔진이 자경 기간에서 뺀다.

### 연수(count)가 아니라 연도인 이유

§168의6은 「양도일 직전 5년 중 2년 초과」·「직전 3년 중 1년 초과」·「소유기간 40% 초과」의
**AND 구조**라 **어느 해가 빠지는지**가 판정을 가른다. 실측(취득 2014-01-01 · 양도 2024-06-01 ·
전 기간 재촌·자경):

| 결격 과세기간 | 개수 | 판정 |
|---|---|---|
| 없음 | 0 | 사업용 |
| **2019~2023**(직전 5개) | 5 | **비사업용** |
| **2014~2018**(초기 5개) | 5 | **사업용** |

**같은 개수인데 결과가 반대다.** 같은 §66⑭를 §69 자경농지 감면 경로는 **연수 차감**으로
구현하지만(`self-farming-reduction.ts` — D7-10), 그쪽은 자경기간 자체가 스칼라라 그것으로 족하다.

⚠️ 두 칸은 **같은 사실을 다른 단위로** 묻는다(감면 = 연수 / NBL = 연도). 통합하려면 감면 쪽
자경기간까지 구간 배열로 바꿔야 하므로 별건이며, **두 칸의 hint가 서로를 지목**한다.

### 조용히 처리하지 않는다

- 실제로 잘라낸 연도와 차감 일수를 warning으로 노출한다.
- 자경 기간과 **겹치지 않는** 연도를 넣으면 「차감된 것이 없습니다」를 따로 알린다.
- 파서는 4자리 정수가 아닌 토큰을 **버리지 않고** `invalid`로 돌려주며, ⑧이 그것을 근거로 차단한다
  (자동 fallback 금지). 범위도 취득연도~양도연도로 차단한다.

### 안전망

| 파일 | 건수 |
|---|---|
| `disqualified-tax-periods.anchor.test.ts` | 17 — 파서 · 과세기간 정의(「소득세법」 §5①) · `subtractPeriods` · **연도별 판정 뒤집힘** |
| `nbl-disqualified-tax-periods-plumbing.anchor.test.tsx` | 16 — ⑫Zod · ⑭매퍼 · ⑧차단 · ⑤위젯 |

**뮤테이션 probe** — 엔진 제외·⑧ 차단·⑫ Zod 세 지점을 각각 무력화하면 33건 중 **9건 RED**.

---

## 11. 별표6 3호바 **표**까지 엔진이 산출한다 (#1435)

#1427은 10% 상한만 강제하고 **표는 사용자가 직접 계산**하게 했다(입력 hint에 「표의 기준면적에
해당하는 면적을 입력하세요」). 표를 잘못 읽으면 그대로 통과했다 — 남아 있던 마지막 별건이다.

### 표는 이미 저장소에 있었다

`lib/tax-engine/non-business-land/data/area-standards.ts`의 `employeeSportsArea(kind, n)`가
**「소득세법 시행규칙」 [별표 5]** 를 구현하고 있었고(§168의11①1호 기타토지 체육시설용지),
그 수치가 **「지방세법 시행규칙」 [별표 6] 3호바와 완전히 동일**했다(양쪽 본문 실측).

⇒ 사본을 만들지 않고 표를 세목 중립 위치(`lib/tax-engine/data/employee-sports-standard-area.ts`)로
옮겨 **양쪽이 같은 수치를 쓴다**. 기존 호출부는 재수출로 그대로 둔다.

🔴 **비고는 이미 다르다** — 표만 공유하고 비고 적용은 각 소비 모듈이 자기 법령대로 한다:

| | 「소득세법 시행규칙」 별표5 | 「지방세법 시행규칙」 별표6 3호바 |
|---|---|---|
| 50인 이하 코트만 | 「50인 이하인 **자**」 (개인·법인 불문) | 「50명 이하인 **법인**」 |
| 실내 배율 단서 | §101①2호 부속토지면 **배율 미적용** | 단서 없음 |

### 인정면적 산식

```
운동장   = min(운동장 용지면적, 표[운동장](종업원수))
코트     = min(코트 용지면적,   표[코트](종업원수))
실내기준 = min(실내 건축물 바닥면적, 표[실내](종업원수))   ← 비고 2-다
실내     = 실내기준 × §101② 용도지역별 적용배율            ← 비고 2-라

인정면적 = (종업원 ≤ 50 且 법인) ? 코트 : 운동장 + 코트 + 실내   ← 비고 2-나
최종     = min(인정면적, 공장입지기준면적 × 10%)               ← 바목 괄호
```

⚠️ **행별로 min을 잡는다.** 합계끼리 비교하면 한 행의 초과분이 다른 행의 여유분을 먹는다 —
운동장 2,000㎡(표 1,000)·코트 0㎡(표 970)이면 행별 1,000 vs 합계끼리 1,970으로 **970㎡ 과대**.

⚠️ 비고 1(운동경기 가능 시설·영구 시설물·탁구대 2면 이상)은 **사실 판단**이라 엔진이 검증하지
않는다. 면적 입력이 곧 요건 충족 선언이며 UI가 그 사실을 밝힌다.

### 미입력은 차단한다

- 시설 면적을 넣고 **종업원수가 없으면** 표를 적용할 수 없어 인정면적이 **통째로 0**이 된다
  (기준면적 과소 → 비사업용·종합합산 과대 = **납세자 불리**) ⇒ ⑧에서 차단.
- 종업원 50명 이하인데 **사업주체 미선택**이면 비고 2-나 적용 여부가 갈린다. 개인에게 적용하면
  법 근거 없이 불리해지므로 ⑧에서 명시 선택을 요구한다.
- 실내 바닥면적을 넣었는데 **§101② 배율을 알 수 없으면**(세분 전 주거지역 등) 실내분을 산입하지
  않고 그 사실을 detail에 드러낸다(추정 배율 금지).

### 안전망

| 파일 | 건수 |
|---|---|
| `factory-employee-sports-table.anchor.test.ts` (신규) | 38 — 표 구간·경계 연속성 · 행별 min · 비고 2-나·다·라 |
| `factory-sports-gate.anchor.test.tsx` (신규) | 13 — ⑧ 차단 · ⑤ 조건부 사업주체 · 비고 1 안내 |
| `factory-land-plumbing` · `factory-employee-sports-cap` · `property-factory-separate-limit` | 갱신 |

**뮤테이션 probe** — 표 상한·비고 2-나·비고 2-다·⑧ 차단 네 지점을 무력화하면 61건 중 **11건 RED**.

---

## 12. 종결 — 남은 것과 재개 조건

### 남은 것은 없다 — 보류 3건 전건 종결

✅ **보류 3건 전건 종결 (2026-09-14)** — 남은 미결 **0건**.

| 항목 | 종결 근거 |
|---|---|
| 레거시 임계 0.8의 **지목 한정** | 개정 **전** 본문(제24356호) 「100분의 20」에 지목 구분 없음 — 부칙이 아니라 구법 본문이 갈랐다 |
| **강원·전북특별자치도의 군** | 제주법 §10① 「시와 군을 두지 아니한다」 — 열거의 「특별자치도」는 군을 상정한 적이 없다 |
| **V5-d** 임야 편입유예 미상 기본값 | 법 §104의3①2호 **본문이 원칙·가목이 예외** ⇒ 사업용을 주장하는 쪽이 요건사실 부담 |

🔑 **셋 중 둘은 「자료가 없어서」가 아니라 「엉뚱한 자료를 기다려서」 멈춰 있었다.** 착수 조건에
적힌 문서(부칙 · 특별자치도 전환 해석례)가 아니라 **본문**이 답을 갖고 있었다. 남은 하나(V5-d)는
성격이 달랐다 — 조문이 정하지 **않은** 것(입증책임)이라 본문을 더 읽어도 안 닫히고, **원칙/예외
구조에서 배분을 도출**해 닫았다.

⚠️ **착수 조건은 코드에도 적혀 있었다.** 종결하면서 그 주석을 ⛔ 재검토 금지로 바꿨다 —
남겨두면 다음 사람이 「아직 미해소」로 오독한다.

### 이 리뷰가 남긴 재사용 가능한 것

| | |
|---|---|
| **완료 판정 측정** | 리뷰 ID ↔ 코드·테스트 **역방향 grep**([§ 종결](#-종결-2026-09-03)의 스크립트). 「배치했다」를 「처리했다」로 적는 것을 막는다 |
| **ID 주석 규약** | 처리한 자리·정정한 테스트에 발견 ID를 남긴다 — 측정의 계측 지점이다 |
| **뮤테이션 probe** | 신규 anchor마다 해당 로직을 되돌려 실제로 RED가 나는지 확인하고 그 수치를 PR 본문에 남긴다 |
| **재발 방지 anchor 5종** | [§7](#7-재발-방지-장치) — DB 시드·E2E 전 구간·factory parity·rowOrder parity·⑫⑭ plumbing |

### 리뷰 산출물 자체가 검증 대상이었다

배경 전제 1건·처방 판별식 1건·착수 조건 1건이 틀렸거나 미해소였고([§4](#4--리뷰-자체가-틀렸던-것)),
E5-04는 **원 처방을 그대로 채택하면 회귀를 만드는** 경우였다. 그대로 받았다면 새 결함이 됐다.

⇒ 다음 리뷰에서도 **처방에 적힌 판별식은 실측으로 재현**하고, 「착수 금지」 조건은 넘어선 뒤에도
살아 있는 것으로 다룬다.

# 비사업용 토지(NBL) 코드리뷰 — 결과 요약

> 2026-09-02 · 원 리뷰 [`nbl-code-review-2026-09.md`](nbl-code-review-2026-09.md) · 배치 계획 [`../00-pm/nbl-review-fix-batching.plan.md`](../00-pm/nbl-review-fix-batching.plan.md)
> 리뷰 기준 `master` **a30833f3** → 종료 시점 **22a0fbbb**

---

## 1. 결과

| | 건수 | 상태 |
|---|---|---|
| 확정 결함 | **78** | 전건 처리 |
| 기각 (1차 3 + 2차 7) | 10 | 재제안 금지로 문서화 |
| 미확정(UNCERTAIN) | 2 | COV-10 **종결** · V5-d **결론** |

**PR 10개** · 소스 **58파일** 변경 · 신규 테스트 **21파일**(anchor 20 + E2E 1).

> ⚠️ 「전건 처리」의 근거는 **계획서 ID ↔ 리뷰 ID 기계 대조**다. 리뷰 문서의 발견 ID를 뽑아
> 배치 계획의 ID 집합과 차집합을 냈고, 배치되지 않은 것은 **COV-10 · U1-02 둘뿐**이었다.
> 둘 다 후속 PR에서 처리했다. 「다 했겠지」로 넘겼으면 **U1-02는 그대로 남았을 것**이다.

---

## 2. 세액이 실제로 틀어졌던 것 (실측)

| 결함 | 실측 세액차 | 방향 | PR |
|---|---|---|---|
| **A1-01** 기타토지 재산세 분류 미선택이 매퍼에서 조용히 「종합합산」으로 | 76,725,000원 | 과대 | #1392 |
| **V7-b** 목장 §104의3①3호 단서를 축산 영위 시 건너뜀 | 76,548,532원 | 과대 | #1394 |
| **V10-f** 지분분할 일괄양도에서 NBL 플래그 미승계 | 74,661,400원 | 과소 | #1392 |
| **A3-01 · V9-a** ⑧ NBL 검증이 조기 return 뒤에 있어 도달 불가 | 65,550,000원 | 과대 | #1392 |
| **V2-b** 기간 배열 빈 행 침묵 drop (농지·목장·별장) | ±57,150,000원 | 양방향 | #1392 |
| **H-2** 2016.1.1. 전 양도분에 장특공제 적용 (구 §95② 배제 미구현) | 44,000,000원 | 과소 | #1396 |
| **E6-01** 일괄양도에서 상가 부수토지 초과분 중과 소실 | 11,683,750원 | 과소 | #1396 |
| **V10-a** 컴패니언 자산 NBL 중과 미전송 | 4,263,600원~ | 과소 | #1392 |

**COV-1**(구법 임계 0.8이 프로덕션 경로에서 도달 불가)은 판정 자체를 뒤집으나 금액을 산출하지 않았다 — #1403.

---

## 3. PR 매핑

| PR | 머지 | 내용 | 발견 |
|---|---|---|---|
| [#1392](https://github.com/kwonohjik/Property-related-Taxes/pull/1392) | `ca601966` | ⑧ 검증 게이트 + `lib/calc` 배선 (**엔진 무변경**) | 23 |
| [#1394](https://github.com/kwonohjik/Property-related-Taxes/pull/1394) | `881cee8f` | 지목 판정 엔진 + 대응 UI | 46 |
| [#1396](https://github.com/kwonohjik/Property-related-Taxes/pull/1396) | `64d8b6b0` | §95② 연혁 + 일괄양도 중과 | 15 |
| [#1398](https://github.com/kwonohjik/Property-related-Taxes/pull/1398) | `265c10bf` | 재촌 일반구 — **양도세·상속세 동시** | 4 |
| [#1400](https://github.com/kwonohjik/Property-related-Taxes/pull/1400) | `7f70ed72` | 도시지역 지역열거 + 보전녹지 (신규 입력 축) | 2 |
| [#1403](https://github.com/kwonohjik/Property-related-Taxes/pull/1403) | `b2f3b49c` | 안전망 — 구법 임계 도달성 · 시드 · E2E | 5 |
| [#1407](https://github.com/kwonohjik/Property-related-Taxes/pull/1407) | `3cbcba19` | 신고서 ③세율구분 코드 + ⑨세율 행 | COV-10 |
| [#1409](https://github.com/kwonohjik/Property-related-Taxes/pull/1409) | `b3cd10f0` | 복원 자산 `assetId` · rowOrder 가드 · CI 함정 | 파생 |
| [#1413](https://github.com/kwonohjik/Property-related-Taxes/pull/1413) | `bd178a63` | 이력 복원 ↔ factory 격차 제거 (COV-6 근본) | 파생 |
| [#1417](https://github.com/kwonohjik/Property-related-Taxes/pull/1417) | `22a0fbbb` | U1-02 거리 스냅샷 + V5-d 정책 명시 | 2 |

**배치 순서의 근거**: PR-1은 엔진 무변경이라 실패 위험이 가장 낮고, 잘못된 입력이 차단되기 시작해 PR-2의 anchor 작성이 쉬워진다. PR-3은 지목 판정이 안정된 뒤라야 합산·장특 축의 회귀 원인을 가릴 수 있다. PR-4(상속세 동반)·PR-5(14지점 신규 필드)는 **회귀 축이 달라** 합치면 CI가 빨개졌을 때 원인을 가릴 수 없다.

---

## 4. 🔴 리뷰 자체가 틀렸던 것

리뷰 산출물을 그대로 채택했다면 **새 결함을 만들었을** 지점들이다.

### 4-1. 배경 전제 오류 — 「비사업용 토지는 장기보유특별공제 배제」

리뷰 착수 시 배경 규칙으로 제시했으나 **현행법과 어긋난다**. §95②의 제외 열거는 **미등기양도자산(§104③)과 §104⑦ 각 호(다주택 중과)뿐**이고, 비사업용 토지는 §104①8호라 열거에 없다 — **표1 공제가 그대로 적용**된다.

2차 검증 에이전트가 잡아냈고 메인 루프가 조문 본문으로 재확인했다. 파급:

- 1차 발견들의 세액영향에 계상된 「장특 배제분」은 **과대 계상**이었다(+10%p 부분만 유효).
- 반대로 **과거 양도분에는 배제가 실재했고 그것이 미구현**이었다 → H-2(44,000,000원).

### 4-2. 처방의 판별식이 틀림 — PR-4 일반구 판정

리뷰 처방은 일반구를 `code[3:5] !== "00"`으로 가르라고 했다. 실측하니 **256건 중 238건**이 걸려 서울 종로구(11110)·의정부시(41150)까지 오탐한다. 대안 「끝자리 ≠ 0」도 12건 오탐.

정본은 **명칭**이었다 — `fullName`이 3토큰(도 + 시 + 구)인 것이 정확히 **39건 · 13개 시**, 반례 0. 그대로 구현했다면 자치구까지 접혀 **반대 방향의 과대적용**이 됐다. 원 리뷰 문서에 정정 블록을 남겼다.

### 4-3. 착수 조건이 미해소인 채였던 것 — COV-1

처방에 「⚠️ 0.8 적용 여부는 개정 전 §168의6 본문을 확인한 뒤 결정 — **확인 전 numeric 변경 금지**」가 걸려 있었고, 리뷰는 그 조회에 실패한 상태였다. 시행일별 MST로 직접 받아냈다:

| 시행본 | §168의6 각 호 다목 |
|---|---|
| 2014.03.11. (MST 151843) | 「소유기간의 **100분의 20**」 |
| 제26067호 [시행 2015.02.03] (MST 168102) | 「**100분의 40**」 |

⇒ 레거시 0.8이 **법적으로 옳다**는 것이 확인돼 배관을 고칠 수 있었다. 같은 조회에서 **경계 상수가 하루 어긋난 것**(`2015-02-02` strict `<` → 구법이 적용될 하루를 신법으로)도 함께 드러났다.

---

## 5. 리뷰 밖에서 나온 것

작업 도중 발견해 함께 처리했다. 원 리뷰에 없던 항목들이다.

| 발견 | 내용 | PR |
|---|---|---|
| 세율 표시 이중 계상 | 상세명세서가 `appliedRate + surchargeRate`로 찍어 비사업용 토지 실효 **55%를 65%로** 표시. `appliedRate`는 이미 중과 포함(`baseRate + additionalRate × ratio`) | #1407 |
| 복원 자산 `assetId` 누락 | 중복 판정 `other.assetId !== a.assetId`가 `undefined !== undefined` → **false** → 서로 다른 자산을 같은 자산으로 취급 | #1409 |
| `migrateAsset` ↔ factory 격차 | factory 462키 중 복원 결과에 없는 것 **147개**. COV-6(16필드)·`assetId`가 모두 이 뿌리 | #1413 |
| rowOrder 복제 | 합산 빌더가 별도 배열을 가져, 단건만 고치면 **합계 단언이 공허하게 통과** | #1409 |
| `gh pr checks --watch` exit 0 | PR 생성 직후 부르면 큐 등록 전이라 감시 대상이 없어 성공 종료. 그 시점 롤업은 conclusion이 **전부 빈 문자열** | #1409 (CLAUDE.md) |

---

## 6. 근거 미확보로 남긴 3건

셋 다 **조문 문언만으로는 갈리지 않아** 멈춘 것이다. 각 파일에 착수 조건을 적어두었다.

| 항목 | 현행 | 바꾸는 방향 | 착수 조건 |
|---|---|---|---|
| **V5-d** 임야 편입유예 미상 기본값 | 유예 미적용(비사업용) | 과소과세 | §168의9①2호 단서의 **입증책임 배분** 판례·예규 |
| 레거시 임계 0.8의 **지목 한정**(농·임·목만) | 기타토지는 0.6 | 불리(과세 증가) | **제26067호 부칙 경과조치**(조회 `NOT_FOUND`) |
| **강원·전북특별자치도의 군**이 §104의3① 지역열거 대상인지 | 대상 밖 | 불리 | 특별자치도 전환 관련 해석 |

### V5-d 상세

리뷰는 「임야만 §168의9①2호가 단서로 과세측 재포섭 구조」라고 지적했다. 확인 결과:

- **계산은 공유가 맞다** — 2014 시행본에서 §168의8⑥ 「2년」 / §168의9①2호 단서도 「편입된 날부터 **2년**이 경과한」이었고, 제26067호에서 셋 다 3년으로 **함께** 개정됐다. 문언 방향은 반대지만(「지나지 아니한」 제외 / 「경과한」 제외) 귀결이 「N년 이내 → 사업용」으로 같다.
- **갈리는 곳은 미상 기본값 하나**다. 농지·목장은 날짜가 **납세자 유리 예외**라 미입증 시 예외 미적용이 자연스럽지만, 임야는 「N년 경과」가 **과세를 되살리는 적극적 사실**이다.

⇒ `checkForestIncorporationGrace` 래퍼로 분리해 현행 기본값이 **의도된 선택**임을 못 박고, 공용 함수 주석에 임야가 이 경로를 쓴다는 사실과 연혁 실측 근거를 적었다. 실무 노출은 좁다 — ⑧ 검증이 도시지역 임야에 편입일을 **필수로 차단**하므로 미상 조합은 API 직접 호출·구 이력 복원에서만 생긴다.

---

## 7. 재발 방지 장치

리뷰가 지적한 **안전망 부재**(착수 시 NBL 전용 테스트 40파일 447건이 전원 통과하던 상태)에 대한 조치다.

| 장치 | 무엇을 막나 |
|---|---|
| `__tests__/db/nbl-judgment-rules-reachability.anchor.test.ts` | DB/시드 경로가 판정 규칙 그룹을 떨어뜨리는 것 · 중과율 키 소실 |
| `e2e/transfer-nbl-surcharge-amount.spec.ts` | UI 토글 → raw → Zod → route → 엔진 게이트 전 구간에서 +10%p 소실 |
| `__tests__/lib/stores/migrate-factory-parity.anchor.test.ts` | factory에 필드를 추가하고 migrate를 잊는 것 |
| `__tests__/components/filing-form-roworder-parity.anchor.test.ts` | 두 rowOrder 중 한쪽에만 행을 추가하는 것 |
| `__tests__/lib/calc/nbl-land-division-plumbing.anchor.test.ts` | ⑫Zod·⑭엔진 매핑 침묵 stripping |

**뮤테이션으로 구별력을 확인했다** — 각 anchor마다 해당 로직을 되돌려 실제로 실패하는지 검증했고, 그 결과를 각 PR 본문에 남겼다.

### 기존 테스트가 결함을 고정하고 있던 사례

PR-2에서 4건이 나왔다. 예: `nbl-result-card-render.test.tsx`가 **별표 1의3에 없는 축종**(`"hanwoo"`)을 써서 자동산출이 0을 반환하는 데 의존해 초과분을 만들고 있었다. 전부 조문 인용을 붙여 정정하고 음성 대조군을 추가했다 — **단언을 약화시키지 않았다**.

---

## 8. 교훈

1. **리뷰 산출물은 그 자체가 검증 대상이다.** 배경 전제 1건·처방 판별식 1건이 틀렸고, 그대로 채택했으면 새 결함이 됐다. 특히 **처방에 적힌 판별식은 실측으로 재현**해야 한다.

2. **「착수 금지」 조건은 넘어선 뒤에도 살아 있다.** COV-1은 리뷰가 조회에 실패해 조건이 미해소인 채였다. 그 조회를 다시 시도해 해소한 뒤에야 손댈 수 있었다.

3. **배치 누락은 ID 대조로만 잡힌다.** U1-02는 🔴 등급인데 계획서에 없었다. 사람 눈으로는 78건 중 1건이 빠진 것을 알 수 없다.

4. **한쪽만 고치면 조용히 통과한다.** rowOrder를 단건만 고쳤을 때 합계 단언이 공허하게 통과했고, 타입도 테스트도 잡지 못했다. **양쪽에 같은 구조가 있으면 parity 테스트**를 둔다.

5. **표시 결함은 계산 결함과 같은 뿌리를 갖는다.** `refCalculatedTax`의 중과 이중 계상을 PR-3에서 고쳤는데, **표시 쪽에 같은 식이 남아** 있어 PR #1407에서야 드러났다. 하나를 고칠 때 같은 식을 쓰는 곳을 전부 grep할 것.

6. **근거가 없으면 멈추는 것도 결과다.** 3건을 미확인으로 남겼고, 각각 착수 조건과 세액 방향을 코드에 적었다. 근거 없이 움직였다면 셋 중 둘은 납세자에게 불리한 방향이었다.

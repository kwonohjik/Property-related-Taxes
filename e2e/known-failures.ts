/**
 * CI E2E 게이트의 **알려진 사전존재 실패** 목록 (2026-08-03 baseline: `b0a289e9` master).
 *
 * ── 왜 목록이 필요한가 ──────────────────────────────────────────────────────
 * E2E를 CI 게이트로 넣는 목적은 **신규 회귀를 잡는 것**이다. 그런데 master에도 이미
 * 16건이 실패하고 있어 그대로 넣으면 CI가 **항상 빨간불**이 된다. 상시 실패하는 게이트는
 * 게이트가 아니다 — 이 저장소는 이미 그 실패를 겪었다(lint가 상시 실패 CI에만 있어
 * 실질 관문이 없던 건, CLAUDE.md 「lint 갭 해소」).
 *
 * ⇒ 사전존재분만 명시적으로 제외하고, **그 밖의 실패는 즉시 CI를 빨갛게** 만든다.
 *
 * ── 규칙 ────────────────────────────────────────────────────────────────────
 * 1. **줄이기만 한다.** 새 실패를 여기 추가하는 것은 회귀를 숨기는 것이다 —
 *    고치거나, 정말 불가피하면 사유와 별건 이슈를 함께 적는다.
 * 2. 항목이 사라지면(고쳐지면) **즉시 삭제**한다. 남겨두면 그 테스트가 CI에서 영영 안 돈다.
 * 3. 제목 **완전일치**가 아니라 부분일치다 — 제목을 바꾸면 제외가 풀려 CI가 빨개진다(의도된 동작).
 *
 * ── 🔍 개별 조사 결과 (2026-08-03 — memory `feedback_e2e_preexisting_failures`
 *    「사전존재는 면책이 아니라 미조사 표시」에 따라 16건 전건 조사) ──────────────
 *
 * | 분류 | 건수 | 판정 |
 * |---|---|---|
 * | 🔴 진짜 신호(데이터 갭) | 1 | `legal-coverage-button` — **✅ 해소 완료** (아래) |
 * | ✅ 엔진 정상·표시 경로 문제 | 1 | `§56⑤ 유상증자` — 엔진 단독 실측은 **정확**(아래 상세) |
 * | 🔧 spec rot(셀렉터·라벨 낡음) | 14 | **6건 해소** — 겸용주택(아래) · 잔여 8건은 각 항목 주석 |
 *
 * ⇒ **세액을 틀리게 계산하는 건은 발견되지 않았다.**
 *
 * ✅ **해소 1건 (2026-08-03)** — `커버리지 점검 버튼…`: 실측 192/201(미검증 9건)이었다.
 *    국세기본법 §45·§45의2·§48·§52 / 소득세법 §111 / 조특법 §77의2·§77의3 / 상증법 §52 /
 *    지방세법 §10의6을 KoreanLaw 본문 verbatim 키워드로 manifest에 등록해
 *    **201/201·`verify:legal` 217건 PASS**로 복구했고 목록에서 제거했다.
 *
 * ✅ **해소 6건 (2026-08-03)** — 겸용주택 전건. 원인이 둘이었다.
 *    · 5건: 시드에 `fixedAcquisitionPrice`가 없어 §100② 실가 안분 validate가 계산을 차단
 *      ("자산: 겸용주택 취득 실거래가액을 입력하세요"). `mixed-use-filing-form-4col.spec.ts`가
 *      정본 시드인데(2026-07-29 그쪽만 보강) 나머지 4개 spec이 따라가지 못했다.
 *    · 1건: `mixed-use-sidebar-acq-after-calc` — 버튼 라벨의 "←"가 아이콘으로 바뀌어
 *      accessible name에서 빠졌다. 부분 매칭 정규식으로 정정.
 */
export const KNOWN_E2E_FAILURES: readonly string[] = [
  // ── ✅ 엔진은 정상 — 표시/입력 경로 문제 ───────────────────────────────────
  /**
   * 표시값 `[0, 25,000,000, 25,000,000]` (기대 `[12,500,000, 25,000,000, 25,000,000]`).
   * **엔진 단독 실측은 정확하다** — `calcCapitalIncreaseAdjustment`에 PDF 사례1을 그대로
   * 넣으면 `[12500000, 25000000, 25000000]`을 반환한다(개시일 명시/미명시 모두).
   * 0이 나오려면 **1년전 사업연도 종료일이 엔진에 도달하지 않아야** 한다
   * (그때만 2·3년전이 「증자 이전 연도」로 full 가산되어 25M/25M이 된다 — 관측과 일치).
   * ⇒ 세액 오류 아님. 사업연도 열 DateInput 셀렉터(`.locator("..")` + `.nth(1)`) 의심.
   *    e2e/CLAUDE.md 「깊은 인덱스 금지·scope 한정」 위반 패턴이다.
   */
  "T1: 유상증자 입력 → 별지 제6쪽 '라. 유상증(감)자시 반영액'이 비0으로 표시된다",

  // ── 🔧 spec rot — 셀렉터·라벨이 UI 변경을 못 따라감 ────────────────────────
  // 「추징세액 계산」 버튼이 30초간 enabled 되지 않음 → 필수 입력이 늘었거나 라벨 변경
  "5년 내 가업 미종사 → 추징 발생(면제 아님)",
  "5년 초과 위반 → 자동 면제(outside_five_year_period)",
  // 화면엔 "2025-07-31"이 보이는데 `getByLabel("연도").first()`가 빈 값 → 첫 "연도"는 상속개시일
  "GAP3-1: 쿼리 prefill → 시뮬레이터 신고기한 사전 입력",
  // G4-2(부분 입력 시 경고 표시)는 통과 → 경고 로직은 정상. 지역 라디오 `getByText` 클릭 미적용 의심
  // (native radio → RadioCardGroup 마이그레이션 이후 클릭 대상이 바뀜)
  "E2E-G4-3: 3필드 전부 입력 시 partial 경고 사라짐",
  // placeholder "세무조정 후 영업손익 (원)" 미발견 → 라벨 변경
  "§45의3 일감몰아주기 사례4 roster 전체 → 36,720,000",
  // strict mode violation — `양도일을 선택하세요.`가 2개 매칭(취약 셀렉터, e2e/CLAUDE.md §3)
  "오류 일괄 표시 — 빈 폼에서 다음 클릭 시 오류 목록 + 건수 헤더",
  // `[data-asset-card-index="0"]` 안에 `소령 §163⑨` 배지 없음 → 배지 위치·노출 조건 변경
  "TLAW-2: 약칭 라벨(소령 §163⑨) 배지 → 정식명 시행령 제163조 헤더 + ESC 닫힘",
  // `중과세 한시 유예 조건 입력` 텍스트 없음 → 섹션 제목 변경
  "중과 한시 유예(gracePeriod) 토글 ON → 매매계약일·토지거래허가 노출",
];

/** 정규식 메타문자 escape — 제목에 §·(·)··가 섞여 있다 */
function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}

/** `grepInvert`용 패턴. 목록이 비면 undefined(제외 없음)를 반환한다. */
export function knownFailurePattern(): RegExp | undefined {
  if (KNOWN_E2E_FAILURES.length === 0) return undefined;
  return new RegExp(KNOWN_E2E_FAILURES.map(escapeRegExp).join("|"));
}

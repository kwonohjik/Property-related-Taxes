/**
 * CI E2E 게이트의 **알려진 사전존재 실패** 목록 (2026-08-03 baseline: `b0a289e9` master).
 *
 * ── 왜 목록이 필요한가 ──────────────────────────────────────────────────────
 * E2E를 CI 게이트로 넣는 목적은 **신규 회귀를 잡는 것**이다. 그런데 master에도 이미
 * 16건이 실패하고 있어 그대로 넣으면 CI가 **항상 빨간불**이 된다. 상시 실패하는 게이트는
 * 게이트가 아니다 — 이 저장소는 이미 그 실패를 겪었다(lint가 상시 실패 CI에만 있어
 * 실질 관문이 없던 건, CLAUDE.md 「lint 갭 해소」).
 *
 * ⇒ 사전존재분만 명시적으로 제외하고, **그 밖의 실패는 즉시 CI를 빨갛게** 만든다.
 *
 * ── 규칙 ────────────────────────────────────────────────────────────────────
 * 1. **줄이기만 한다.** 새 실패를 여기 추가하는 것은 회귀를 숨기는 것이다 —
 *    고치거나, 정말 불가피하면 사유와 별건 이슈를 함께 적는다.
 * 2. 항목이 사라지면(고쳐지면) **즉시 삭제**한다. 남겨두면 그 테스트가 CI에서 영영 안 돈다.
 * 3. 제목 **완전일치**가 아니라 부분일치다 — 제목을 바꾸면 제외가 풀려 CI가 빨개진다(의도된 동작).
 *
 * ── 분류 (memory `feedback_e2e_preexisting_failures` — 「사전존재는 면책이 아니라 미조사 표시」) ──
 * 아래 대부분은 spec rot(구조 변경으로 셀렉터가 낡음)으로 추정되나 **개별 조사는 미수행**이다.
 * 제품 버그가 섞여 있을 수 있다.
 */
export const KNOWN_E2E_FAILURES: readonly string[] = [
  // 상속 — 가업/영농 사후관리 시뮬레이터
  "5년 내 가업 미종사 → 추징 발생(면제 아님)",
  "5년 초과 위반 → 자동 면제(outside_five_year_period)",
  "GAP3-1: 쿼리 prefill → 시뮬레이터 신고기한 사전 입력",
  // 상속 — 동거주택·비상장주식
  "E2E-G4-3: 3필드 전부 입력 시 partial 경고 사라짐",
  "T1: 유상증자 입력 → 별지 제6쪽 '라. 유상증(감)자시 반영액'이 비0으로 표시된다",
  // 증여 — §45의3 일감몰아주기
  "§45의3 일감몰아주기 사례4 roster 전체 → 36,720,000",
  // 법령 커버리지 (manifest 미등록 조문 — memory에 조사 이력 있음)
  "커버리지 점검 버튼이 검증 커버리지를 보여준다",
  // 양도 — 겸용주택 (seed rot 의심, 2026-07-30 조사분)
  "수정신고 — 추가납부 hero + 표 라벨 '수정 후 전체 세액'",
  "경정청구 — 환급 hero + 표 라벨 '경정 후 전체 세액'",
  "비-정정 겸용주택은 기존 표시 유지 — hero 없음 + '총 납부세액'",
  "토글 ON → 12억 비과세 양도차익 > 0 + 거주 기간분 장특 > 0(표2)",
  "겸용 자산 카드에 ④ 거주 위젯 없음 + 계산 흐름 무회귀",
  "계산 후 자산 목록 복귀 → 사이드바 취득가액·필요경비 금액 표시(«-» 아님)",
  // 양도 — 기타
  "오류 일괄 표시 — 빈 폼에서 다음 클릭 시 오류 목록 + 건수 헤더",
  "TLAW-2: 약칭 라벨(소령 §163⑨) 배지 → 정식명 시행령 제163조 헤더 + ESC 닫힘",
  "중과 한시 유예(gracePeriod) 토글 ON → 매매계약일·토지거래허가 노출",
];

/** 정규식 메타문자 escape — 제목에 §·(·)··가 섞여 있다 */
function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}

/** `grepInvert`용 패턴. 목록이 비면 undefined(제외 없음)를 반환한다. */
export function knownFailurePattern(): RegExp | undefined {
  if (KNOWN_E2E_FAILURES.length === 0) return undefined;
  return new RegExp(KNOWN_E2E_FAILURES.map(escapeRegExp).join("|"));
}
